# Activity Summary for 4/18/2026

## 9:39:25 AM
The `values.yaml` file for the `transaction-processing-service` (timestamped 4/9/2026, 1:13:46 PM) outlines a comprehensive Kubernetes deployment and runtime configuration. Key aspects include:
*   **Security & Configuration:** Vault is enabled for secrets management, `springProfile` is set to `staging`, and JVM RAM percentage is `70.0`.
*   **Deployment & Resources:** Specifies a rolling update strategy, with CPU limits/requests at `2000m` and memory at `4Gi`.
*   **Service Specifics:** Configures `transaction-processing-service` with `10` processing workers, a queue size of `200`, and `12` months for processing data.
*   **Data Handling:** `categoryCache` is enabled, but various categorization learning features (`mxCategoryMappingEnabled`, `userLearningEnabled`, `bfmUserLearningEnabled`, `bfmEnabled`) are disabled. Transaction reconciliation uses an S3 bucket (`processing-reconciliation-output-transit`) and an SNS topic (`reconciliation-output`), with an SNS flow callback enabled.
*   **Observability & Monitoring:** Datadog is explicitly disabled for general observability. However, extensive Kubernetes monitors are enabled, covering CPU throttling, replica counts, deployment availability, pod crashes, OOM kills, and container restarts, all configured to notify `@slack-Tink-squad-transactions-oncall`. Detailed Service Level Objectives (SLOs) are defined for availability and latency across healthcheck endpoints and specific API paths like `/ingest/task`, `/process/transactions/update`, and `/process/transactions/callback`, with varying targets and timeframes.
*   **Infrastructure:** Redis is disabled. Horizontal Pod Autoscaler (HPA) targets CPU usage at `1200m` and requests per second at `100`, with pod replicas ranging from 4 to 25. Pod Disruption Budget (PDB) allows `20%` unavailability. Cassandra (distributed database) is configured for TLS, a custom driver, and specific connection limits. Kafka also shows private CA settings, with `awsPrivateCAIssuer` in use.
*   **Feature Management:** Unleash is enabled for feature toggles. `firehoseEnabled` is `true`, while `rateLimitEnabled` is `false`. Canary deployments are explicitly `DISABLED`. Fasttext integration is also disabled.

The `settings.gradle.kts` file (timestamped 4/15/2026, 10:43:57 AM) focuses on the build configuration for the `transaction-processing-service`. It sets up plugin management to use an internal Tink Artifactory Maven repository (`https://tink.jfrog.io/artifactory/gradle-plugins`), with credentials sourced from `JFROG_READ_USERNAME` and `JFROG_READ_TOKEN` environment variables. It also enables the `TYPESAFE_PROJECT_ACCESSORS` feature preview and includes `service` and `tink-protos` as project modules.

**Key Patterns and Recurring Elements:**
*   Both files are central to the `transaction-processing-service`, indicating a holistic approach to its build and operational lifecycle.
*   There's a strong reliance on Tink-specific internal infrastructure and tooling, such as `tink.jfrog.io`, `tink-fasttext-proxy-api`, and `tink.edge.getunleash.io`.
*   Extensive use of Kubernetes-native configurations (HPA, PDB, deployment strategies, resource limits) is evident in `values.yaml`, pointing to a containerized and orchestrated deployment environment.
*   The configuration highlights a clear intent for robust monitoring and observability with detailed SLOs and alerts, even though Datadog itself is currently disabled.
*   Many features are toggleable via boolean flags (e.g., observability, caching, various categorization aspects, Redis, Fasttext, canary deployments, private CAs), suggesting a flexible and feature-driven development environment.
*   Security considerations are present with references to Vault, SSL modes, keystores, and private CAs.

## 9:39:30 AM
The changes log details updates across two distinct files within the `tink-backend` project.

**File: `/Users/mmuruts/projects/tink/tink-backend/logggggg`**
*   **Timestamp:** 4/16/2026, 11:49:10 AM
*   **Update:** This entry is a Java stack trace indicating an `ApiRuntimeException` that occurred during a database operation. The core issue is a `ResultSetException` while mapping results from a SQL query (`SELECT * FROM accounts WHERE userId = :userId limit :limit`). The root cause is `java.lang.IllegalArgumentException: HOUR_OF_DAY: 2 -> 3`, which typically points to a daylight saving time transition problem or a timezone misconfiguration when a database timestamp is converted into a Java `Calendar` object.

**File: `/Users/mmuruts/projects/tink/tink-backend/WORKSPACE`**
*   **Timestamp:** 4/17/2026, 9:25:26 AM
*   **Update:** This is a Bazel `WORKSPACE` file specifying numerous external dependencies for the `tink_backend`. Key configurations include:
    *   Version definitions for Bazel rules like `platforms` (0.0.4), `bazel_skylib` (1.2.0), `rules_java` (4.0.0), and `rules_pkg` (0.6.0).
    *   `PROTOBUF_VERSION` is set to `3.25.5`.
    *   gRPC Java versions are `1.29.0` and `1.21.1` (nano), with Netty versions `4.1.48.Final` and `2.0.30.Final`.
    *   Explicit overrides for "Transaction Store" Netty versions (`TX_STORE_GRPC_JAVA_VERSION = "1.49.2"`, `TX_STORE_IO_NETTY_BORINGSSL_VERSION = "2.0.53.Final"`) are included, with a comment stating they are "created in order to remove vulnerabilities".
    *   `ZLIB_VERSION` is `1.3.1`.
    *   Build tools (`buildifier`, `bazel_unused_deps`) are configured for various platforms (darwin_x86_64, darwin_arm64, linux_x86_64).
    *   Go-related dependencies include `rules_go` (0.22.11) and `bazel_gazelle` (0.22.2), along with specific Go repositories (`com_github_boltdb_bolt`, `io_gorm_plugin_opentracing`).
    *   Docker rules (`io_bazel_rules_docker` v0.26.0) are noted as having been upgraded to fix GCS bucket 403 errors.
    *   Multiple `container_pull` entries are defined for OpenJDK-8 and Eclipse Temurin Java 8 images across different minor versions and distributions (e.g., `8u345`, `8u352`, `8u362` for focal and jammy). A cautionary note about past incidents related to Java 8u292 TLS changes is present.

*   **Timestamp:** 4/17/2026, 9:25:32 AM
*   **Update:** This update to the `WORKSPACE` file removes the `# END GENAI` comment from the `PROTOBUF_VERSION` line and introduces a new `container_pull` definition for `eclipse-temurin-8u362-jre-jammy`, specifying its digest.

*   **Timestamp:** 4/17/2026, 9:25:38 AM
*   **Update:** This entry shows no functional changes to the `WORKSPACE` file compared to the previous update at 9:25:32 AM. It appears to be an identical state or a minor, non-functional re-save.

**Patterns and Recurring Elements:**
*   **Dependency Management:** The `WORKSPACE` file demonstrates meticulous management of external dependencies, with specific versions, SHA256 hashes, and URLs for various tools and libraries (e.g., Bazel rules, Protobuf, gRPC, Netty, Zlib, Go libraries, Docker images).
*   **Security and Vulnerability Patching:** There's an explicit focus on security, as seen in the "Transaction Store netty version override" to address vulnerabilities and the cautionary notes regarding Java image updates (e.g., Java 8u292 TLS changes).
*   **Iterative Configuration:** The `WORKSPACE` file experienced three updates within a span of 12 seconds on 4/17/2026, suggesting rapid, iterative configuration changes or saves by a developer.
*   **Java Environment:** The project heavily relies on Java 8, with detailed configurations for various OpenJDK and Eclipse Temurin Docker images, highlighting a controlled approach to Java runtime environments.

## 9:39:49 AM
**File-Specific Updates and Significant Changes:**

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml (4/7/2026, 1:41:10 PM):**
    *   A Kubernetes deployment manifest for a `transaction-service-reconciliation-worker`.
    *   Features conditional deployment of an `initContainer` (vaultify:0.4.2) for fetching credentials from Vault if `.Values.useVault` is enabled.
    *   Configures security contexts like `runAsNonRoot: true` and `readOnlyRootFilesystem: true` for containers.
    *   Integrates Datadog APM with specific tags and environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`).
    *   Defines a second container, `transaction-service-tags-updater`, also conditional, with similar resource and image configurations.
    *   Environment variables for secrets (e.g., `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`) are sourced from Kubernetes secrets.
    *   Includes liveness and readiness probes and pod anti-affinity for non-local clusters.

*   **/Users/mmuruts/projects/tink/transaction-service/redis/client.go (4/7/2026, 1:44:15 PM, 1:45:43 PM, 2:33:18 PM):**
    *   A Redis client for caching `SearchResponse` (based on `SearchRequest`) and `UserProfileCache` data.
    *   The `SearchResultCacheClient` struct and `Config` are defined, supporting TLS and read-only routing for Redis clusters.
    *   Methods `CacheSearchResult`, `GetSearchResult`, `GetUserProfile`, and `CacheUserProfile` are implemented, using `hashstructure` for keys and `proto.Marshal`/`json.Marshal` for values.
    *   DataDog tracing (`dd-trace-go`) is integrated into caching operations.
    *   Multiple updates on this file show minor reordering, specifically involving `/* START GENAI */` and `/* END GENAI */` markers for `userProfileCacheKeyPrefix` and its associated functions, suggesting experimentation or tooling adjustments around these blocks. The final state at 2:33:18 PM removes these markers and reverts to the initial structure.

*   **/Users/mmuruts/projects/tink/transaction-service/interfaces.go (4/7/2026, 1:48:13 PM, 2:35:48 PM):**
    *   The `SearchResultCacheClient` interface was expanded at 1:48:13 PM to include methods for pagination state caching (`CachePaginationState`, `GetPaginationState`) and user profile caching (`GetUserProfile`, `CacheUserProfile`), along with a new `UserProfileCache` struct.
    *   A subsequent change at 2:35:48 PM removed the pagination state caching methods from the `SearchResultCacheClient` interface, but retained the `UserProfileCache` struct and its related methods (`GetUserProfile`, `CacheUserProfile`). The `GENAI` markers around `UserProfileCache` and `SearchResultCacheClient` were also removed.

*   **/Users/mmuruts/projects/tink/transaction-service/test/mock/cache.go (4/7/2026, 1:50:13 PM, 1:51:05 PM, 2:10:04 PM, 2:13:00 PM, 2:15:56 PM, 2:34:17 PM, 2:59:19 PM):**
    *   This mock file was frequently updated to mirror the changes in `interfaces.go`.
    *   Initially, new mock functions (`GetUserProfileFn`, `CacheUserProfileFn`) were added to support user profile caching.
    *   Several rapid updates (including a merge conflict at 2:10:04 PM) and apparent partial reverts occurred, leading to fluctuations in the completeness of the `mockTransactionCache` definition.
    *   The final state at 2:59:19 PM significantly simplifies `mockTransactionCache` to *only* include `GetUserProfileFn` and `CacheUserProfileFn`, indicating a targeted scope for this specific mock, while removing other general caching mock functions.

*   **/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server.go (4/7/2026, 1:55:37 PM, 1:56:03 PM, 2:27:39 PM, 2:31:30 PM):**
    *   Introduces an `InternalTransactionsServiceServerImpl` that can fetch transactions from Elasticsearch or Cassandra based on feature flags.
    *   Defines a `cassandraRepository` interface for Cassandra operations specific to V2 API.
    *   Implements "comparison mode" functions (`compareTransactionAsync`, `compareTransactionListAsync`) that asynchronously fetch data from both ES and Cassandra and log discrepancies when a feature toggle (`CompareEsAndCassandraTransactionResponsesToggle`) is enabled, designed for data validation without impacting primary API latency.
    *   The constructor `NewInternalTransactionsServiceServer` and the struct `InternalTransactionsServiceServerImpl` saw several rapid changes, with the `cacheClient` argument/field being added, then removed, then re-added (final state at 2:31:30 PM includes `cacheClient`).
    *   Configuration for `ProviderMarkets` is loaded from an environment variable.

*   **/Users/mmuruts/projects/tink/transaction-service/internal/commands/es_service_cmd/esservice.go (4/7/2026, 1:57:11 PM):**
    *   Configures the main Elasticsearch service command.
    *   Redirects gRPC internal logging to a custom JSON logger for better integration and masking.
    *   Initializes `SecretManager` and a `SecretsLoader` to fetch sensitive keys (e.g., `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`) from AWS Secrets Manager with fallback to environment variables.
    *   Conditionally sets up a Redis client if `redis-enabled` is true, explicitly passing the configured `cacheClient` to `NewTransactionStoreServer`.

*   **/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server_test.go (4/7/2026, 2:06:19 PM, 2:10:04 PM, 2:13:00 PM, 2:15:56 PM, 2:59:19 PM):**
    *   This test file was updated frequently. Initially, it expanded `mockCassandraRepo` with various methods and introduced a comprehensive `mockTransactionCache`.
    *   There was a merge conflict at 2:10:04 PM.
    *   Subsequent changes (2:13:00 PM, 2:15:56 PM) showed some regression in the `mockTransactionCache` definition (missing various cache/pagination mocks).
    *   The final update at 2:59:19 PM resolved inconsistencies, simplified `mockTransactionCache` to focus solely on `GetUserProfile` and `CacheUserProfile` mocks, and updated test calls to `NewInternalTransactionsServiceServer` to correctly pass a `mockTransactionCache` instance, aligning with the server's updated constructor.
    *   Test cases for `GetTransaction` and `ListTransactions` validate various scenarios including valid requests, missing IDs, and Cassandra errors, using feature flags to simulate different fetching behaviors.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values.yaml (4/9/2026, 11:07:44 AM):**
    *   Helm values for the main `transaction-service`.
    *   Enables `internalV2Enabled` and `snsEnabled`, `secretManagerEnabled`.
    *   Disables `redis` by default (`redis: enabled: false`).
    *   Configures Elasticsearch and Cassandra, including `cassandraV2: enabled: false`.
    *   Sets up extensive Datadog monitoring for various Kubernetes metrics and defines `slo` targets for availability and latency.
    *   Enables `awsPrivateCAIssuer`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml (4/9/2026, 11:08:30 AM):**
    *   Helm values for the `reconciliation-worker`.
    *   `reconciliation: enabled: false` by default, but specifies `firehoseTransactionsTopic`.
    *   Includes specific Datadog monitors, notably for `namespaceCpuQuotaExceeded` and `namespaceMemoryQuotaExceeded`.

*   **/Users/mmuruts/projects/tink/transaction-service/tnx_1 (4/13/2026, 8:59:27 AM):**
    *   A JSON file containing sample financial transaction data. Each transaction entry includes a detailed structure with `id`, `accountId`, `amount`, `descriptions`, `dates`, `status`, and `counterparties` with financial institution identifiers.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values/barnsley-production.yaml (4/13/2026, 12:05:52 PM):**
    *   Production-specific Helm overrides for the `reconciliation-worker` in the "barnsley" cluster.
    *   Enables `reconciliation` (`enabled: true`, `dryRun: false`) with `sqs: batchSize: 5`.
    *   Configures production Elasticsearch and Cassandra hosts, index shard counts (`nshards: 50`), and replica scaling.
    *   Sets `canaryDeployments: phase: COMPLETE`.

**Patterns and Recurring Elements:**

*   **Caching Integration:** A clear pattern of integrating and refining Redis caching for both search results and user profiles (`redis/client.go`, `interfaces.go`, `test/mock/cache.go`). This involved iterative changes, including the temporary removal and re-addition of certain caching functionalities and their corresponding mock implementations.
*   **Dual Read/Comparison for Data Migration/Validation:** The `internal/grpc/internal_server.go` file consistently demonstrates a strategy for comparing data fetched from both Elasticsearch and Cassandra, indicating a transition phase, data validation, or dual-read setup, controlled by feature toggles. This comparison logic runs asynchronously to prevent latency impact.
*   **Feature Flagging:** Widespread use of `featuretoggle` (e.g., `fetch-v1-from-cassandra-for-v2`, `CompareEsAndCassandraTransactionResponsesToggle`) to control application behavior and potentially enable/disable features during migrations or A/B testing.
*   **Observability and Security:** Persistent focus on observability via Datadog APM tracing and extensive Kubernetes monitoring (CPU, memory, deployment health, container restarts). Security measures like `runAsNonRoot`, `readOnlyRootFilesystem`, and Vault integration for secret management are consistently applied across deployments.
*   **Helm for Configuration:** Helm charts (`.charts/transaction-service/values.yaml`, `.charts/transaction-service-reconciliation-worker/values.yaml`, `values/barnsley-production.yaml`) are central to managing service configurations, allowing for environment-specific overrides for hosts, feature enablement, and scaling.
*   **Rapid Iteration on 4/7/2026:** The numerous timestamps on April 7, 2026, for `redis/client.go`, `interfaces.go`, `test/mock/cache.go`, `internal/grpc/internal_server.go`, and `internal/grpc/internal_server_test.go` suggest a period of active development, refactoring, and potentially resolving merge conflicts, particularly around caching and server logic. This is highlighted by the back-and-forth changes regarding the `cacheClient` and the `GENAI` markers.
*   **Structured Data:** The `tnx_1` file illustrates a consistent, detailed JSON structure used for representing financial transactions within the system.