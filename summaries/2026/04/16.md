# Activity Summary for 4/16/2026

## 8:59:49 AM
The provided log details configuration and build system changes for a `transaction-processing-service`.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml`**
Timestamp: 4/9/2026, 1:13:46 PM
This file, likely a Helm `values.yaml`, configures a Kubernetes deployment for the `transaction-processing-service`. Key updates and configurations include:
*   **Security:** `useVault` is enabled, `authService` is enabled, and `sslConfig` for the database specifies `sslMode=VERIFY_IDENTITY`. Cassandra and Kafka both have private CA configurations, with `awsPrivateCAIssuer` enabled for Kafka.
*   **Observability & Monitoring:** Datadog `observability` is generally `false` for logs and profiling, but comprehensive `monitors` are enabled, including Kubernetes-specific alerts for CPU throttled seconds, low replica count, deployment unavailability, pod crash loops, OOMKilled containers, and container restarts. SLOs are defined for `liveness` and `readiness` health checks, and specific endpoints like `/ingest/task`, `/process/transactions/update`, and `/process/transactions/callback` have customized latency and availability targets.
*   **Resource & Scaling:** Deployment uses a `rollingUpdate` strategy. `resources` are set with identical CPU and memory `limits` and `requests` (2000m CPU, 4Gi memory). Horizontal Pod Autoscaler (HPA) targets `cpuUsage: 1200m` (60% of requests) and `requestsPerSecond: 100`, with `podMinReplicas: 4` and `podMaxReplicas: 25`. A Pod Disruption Budget (`PDB`) allows `maxUnavailable: "20%"`.
*   **Application-Specific Configuration:**
    *   `springProfile` is set to `staging`.
    *   `categoryCache` and `categorization` are enabled, though most learning/BFM features within `categorization` are disabled.
    *   `processingWorkers` are set to 10 with a `workersMaxQueueSize` of 200.
    *   SNS flow for reconciliation is enabled, pointing to a specific `reconciliationS3BucketName`, `reconciliationSNSTopicName`, and `snsFlowCallbackUrl`.
    *   `takeProcessingLockForTransactionIngestion` is true.
*   **Database & Distributed Systems:** `database.maxPoolSize` is 20, and `runFlywayMigrations` is `false`. Cassandra is configured as a `distributedDatabase` with 3 `seedNodeCount` and specific connection settings.
*   **Feature Flags & Task Queues:** Unleash is `enabled: "true"` with its API configured. A `tasksQueue` is present but has an empty `topicPrefix`.
*   **Global Settings:** Defines `team: transactions`, `service: transaction-processing-service`, and `namespace: transaction-processing-service`, along with a `teamRoleId` and Datadog `baseUrl`.
*   **Other Integrations:** Redis and Fasttext are `enabled: false`. Canary deployments are `DISABLED`.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/settings.gradle.kts`**
Timestamp: 4/15/2026, 10:43:57 AM
This Kotlin-based Gradle settings file configures the build environment for the `transaction-processing-service`:
*   **Plugin Management:** Specifies a Maven repository (`https://tink.jfrog.io/artifactory/gradle-plugins`) for plugin resolution, requiring `JFROG_READ_USERNAME` and `JFROG_READ_TOKEN` from environment variables for authentication.
*   **Project Structure:** Sets the `rootProject.name` to `transaction-processing-service` and includes `service` and `tink-protos` as sub-modules.
*   **Gradle Features:** Enables the `TYPESAFE_PROJECT_ACCESSORS` feature preview.

**Patterns and Recurring Elements:**
*   Both files are explicitly linked to a `transaction-processing-service`.
*   There's a strong emphasis on integration with Tink's internal infrastructure (e.g., `tink.jfrog.io`, `tink-fasttext-proxy-api`, `tink-internal-ca.jks`).
*   The `values.yaml` shows a detailed, production-ready configuration for a microservice, covering aspects like security, resource management, monitoring, and service-specific business logic.
*   Both timestamps are in April 2026, indicating these represent a future or projected state of configurations.