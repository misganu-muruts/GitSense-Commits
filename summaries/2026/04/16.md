# Activity Summary for 4/16/2026

## 8:59:49 AM
The provided log details configuration and build system changes for a `transaction-processing-service`.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml`**
Timestamp: 4/9/2026, 1:13:46 PM
This file, likely a Helm `values.yaml`, configures a Kubernetes deployment for the `transaction-processing-service`. Key updates and configurations include:
*   **Security:** `useVault` is enabled, `authService` is enabled, and `sslConfig` for the database specifies `sslMode=VERIFY_IDENTITY`. Cassandra and Kafka both have private CA configurations, with `awsPrivateCAIssuer` enabled for Kafka.
*   **Observability & Monitoring:** Datadog `observability` is generally `false` for logs and profiling, but comprehensive `monitors` are enabled, including Kubernetes-specific alerts for CPU throttled seconds, low replica count, deployment unavailability, pod crash loops, OOMKilled containers, and container restarts. SLOs are defined for `liveness` and `readiness` health checks, and specific endpoints like `/ingest/task`, `/process/transactions/update`, and `/process/transactions/callback` have customized latency and availability targets.
*   **Resource & Scaling:** Deployment uses a `rollingUpdate` strategy. `resources` are set with identical CPU and memory `limits` and `requests` (2000m CPU, 4Gi memory). Horizontal Pod Autoscaler (HPA) targets `cpuUsage: 1200m` (60% of requests) and `requestsPerSecond: 100`, with `podMinReplicas: 4` and `podMaxReplicas: 25`. A Pod Disruption Budget (`PDB`) allows `maxUnavailable: "20%"`.
*   **Application-Specific Configuration:**
    *   `springProfile` is set to `staging`.
    *   `categoryCache` and `categorization` are enabled, though most learning/BFM features within `categorization` are disabled.
    *   `processingWorkers` are set to 10 with a `workersMaxQueueSize` of 200.
    *   SNS flow for reconciliation is enabled, pointing to a specific `reconciliationS3BucketName`, `reconciliationSNSTopicName`, and `snsFlowCallbackUrl`.
    *   `takeProcessingLockForTransactionIngestion` is true.
*   **Database & Distributed Systems:** `database.maxPoolSize` is 20, and `runFlywayMigrations` is `false`. Cassandra is configured as a `distributedDatabase` with 3 `seedNodeCount` and specific connection settings.
*   **Feature Flags & Task Queues:** Unleash is `enabled: "true"` with its API configured. A `tasksQueue` is present but has an empty `topicPrefix`.
*   **Global Settings:** Defines `team: transactions`, `service: transaction-processing-service`, and `namespace: transaction-processing-service`, along with a `teamRoleId` and Datadog `baseUrl`.
*   **Other Integrations:** Redis and Fasttext are `enabled: false`. Canary deployments are `DISABLED`.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/settings.gradle.kts`**
Timestamp: 4/15/2026, 10:43:57 AM
This Kotlin-based Gradle settings file configures the build environment for the `transaction-processing-service`:
*   **Plugin Management:** Specifies a Maven repository (`https://tink.jfrog.io/artifactory/gradle-plugins`) for plugin resolution, requiring `JFROG_READ_USERNAME` and `JFROG_READ_TOKEN` from environment variables for authentication.
*   **Project Structure:** Sets the `rootProject.name` to `transaction-processing-service` and includes `service` and `tink-protos` as sub-modules.
*   **Gradle Features:** Enables the `TYPESAFE_PROJECT_ACCESSORS` feature preview.

**Patterns and Recurring Elements:**
*   Both files are explicitly linked to a `transaction-processing-service`.
*   There's a strong emphasis on integration with Tink's internal infrastructure (e.g., `tink.jfrog.io`, `tink-fasttext-proxy-api`, `tink-internal-ca.jks`).
*   The `values.yaml` shows a detailed, production-ready configuration for a microservice, covering aspects like security, resource management, monitoring, and service-specific business logic.
*   Both timestamps are in April 2026, indicating these represent a future or projected state of configurations.

## 9:59:53 AM
The `transaction-processing-service` project underwent updates in its Helm chart values and Gradle build configuration across two dates in April 2026.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml`**
*   **Timestamp:** April 9, 2026, 1:13:46 PM
*   This file details the Kubernetes deployment configuration for the `transaction-processing-service`.
*   **General Configuration:** The service is configured for a `staging` Spring profile, uses Vault for secrets (`useVault: true`), and allocates 70% of JVM RAM. It's set up with 200 server worker threads, a 180s shutdown timeout per phase, and a 15-second pre-stop sleep.
*   **Deployment & Resources:** Rolling updates are configured with `maxSurge: "50%"` and `maxUnavailable: "20%"`. Resource limits and requests are set uniformly to 2000m CPU and 4Gi memory. Horizontal Pod Autoscaler (HPA) targets 1200m CPU usage and 100 requests per second, scaling between 4 and 25 pods, with specific stabilization windows. Pod Disruption Budget (`maxUnavailable: "20%"`) is also defined.
*   **Feature Toggles & Caching:** A category cache is enabled with a 60-minute TTL and a max size of 10,000. However, several categorization features like MX category mapping, user learning, and BFM are explicitly disabled. Redis is also disabled. Unleash feature toggles are enabled and configured.
*   **Transaction Processing:** Configured with 10 processing workers, a max queue size of 200, and processes data for 12 months. Reconciliation logic is detailed, using S3 (`processing-reconciliation-output-transit`) and SNS (`reconciliation-output`) for output, with SNS flow enabled. A processing lock is taken for transaction ingestion.
*   **Database:** A max pool size of 20 for the database is set, Flyway migrations are disabled, and Jdbi is always used. SSL is configured with `sslMode=VERIFY_IDENTITY`.
*   **Distributed Database (Cassandra):** Configured for 3 seed nodes, with TLS and a custom driver enabled, and a private CA enabled but marked as not used.
*   **Observability & Monitoring:** Datadog observability is largely disabled (log injection, profiling), but extensive Kubernetes monitors are enabled. Numerous alerts are defined for critical conditions like CPU throttling, low replica counts, deployment unavailability, pod crashes, OOMKilled containers, and container restarts, all configured to notify `@slack-Tink-squad-transactions-oncall`.
*   **Service Level Objectives (SLOs):** Detailed SLOs are defined for availability and latency across general health checks and specific transaction processing endpoints (`/ingest/task`, `/process/transactions/update`, `/process/transactions/callback`). These include target percentiles and external target seconds over a 30-day timeframe.
*   **Integrations:** Auth service is enabled. Canary deployments are disabled. Kafka uses an AWS Private CA Issuer, with its internal private CA disabled. Fasttext integration is disabled.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/settings.gradle.kts`**
*   **Timestamp:** April 15, 2026, 10:43:57 AM
*   This Gradle Kotlin DSL file configures the build for the `transaction-processing-service`.
*   **Project Structure:** Sets the root project name to `transaction-processing-service` and includes `service` and `tink-protos` as sub-projects.
*   **Dependency Management:** Configures a custom Maven repository (`https://tink.jfrog.io/artifactory/gradle-plugins`) for plugin management, with credentials sourced from environment variables (`JFROG_READ_USERNAME`, `JFROG_READ_TOKEN`) for secure access.
*   **Build Features:** Enables the `TYPESAFE_PROJECT_ACCESSORS` feature preview.

**Patterns and Recurring Elements:**
*   **Tink-specific Infrastructure:** Both files indicate integration with Tink's internal infrastructure, referencing `tink.jfrog.io` for Gradle plugins, `tink-protos` for shared definitions, `tink-fasttext-proxy-api`, and a dedicated Slack channel (`@slack-Tink-squad-transactions-oncall`) for alerts.
*   **Focus on Reliability and Observability:** The `values.yaml` shows a strong emphasis on operational stability through detailed HPA, PDB, extensive monitoring, and explicit SLO definitions for key service endpoints.
*   **Cloud-Native Deployment:** The `values.yaml` is a comprehensive Kubernetes Helm configuration, demonstrating a cloud-native deployment strategy.
*   **Security Considerations:** Use of Vault, SSL/TLS for database connections, and sourcing sensitive credentials from environment variables (in `settings.gradle.kts`) highlight security-conscious practices.
*   **Modular Architecture:** The `settings.gradle.kts` including `service` and `tink-protos` indicates a modular project structure.