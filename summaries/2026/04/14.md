# Activity Summary for 4/14/2026

## 9:45:09 AM
The `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml` file, as of `4/9/2026, 1:13:46 PM`, provides comprehensive configuration for the `transaction-processing-service`.

**Key updates and configurations within the file:**

*   **Security and Secrets Management**: Vault is enabled (`useVault: true`), while Secrets Manager is disabled (`useSecretsManager: false`). SSL is configured for the database (`sslMode=VERIFY_IDENTITY`), and Cassandra uses TLS with a custom driver. A truststore path is specified, and AWS Private CA Issuer is used for Kafka (`awsPrivateCAIssuer: used: true`).
*   **Application Settings**: The Spring profile is set to `staging`. JVM RAM usage is capped at `70.0%`, with `200` worker threads and a `180s` timeout per shutdown phase. A `15` second pre-stop sleep is configured.
*   **Deployment and Resource Management**: Rolling updates are configured with `50%` max surge and `20%` max unavailable. CPU and memory limits/requests are both `2000m` and `4Gi` respectively. Horizontal Pod Autoscaling (HPA) is enabled with targets of `1200m` CPU usage and `100` requests per second, scaling between `4` and `25` replicas. Canary deployments are currently `DISABLED`.
*   **Observability and Monitoring**: Datadog is disabled for core observability, but extensive Kubernetes monitoring is enabled, including alerts for CPU throttled seconds, low deployment replicas, unavailable deployments, pod crash loops, OOMKilled containers, and container restarts. Service Level Objectives (SLOs) are defined for availability (99.9% for 30 days) and latency (99th percentile, 0.1s external target) for general endpoints, with stricter overrides for specific POST endpoints like `/ingest/task`, `/process/transactions/update`, and `/process/transactions/callback`.
*   **Transaction Processing Logic**:
    *   A category cache is enabled with a `60` minute TTL and `10000` max size.
    *   Categorization is enabled, but MX category mapping and various user/BFM learning features are disabled.
    *   Processing utilizes `10` workers with a `200` max queue size, processing `12` months of data with a `30` day default look-back.
    *   Reconciliation settings are detailed, with provider-based reconciliation generally disabled, but flags for logging duplicate transactions, S3 bucket, SNS topic, and SNS flow callback URL are specified. A processing lock is taken for transaction ingestion.
*   **Data Stores**: Redis is disabled (`redisConfiguration: enabled: false`). The database max pool size is `20`, Flyway migrations are not run, and JDBI is always used. Distributed database settings point to Cassandra with `3` seed nodes.
*   **Feature Toggles**: Unleash is enabled (`unleash: enabled: "true"`) with its API and a `30` second fetch interval.
*   **External Integrations**: An auth service is enabled. Fasttext is disabled. Kafka configurations include `privateCA: enabled: false` and `awsPrivateCAIssuer: used: true`.
*   **Queuing and Streaming**: A tasks queue, stream fetcher with `100000` max bytes, and `5` workers are configured. Firehose is enabled, while rate limiting is disabled.
*   **Global Configuration**: The service belongs to the `transactions` team, namespace `transaction-processing-service`, and uses `https://app.datadoghq.eu` as its Datadog base URL.

**Patterns and Recurring Elements:**

*   **Extensive use of boolean flags** to enable/disable features and services across security, observability, processing logic, and data stores (e.g., `useVault`, `datadog: enabled`, `categoryCache: enabled`, `categorization: enabled`, `redisConfiguration: enabled`, `unleash: enabled`, `fasttext: enabled`, `firehoseEnabled`, `rateLimitEnabled`, `monitors: enabled`, `snsFlowEnabled`).
*   **Detailed resource allocation and scaling parameters** are specified for Kubernetes deployments, including CPU/memory requests/limits, HPA targets, and rolling update strategies.
*   **Comprehensive observability setup** is a significant focus, with specific alerts and SLO targets configured for various operational metrics and endpoints, demonstrating a strong emphasis on service reliability and performance monitoring.
*   **Explicit configuration for multiple data storage technologies** (PostgreSQL/JDBI, Redis, Cassandra) with security considerations like SSL/TLS.
*   **Specific configurations for "reconciliation" features**, indicating this is a core aspect of the service's functionality, with multiple flags governing its behavior.

## 9:45:52 AM
Changes to `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml` (4/7/2026, 1:41:10 PM) introduce a Kubernetes deployment for the `transaction-service-reconciliation-worker`. This configuration uses Helm templating extensively for conditional logic, enabling features like Datadog APM integration (adding tags and admission annotations) and Vault for fetching credentials via an `initContainer` running `vaultify`. The deployment defines a main reconciliation worker container, optionally a `tags-updater` sidecar, and a `renew-leases` container (for Vault), each with specific resource requests, images, arguments, and environment variables (some referencing Kubernetes secrets for sensitive data like HMAC keys and Cassandra passwords). All containers enforce `readOnlyRootFilesystem: true`. Volume mounts are configured for TLS certificates, CA bundles, and configuration/credentials based on whether Vault is used. The deployment also includes liveness/readiness probes and conditional pod anti-affinity.

The `redis/client.go` file saw multiple updates. At 4/7/2026, 1:44:15 PM, it introduced a `SearchResultCacheClient` for Redis, including methods like `CacheSearchResult`, `GetSearchResult`, `GetUserProfile`, and `CacheUserProfile`. It supports TLS for Redis connections and uses Datadog tracing. `SearchRequest` keys are hashed and marshaled with protobuf, while `UserProfileCache` data uses JSON. A minor refactoring occurred at 4/7/2026, 1:45:43 PM, where `userProfileCacheKeyPrefix` and the `UserProfileCache` methods were reordered and wrapped in `/* START GENAI */` and `/* END GENAI */` comments. This file was updated again at 4/7/2026, 2:33:18 PM, moving the `userProfileCacheKeyPrefix` definition inside the `/* START GENAI */` block.

The `interfaces.go` file also evolved. At 4/7/2026, 1:48:13 PM, it defined a new `UserProfileCache` struct and extended the `SearchResultCacheClient` interface to include `GetUserProfile`, `CacheUserProfile`, `CachePaginationState`, and `GetPaginationState` methods. These additions were initially placed within `/* START GENAI */` and `/* END GENAI */` comments. By 4/7/2026, 2:35:48 PM, these `GENAI` comments were removed, and the pagination state caching methods (`CachePaginationState`, `GetPaginationState`) were removed from the `SearchResultCacheClient` interface, indicating a stabilization and refinement of the caching interface.

Changes to `internal/grpc/internal_server.go` highlight a strategy for data handling and comparison. At 4/7/2026, 1:55:37 PM, a `cassandraRepository` interface was defined, and the server implemented asynchronous comparison functions (`compareTransactionAsync`, `compareTransactionListAsync`) to validate data consistency between Elasticsearch and Cassandra. These functions, along with configuration initialization for `ProviderMarkets`, were marked with `/* START GENAI */` comments. Shortly after, at 4/7/2026, 1:56:03 PM, the `InternalTransactionsServiceServerImpl` struct and its constructor were updated to include a `cacheClient`, integrating the Redis caching functionality. This `cacheClient` was temporarily removed at 4/7/2026, 2:27:39 PM, only to be re-added at 4/7/2026, 2:31:30 PM, suggesting an iterative integration process.

`internal/commands/es_service_cmd/esservice.go` (4/7/2026, 1:57:11 PM) shows further integration points. It includes `/* START GENAI */` comments for redirecting gRPC internal logs and for new `secretsConfig` and `secretsLoader` logic to manage `PAGE_TOKEN_HMAC_KEY` and `SYSTEM_ACCESS_TOKEN` via AWS Secrets Manager with environment variable fallbacks. The `cacheClient` is also explicitly passed to the `NewTransactionStoreServer` here.

The test file `internal/grpc/internal_server_test.go` underwent several changes to support the new features. At 4/7/2026, 2:06:19 PM, it extensively expanded `mockCassandraRepo` with various data retrieval mock functions and introduced a comprehensive `mockTransactionCache` with many mock methods for search results, internal lists, and pagination states. A merge conflict at 4/7/2026, 2:10:04 PM, further added mock functions related to Cassandra and explicitly to `GetUserProfileFn` and `CacheUserProfileFn` for `mockTransactionCache`. This conflict was resolved by 4/7/2026, 2:13:00 PM (and confirmed at 4/7/2026, 2:15:56 PM), simplifying `mockTransactionCache` by removing most pagination and internal list caching mocks, focusing on search results and UserProfile. Finally, at 4/7/2026, 2:59:19 PM, `mockTransactionCache` was simplified further to primarily mock `GetUserProfile` and `CacheUserProfile`, with other cache methods becoming no-ops, reflecting a targeted testing scope.

Helm values files for deployment were also updated. `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values.yaml` (4/9/2026, 11:07:44 AM) shows default configurations, with Redis caching and various deletion listeners disabled by default. It enables `internalV2Enabled` and `datadog.apmEnabled`, and defines extensive Datadog monitoring and SLO targets. `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml` (4/9/2026, 11:08:30 AM) provides similar default settings for the reconciliation worker, also disabling reconciliation and tags updater by default, and includes worker-specific Datadog monitors. An environment-specific override for `barnsley-production.yaml` (4/13/2026, 12:05:52 PM) enables `userSvc` and `reconciliation` (`dryRun: false`), adjusts Elasticsearch and Cassandra hosts, increases replica counts, and sets canary deployments to `COMPLETE` phase, demonstrating production-specific scaling and feature activation.

A data file, `tnx_1` (4/13/2026, 8:59:27 AM), provides sample JSON-formatted transaction data, including IDs, account details, amounts (in EUR), detailed descriptions, booking/value dates, and counterparties with financial institution identifiers (IBANs).

**Key Patterns and Recurring Elements:**

*   **Redis Caching:** A strong focus on introducing and integrating Redis for caching search results and user profiles, evident across `redis/client.go`, `interfaces.go`, `internal/grpc/internal_server.go`, and `test/mock/cache.go`. This includes the definition of cache structures, client implementation, interface updates, and mock object adaptation.
*   **"GENAI" Comments:** Frequent use of `/* START GENAI */` and `/* END GENAI */` comments around newly introduced code blocks, particularly for caching logic, Cassandra repository definitions, configuration parsing, asynchronous comparison functions, and logger redirection. This suggests the involvement of an AI assistant in generating or assisting with these specific code segments.
*   **Data Consistency & Migration:** The implementation of asynchronous data comparison between Elasticsearch and Cassandra in `internal/grpc/internal_server.go` (controlled by feature flags) points to a strategy for validating data consistency during a potential migration or dual-write phase.
*   **Kubernetes/Helm for Deployment:** Consistent use of Helm charts (`.charts/`) for defining application deployments, handling environment-specific overrides, secret management via Vault, and integrating observability tools like Datadog.
*   **Observability:** Datadog APM tracing and detailed Kubernetes monitoring configurations (for CPU, memory, deployment health, etc.) are integrated across both the main service and the reconciliation worker deployments.
*   **Iterative Development:** The back-and-forth changes regarding the `cacheClient` integration in `internal_server.go` and the evolution of mock objects in `internal_server_test.go` suggest an iterative development and testing process, with adjustments and refinements made over time.
*   **Secrets Management:** A clear pattern of sourcing sensitive configuration (HMAC keys, access tokens) from a secure store (AWS Secrets Manager) with environment variable fallbacks is present.

## 9:45:58 AM
The code changes primarily involve Helm chart configurations for the `account` and `account-processing` services, and system-wide configurations within `tink-backend-system`. Most modifications occurred on April 13, 2026, with template definitions observed earlier on April 9-10, 2026.

### File-Specific Updates:

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/delete-accounts-and-transactions-job.yaml` (April 9, 2026, 9:55:49 AM and 9:56:10 AM):** This file defines a Kubernetes Job named `delete-accounts-and-transactions-job` for the `account` namespace. It's a templated job conditionally enabled based on the cluster and environment. The job uses a `net-tools:latest` Docker image, executes a shell script `/scripts/delete-accounts-and-transactions.sh`, and mounts several volumes including a persistent volume claim for large data, a config map for scripts, and empty directories for temporary data, credentials, and CA truststores. It sets resource limits/requests to 500m CPU and 1Gi memory, a `ttlSecondsAfterFinished` of 300 seconds, and a `terminationGracePeriodSeconds` of 345600 seconds. Security contexts are strictly defined, requiring `runAsNonRoot`, specific `runAsUser`/`runAsGroup`/`fsGroup`, and `readOnlyRootFilesystem`. The content remained identical across both timestamps.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/config.yaml` (April 10, 2026, 10:40:52 AM and 10:41:00 AM):** This Helm template defines a Kubernetes ConfigMap for the `account` service, containing a comprehensive `server.yml` configuration.
    *   It dynamically sets database (MySQL) connection URLs for `maindb` or `accountdb` based on the environment and cluster, deriving hostnames. It includes placeholders for SQL user/password, indicating potential integration with HashiCorp Vault for credential retrieval.
    *   Task Queue hosts and ports are configured, with conditional TLS settings and truststore paths.
    *   Configurations for `eventProducerService` and `tracing` are included, along with feature flags like `accountPartyServiceEnabled`, `accountDataRepositoryEnabled`, and `accountVerificationEnabled`.
    *   Extensive Cassandra configuration is present, detailing seeds, datacenter, credentials (also Vault-integrated placeholders), keyspace/table creation, data retention, read/write enablement, request timeouts, and SSL settings with keystore/truststore paths.
    *   A `userDeletionTaskQueue` is configured, specifying hosts, topic, groupId, SSL settings, and truststore/keystore paths, with `truststorePassword: notAsecret` as a placeholder.
    *   Numerous internal service endpoints for account verification (e.g., `identity`, `credentials`, `statistics`, `riskInsights`, `incomeCheck`, `holderNameCheck`, `providersSearchApi`, `user`) are defined with their URLs and conditionally pinned certificates.
    *   The content remained identical across both timestamps.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/oxford-production-product001.yaml` (April 13, 2026, 12:50:08 PM):** This file specifies environment-specific values for `oxford-production-product001`, enabling `awsPrivateCAIssuer` and setting `firehosePrefix`, `userDeletionTaskQueue.topicPrefix` to `oxford-production-product001`, and `jobs.changeCertainDateJobEnabledEnv` to `oxford-production`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/jersey-production.yaml` (April 13, 2026, 12:50:16 PM):** Defines `jersey-production` specific values, including enabling `useAccountdb`, `databaseMigrationsEnabled`, `accountDataRepositoryEnabled`, and `cassandra`. It sets the `accountdb.host` to a specific AWS RDS endpoint. Various service endpoints (e.g., `credentials`, `identity`, `user`, `providersSearchApi`) are configured with their URLs and pinned certificates, while `statistics`, `riskInsights`, `incomeCheck`, and `holderNameCheck` endpoints are explicitly disabled. Several account-related feature flags are enabled (`accountVerificationEnabled`, `accountPartyServiceEnabled`, etc.). `awsPrivateCAIssuer` is enabled and trusted.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/barnsley-production.yaml` (April 13, 2026, 12:50:35 PM):** This file provides `barnsley-production` values, primarily setting the `maindb.host` to an AWS RDS endpoint and enabling `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/barnsley-production.yaml` (April 13, 2026, 12:50:45 PM and 12:50:52 PM):** This file configures `barnsley-production` settings for `account-processing`. It enables the `taskQueue` endpoint and feature flags `enableAccountEvents` and `enableBalanceCheckWebhooks`. The primary change between the two timestamps was a typo correction for `awsPrivateCAIssuer.used` from `ftruee` to `true`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/farnham-production.yaml` (April 13, 2026, 12:51:01 PM):** Sets `farnham-production` values, enabling the `enableAccountEvents` feature flag and `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/oxford-staging.yaml` (April 13, 2026, 12:51:25 PM):** Defines `oxford-staging` values, enabling the `taskQueue` endpoint and several feature flags related to account and balance events (`enableAccountEvents`, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, `balanceCheckEnabled`, `enableBalanceCheckWebhooks`). It also includes specific application IDs for `accountDeduplication` settings and enables `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/oxford-production-product001.yaml` (April 13, 2026, 12:51:37 PM):** Configures `oxford-production-product001` for account-processing, setting `endpoints.taskQueue.topicPrefix` and enabling `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/barnsley-production.yaml` (April 13, 2026, 12:52:08 PM):** This file specifies `barnsley-production` values for `tink-backend-system`. It sets the `clusterName` to "BARNSLEY" and configures various endpoints (e.g., `identityAggregatorServiceConfiguration` disabled, `userInternalClient` enabled). It defines `taskQueue.streamFetcherMaxBytes`, cache host (AWS ElastiCache), and transaction access settings. Redis configuration for transaction processing is enabled with a clustered host (AWS ElastiCache) and timeout. Categorization settings are enabled, and `useIdentityAggregatorService` is disabled. Feature flags `enableAccountEvents` and `enableTransactionEvents` are set. Database URL (AWS RDS), `useAwsSecretManager`, and `awsAccountId` are specified. `awsPrivateCAIssuer` is enabled and trusted.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/farnham-production.yaml` (April 13, 2026, 12:52:14 PM):** Configures `farnham-production` for `tink-backend-system`, setting `clusterName` to "FARNHAM". Database `maxPoolSize` and URL (AWS RDS) are specified. HPA settings for min/max pod replicas are defined. Various endpoints are configured, with `identityAggregatorServiceConfiguration`, `oauth`, and `oauthService` disabled. Cache host (AWS ElastiCache) is set. Transaction access, categorization settings, and aggregation policy service are configured. Feature flags `enableAccountEvents` and `enableTransactionEvents` are enabled. A list of supported locales is provided. Persistent volume is enabled. Task queue rate limiting and transaction processing worker settings are detailed. Redis configuration for transaction processing is enabled with a clustered host (AWS ElastiCache). `useAwsSecretManager`, `awsAccountId`, and `awsPrivateCAIssuer` are enabled.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/oxford-production-product001.yaml` (April 13, 2026, 12:52:22 PM):** This file defines `oxford-production-product001` values for `tink-backend-system`. It sets feature flag prefixes for `paymentStatusUpdatePrefix`, `credentialsUpdatePrefix`, and `taskQueue.topicPrefix` to `oxford-production-product001`. Job-specific settings for `resetCertainDate` and `changeCertainDateFile` are included. `awsPrivateCAIssuer` is enabled and trusted.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/production.yaml` (April 13, 2026, 12:52:28 PM):** Defines general production values for `tink-backend-system`. It sets default CPU and memory resources, HPA targets, and `legacyBackgroundRefreshRatio`. Kafka private CA is enabled, and `awsPrivateCAIssuer` is enabled and trusted.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/jersey-production.yaml` (April 13, 2026, 12:52:45 PM and 12:55:04 PM):** Configures `jersey-production` for `tink-backend-system`, setting `clusterName` to "JERSEY". It enables `userInternalClient` endpoint, specifies a cache host (AWS ElastiCache), and configures transaction access. Categorization is explicitly disabled with `enabled: false` and `userLearningEnabled: false`. Aggregation policy service is enabled. Feature flags `enableAccountEvents` and `enableTransactionEvents` are enabled. Supported locales are listed. Persistent volume is enabled. Redis configuration for transaction processing is enabled with a clustered host (AWS ElastiCache). Database URL (AWS RDS), `useAwsSecretManager`, and `awsAccountId` are specified. `awsPrivateCAIssuer` is enabled and trusted. The content remained identical across both timestamps.

### Patterns and Recurring Elements:

*   **Helm Templating:** Extensive use of Helm templating (`{{- if ... }}` `{{- range ... }}` `{{- end }}`) is evident across `.yaml` files, allowing for dynamic configuration based on `.Values` parameters like `Cluster`, `Environment`, and `Version`.
*   **AWS Private CA Integration:** A dominant pattern is the consistent enabling and trusting of `awsPrivateCAIssuer` across almost all environment-specific `values` files (`trusted: true`, `enabled: true`, `used: true`). This indicates a standardized approach to using AWS Private CA for internal TLS/SSL certificate management within the Tink backend ecosystem.
*   **Service-Oriented Architecture:** Configurations frequently define endpoints for various internal services (e.g., `identity-aggregator-headless`, `credentials-headless`, `auth-headless`, `event-producer-service`, `user2-headless`), suggesting a microservices architecture where services communicate via cluster-internal DNS.
*   **Database and Caching:** Recurring themes include the configuration of database connections (AWS RDS for MySQL, Cassandra seeds) and caching solutions (AWS ElastiCache for Redis/Memcached), with hostnames dynamically generated or explicitly specified for each environment.
*   **Feature Flags:** Numerous feature flags are defined across different `values` files, controlling the enablement of various functionalities such as `enableAccountEvents`, `enableTransactionEvents`, `accountVerificationEnabled`, `balanceCheckEnabled`, and others, allowing for granular control over feature rollout per environment.
*   **Security Contexts:** The `delete-accounts-and-transactions-job.yaml` explicitly sets a strict security context for containers, emphasizing `runAsNonRoot`, specific user/group IDs, and `readOnlyRootFilesystem`, indicating a focus on container security best practices.
*   **Vault Integration:** The `config.yaml` template shows placeholders for database credentials to be fetched from Vault (`<{- $secret := vault "..." }>`, `<{ $secret.Data.username }>`), indicating a robust secret management strategy.
*   **Environment-Specific Overrides:** The structure of `/charts/<service>/values/<environment>.yaml` clearly demonstrates the pattern of overriding default chart values with environment-specific configurations for different clusters (e.g., `oxford-production-product001`, `jersey-production`, `barnsley-production`, `farnham-production`, `oxford-staging`).
*   **Typo Correction:** A minor but notable change involved correcting a typo (`ftruee` to `true`) in `awsPrivateCAIssuer.used` within `account-processing/values/barnsley-production.yaml`.

## 11:45:14 AM
The provided log details a `values.yaml` configuration file for the `transaction-processing-service` Helm chart, captured at `4/9/2026, 1:13:46 PM`.

**File-Specific Updates and Configuration Highlights:**

*   **Security & Secrets:** The service is configured to `useVault: true` for secrets management, with `useSecretsManager: false`.
*   **Environment & Resources:** It targets the `springProfile: staging`. Resource limits and requests are set uniformly to `cpu: 2000m` and `memory: 4Gi`. The Horizontal Pod Autoscaler (HPA) is configured for a CPU target of `1200m` (60% of requests) and `requestsPerSecond: 100`, allowing between `4` and `25` replicas.
*   **Observability:** Datadog integration is generally `disabled` for basic monitoring, log injection, and profiling. However, comprehensive `monitors` and `k8sMonitors` are `enabled` for various critical conditions (CPU throttling, low replicas, deployment unavailability, crash loops, OOMKilled, container restarts), all configured to notify `@slack-Tink-squad-transactions-oncall`.
*   **Core Processing Logic:** The `transaction-processing-service` is set up with `10` processing workers and a `workersMaxQueueSize` of `200`. Transaction reconciliation is enabled, utilizing `processing-reconciliation-output-transit` S3 bucket and `reconciliation-output` SNS topic, with `snsFlowEnabled: true` and a specific callback URL.
*   **Caching & Categorization:** `categoryCache` is `enabled` with a 60-minute TTL and a max size of 10,000. Basic `categorization` is enabled, but advanced features like MX category mapping, user learning, and BFM are explicitly `false`.
*   **Database & Distributed Systems:** The database connection pool size is `20`, with Flyway migrations `disabled`. Cassandra is configured as the `distributedDatabase` with 3 seed nodes, using TLS and a custom driver; a `privateCA` for Cassandra is enabled but not marked as `used`. For Kafka, `awsPrivateCAIssuer: used: true` is set, though its direct `privateCA` is `disabled`.
*   **Feature Toggles:** Unleash is `enabled: "true"` for feature toggling, with its API and fetch interval configured.
*   **Performance & SLOs:** Detailed Service Level Objectives (SLOs) are defined for both availability (99.9% over 30d) and latency (99th percentile at 0.1s over 30d). Critical endpoints like `/ingest/task`, `/process/transactions/update`, and `/process/transactions/callback` have stricter overrides for availability (99.99%) and specific latency targets.
*   **Disabled Features:** Several features are explicitly `disabled` in this configuration, including Redis, Fasttext integration, and canary deployments (`phase: DISABLED`) due to HPA pointing to the deployment directly.

**Patterns and Recurring Elements:**

*   **Extensive Use of Boolean Flags:** A dominant pattern is the frequent use of `enabled: true/false` flags to toggle various functionalities, integrations (Datadog, Redis, Fasttext, Unleash), and specific sub-features (e.g., within categorization, private CAs).
*   **Detailed Resource and Scaling Definitions:** Consistent and detailed configuration for `resources` (limits/requests), `hpa` (targets, min/max replicas, stabilization windows), and `podDisruptionBudget`.
*   **Monitoring and Alerting Structure:** A standardized approach to defining alerts with `alertThreshold`, `alertNotify` (consistently `@slack-Tink-squad-transactions-oncall`), and `priority` for various operational events.
*   **Service-Specific Configuration Blocks:** Many parameters are nested under the `service: transaction-processing-service` block, demonstrating granular control over the application's unique operational aspects.
*   **Reconciliation & Callback Mechanisms:** Recurring configuration for S3 and SNS, indicating a common pattern for asynchronous processing and external communication within the service.

## 12:45:11 PM
The `values.yaml` file for the `transaction-processing-service` Kubernetes chart, last updated on 4/9/2026, 1:13:46 PM, details an extensive set of configurations primarily for a `staging` Spring profile environment.

**File-Specific Updates (`transaction-processing-service/values.yaml`):**

The file defines a wide range of operational and application-specific settings, organized into distinct sections:

*   **Security & Secrets Management:** Configures `useVault: true` for secret management, disables `useSecretsManager`, and specifies paths for `keystore` and `truststore`. It also sets `sslMode=VERIFY_IDENTITY` for database connections and enables TLS for Cassandra with `useCustomDriver: true`, although `cassandra.privateCA.used` is false, `kafka.privateCA.enabled` is false but `awsPrivateCAIssuer.used` is true.
*   **Observability & Monitoring:** While `datadog.enabled` is false, comprehensive `monitors` and `k8sMonitors` are enabled. Various alerts are configured for CPU throttling, low deployment replicas, deployment unavailability, out-of-date deployments, pod crash loop backoffs, OOMKilled containers, and container restarts, all notifying `@slack-Tink-squad-transactions-oncall`. Global Datadog settings include `team`, `service`, `namespace`, `teamRoleId`, and `baseUrl`.
*   **Application Configuration:**
    *   Sets `jvmRAMPercentage` to "70.0", `server.workerThreads` to 200, and `lifecycle` timeouts.
    *   Enables `categoryCache` (60 min TTL, 10000 max size) and `categorization` (with specific sub-features like user learning and MX category mapping disabled).
    *   Defines `processing` workers (10), queue size (200), and lookback periods.
    *   Many feature flags are present, mostly disabled, controlling reconciliation logic, logging, and specific provider interactions.
    *   Integrates with S3 (`reconciliationS3BucketName`) and SNS (`reconciliationSNSTopicName`, `snsFlowCallbackUrl`) for reconciliation output.
    *   `redisConfiguration` is disabled.
    *   `unleash` feature toggles are enabled, configured with an API endpoint and a fetch interval.
    *   `streamFetcherMaxBytes`, `workers`, `firehoseEnabled`, and `rateLimitEnabled` are set.
    *   `fasttext` is explicitly disabled.
*   **Deployment & Scaling:**
    *   `rollingUpdate` strategy is defined with `maxSurge` "50%" and `maxUnavailable` "20%".
    *   Resource `limits` and `requests` are set to `cpu: 2000m` and `memory: 4Gi`.
    *   Horizontal Pod Autoscaler (HPA) targets `cpuUsage: 1200m` (60%) and `requestsPerSecond: 100`, with `podMinReplicas: 4` and `podMaxReplicas: 25`. Scaling stabilization windows are also configured.
    *   `podDisruptionBudget` allows "20%" `maxUnavailable`.
    *   `canaryDeployments` are explicitly `DISABLED`.
*   **Database Configuration:**
    *   `database.maxPoolSize` is 20, `runFlywayMigrations` is false, and `alwaysUseJdbi` is true.
    *   `distributedDatabase` uses Cassandra, specifying 3 `seedNodeCount` and connection request limits.
*   **Service Level Objectives (SLO):**
    *   Defines `healthcheckEndpoints` (`/health/liveness`, `/health/readiness`).
    *   Sets global targets for `availability` (99.9% over 30 days) and `latency` (99% percentile over 30 days, 0.1s external target).
    *   Includes specific `endpointOverrides` for `POST /ingest/task`, `POST /process/transactions/update`, and `POST /process/transactions/callback`, generally aiming for higher availability (99.99%) and a slightly higher latency target (0.2s for some, 0.1s for callback).

**Patterns and Recurring Elements:**

*   **Feature Flagging:** Extensive use of boolean `enabled` flags across various features like `observability`, `categoryCache`, `categorization`, `redisConfiguration`, `unleash`, `firehoseEnabled`, `rateLimitEnabled`, and `fasttext`, indicating a highly configurable application.
*   **Kubernetes-Native Configuration:** Strong emphasis on Kubernetes-specific settings, including deployment strategies, resource requests/limits, HPA, PDB, and integrated monitoring.
*   **External Service Integration:** The service integrates with multiple external systems for functionality (Cassandra, Redis, S3, SNS, Kafka) and observability/management (Vault, Unleash, Datadog).
*   **Performance and Resilience:** Configurations like JVM RAM percentage, worker threads, HPA targets, rolling update parameters, and database connection pools highlight a focus on application performance and resilience.
*   **Staging Environment Focus:** The `springProfile: staging` indicates that these values are tailored for a non-production environment, potentially influencing the enabling/disabling of certain features or resource allocations.