# Activity Summary for 4/15/2026

## 10:42:19 AM
The provided log entry details a comprehensive configuration update for the `transaction-processing-service` application, captured in its Helm `values.yaml` file on April 9, 2026, at 1:13:46 PM.

The update introduces or modifies a wide array of settings across various operational aspects:

*   **Security & Secrets Management:** `useVault` is enabled, while `useSecretsManager` is disabled. Configurations for keystore and truststore paths (`data/security/production.keystore`, `/var/lib/kubernetes/tink-internal-ca.jks`) are specified, alongside `sslMode=VERIFY_IDENTITY` for database connections and TLS settings for Cassandra (`useTls: true`, `useCustomDriver: true`, `privateCA: enabled: true, used: false`) and Kafka (`privateCA: enabled: false, used: false`, `awsPrivateCAIssuer: used: true`).
*   **Observability & Monitoring:** Direct Datadog integration for `observability` is `disabled`, as is `logInjection` and `profiling`. However, `monitors` and `k8sMonitors` are explicitly `enabled`, featuring detailed alerts for `cpuThrottledSeconds`, `serviceDeploymentReplicasLow`, `deploymentCompletelyUnavailable`, `podCrashLoopBackoff`, `podContainerOOMKilled`, and `containerRestartsOnDeployment`, all configured to notify `@slack-Tink-squad-transactions-oncall`.
*   **Service Level Objectives (SLOs):** Specific SLOs are defined with `healthcheckEndpoints`, availability targets (99.9% over 30 days), and latency targets (99th percentile, 0.1s external target) for general traffic. Overrides provide stricter latency (95th percentile, 0.2s) and availability (99.99%) targets for critical `POST /ingest/task`, `POST /process/transactions/update`, and `POST /process/transactions/callback` endpoints.
*   **Application Configuration:** The `springProfile` is set to `staging`, `jvmRAMPercentage` to "70.0", and `workerThreads` to `200`. Lifecycle timeouts (`timeoutPerShutdownPhase: "180s"`) and `preStopSleepSeconds: 15` are defined.
*   **Deployment & Scaling:** A `rollingUpdate` strategy is configured with `maxSurge: "50%"` and `maxUnavailable: "20%"`. Resource limits and requests are uniform at `cpu: 2000m` and `memory: 4Gi`. The Horizontal Pod Autoscaler (HPA) is configured with `cpuUsage: 1200m`, `requestsPerSecond: 100`, `podMinReplicas: 4`, `podMaxReplicas: 25`, and specific `scaleUp/DownStabilizationWindowSeconds`. `canaryDeployments` are `DISABLED`.
*   **Transaction Processing Specifics:**
    *   `processingWorkers` are set to `10` with a `workersMaxQueueSize` of `200`.
    *   `monthsForProcessing: 12` and `defaultCertainDayLookBack: 30`.
    *   Reconciliation mechanisms include `reconciliationS3BucketName: 'processing-reconciliation-output-transit'`, `reconciliationSNSTopicName: 'reconciliation-output'`, `snsFlowCallbackUrl` pointing to the service itself, and `snsFlowEnabled: true`.
    *   `takeProcessingLockForTransactionIngestion: true` is enabled.
*   **Feature Flags & Components:**
    *   `categoryCache` is `enabled` with `ttlMinutes: 60` and `maxSize: 10000`.
    *   `categorization` is `enabled`, but `mxCategoryMappingEnabled`, `userLearningEnabled`, `bfmUserLearningEnabled`, and `bfmEnabled` are `false`.
    *   `redisConfiguration` is `disabled`.
    *   `authService` is `enabled`.
    *   `unleash` is `enabled`, configured with an API endpoint and a `fetchFeatureToggleInterval`.
    *   `fasttext` is `disabled`.
*   **Database Configuration:** `maxPoolSize: 20`, `runFlywayMigrations: false`, and `alwaysUseJdbi: true`. Distributed database settings for Cassandra include `datacenter: Cassandra`, `seedNodeCount: 3`, and specific `maxRequestsPerConnection` limits.
*   **Global Settings:** Team, service, namespace, and a `teamRoleId` are defined, along with a `baseUrl` for Datadog.

A recurring pattern in this configuration is the extensive use of `enabled: true/false` flags to control the activation of various features and integrations, indicating a highly modular and configurable service architecture. There's also a strong emphasis on detailed performance and reliability metrics through HPA, PDB, and specific SLO targets for key API endpoints.

## 11:42:48 AM
The changes reflect a significant evolution in the `transaction-service`, focusing on data access strategies, caching, deployment configurations, and data lifecycle management.

**File-specific updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml` (4/7/2026, 1:41:10 PM):** This Kubernetes deployment template for the reconciliation worker received comprehensive updates. It now includes enhanced Datadog APM integration (with tags like `team: transactions`), Vault integration for secret management via an `initContainer` (vaultify), and a conditional `tags-updater` sidecar container. Resource requests and limits are defined for all containers, along with liveness and readiness probes. Volume mounts are configured to support various configurations (AWS Private CA, `ca-trust-bundle`, reconciliation config, Datadog agent). Pod anti-affinity is configured for non-local clusters.

*   **`/Users/mmuruts/projects/tink/transaction-service/redis/client.go` (4/7/2026, 1:44:15 PM and 4/7/2026, 1:45:43 PM and 4/7/2026, 2:33:18 PM):** This Go file defines a Redis client (`SearchResultCacheClient`) for caching search results and user profiles. The initial update (1:44:15 PM) introduced `GetUserProfile` and `CacheUserProfile` functions, along with a `userProfileCacheKeyPrefix` constant, encapsulated within a `/* START GENAI */` block. A subsequent change (1:45:43 PM) reordered the `userProfileCacheKeyPrefix` constant and the `CacheUserProfile`/`GetUserProfile` functions, moving the constant outside the `GENAI` block, only for it to be reverted back inside the block in a later commit (2:33:18 PM). The client uses `hashstructure` for key generation, `proto.Marshal` for serialization, and integrates with DataDog tracing. It supports TLS, configurable TTL, and a minimum number of transactions for caching.

*   **`/Users/mmuruts/projects/tink/transaction-service/interfaces.go` (4/7/2026, 1:48:13 PM and 4/7/2026, 2:35:48 PM):** The `SearchResultCacheClient` interface was expanded (1:48:13 PM) to include methods for caching user profiles (`GetUserProfile`, `CacheUserProfile`) and pagination state (`CachePaginationState`, `GetPaginationState`). A `UserProfileCache` struct was also introduced. Later (2:35:48 PM), the pagination state caching methods were removed from the `SearchResultCacheClient` interface, streamlining its responsibilities.

*   **`/Users/mmuruts/projects/tink/transaction-service/test/mock/cache.go` (4/7/2026, 1:50:13 PM, 4/7/2026, 1:51:05 PM, 4/7/2026, 2:34:17 PM):** Mock implementations for the cache client (`ProtoMessageCacheClient`) were updated to reflect the evolving `SearchResultCacheClient` interface. The 1:50:13 PM update added `GetUserProfileFn` and `CacheUserProfileFn` to the mock struct and implemented the corresponding mock methods. A minor update (1:51:05 PM) added an `internal_proto` import. Subsequent changes (2:34:17 PM) kept the `GetUserProfile` and `CacheUserProfile` mock methods.

*   **`/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server.go` (4/7/2026, 1:55:37 PM, 4/7/2026, 1:56:03 PM, 4/7/2026, 2:27:39 PM, 4/7/2026, 2:31:30 PM):** This internal gRPC server experienced frequent structural changes.
    *   Initially (1:55:37 PM), it introduced a `cassandraRepository` interface, configured `Config.ProviderMarkets` from environment variables, and added `compareTransactionAsync`/`compareTransactionListAsync` for asynchronous comparison between Elasticsearch (ES) and Cassandra responses (controlled by feature toggles), suggesting a data migration or validation effort. The `cacheClient` was *removed* from the server struct.
    *   Almost immediately (1:56:03 PM), the `cacheClient` was *re-added* to the `InternalTransactionsServiceServerImpl` struct and its constructor. This pattern of adding and removing `cacheClient` from the `InternalTransactionsServiceServerImpl` and its constructor recurred at 2:27:39 PM (removed) and 2:31:30 PM (re-added), indicating ongoing refactoring or design iteration around caching responsibilities within the server.

*   **`/Users/mmuruts/projects/tink/transaction-service/internal/commands/es_service_cmd/esservice.go` (4/7/2026, 1:57:11 PM):** The main command for the Elasticsearch service was significantly updated. It now redirects gRPC internal logs to a custom JSON logger and incorporates a new `common.SecretsLoader` to retrieve `PAGE_TOKEN_HMAC_KEY` from AWS Secrets Manager (with K8s fallback), centralizing secret handling. It also includes conditional initialization for a `redis.SearchResultCacheClient` based on a feature flag, and sets up Kafka listeners for user and account deletion events with DLQ (Dead Letter Queue) support.

*   **`/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server_test.go` (4/7/2026, 2:06:19 PM, 4/7/2026, 2:10:04 PM, 4/7/2026, 2:13:00 PM, 4/7/2026, 2:15:56 PM, 4/7/2026, 2:59:19 PM):** This test file for the internal gRPC server saw multiple iterations.
    *   The `mockCassandraRepo` was expanded to mock various Cassandra V1 and V2 methods.
    *   A `mockTransactionCache` was introduced, initially with comprehensive mock functions for various caching operations (2:06:19 PM).
    *   A merge conflict (2:10:04 PM) highlighted concurrent work, specifically on extending `mockCassandraRepo` and the full definition of `mockTransactionCache` including user profile caching.
    *   After conflict resolution (2:13:00 PM and 2:15:56 PM), `mockTransactionCache` was simplified, removing some general caching mocks. The test setup also showed `NewInternalTransactionsServiceServer` being called *without* a `cacheClient`.
    *   Finally (2:59:19 PM), `mockTransactionCache` was significantly pared down to primarily mock user profile caching, with basic mock implementations for `GetSearchResult` and `CacheSearchResult`. The `NewInternalTransactionsServiceServer` call in the tests was updated to include a `cacheClient` argument (aligning with the 2:31:30 PM change in `internal_server.go`).

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values.yaml` (4/9/2026, 11:07:44 AM):** This Helm `values.yaml` file for the main transaction service introduced several new configurations: `internalV2Enabled: true`, `redis` client settings (disabled by default, with `ttl` and `minResponseTransactionsNumber`), `userDeletionListener` and `accountDeletionListener` settings for Kafka-based deletion, a `dataretention` service configuration, `providerMarkets: "GB,UK"`, `awsPrivateCAIssuer: used: true`, and detailed Service Level Objective (SLO) configurations for availability and latency targets.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml` (4/9/2026, 11:08:30 AM):** This `values.yaml` for the reconciliation worker also saw `internalV2Enabled: true`, `redis` settings, `dataretention` configuration, `providerMarkets`, and `awsPrivateCAIssuer`. Additionally, `tagsUpdater` settings were introduced, and Datadog monitors for namespace CPU/memory quota exceeded alerts were added. The `reconciliation` section was updated with a `firehoseTransactionsTopic` and `sqs` batch size/shutdown timeout adjustments.

*   **`/Users/mmuruts/projects/tink/transaction-service/tnx_1` (4/13/2026, 8:59:27 AM):** This is a JSON file containing sample transaction data, likely used for testing or demonstration purposes. It includes multiple transactions with detailed information such as IDs, amounts (in EUR), descriptions, dates, status, and counterparties.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values/barnsley-production.yaml` (4/13/2026, 12:05:52 PM):** This environment-specific `values.yaml` for "barnsley-production" overrides several default settings. Notably, it enables user service (`userSvc: true`), enables and disables dry run for reconciliation (`reconciliation: enabled: true, dryRun: false`), increases the Elasticsearch index shards (`common: nshards: 50`), and sets `canaryDeployments: phase: COMPLETE`, indicating that canary deployments are fully active in this production environment.

**Patterns and Recurring Elements:**

*   **Caching Integration:** A consistent pattern is the introduction and refinement of Redis caching. This involved defining a new `UserProfileCache` and corresponding methods in the `SearchResultCacheClient` interface, implementing them in `redis/client.go`, and creating mock versions for testing. The frequent changes to `internal_server.go` regarding the `cacheClient` argument suggest an iterative process of integrating and refining the caching layer.
*   **Elasticsearch and Cassandra Coexistence/Comparison:** Several changes in `internal_server.go` and its tests point to a strategy of running Elasticsearch and Cassandra in parallel, especially for V2 API requests, with asynchronous comparison to ensure data consistency or facilitate migration.
*   **Kubernetes Configuration Evolution:** Helm chart values across both the main service and the reconciliation worker are continuously updated to support new features like Redis, data retention, deletion listeners, and enhanced monitoring (Datadog, SLOs). Environment-specific overrides demonstrate fine-grained control over deployments, with "barnsley-production" explicitly enabling canary deployments.
*   **Secret Management Centralization:** The adoption of `common.SecretsLoader` in `esservice.go` alongside existing Vault integration in `deployment.yaml` points to a more robust and centralized approach to handling application secrets.
*   **Feature Flag Driven Development:** The use of `featuretoggle` throughout the code base (e.g., for ES/Cassandra comparison, fetching from Cassandra for V2 API) indicates a strategy for safely rolling out new features and changing data access patterns.
*   **"GENAI" Comments:** Multiple sections of Go code are explicitly marked with `/* START GENAI */` and `/* END GENAI */` comments. This consistent annotation pattern suggests that these code blocks were either generated by an AI assistant or are specific areas designated for AI-assisted development or review.