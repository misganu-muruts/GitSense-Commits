# Activity Summary for 4/1/2026

## 9:38:40 AM
**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**

*   **Initial State (3/27/2026, 11:24:34 AM and 11:26:02 AM):** This Kubernetes Deployment manifest configures the `transaction-service-reconciliation-worker`. Key features include:
    *   **Conditional Deployment:** The entire deployment is enabled only if `config.reconciliation.enabled` is true.
    *   **Datadog Integration:** Extensive Datadog APM setup, including labels (`tags.datadoghq.com/env`, `/service`, `/version`), annotations (`admission.datadoghq.com/enabled`, `ad.datadoghq.com/tags`), and environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`).
    *   **Security Contexts:** Enforces `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, `fsGroup: 65535`, and `readOnlyRootFilesystem: true` for most containers.
    *   **Vault Integration:** An optional `initContainer` named `fetch-credentials` uses the `vaultify` tool to retrieve `reconciliation-config.yaml` and secrets, conditional on `.Values.useVault`.
    *   **Containers:**
        *   `transaction-service-reconciliation-worker`: The main worker, configured with resource requests/limits (`podMemoryRequest`, `podCpuRequest`), pulls an image from ECR, and runs as `recon-worker`. It mounts various volumes including `ca-trust-bundle` and a certificate (either `grpc-certificate-aws` or a custom `certificateName`), along with reconciliation configuration.
        *   `transaction-service-tags-updater`: An optional container (`tagsUpdater.enabled`) running as `tags-consumer` with separate resources and listening on a different port (8181), but sharing similar secret references and volume mounts.
        *   `renew-leases`: An optional container (`useVault`) for Vault lease renewal, using `vaultify`.
    *   **Environment Variables:** Many are configured, with sensitive values fetched from Kubernetes Secrets (`PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`, `FEATURE_TOGGLE_API_KEY`) and others directly from Helm values.
    *   **Probes:** Standard HTTP liveness and readiness probes are defined.
    *   **Volumes:** Includes secrets for certificates, a ConfigMap for `ca-trust-bundle`, a ConfigMap for `reconciliation-worker-configuration`, an `emptyDir` for credentials, and an optional `hostPath` for the `datadog-agent`.
    *   **Affinity:** Pod anti-affinity is configured for non-local clusters to ensure high availability.
    *   No functional changes were observed between the two timestamps on March 27, 2026.

*   **Update (3/31/2026, 11:24:58 AM):** A minor, likely accidental, structural change was introduced in the `volumeMounts` section for the `transaction-service-reconciliation-worker` container and the `volumes` section at the deployment level. The conditional `if/else` block for selecting the certificate volume (`grpc-certificate-aws` or `{{ .Values.certificateName }}`) was duplicated, resulting in nested `if/else` logic that, while functionally equivalent in its outcome, adds redundancy to the manifest.

**File: `/Users/mmuruts/projects/tink/transaction-service/templates/hpa.yaml`**

*   **Update (3/27/2026, 11:58:17 AM):** This file defines a Horizontal Pod Autoscaler (HPA) for the `transaction-service`.
    *   **Dynamic Target Ref:** It dynamically targets either a Kubernetes `Rollout` or a `Deployment` based on the `.Values.canaryDeployments.phase`, allowing for HPA control over canary deployments.
    *   **Scaling Parameters:** Configured with `minReplicas` (default 1) and `maxReplicas` (default 3), with a `scaleDown` stabilization window of 600 seconds and a policy to remove 1 pod every 60 seconds.
    *   **Metrics:** Scales based on external Datadog metrics for CPU and Memory usage.
        *   `cpu-usage` targets an `averageValue` defined by `.Values.podTargetCPU`.
        *   `memory-usage` targets a fixed `value` of 250Mi.
    *   **DatadogMetric Definitions:** Includes `DatadogMetric` custom resource definitions for `cpu-usage` and `memory-usage`, specifying Datadog queries that aggregate Kubernetes CPU and container memory usage across `transaction-service` and `renew-leases` containers, filtering by `kube_cluster_name` and `kube_deployment`.

**File: `/Users/mmuruts/projects/tink/transaction-service/values/oxford-staging-product001.yaml`**

*   **Update (3/31/2026, 11:16:49 AM):** This Helm values file provides environment-specific configurations for the `transaction-service` in the `oxford-staging-product001` cluster.
    *   **General Configuration:** Sets `logLevel: "debug"`, enables multi-Elasticsearch clusters, SNS, and defines a Kafka topic prefix.
    *   **Elasticsearch:** Configures two Elasticsearch clusters (`common` and `nordea`), each with distinct hosts, sync behavior, index year ranges, shard counts (10), and other settings. The `nordea` cluster is associated with specific `appIds`.
    *   **Service Endpoints:** Specifies `server` HTTPS listen port, Cassandra hosts, `userSvc` address, and `redis` cache hosts.
    *   **Feature Toggles:** Enables feature toggles.
    *   **Module Enablement:** `dataretention` and `reconciliation` are enabled but `dryRun: false`. `userSvc`, `accountSvc`, and `redis` are enabled.
    *   **Resource Allocation:** Sets explicit resource requests for pods (`podMemoryRequest: "2Gi"`, `podCpuRequest: "1.25"`), HPA target CPU (`podTargetCPU: "0.6"`), and replica counts (`reconWorkerMinReplicas`, `reconWorkerMaxReplicas`, `podMinReplicas`, `podMaxReplicas`).
    *   **Canary Deployments:** The `canaryDeployments` phase is set to `COMPLETE`, indicating a mature rollout strategy.
    *   **AWS Integration:** Specifies `awsAccountId` and enables `awsPrivateCAIssuer`.

**Patterns and Recurring Elements:**

*   **Helm Templating:** Extensive use of `{{- if .Values.property }}` for conditional configuration, allowing for highly customizable deployments based on environment-specific `values.yaml` files.
*   **Datadog Observability:** Consistent integration of Datadog for APM and metrics across deployment manifests and HPA configurations, reflecting a strong emphasis on monitoring.
*   **Container Image Sourcing:** All container images are sourced from a specific AWS ECR registry (`784217881480.dkr.ecr.eu-west-1.amazonaws.com`), indicating a standardized image management approach.
*   **Secret Management:** A recurring pattern of using Kubernetes `secretKeyRef` for sensitive environment variables and optional Vault integration via `vaultify` for dynamic secret injection.
*   **Security Best Practices:** Repeated application of `runAsNonRoot: true` and `readOnlyRootFilesystem: true` in container security contexts.
*   **Configuration Consistency:** Configuration for workers and consumers often references a common `reconciliation-config.yaml` file, managed either through Vault or a Kubernetes ConfigMap.
*   **Kubernetes Standard Practices:** Utilization of liveness/readiness probes, resource requests/limits, and affinity rules for robust and resilient deployments.

## 10:38:37 AM
The provided log details changes across Helm charts for a `transaction-service` application.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
    *   **Timestamp: 3/27/2026, 11:24:34 AM**: This entry outlines the initial configuration for the `transaction-service-reconciliation-worker` Kubernetes Deployment. Key features include:
        *   Deployment for a reconciliation worker with Datadog APM integration (labels, annotations, environment variables for APM).
        *   Security contexts configured with `runAsNonRoot`, specific `runAsUser`/`runAsGroup`/`fsGroup`, and `readOnlyRootFilesystem`.
        *   Conditional `initContainer` (`fetch-credentials` using `vaultify`) and a `renew-leases` sidecar container if Vault integration (`.Values.useVault`) is enabled.
        *   Primary `transaction-service-reconciliation-worker` container with resource requests/limits, image from AWS ECR, and arguments to run as `recon-worker`.
        *   Extensive environment variables, many sourcing values from Kubernetes Secrets (`PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`, `FEATURE_TOGGLE_API_KEY`).
        *   Optional `transaction-service-tags-updater` sidecar container if `.Values.tagsUpdater.enabled` is true, sharing many configurations with the main worker.
        *   Volume mounts for certificates (conditional on `awsPrivateCAIssuer.used`), CA bundles, configuration, and credentials (from Vault if enabled).
        *   Liveness and readiness probes defined for both main and tags-updater containers.
        *   Pod anti-affinity rules for non-local clusters.
    *   **Timestamp: 3/27/2026, 11:26:02 AM**: No functional changes were observed compared to the previous entry for this file. This appears to be a re-save or minor formatting adjustment without altering the logic.
    *   **Timestamp: 3/31/2026, 11:24:58 AM**: A change was introduced in the `volumeMounts` and `volumes` sections. Specifically, the conditional logic for mounting `/tls` certificates became nested and redundant. While functionally it still aims to use `grpc-certificate-aws` if `.Values.awsPrivateCAIssuer.used` is true, otherwise falling back to `{{ .Values.certificateName }}`, the Helm template syntax became more verbose and less clean due to a duplicated `if/else` block within an `else` clause.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`**
    *   **Timestamp: 3/27/2026, 11:58:17 AM**: This file defines a Horizontal Pod Autoscaler (HPA) for the `transaction-service`.
        *   The HPA dynamically targets either a `Rollout` (for canary deployments, if `canaryDeployments.phase` is "PARTIAL" or "COMPLETE") or a standard `Deployment`.
        *   `minReplicas` and `maxReplicas` are configured via `.Values`.
        *   Scale-down behavior includes a 10-minute `stabilizationWindowSeconds`.
        *   Scaling metrics are driven by DatadogMetrics for CPU usage (`kubernetes.cpu.usage.total`) and memory usage (`container.memory.usage`), including usage from a potential `renew-leases` container.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml`**
    *   **Timestamp: 3/31/2026, 11:16:49 AM**: This `values` file configures the `transaction-service` for the `oxford-staging-product001` environment. Key configurations include:
        *   Setting `logLevel` to "debug", enabling `MultiElasticSearchClusters`, `snsEnabled`, and defining `kafkaTopicPrefix`.
        *   Specifying Elasticsearch hosts for `common` and `nordea` clusters with detailed indexing and sniffing settings, along with Nordea-specific `appIds`.
        *   Configuring Cassandra hosts with TLS enabled, and enabling `dataretention` and `reconciliation` (both with `dryRun` set to false).
        *   Defining SQS batch size and max concurrency.
        *   Enabling `userSvc` and `accountSvc` with user service address and CA path.
        *   Enabling Redis with specific cache hosts and TTL.
        *   Enabling `featureToggles`.
        *   Setting resource requests/limits for worker pods (`podMemoryRequest`, `podCpuRequest`), and defining `reconWorkerMinReplicas`/`MaxReplicas` and `podMinReplicas`/`MaxReplicas`.
        *   Setting `podTargetCPU` for HPA.
        *   Setting `canaryDeployments.phase` to `COMPLETE`.
        *   Specifying `awsAccountId` and enabling `awsPrivateCAIssuer`.

**Patterns and Recurring Elements:**

*   **Datadog Integration:** A prominent recurring pattern is the extensive integration with Datadog for APM (`tags.datadoghq.com`, `DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`) and monitoring (HPA metrics from `datadoghq.com/v1alpha1 DatadogMetric` resources for CPU and memory).
*   **Conditional Feature Toggling:** Heavy use of Helm's conditional logic (`{{- if .Values... }}`) to enable or disable features like Datadog APM, Vault integration, a tags updater sidecar, feature toggles, and AWS Private CA Issuer based on `.Values` parameters.
*   **Kubernetes and Helm Focus:** All changes are within Helm charts, defining Kubernetes resources (Deployments, HPAs, DatadogMetrics) and their associated configuration values.
*   **AWS ECR:** Container images for `transaction-service` and `vaultify` are consistently sourced from `784217881480.dkr.ecr.eu-west-1.amazonaws.com`.
*   **Secret Management via Kubernetes and Vault:** Secrets like API keys and passwords are injected into containers either via Kubernetes `secretKeyRef` or through a `vaultify` init container and renew-leases sidecar for dynamic secret fetching and renewal.
*   **Service Account and Security Contexts:** Consistent application of `serviceAccountName: transaction-service` and strict security contexts for containers, including `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, and `readOnlyRootFilesystem: true`.
*   **Configuration via `ConfigMap` and `.Values`:** Application configuration is managed through `configMap` mounts or directly injected via Helm `.Values`.

## 11:38:41 AM
The provided log details changes within the `transaction-service` project, specifically concerning its Kubernetes Helm charts. The core components involved are a reconciliation worker, the main transaction service, and environment-specific configurations.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
    *   **Initial entries (3/27/2026, 11:24:34 AM and 3/27/2026, 11:26:02 AM):** These two entries are identical, defining the Kubernetes Deployment for the `transaction-service-reconciliation-worker`. The deployment includes standard configurations such as Datadog APM integration (labels and annotations), security contexts (`runAsNonRoot`, `readOnlyRootFilesystem`), and a configurable image sourced from AWS ECR. It features an optional Vault integration using an `initContainer` (`fetch-credentials`) to retrieve configurations and a `renew-leases` sidecar for managing Vault leases. The primary container (`recon-worker`) and an optional `tags-updater` container both define resource requests/limits, port mappings, liveness/readiness probes, and numerous environment variables, including sensitive ones sourced from Kubernetes Secrets (e.g., `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`). Volume mounts are configured for TLS certificates, CA bundles, and application configuration.
    *   **Later entry (3/31/2026, 11:24:58 AM):** This update introduced a redundant nested `if` statement within the `volumeMounts` section of the main container and the `volumes` section of the deployment. The original conditional logic to choose between `grpc-certificate-aws` or a generic `{{ .Values.certificateName }}` was enclosed in an identical `if/else` block, effectively adding unnecessary templating code without altering the deployment's functional behavior.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`**
    *   **Timestamp (3/27/2026, 11:58:17 AM):** This file defines a Horizontal Pod Autoscaler (HPA) using `autoscaling/v2` for the main `transaction-service`. It dynamically targets either a `Rollout` resource (for canary deployments in "PARTIAL" or "COMPLETE" phases) or a standard `Deployment`. The HPA is configured with minimum and maximum replicas (defaulting to 1 and 3, respectively, but overridden by values). Scaling down has a `stabilizationWindowSeconds` of 600 seconds and scales down by 1 pod every 60 seconds. The HPA uses external Datadog metrics for scaling:
        *   CPU usage, targeting an `AverageValue` from `datadogmetric@transaction:cpu-usage`.
        *   Memory usage, targeting a `Value` of `250Mi` from `datadogmetric@transaction:memory-usage`.
    The file also defines the corresponding `DatadogMetric` custom resources, detailing the queries to Datadog for CPU and memory usage across `transaction-service` and `renew-leases` containers.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml`**
    *   **Timestamp (3/31/2026, 11:16:49 AM):** This file provides specific configuration values for the `oxford-staging-product001` environment. Key configurations include:
        *   `logLevel: "debug"`, `enableMultiElasticSearchClusters: true`, `snsEnabled: true`.
        *   Kafka topic prefix, and detailed Elasticsearch configurations for `common` and `nordea` clusters (hosts, shard counts, indexing years, `appIds` for Nordea).
        *   HTTPs server listening on port 8443.
        *   Cassandra hosts with TLS enabled.
        *   `dataretention` and `reconciliation` features enabled but not in dry-run mode.
        *   SQS `batchSize` and `maxConcurrency`.
        *   User and Account services enabled with specific addresses.
        *   Redis caching enabled with TTL and host details.
        *   `featureToggles` enabled.
        *   Resource and replica settings for `reconWorker` (`minReplicas: 2`, `maxReplicas: 3`) and main pods (`minReplicas: 2`, `maxReplicas: 8`), including `podMemoryRequest`, `podCpuRequest`, and `podTargetCPU`.
        *   `canaryDeployments.phase` set to `COMPLETE`.
        *   AWS account ID, `serviceLevels: enabled: false`, and `awsPrivateCAIssuer: used: true`.

**Patterns and Recurring Elements:**

*   **Helm Templating:** All files heavily rely on Helm's templating syntax (`{{ .Values.someProperty }}`) to make configurations dynamic and environment-agnostic, often using `default` values.
*   **Datadog Integration:** There is a consistent pattern of integrating Datadog for APM and metrics. This includes specific labels, annotations, environment variables in deployments, and dedicated `DatadogMetric` resources for HPA scaling.
*   **Vault for Secrets:** The explicit use of `vaultify` containers (init and sidecar) and the `useVault` flag indicate a robust secret management strategy leveraging HashiCorp Vault.
*   **Microservices Architecture:** The presence of a "transaction service," a "reconciliation worker," a "tags updater," and external "userSvc" and "accountSvc" alongside various data stores (Cassandra, Elasticsearch, Redis) points to a distributed microservices architecture.
*   **Kubernetes Best Practices:** The configurations demonstrate adherence to Kubernetes best practices, including security contexts (`runAsNonRoot`, `readOnlyRootFilesystem`), liveness and readiness probes, rolling updates, service accounts, and pod anti-affinity for resilience.
*   **AWS ECR and Certificate Management:** Images are consistently sourced from AWS ECR, and there's a flexible approach to certificate management, conditionally using `awsPrivateCAIssuer` for TLS.
*   **Feature Toggles:** The explicit `FEATURE_TOGGLE_ENABLED` environment variable and `featureToggles` configuration in `values` indicate a system for enabling/disabling features during runtime, likely for controlled rollouts and experimentation.

## 12:43:00 PM
The `transaction-service-reconciliation-worker/templates/deployment.yaml` file, observed on `3/27/2026` at `11:24:34 AM` and `11:26:02 AM`, defines a Kubernetes Deployment for a reconciliation worker. This deployment includes conditional integration with Datadog APM for observability, and HashiCorp Vault via an `initContainer` for fetching credentials and a `renew-leases` sidecar for lease management. It specifies two main application containers (`transaction-service-reconciliation-worker` and a `transaction-service-tags-updater` if enabled), each with defined resource requests/limits, security contexts (`runAsNonRoot`, `readOnlyRootFilesystem`), and environment variables, many sourced from Kubernetes Secrets (e.g., `PAGE_TOKEN_HMAC_KEY`, `CASSANDRA_V2_PASSWORD`). A subsequent change to this file on `3/31/2026, 11:24:58 AM` introduced a redundant nested conditional block for mounting TLS certificates within the `volumeMounts` section of the main container and the `volumes` definition, effectively duplicating the logic for selecting between `grpc-certificate-aws` and `{{ .Values.certificateName }}` based on `awsPrivateCAIssuer.used`.

The `transaction-service/templates/hpa.yaml` file, changed on `3/27/2026, 11:58:17 AM`, outlines a Horizontal Pod Autoscaler (HPA) for the main `transaction-service`. This HPA dynamically scales based on whether canary deployments are in `PARTIAL` or `COMPLETE` phases, targeting either a `Rollout` or a `Deployment`. It configures a scale-down stabilization window of 600 seconds and uses external Datadog metrics (`cpu-usage` and `memory-usage`) for its scaling decisions. The DatadogMetric custom resources are also defined here, querying specific `kubernetes.cpu.usage.total` and `container.memory.usage` metrics, with the memory metric specifically summing usage for both the `transaction-service` and `renew-leases` containers.

The `transaction-service/values/oxford-staging-product001.yaml` file, updated on `3/31/2026, 11:16:49 AM`, specifies environment-specific configurations for the `oxford-staging-product001` cluster. This includes setting `logLevel` to "debug", enabling `snsEnabled` and `featureToggles`, and providing detailed connection information for critical services like Cassandra, Redis, User Service, and two Elasticsearch clusters ("common" and "nordea"), each with distinct hosts and configuration parameters. It also defines resource allocations (`podMemoryRequest`, `podCpuRequest`) and replica ranges (`podMinReplicas`, `podMaxReplicas`) for both the main service and the reconciliation worker, sets the `canaryDeployments.phase` to `COMPLETE`, and enables `awsPrivateCAIssuer.used`.

**Key patterns observed across the changes include:**

*   **Extensive use of Helm templating** for dynamic Kubernetes resource generation and conditional configuration based on `.Values`.
*   **Deep integration with Datadog** for both application monitoring (APM) and infrastructure metric-driven Horizontal Pod Autoscaling, indicated by Datadog-specific tags, environment variables, and custom metric definitions.
*   **Adherence to Kubernetes security best practices**, such as `runAsNonRoot`, `readOnlyRootFilesystem`, and specifying `serviceAccountName`.
*   **Reliance on Kubernetes Secrets** for sensitive data like API keys and passwords.
*   **Integration with AWS services** (ECR, Elasticsearch endpoints) and HashiCorp Vault for secure credential management.
*   **Microservice architecture** with distinct components like a main service, a reconciliation worker, and a tags updater.
*   **Environment-specific configurations** managed through dedicated Helm values files, illustrating how different deployment environments are customized.

## 1:42:55 PM
The log details changes across Kubernetes/Helm configuration files for a `transaction-service` and its `reconciliation-worker` component.

**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
*   **Timestamps:** 3/27/2026, 11:24:34 AM; 3/27/2026, 11:26:02 AM; 3/31/2026, 11:24:58 AM
*   The entries from 3/27/2026 appear identical, suggesting minor or non-functional saves.
*   A significant change occurred on 3/31/2026, 11:24:58 AM, specifically within the `transaction-service-reconciliation-worker` container's `volumeMounts` section. The conditional logic for mounting the TLS certificate was duplicated. Previously, it checked `.Values.awsPrivateCAIssuer.used` once; the updated version nested this check, leading to redundant `if/else` blocks and potentially unintended behavior if `.Values.awsPrivateCAIssuer.used` is false.
*   This file defines the Kubernetes Deployment for the `reconciliation-worker`. It includes an optional `vaultify` init container for credential fetching and a `renew-leases` sidecar for Vault lease management if `.Values.useVault` is enabled. It also configures resources, image details, environment variables (including feature toggles and secret references), and liveness/readiness probes. Datadog APM integration is conditional based on `.Values.datadog.apmEnabled`. An optional `tags-updater` container is included if `tagsUpdater.enabled` is true.

**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`**
*   **Timestamp:** 3/27/2026, 11:58:17 AM
*   This file configures the Horizontal Pod Autoscaler (HPA) for the `transaction-service`.
*   It dynamically targets either a `Rollout` or a `Deployment` based on `canaryDeployments.phase`.
*   Sets default `minReplicas` to 1 and `maxReplicas` to 3, with a 10-minute stabilization window for scale-down.
*   HPA metrics are driven by external Datadog metrics for CPU usage (`datadogmetric@transaction:cpu-usage`) and memory usage (`datadogmetric@transaction:memory-usage`), specifying target values.
*   The DatadogMetrics for CPU and memory are defined inline, querying `kubernetes.cpu.usage.total` and `container.memory.usage` (for both `transaction-service` and `renew-leases` containers) respectively.

**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml`**
*   **Timestamp:** 3/31/2026, 11:16:49 AM
*   This is a Helm values file specific to the `oxford-staging-product001` environment, providing configuration overrides.
*   It sets `logLevel` to "debug" and enables `snsEnabled`, `reconciliation`, `dataretention`, `userSvc`, `accountSvc`, `redis`, and `featureToggles`.
*   Detailed Elasticsearch hosts are configured for both `common` and `nordea` clusters, along with indexing parameters (e.g., `startIndexYear`, `endIndexYear`, `nshards`).
*   Cassandra hosts are specified with TLS enabled.
*   SQS `batchSize` and `maxConcurrency` are defined.
*   Redis hosts and TTL are set for the `transaction-service-cache`.
*   Resource requests (`podMemoryRequest: "2Gi"`, `podCpuRequest: "1.25"`) and HPA parameters (`podTargetCPU: "0.6"`, `podMinReplicas: 2`, `podMaxReplicas: 8`) are explicitly defined.
*   `canaryDeployments.phase` is set to `COMPLETE`.
*   `awsPrivateCAIssuer.used` is set to `true`, indicating the use of AWS Private CA for certificates.

**Patterns and Recurring Elements:**
*   **Helm templating:** Extensive use of Helm's conditional logic (`if/else`) and value interpolation (`.Values`, `.Files.Get`) for dynamic configuration.
*   **Datadog Integration:** Consistent configuration for Datadog APM (`tags.datadoghq.com`, `admission.datadoghq.com`) across deployments and the use of DatadogMetrics for horizontal pod autoscaling.
*   **Vault Integration:** The presence of `vaultify` init containers and `renew-leases` sidecars, conditioned on `.Values.useVault`, indicates a standardized approach to secret management using Vault.
*   **Security Contexts:** Recurring application of `runAsNonRoot`, `runAsUser`, `runAsGroup`, `fsGroup`, and `readOnlyRootFilesystem` for security hardening of containers.
*   **ECR Image Source:** All container images consistently originate from the same AWS ECR registry (`784217881480.dkr.ecr.eu-west-1.amazonaws.com`).
*   **Secret References:** Frequent use of `secretKeyRef` to inject sensitive information (API keys, HMAC keys, database passwords) from Kubernetes secrets into environment variables.
*   **Service Configurations:** Common configurations include Elasticsearch, Cassandra, SQS, Redis, and internal service addresses (e.g., `user2-headless.user.svc.cluster.local`).

## 2:42:58 PM
The `transaction-service-reconciliation-worker/templates/deployment.yaml` file experienced two updates on March 27, 2026, at 11:24:34 AM and 11:26:02 AM. Both entries for this timestamp contain identical Helm template code. This Kubernetes Deployment manifest defines the reconciliation worker, including details like:
*   Standard labels, annotations, and a `RollingUpdate` strategy.
*   Security context settings (`runAsNonRoot: true`, `runAsUser: 65535`, `readOnlyRootFilesystem: true`).
*   Conditional `initContainers` and `renew-leases` sidecar container based on `useVault`, utilizing `vaultify:0.4.2` for credential fetching and renewal.
*   The main `transaction-service-reconciliation-worker` container running `/bin/transaction-service recon-worker`.
*   Extensive environment variables, many pulled from Kubernetes secrets (e.g., `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`, `FEATURE_TOGGLE_API_KEY`).
*   Datadog APM configuration (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`) and associated volume mounts.
*   Conditional inclusion of a `transaction-service-tags-updater` container, running `/bin/transaction-service tags-consumer`.
*   Liveness and readiness probes for all containers.
*   Volume definitions for certificates (either AWS Private CA or generic), CA trust bundle, configuration, and credentials.
*   Pod anti-affinity rules, unless the cluster is "local".

On March 27, 2026, at 11:58:17 AM, the `transaction-service/templates/hpa.yaml` file was updated. This Horizontal Pod Autoscaler (HPA) targets the `transaction-service`, dynamically scaling it between 1 and 3 pods by default, or values specified by `.Values.podMinReplicas` and `.Values.podMaxReplicas`. It uses a 10-minute stabilization window for scale-down. The HPA utilizes external Datadog metrics for scaling decisions:
*   CPU usage, targeting an `averageValue` defined by `.Values.podTargetCPU`.
*   Memory usage, targeting a `value` of 250Mi.
The corresponding `DatadogMetric` definitions are included, querying `kubernetes.cpu.usage.total` and `container.memory.usage` for the `transaction-service` deployment (including `renew-leases` container). The `scaleTargetRef` dynamically switches between `Rollout` (for Canary deployments in "PARTIAL" or "COMPLETE" phases) and `Deployment`.

On March 31, 2026, at 11:16:49 AM, the `transaction-service/values/oxford-staging-product001.yaml` file was modified. This values file provides environment-specific configurations for the `transaction-service`, including:
*   `logLevel: "debug"`, `enableMultiElasticSearchClusters: true`, `snsEnabled: true`, and a `kafkaTopicPrefix`.
*   Detailed Elasticsearch configurations for "common" and "nordea" clusters, specifying `hosts`, `syncbehaviour`, `startIndexYear`, `endIndexYear`, and `appIds`.
*   Cassandra connection details, SQS batching parameters, and Redis cache hosts.
*   Service addresses for `userSvc` and `accountSvc`.
*   Feature toggles enabled.
*   Resource requests and limits (`podMemoryRequest: "2Gi"`, `podCpuRequest: "1.25"`), and scaling parameters for both the main service and the reconciliation worker (`reconWorkerMinReplicas: 2`, `reconWorkerMaxReplicas: 3`).
*   Canary deployment phase set to `COMPLETE`.
*   `awsPrivateCAIssuer.used` is set to `true`.

Finally, on March 31, 2026, at 11:24:58 AM, the `transaction-service-reconciliation-worker/templates/deployment.yaml` file was updated again. This change introduced redundant conditional logic within the `volumeMounts` and `volumes` sections for mounting certificates. Specifically, the `{{- else }}` block now contains another full `{{- if .Values.awsPrivateCAIssuer.used }} ... {{- else }} ... {{- end }}` block, effectively duplicating the initial `if/else` condition for selecting between `grpc-certificate-aws` and a generic `certificateName`.

**Patterns and Recurring Elements:**
*   **Helm Templating:** All files heavily rely on Helm's templating language (`{{- if .Values.someValue }}` and `default`) for flexible configuration.
*   **Datadog Integration:** There's a strong emphasis on Datadog for monitoring and APM, evidenced by numerous annotations and HPA metrics.
*   **Vault Integration:** The consistent use of `vaultify` for secret management via `initContainers` and `renew-leases` sidecar is a recurring security pattern.
*   **AWS Services:** Image registries point to AWS ECR, and configurations include AWS-specific elements like `awsPrivateCAIssuer` and Elasticsearch hostnames within `amazonaws.com` domains.
*   **Security Hardening:** `readOnlyRootFilesystem: true` and `runAsNonRoot: true` are common security contexts applied to containers.
*   **Configurability via Values:** Many operational parameters, resource limits, and service endpoints are exposed as configurable values (`.Values.podMemoryRequest`, `.Values.Cluster`, etc.).
*   **Reconciliation Worker:** The "reconciliation worker" is a central component, with its deployment and specific scaling parameters configured.