# Activity Summary for 4/9/2026

## 10:35:52 AM
The `/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/delete-accounts-and-transactions-job.yaml` file was logged twice on April 9, 2026, at 9:55:49 AM and 9:56:10 AM, without any changes in its content between these timestamps.

This file defines a Kubernetes Job named `delete-accounts-and-transactions-job` within the `account` namespace. This job is designed to delete accounts and transactions, activated conditionally if the combined `Cluster-Environment` matches `jobs.deleteAccountDeleteJobEnabledEnv`.

Key features of this job definition include:
*   **Purpose:** Executes a shell script (`/scripts/delete-accounts-and-transactions.sh`) using the `tink-containers/net-tools:latest` image from AWS ECR.
*   **Lifecycle:** Configured with `ttlSecondsAfterFinished: 300` and `restartPolicy: "Never"`.
*   **Security Context:** Runs as non-root user (`65535`) with `readOnlyRootFilesystem: true`, enforcing a secure execution environment.
*   **Resource Allocation:** Requests and limits both CPU at 500m and memory at 1Gi.
*   **Volume Mounts:** Utilizes several volumes:
    *   `large-data` via a Persistent Volume Claim (`delete-accounts-pvc`).
    *   `scripts` from a ConfigMap, with its name dynamically set to `scripts-{{ .Chart.AppVersion }}` unless the cluster is "cornwall," in which case it defaults to `scripts`.
    *   `config-volume` from a ConfigMap named `configuration`.
    *   `tmp`, `credentials`, and `tink-internal-ca-truststore-volume` as `emptyDir` volumes, with `credentials` and `tink-internal-ca-truststore-volume` specifically using `medium: Memory`.
*   **Cluster-Specific Configuration:** Includes an `imagePullSecrets` entry (`ecr-regcred`) specifically for the "cornwall" cluster.

The recurring element is the identical configuration of this Kubernetes job, indicating a stable state of this particular template across the two logged timestamps.