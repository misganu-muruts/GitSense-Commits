# Activity Summary for 2/26/2026

## 9:38:55 AM
All changes are confined to the `/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/values.yaml` file.

The primary update across the timestamps revolves around the `awsPrivateCAIssuer` configuration.
- At 2/25/2026, 9:43:16 AM, `awsPrivateCAIssuer` was initially configured with `trusted: true`, `enabled: true`, and `used: false`.
- A subsequent change at 2/25/2026, 9:44:13 AM, modified this configuration, setting both `trusted` and `enabled` to `false`.
- Within a minute, at 2/25/2026, 9:44:48 AM, the `awsPrivateCAIssuer` settings were reverted to their original state, with `trusted: true` and `enabled: true`.

A clear pattern emerges where a specific configuration change (`awsPrivateCAIssuer` being disabled) was immediately undone, returning to the previous state within a very short timeframe. All other configurations, including `useVault`, `springProfile`, `jvmRAMPercentage`, `s3OutputV2`, `datadog` settings, deployment resources, HPA targets, pod replica counts, pod disruption budget, `unleash` settings, and global service details, remained entirely consistent across all logged changes.

## 9:47:38 AM
The provided log entry details a Kubernetes Job configuration, but its content includes sensitive information such as `PROVIDER_USERNAME`, `PROVIDER_PASSWORD`, `CLUSTER_TEST_APP_ID`, and references to Kubernetes secrets like `mtls-api-client-cert`. As per the instructions, summaries are not generated for filepaths or content that may store keys or sensitive environment configurations. Therefore, a summary of this log entry will not be provided.

## 10:22:29 AM
The `transaction-processing-service/values.yaml` file, last updated on `2/25/2026, 11:08:58 AM`, provides a comprehensive configuration for the `transaction-processing-service` within a Kubernetes environment, specifically for a `staging` Spring profile.

Key updates and configurations include:

*   **Security and Secrets Management**: Vault is enabled (`useVault: true`), while Secrets Manager is disabled (`useSecretsManager: false`). A keystore path is specified at `data/security/production.keystore`.
*   **Observability and Monitoring**: Datadog is largely disabled (`datadog: enabled: false`), but Kubernetes monitors are enabled (`k8sMonitors: enabled: true`). A wide array of alerts are defined for CPU throttling, low replica counts, deployment unavailability, pod crashes, OOMKills, and container restarts, all configured to notify `@slack-Tink-squad-transactions-oncall`.
*   **Resource Allocation and Scaling**: The service requests and limits CPU to `2000m` and memory to `4Gi`. Horizontal Pod Autoscaler (HPA) targets a CPU usage of `1200m` and `100` requests per second, with a minimum of `4` and a maximum of `25` replicas. A Pod Disruption Budget (PDB) allows for `20%` unavailability.
*   **Processing and Reconciliation**: Configured with `10` processing workers, a queue size of `200`, and processing data for `12` months. Reconciliation features are detailed, including `reconciliationS3BucketName` (`processing-reconciliation-output-transit`) and `reconciliationSNSTopicName` (`reconciliation-output`), with `snsFlowEnabled: true`. Several feature flags related to reconciliation based on provider external IDs, duplicate transactions, and specific allowed apps/providers/users are present, mostly disabled or empty.
*   **Database and Caching**: Utilizes a Cassandra distributed database with `3` seed nodes and specific connection limits, configured with `useTls: true`. The primary database has a `maxPoolSize: 20`, and `runFlywayMigrations` is set to `false`. A `categoryCache` is enabled with a `60` minute TTL and a `10000` item max size. Redis is disabled (`redisConfiguration: enabled: false`).
*   **Feature Toggles and Control**: Unleash feature toggles are enabled (`unleash: enabled: "true"`) with a specified API endpoint and `30` second fetch interval. Canary deployments are explicitly `DISABLED`.
*   **Service Level Objectives (SLO)**: Detailed SLOs are defined for availability (99.9%) and latency (99% percentile at 0.1 seconds) over a `30d` timeframe. Specific endpoint overrides for `/ingest/task`, `/process/transactions/update`, and `/process/transactions/callback` define stricter SLOs (99.99% availability, 95% latency at 0.2s or 0.1s).
*   **Other Configurations**: Includes settings for JVM RAM percentage (`70.0`), server worker threads (`200`), lifecycle timeouts, `firehoseEnabled: true`, and `rateLimitEnabled: false`. Fasttext is disabled (`fasttext: enabled: false`). AWS Private CA Issuer is enabled but not used (`used: false`).

**Patterns and Recurring Elements**:
The file consistently uses boolean flags (`enabled: true/false`) to control various features across different modules like Datadog, caching, categorization, Redis, AuthService, Unleash, Fasttext, and AWS Private CA Issuer. There's a strong emphasis on `transaction-processing-service` as the core service, appearing in the `service` and `global.service` fields, and the `namespace`. The configuration is structured logically, grouping related settings such as `observability`, `deployment`, `resources`, `processing`, `database`, `hpa`, and `slo`. Monitoring and alert configurations are highly detailed, often specifying `@slack-Tink-squad-transactions-oncall` as the notification channel.