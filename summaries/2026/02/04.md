# Activity Summary for 2/4/2026

## 9:14:17 AM
The provided log details changes to Helm chart `values.yaml` files for a `transaction-processing-service` project, occurring on February 2, 2026, between 1:18 PM and 1:49 PM.

**File-Specific Updates:**

*   **/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml:**
    *   At **1:18:43 PM**, the `awsPrivateCAIssuer` was initially configured with `trusted: true`, `enabled: false`, and `used: false`.
    *   By **1:27:29 PM**, the `awsPrivateCAIssuer.used` parameter was changed from `false` to `true`.
    *   Later at **1:43:49 PM**, the `awsPrivateCAIssuer.trusted` setting was reverted from `true` to `false`, and `used` was also reverted from `true` to `false`.
    *   Several subsequent entries for this file (at 1:27:39 PM and 1:45:35 PM) show no functional content changes from the preceding timestamp.

*   **/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/barnsley-staging.yaml:**
    *   At **1:42:30 PM**, this staging environment's configuration enabled Datadog, Redis, and Fasttext, and set `canaryDeployments.phase` to `COMPLETE`. It also had `awsPrivateCAIssuer.enabled` as `false`.
    *   At **1:49:05 PM**, `awsPrivateCAIssuer.enabled` was changed from `false` to `true`.
    *   By **1:49:40 PM**, this setting was reverted, with `awsPrivateCAIssuer.enabled` changing back to `false`.

*   **/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/oxford-staging.yaml:**
    *   At **1:43:21 PM**, this configuration enabled Datadog, Redis, Fasttext, and `canaryDeployments.phase: COMPLETE`. Categorization features like `userLearningEnabled`, `bfmEnabled`, and `bfmUserLearningEnabled` were set to `true`, and specific `bfmAppIds` were listed. `processing.reconcileUsingProviderExternalId` was `true`, and `tasksQueue.topicPrefix` was configured for "oxford-staging-product001".
    *   Multiple entries for this file (at 1:43:27 PM, 1:43:34 PM, 1:43:56 PM, and 1:49:28 PM) show identical content to the 1:43:21 PM entry, indicating no functional changes across these timestamps.

*   **/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/jersey-staging.yaml:**
    *   At **1:45:19 PM**, this configuration enabled Datadog and Redis, set `canaryDeployments.phase: COMPLETE`, but explicitly disabled categorization (`categorization: enabled: false`) and Fasttext (`fasttext: enabled: false`). Uniquely, `awsPrivateCAIssuer.enabled` was set to `true`.

**Patterns and Recurring Elements:**

*   **Environment-Specific Overrides:** The base `values.yaml` provides default settings (e.g., Datadog, Redis, Fasttext, Canary Deployments often disabled), which are then extensively overridden in environment-specific files like `barnsley-staging.yaml`, `oxford-staging.yaml`, and `jersey-staging.yaml`. These staging environments generally enable more features (Datadog, Redis, Fasttext, `canaryDeployments.phase: COMPLETE`) compared to the base.
*   **Datadog Configuration:** `observability.datadog` is consistently disabled in the base `values.yaml` but enabled with `logInjection: true` and `profiling: true` in all staging environments.
*   **AWS Private CA Issuer:** The `awsPrivateCAIssuer` settings (`trusted`, `enabled`, `used`) are frequently modified and sometimes reverted within the same file (`values.yaml` and `barnsley-staging.yaml`), suggesting active experimentation or debugging around this specific feature across environments.
*   **Categorization Features:** There's a clear divergence in categorization settings among staging environments. `barnsley-staging` and `oxford-staging` enable `userLearningEnabled` (with `oxford-staging` also enabling BFM-related features), while `jersey-staging` explicitly disables most categorization features and sets `bfmAppIds: null`.
*   **Identical Entries:** Several log entries for the same file at different timestamps show no actual content change, particularly for `values.yaml` and `oxford-staging.yaml`. This might indicate frequent saves, automated checks, or an artifact of the logging process rather than distinct code modifications.
*   **Fasttext and Redis:** `fasttext` is disabled in the base and `jersey-staging`, but enabled in `barnsley-staging` and `oxford-staging`. `redisConfiguration` is consistently enabled with specific clustered hosts and timeouts in all staging environments, while disabled in the base `values.yaml`.
*   **Processing Workers:** `processing.workersMaxQueueSize` is set to `1000` in staging environments, significantly higher than the default `200` in the base `values.yaml`.
*   **Timestamps:** All changes occurred within a relatively short period on a single day, indicating focused activity.

## 9:14:30 AM
The provided log details changes primarily within the Helm charts for a `transaction-history` service, focusing on configuration management and deployment strategies. The most significant activity revolves around integrating and configuring AWS Private Certificate Authority (CA).

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/oxford-staging.yaml`**
    *   **(2/3/2026, 11:27:06 AM):** Initial configuration with `awsPrivateCAIssuer` enabled.
    *   **(2/4/2026, 9:09:45 AM):** The `awsPrivateCAIssuer.enabled` flag was temporarily set to `false`.
    *   **(2/4/2026, 9:11:56 AM):** The `awsPrivateCAIssuer.enabled` flag was reverted back to `true`.
    *   **Summary:** This environment's `awsPrivateCAIssuer` configuration saw a brief deactivation and re-activation of its `enabled` flag.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/houston-staging.yaml`**
    *   **(2/3/2026, 11:28:45 AM):** Initial setup, configuring Datadog cluster to "us" and `awsPrivateCAIssuer` as `trusted: true`, `enabled: true`, `used: false`.
    *   **Summary:** This file defines specific staging environment values, notably setting the Datadog cluster to "us" and enabling `awsPrivateCAIssuer` but not marking it as "used".

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/templates/deployment.yaml`**
    *   **(2/3/2026, 11:55:54 AM - 11:56:11 AM):** Initial deployment configuration, including Vault integration for credential fetching, security contexts, container resource limits, probes (readiness, liveness, startup), and volume mounts for TLS certificates. It already included conditional logic for `VAULT_CACERT` based on `awsPrivateCAIssuer.trusted`.
    *   **(2/3/2026, 12:01:46 PM):** Modified volume mounts for the main container. The external `grpc-certificate` mount became conditional, only occurring if `.Values.awsPrivateCAIssuer.used` is `false`.
    *   **(2/3/2026, 5:37:21 PM - 5:37:32 PM):** Further refined the `volumes` section. The `grpc-certificate` and `grpc-internal-certificate` volumes now dynamically choose between `grpc-certificate-aws` (if `awsPrivateCAIssuer.used` is `true`) and generic `grpc-certificate` secrets (if `false`), reflecting a more direct integration with AWS-managed certificates.
    *   **Summary:** This file underwent several updates to integrate AWS Private CA, primarily by making certificate volume mounts and secrets conditional based on the `awsPrivateCAIssuer.used` flag, to support fetching AWS-specific certificates.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/templates/config.yaml`**
    *   **(2/3/2026, 12:04:27 PM):** Initial configuration defining gRPC endpoints, API settings, storage, auth, metrics, and service clients. It started with conditional `tls` paths for external gRPC and `ca_file` paths for auth and service clients, based on `awsPrivateCAIssuer.used` and `awsPrivateCAIssuer.trusted`.
    *   **(2/3/2026, 12:04:41 PM - 12:05:08 PM):** A rapid series of changes added an `else if .Values.awsPrivateCAIssuer.used` condition to determine the `ca_file` path (setting it to `/tls/internal/ca.crt`) for `auth`, `account_service_client`, `transaction_service_client`, `credential_service_client`, and `event_producer_service_client`.
    *   **(2/3/2026, 12:05:29 PM):** The `else if` condition for `transaction_service_client.ca_file` was temporarily removed.
    *   **(2/3/2026, 12:05:47 PM):** The conditional `cert_file` and `key_file` for external gRPC TLS were removed, making them always point to `/tls/external/tls.crt` and `/tls/external/tls.key`.
    *   **(2/3/2026, 12:08:23 PM - 12:08:57 PM):** The `else if .Values.awsPrivateCAIssuer.used` condition for `ca_file` was re-added for `auth`, `account_service_client`, `transaction_service_client`, `credential_service_client`, and `event_producer_service_client`.
    *   **(2/3/2026, 12:20:35 PM - 12:20:41 PM):** A broad rollback occurred, removing the `else if .Values.awsPrivateCAIssuer.used` condition for `ca_file` from `auth` and all service clients, simplifying their CA file logic to depend only on `awsPrivateCAIssuer.trusted` or a default path.
    *   **(2/3/2026, 12:26:37 PM - 12:28:49 PM):** Introduced new complex conditional logic for internal and external gRPC `tls` settings, specifically for `cert_file` and `ca_file` based on both `awsPrivateCAIssuer.trusted` and `awsPrivateCAIssuer.used`.
    *   **(2/3/2026, 5:30:02 PM):** A final significant rollback occurred. Most of the complex conditional `ca_file` and gRPC TLS certificate path logic introduced throughout the day was reverted. The `tls` blocks for both internal and external gRPC were simplified to non-conditional `cert_file` and `key_file` paths, and the `ca_file` paths for `auth` and service clients returned to a simpler `trusted` or default `external/ca.crt` logic.
    *   **Summary:** This file saw intense and often experimental changes throughout Feb 3rd, focused on how `ca_file` and TLS certificate paths are determined for various gRPC services and authentication. These changes frequently added and then reverted conditional logic based on `awsPrivateCAIssuer.used` and `awsPrivateCAIssuer.trusted`, indicating an active debugging or refactoring effort for certificate handling. By the end of the day, many of the more complex conditional paths were simplified.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/jersey-staging.yaml`**
    *   **(2/4/2026, 9:10:00 AM):** Initial configuration with `awsPrivateCAIssuer` enabled but `used: false`.
    *   **(2/4/2026, 9:11:19 AM):** The `awsPrivateCAIssuer.used` flag was temporarily set to `true`.
    *   **(2/4/2026, 9:11:39 AM):** The `awsPrivateCAIssuer.used` flag was reverted back to `false`.
    *   **Summary:** Similar to Oxford staging, this environment's `awsPrivateCAIssuer` configuration had its `used` flag toggled on and off within a short period.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values.yaml`**
    *   **(2/4/2026, 9:11:03 AM):** Initial global values, with `awsPrivateCAIssuer` set to `trusted: true`, `enabled: false`, `used: false`.
    *   **(2/4/2026, 9:12:09 AM):** The `awsPrivateCAIssuer.enabled` and `awsPrivateCAIssuer.used` flags were both set to `true`.
    *   **Summary:** The global default configuration for `awsPrivateCAIssuer` was updated to explicitly enable and use it, suggesting a broader intent to adopt this feature.

**Patterns and Recurring Elements:**

*   **AWS Private CA Integration:** The overarching theme is the ongoing work to integrate and configure `awsPrivateCAIssuer`. This involves frequent modifications to `enabled`, `used`, and `trusted` flags in various `values` files, and complex conditional logic in `deployment.yaml` and `config.yaml` to handle certificate and CA file paths based on these flags.
*   **Rapid Iteration and Rollbacks:** The timestamps on 2/3/2026, particularly for `config.yaml`, show a high frequency of changes within minutes, including additions and subsequent reversals of conditional logic. This suggests an intensive debugging or feature development session where different configurations were tested and refined.
*   **Helm Templating:** All files are Helm chart templates, using `{{ .Values.* }}` for dynamic configuration, indicating that these changes are part of a Kubernetes deployment strategy.
*   **Service Client Configuration:** Service clients (account, transaction, credential, event producer) consistently use `round_robin: true` in staging environments and frequently have their `ca_file` configuration tied to the `awsPrivateCAIssuer` logic.
*   **Canary Deployments:** The `canaryDeployments.phase` is configured, typically `COMPLETE` for staging environments and `DISABLED` as a default in the main `values.yaml`, alongside explanatory comments for deployment phases.
*   **Monitoring and Tracing:** Datadog APM, Prometheus metrics, and tracing (`enabled: false`, `sample_rate: 0.5`) are consistently configured across the environments, demonstrating a focus on observability.

## 10:14:17 AM
The log details configuration changes across several YAML files within a `transaction-processing-service` project, primarily impacting Kubernetes Helm chart values for different staging environments and a base configuration. The changes occurred on 2/2/2026.

### File-Specific Updates:

1.  **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml`**
    *   **Timestamp (2/2/2026, 1:18:43 PM - 1:45:35 PM):** The main activity in this file revolves around the `awsPrivateCAIssuer` configuration. Initially, `awsPrivateCAIssuer.used` was `false`, then briefly set to `true` (at 1:27:29 PM), before both `trusted` and `used` flags were set to `false` (at 1:43:49 PM). The `enabled` flag remained `false` throughout. Many entries for this file show identical content, suggesting multiple saves without functional changes. This file generally maintains a more conservative or default set of configurations, with Datadog observability, Redis, and Fasttext being disabled.

2.  **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/barnsley-staging.yaml`**
    *   **Timestamp (2/2/2026, 1:42:30 PM):** This file was introduced, defining specific configurations for the `barnsley-staging` environment. It enables Datadog observability (logging, profiling), Vault and Secrets Manager integration, Redis, Cassandra TLS, and Fasttext. Key processing settings include `processingWorkers: 10`, `workersMaxQueueSize: 1000`, `userLearningEnabled: true` for categorization, and `providerNamePrefixesAllowedForFullMetrics: 'uk'`. The `awsPrivateCAIssuer` was initially `enabled: false`.
    *   **Timestamp (2/2/2026, 1:49:05 PM):** `awsPrivateCAIssuer.enabled` was changed from `false` to `true`.
    *   **Timestamp (2/2/2026, 1:49:40 PM):** The `awsPrivateCAIssuer.enabled` flag was reverted from `true` back to `false`.

3.  **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/oxford-staging.yaml`**
    *   **Timestamp (2/2/2026, 1:43:21 PM - 1:49:28 PM):** This file was introduced and remained unchanged across multiple timestamps. It defines the `oxford-staging` environment with enabled Datadog observability, Vault/Secrets Manager, Redis, Cassandra TLS, and Fasttext. It stands out by enabling `bfmEnabled` and `bfmUserLearningEnabled` under `categorization` and includes specific `bfmAppIds`. The `processing.reconcileUsingProviderExternalId` is set to `true`, unlike the `barnsley-staging` environment. It also specifies a unique `tasksQueue.topicPrefix` (`oxford-staging-product001`) and a distinct `awsAccountId`.

4.  **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/jersey-staging.yaml`**
    *   **Timestamp (2/2/2026, 1:45:19 PM):** This file was introduced, configuring the `jersey-staging` environment. Similar to other staging environments, it enables Datadog observability, Vault/Secrets Manager, Redis, and Cassandra TLS. However, it explicitly disables `fasttext` and most `categorization` features (`enabled`, `mxCategoryMappingEnabled`, `userLearningEnabled`, `bfmUserLearningEnabled` are `false`, `bfmAppIds: null`). Notably, `awsPrivateCAIssuer.enabled` is set to `true` in this configuration.

### Patterns and Recurring Elements:

*   **Staging Environment Characteristics:** All specific staging environment files (`barnsley`, `oxford`, `jersey`) consistently enable Datadog observability, Vault/Secrets Manager, Redis, and Cassandra TLS, in contrast to the base `values.yaml` where these are largely disabled.
*   **Spring Profile:** The `springProfile` is uniformly set to `staging` across all configurations.
*   **Processing Workers:** The `workersMaxQueueSize` for `processing` is consistently `1000` in all staging environments, significantly higher than the `200` in the base `values.yaml`.
*   **Canary Deployments:** `canaryDeployments.phase` is `DISABLED` in the base `values.yaml` but `COMPLETE` in all staging environment configurations.
*   **Fasttext and Categorization Variation:** Fasttext is enabled in `barnsley-staging` and `oxford-staging` but disabled in `jersey-staging` and the base `values.yaml`. Similarly, categorization features (like BFM and user learning) vary significantly between staging environments.
*   **AWS Private CA Issuer:** The configuration of `awsPrivateCAIssuer` shows environment-specific overrides, with `enabled: true` in `jersey-staging`, `enabled: false` (after a brief change) in `barnsley-staging`, and consistently `enabled: false` in `oxford-staging` and the base `values.yaml`.
*   **Redundant Entries:** Several entries for `values.yaml` and `oxford-staging.yaml` at different timestamps have identical code content, possibly indicating repeated saves or non-functional changes not captured in the log.

## 10:14:33 AM
The recent code changes primarily focus on refining the deployment and configuration of the `transaction-history` service, with a strong emphasis on **AWS Private Certificate Authority (CA) integration** and **TLS certificate management**.

Here's a breakdown of the key information:

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/oxford-staging.yaml` (Significant timestamps: 2/3/2026, 11:27:06 AM; 2/4/2026, 9:09:45 AM; 2/4/2026, 9:11:56 AM)**:
    *   The `awsPrivateCAIssuer.enabled` flag was toggled. Initially `true`, it was changed to `false` (2/4, 9:09 AM) and then back to `true` (2/4, 9:11 AM), while `awsPrivateCAIssuer.trusted` remained `true` and `awsPrivateCAIssuer.used` remained `false` throughout these changes.
    *   Basic service client configurations (`account_service_client`, `transaction_service_client`, `event_producer_service_client`, `credential_service_client`) consistently use `round_robin: true` and `tracing: enabled: false, sample_rate: 0.5`.
    *   `transactions_api.enable_get_single_transaction` is `true`.
    *   `podCpu` and `podMemory` are explicitly set to `1000m` and `1Gi` respectively.
    *   `canaryDeployments.phase` is `COMPLETE`.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/houston-staging.yaml` (Timestamp: 2/3/2026, 11:28:45 AM)**:
    *   Configures `awsPrivateCAIssuer` similarly to `oxford-staging.yaml` (trusted: true, enabled: true, used: false).
    *   Maintains consistent service client and tracing configurations.
    *   Includes `datadog.cluster: us`.
    *   `canaryDeployments.phase` is `COMPLETE`.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/templates/deployment.yaml` (Significant timestamps: 2/3/2026, 12:01:46 PM; 2/3/2026, 5:37:21 PM; 2/4/2026, 9:22:53 AM; 2/4/2026, 10:10:44 AM; 2/4/2026, 10:12:33 AM)**:
    *   This Kubernetes Deployment template for the `transaction-history-service` underwent several iterative changes related to certificate volume mounts.
    *   Initially, the `grpc-certificate` volume mount for the main container was made conditional, only appearing if `awsPrivateCAIssuer.used` was `false` (2/3, 12:01 PM).
    *   The naming convention for AWS-specific `grpc-certificate` secrets was adjusted, with the volume name changing from `grpc-certificate` to `grpc-certificate-aws` when `awsPrivateCAIssuer.used` is `true` (2/3, 5:37 PM).
    *   The `internal-ca` volume definition became conditional, only mounting if `awsPrivateCAIssuer.used` was `false` (2/4, 9:22 AM). This condition was later updated to `!trusted` (2/4, 10:10 AM) and then reverted back to an unconditional mount (2/4, 10:12 AM), with the `vault-tls` volume being conditionally mounted from `internal-ca` secret when `useVault` is `true` and `awsPrivateCAIssuer.trusted` is `false`.
    *   Datadog annotations and volume mounts for APM are conditionally enabled.
    *   Init containers use `vaultify` for credential fetching, with `VAULT_CACERT` path conditional on `awsPrivateCAIssuer.trusted`.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/templates/config.yaml` (Significant timestamps: 2/3/2026, 12:04:41 PM; 2/3/2026, 12:04:53 PM; 2/3/2026, 12:05:47 PM; 2/3/2026, 12:08:23 PM; 2/3/2026, 12:20:35 PM; 2/3/2026, 12:26:37 PM; 2/3/2026, 12:28:49 PM; 2/3/2026, 5:30:02 PM)**:
    *   This ConfigMap defines various service configurations, with a high frequency of changes concerning TLS CA file paths.
    *   Initially, `auth.ca_file` and all service client `ca_file` paths (`account_service_client`, `transaction_service_client`, `credential_service_client`, `event_producer_service_client`) were conditional on `awsPrivateCAIssuer.trusted` (using `/ca-trust-bundle/bundle.pem` if true, otherwise `/tls/external/ca.crt`).
    *   An `else if .Values.awsPrivateCAIssuer.used` condition was introduced (2/3, 12:04 PM) and then expanded to apply to multiple service clients (2/3, 12:04 PM), specifying `/tls/internal/ca.crt` in that case. This `else if` condition was repeatedly added and removed across several commits (2/3, 12:05 PM removed; 2/3, 12:08 PM re-added for some clients; 2/3, 12:20 PM removed again for all clients).
    *   Conditional logic for `grpc.internal.tls.cert_file` and `grpc.external.tls.cert_file`/`ca_file` based on `awsPrivateCAIssuer.trusted` or `awsPrivateCAIssuer.used` was introduced and then ultimately reverted to simpler, unconditional paths (2/3, 5:30 PM).

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/jersey-staging.yaml` (Significant timestamps: 2/4/2026, 9:10:00 AM; 2/4/2026, 9:11:19 AM; 2/4/2026, 9:11:39 AM)**:
    *   The `awsPrivateCAIssuer.enabled` and `awsPrivateCAIssuer.used` flags were actively toggled, moving from `false`/`false` to `true`/`true` (2/4, 9:11 AM) and then `true`/`false` (2/4, 9:11 AM), indicating testing or refinement of this feature.
    *   Similar to other staging configs, it sets `canaryDeployments.phase: COMPLETE`.

*   **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values.yaml` (Significant timestamps: 2/4/2026, 9:11:03 AM; 2/4/2026, 9:12:09 AM)**:
    *   This global values file saw `awsPrivateCAIssuer.enabled` and `awsPrivateCAIssuer.used` change from `false`/`false` to `true`/`true` (2/4, 9:12 AM).
    *   It defines default configurations for various components, including `canaryDeployments.phase: DISABLED` and `datadog.cluster: eu`.
    *   Detailed `hpa` and `monitors` configurations are present, including `cpuThrottledSeconds`, `serviceDeploymentReplicasLow`, `deploymentCompletelyUnavailable`, `deploymentNotUpToDate`, `podCrashLoopBackoff`, `podContainerOOMKilled`, `containerRestartsOnDeployment`, `namespaceCpuQuotaExceeded`, and `namespaceMemoryQuotaExceeded` alerts with `@slack-Tink-squad-transactions-oncall` notifications.

**Patterns and Recurring Elements:**

1.  **AWS Private CA Configuration**: There's a persistent theme of implementing and adjusting how AWS Private CA is handled. This is evident through the `awsPrivateCAIssuer` flags (`trusted`, `enabled`, `used`) in `values` files and the conditional logic in `deployment.yaml` and `config.yaml` for TLS certificate and CA bundle paths. This suggests a complex or evolving integration strategy.
2.  **TLS and Certificate Paths**: Repeated modifications in `config.yaml` and `deployment.yaml` regarding `cert_file`, `key_file`, and `ca_file` paths (`/tls/internal`, `/tls/external`, `/ca-trust-bundle`, `/vault_ca`) highlight the criticality and active management of secure communication within the service and its clients.
3.  **Helm Chart Customization**: The extensive use of Helm templating (`{{- if .Values.xyz }}`) demonstrates a flexible and parameterized deployment strategy, allowing different behaviors and configurations based on environment-specific `values` files and feature flags.
4.  **Staging vs. Global Configuration**: Staging `values` files (`oxford-staging.yaml`, `houston-staging.yaml`, `jersey-staging.yaml`) often override global defaults (`values.yaml`), particularly for `canaryDeployments.phase`, `awsPrivateCAIssuer` settings, and Datadog cluster.
5.  **Service Client Round-Robin and Tracing**: All `values` files consistently configure service clients (`account_service_client`, `transaction_service_client`, `event_producer_service_client`, `credential_service_client`) with `round_robin` enabled (or configurable) and `tracing` disabled with a `sample_rate` of `0.5`.
6.  **Monitoring and Alerts**: The global `values.yaml` includes detailed Datadog monitor configurations and Slack notification channels, indicating robust observability and alerting practices for the service.
7.  **Vault Integration**: The `useVault` flag and the `vaultify` init container show a reliance on Vault for secret management, with its CA certificate path also being conditional.

In summary, the changes reflect an active development cycle focused on refining AWS Private CA integration for TLS, managing environment-specific configurations via Helm, and ensuring proper monitoring and secret management for the `transaction-history` service.

## 10:14:44 AM
The provided log details a series of changes to Helm chart configurations for a `transaction-service` and its `reconciliation-worker` component, primarily focused on infrastructure and operational settings. Key updates span across several YAML files, concentrated between February 2nd and February 3rd, 2026.

**File-Specific Updates:**

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/config.yaml:**
    *   **Timestamp: 2/2/2026, 10:55:15 AM - 11:01:28 AM:** Repeatedly introduced conditional logic for certificate authority (CA) paths (`user-svc-ca`, `cassandra-ca-path`, `cassandra-v2-ca-path`, `cert-path`). These paths are set to `/ca-trust-bundle/bundle.pem` if `awsPrivateCAIssuer.trusted` is true, otherwise defaulting to `/tls/ca.crt` or existing `caPath` variables.
    *   **Timestamp: 2/2/2026, 11:04:23 AM:** Temporarily reverted these CA path changes back to non-conditional defaults.
    *   **Timestamp: 2/2/2026, 11:14:46 AM:** The conditional CA path logic was re-introduced and persisted through subsequent minor saves.
    *   **Timestamp: 2/3/2026, 9:15:38 AM:** A new variable `$tagsUpdater` was defined and a new configuration parameter `tags-updater-shutdown-timeout` was added, defaulting to 30 seconds.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml:**
    *   **Timestamp: 2/2/2026, 11:04:40 AM - 11:13:55 AM:** The `awsPrivateCAIssuer` block was repeatedly added, removed, and re-added, fluctuating its `trusted`, `enabled`, and `used` states, indicating active development or testing of this feature.
    *   **Timestamp: 2/3/2026, 9:11:59 AM:** The `reconciliation.sqs.shutdownTimeout` was increased from its default to `110`, with a crucial comment added about its impact on per-message timeout. A `shutdownTimeout` field was also confusingly added at the root level, also with a value of `110` and a similar warning comment.
    *   **Timestamp: 2/3/2026, 9:23:50 AM - 11:06:20 AM:** Further changes to `awsPrivateCAIssuer` flags (`trusted`, `enabled`, `used`), repeatedly enabling and disabling the issuer.
    *   **Timestamp: 2/3/2026, 4:13:57 PM:** The `awsPrivateCAIssuer` block was adjusted to `trusted: false, used: false`, removing the `enabled` field.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml:**
    *   **Timestamp: 2/2/2026, 11:18:02 AM:** Introduced an `initContainer` named `fetch-credentials` using `vaultify:0.4.2` for fetching credentials, conditionally mounting `vault-tls` or `ca-trust-bundle` based on `awsPrivateCAIssuer.trusted`. Also added a `renew-leases` sidecar container.
    *   **Timestamp: 2/2/2026, 11:18:29 AM - 11:35:28 AM:** Minor adjustments to `VAULT_CACERT` conditional logic within the `fetch-credentials` init container and volume mounts for `ca-trust-bundle` vs `vault-tls`.
    *   **Timestamp: 2/2/2026, 11:35:54 AM:** The `vault-tls` volume mount for the main container was changed to also conditionally mount `ca-trust-bundle` if `awsPrivateCAIssuer.trusted` is true. Similar changes were applied to the `tags-consumer` container (if enabled).
    *   **Timestamp: 2/2/2026, 11:36:04 AM:** The `vault-tls` volume declaration in the `volumes` section was adjusted to conditionally define the `vault-tls` secret based on `Cluster != "local"`.
    *   **Timestamp: 2/2/2026, 11:41:55 AM:** A `ca-trust-bundle` ConfigMap volume was introduced, conditionally used if `awsPrivateCAIssuer.trusted` is true, providing `bundle.pem` and `bundle.p12`. This replaced the `internal-ca` secret for this specific case.
    *   **Timestamp: 2/3/2026, 10:11:00 AM:** The `terminationGracePeriodSeconds` for the deployment was increased from 90 to `120`. Conditional logic for `VAULT_CACERT` within the `fetch-credentials` init container's environment variables was further refined, introducing nested conditionals.
    *   **Timestamp: 2/3/2026, 4:23:46 PM:** A new variable `$certificateName` was introduced to dynamically select between `grpc-certificate` and `grpc-certificate-aws` based on `awsPrivateCAIssuer.used`. This variable is then used for the `volumeMounts` and `volumes` related to certificates for both `reconciliation-worker` and `tags-updater` containers.
    *   **Timestamp: 2/3/2026, 4:32:15 PM:** Reverted `terminationGracePeriodSeconds` back to 90 from 120 seconds. Also reverted the conditional logic for `VAULT_CACERT` inside `fetch-credentials` back to a simpler, single-level conditional (less nested than the 10:11 AM change). Removed the dynamic `$certificateName` and reverted to using `.Values.certificateName` directly.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/certificate-grpc-certificate-aws.yaml:**
    *   **Timestamp: 2/3/2026, 9:30:38 AM:** A new Helm template file was added to create a `Certificate` resource named `grpc-certificate-aws`. It is conditionally enabled by `awsPrivateCAIssuer.enabled`, uses `AWSPCAClusterIssuer` with a short-lived issuer (7-day duration, 2-day renewal), and specifies various DNS names for the transaction service APIs and headless service.

*   **/Users/mmuruts/projects/tink/transaction-service/master_render_7.yaml:**
    *   **Timestamp: 2/2/2026, 11:37:01 AM:** This rendered output shows the state of configurations, including a `PodDisruptionBudget`, `ConfigMap` (with `user-svc-ca`, `cassandra-ca-path`, `cassandra-v2-ca-path`, `cert-path` all pointing to `/ca-trust-bundle/bundle.pem`), and several DatadogMonitor definitions for Kubernetes health alerts.

*   **/Users/mmuruts/projects/tink/transaction-service/master_render_10.yaml:**
    *   **Timestamp: 2/3/2026, 11:10:28 AM:** This later rendered output reflects updates:
        *   `reconciliation-shutdown-timeout` in the ConfigMap increased to `110`.
        *   A new `tags-updater-shutdown-timeout: 110` entry is present in the ConfigMap.
        *   The new `Certificate` resource `grpc-certificate-aws` is included, indicating its template was processed.
        *   Datadog monitors remain consistent with `master_render_7.yaml`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/certificate-grpc-certificate-aws.yaml:**
    *   **Timestamp: 2/3/2026, 9:29:48 AM:** A similar `Certificate` resource for `grpc-certificate-aws` was added to the main `transaction-service` chart, but configured with a longer duration (90 days) using the regular `aws-private-ca-issuer`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values/barnsley-production.yaml**, **/jersey-staging.yaml**, **/farnham-staging.yaml**, **/oxford-staging.yaml**, **/oxford-staging-product001.yaml**, **/houston-staging.yaml**:
    *   These environment-specific `values.yaml` files show consistent patterns of overriding default settings for Elasticsearch hosts, Cassandra hosts, reconciliation enablement, SQS batch sizes, and resource allocations.
    *   `oxford-staging.yaml` is notable for enabling `enableMultiElasticSearchClusters`, `snsEnabled`, and providing detailed Elasticsearch configurations for "common" and "nordea" clusters, as well as enabling `tagsUpdater`.
    *   The `awsPrivateCAIssuer` settings in these environment-specific files frequently toggled `enabled` and `used` flags, indicating testing and configuration for AWS Private CA integration in various environments.
    *   `oxford-staging-product001.yaml` specifically overrides `reconciliation.firehoseTransactionsTopic`.
    *   `houston-staging.yaml` sets `datadog.cluster` to `us`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/farnham-staging.yaml**, **/barnsley-staging.yaml**, **/oxford-staging.yaml**, **/oxford-staging-product001.yaml**, **/oxford-production.yaml**:
    *   These are environment-specific `values.yaml` files for the main `transaction-service` chart.
    *   Similar overrides as the reconciliation worker chart, but also include settings for `userDeletionListener` and `accountDeletionListener` (e.g., enabled in `oxford-staging.yaml` and `oxford-production.yaml`, but disabled by default in the base `values.yaml`).
    *   `oxford-production.yaml` includes a third Elasticsearch cluster, "seb", with its own configuration, and higher resource requests for pods.
    *   `oxford-staging-product001.yaml` specifically defines `kafkaTopicPrefix` and adjusts `podCpuRequest` and `serviceLevels.enabled`.
    *   The `awsPrivateCAIssuer` settings were also toggled in these files, similar to the reconciliation worker chart.

**Patterns and Recurring Elements:**

*   **AWS Private CA Integration:** This is a major recurring theme. There's a consistent effort to implement and configure conditional logic for certificate paths based on `awsPrivateCAIssuer.trusted` and `awsPrivateCAIssuer.enabled`/`used` flags across multiple configurations (`config.yaml`, `deployment.yaml`, various `values.yaml`). New certificate resources (`certificate-grpc-certificate-aws.yaml`) were introduced for both the main service and the reconciliation worker, with differing certificate durations (90 days vs 7 days) and issuers. This indicates a shift towards AWS Certificate Manager Private CA for TLS.
*   **Reconciliation Worker Focus:** A significant portion of the changes revolves around the `transaction-service-reconciliation-worker` chart, including its configuration, deployment, and resource management.
*   **Datadog Monitoring:** Kubernetes health monitors for common issues (restarts, OOM kills, CPU throttling, deployment availability) are consistently defined and configured with specific alert thresholds and notification channels (`@slack-Tink-squad-transactions-oncall`). All monitors share `team:transactions` and a `restrictedRoles` UUID.
*   **Environment-Specific Customization:** Heavy use of `values/*.yaml` files to customize base chart configurations for specific environments (e.g., `barnsley-production`, `jersey-staging`, `oxford-staging`). These customizations include varying Elasticsearch endpoints, Cassandra hosts, log levels, resource allocations, and enabling/disabling specific features like multi-Elasticsearch clusters or deletion listeners.
*   **Vault Integration:** The chart continues to utilize Vault for secret management, with `vaultify` init and renew-leases containers configured to retrieve credentials and manage leases.
*   **Kafka and SQS Messaging:** Configuration of Kafka hosts, `reconciliation-queue-name`, `firehose-transactions-topic`, and SQS batch sizes/concurrency are present across the configurations.
*   **Resource Allocation and Scaling:** `podMemoryRequest`, `podCpuRequest`, `podMinReplicas`, `podMaxReplicas`, `reconWorkerMinReplicas`, and `reconWorkerMaxReplicas` are frequently defined and overridden in environment-specific `values.yaml` files, reflecting tailored resource management per environment.
*   **Shutdown Timeout Management:** The `shutdownTimeout` parameter in `reconciliation.sqs` and `tags-updater-shutdown-timeout` was introduced or adjusted, along with a warning about its effect on message processing duration, suggesting attention to graceful shutdown and message handling.