# Activity Summary for 2/5/2026

## 9:29:40 AM
The log details configuration changes across several Helm chart `values` files for the `reconciliation-storage-sink` service within the `/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/` directory.

**File-Specific Updates:**

*   **`barnsley-staging.yaml` (2/4/2026, 12:57:46 PM & 12:57:51 PM):** Configures `awsAccountId` to "768381140121" and sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: false`. The second entry at 12:57:51 PM is identical, indicating no functional change.
*   **`farnham-staging.yaml` (2/4/2026, 12:57:58 PM):** Sets `awsAccountId` to "982426314530" with the same `awsPrivateCAIssuer` configuration as `barnsley-staging.yaml`.
*   **`jersey-staging.yaml` (2/4/2026, 12:58:07 PM):** Configures `awsAccountId` to "357986230030" and the same `awsPrivateCAIssuer` settings as other staging files.
*   **`oxford-staging.yaml` (2/4/2026, 12:58:13 PM):** This file contains more extensive configurations. It enables `s3OutputV2` for `ALL` `appIds` with a `prefix: output-v2`. It also enables `unleash` and sets its `awsAccountId` to "232388878475", maintaining the standard `awsPrivateCAIssuer` settings (`trusted: true`, `enabled: true`, `used: false`).
*   **`values.yaml` (main configuration file):**
    *   **2/4/2026, 1:00:14 PM:** This entry shows a comprehensive default configuration. Key settings include `useVault: false`, `springProfile: production`, `s3OutputV2: enable: false`, `datadog: apmEnabled: true`, detailed `deployment` and `resources` (CPU/memory limits, HPA targets), `unleash: enabled: "false"`, and `awsPrivateCAIssuer: trusted: true enabled: false used: false`. It also defines global service metadata for Datadog and namespace.
    *   **2/4/2026, 1:00:47 PM:** The `awsPrivateCAIssuer` setting is updated to `trusted: true enabled: true used: false`, changing `enabled` from `false` to `true`.
    *   **2/4/2026, 1:02:29 PM:** The `awsPrivateCAIssuer` setting is further modified to `trusted: false enabled: false used: false`, disabling it again and removing trust.

**Timestamps of Significant Changes:**

*   A concentrated set of updates to individual staging environment configurations occurred between 12:57:46 PM and 12:58:13 PM on 2/4/2026, suggesting a batch update for these environments.
*   The primary `values.yaml` file saw rapid iterative changes between 1:00:14 PM and 1:02:29 PM on the same date, particularly focusing on the `awsPrivateCAIssuer` configuration, which was enabled and then fully disabled/untrusted within a short period.

**Patterns or Recurring Elements:**

*   **Helm Chart Configuration:** All changes are to `.yaml` files located under a `.charts` directory, indicating they are Helm chart values files used for deploying the `reconciliation-storage-sink` service.
*   **AWS Account IDs:** Each staging environment (`barnsley`, `farnham`, `jersey`, `oxford`) is configured with a unique `awsAccountId`.
*   **`awsPrivateCAIssuer` Configuration:** This block is consistently present across all files. In staging environments, it's generally set to `trusted: true enabled: true used: false`. However, in the main `values.yaml`, its `enabled` and `trusted` states were toggled several times in quick succession.
*   **Feature Toggles:** Features like `s3OutputV2` and `unleash` are explicitly enabled in `oxford-staging.yaml` but disabled in the main `values.yaml`, demonstrating environment-specific overrides for feature activation.
*   **Resource Management:** The main `values.yaml` contains detailed resource requests/limits and Horizontal Pod Autoscaler (HPA) targets, suggesting a focus on resource efficiency and scaling for the service.
*   **Service Metadata:** Global settings like `team`, `service`, `namespace`, `teamRoleId`, `baseUrl`, and `logLevel` are defined in the main `values.yaml`, providing consistent metadata for the deployed service.

## 9:57:07 AM
This log details configuration and deployment changes for several event muxer applications, specifically `tx-ingestion`, `risk-tx-ingestion`, `ica-tx-ingestion`, and `data-retention-tx`, across various staging environments. All modifications occurred on **2/4/2026**, indicating a concentrated period of updates.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tx-ingestion/templates/tx-ingestion.yaml` (11:32:27 AM, 11:32:45 AM, 11:43:17 AM)**: This Kubernetes Helm template for the `tx-ingestion` application remained consistent across these timestamps. It defines a Deployment and PodDisruptionBudget. Key aspects include:
    *   Configuration of Kafka endpoints, consumer groups, source topics, and quarantine topics.
    *   Integration with Datadog for APM (Application Performance Monitoring) and Prometheus for metrics.
    *   Utilizing a `vaultify` initContainer to securely fetch credentials.
    *   Strict security contexts (`runAsNonRoot`, `readOnlyRootFilesystem`).
    *   Conditional TLS certificate handling using either AWS Private CA or Vault.
    *   Includes Prometheus RuleGroup alerts for message loss and high HTTP failure rates specific to `tx-ingestion`.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tx-ingestion/values/farnham-staging.yaml` (11:40:49 AM)**: Configured the `farnham-staging` environment to enable `txIngestionAppEnabled`, set resource limits (1500Mi RAM, 1500m CPU), enable Datadog APM, and specify a trusted AWS Private CA Issuer and a `systemTargets` for `transaction-processing-service`.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tx-ingestion/values/barnsley-staging.yaml` (11:41:00 AM)**: Similar to `farnham-staging.yaml`, it enabled `txIngestionAppEnabled`, specified resource limits, Datadog APM, and a trusted AWS Private CA Issuer. The `awsAccountId` value was specific to Barnsley.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tx-ingestion/values/oxford-staging.yaml` (11:41:51 AM)**: Expanded the enabled applications for `oxford-staging`, including `txIngestionAppEnabled`, `riskTxIngestionAppEnabled`, and `dataRetentionAppEnabled`. It also set initial `dataRetentionTxReplicas` and `dataRetentionTxWorkers` to 1, configured Datadog APM, a specific `awsAccountId`, and a trusted AWS Private CA Issuer.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tx-ingestion/values/oxford-staging-product001.yaml` (11:44:33 AM, 11:45:08 AM, 11:45:50 AM)**: This file received multiple, rapid updates:
    *   Initially (11:44:33 AM), it defined specific Kafka `sourceTopics` and `quarantineTopic` for `oxford-staging-product001`, enabled `txIngestionAppEnabled`, `riskTxIngestionAppEnabled`, `dataRetentionAppEnabled`, set `dataRetentionTxReplicas/Workers` to 1, enabled Datadog APM, specified `awsAccountId`, `systemTargets`, and a trusted `awsPrivateCAIssuer`.
    *   A subsequent change (11:45:08 AM) removed the `systemTargets` and `awsPrivateCAIssuer: trusted: true` lines.
    *   Immediately after (11:45:50 AM), `awsPrivateCAIssuer: trusted: true` was re-added, while `systemTargets` remained removed.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-risk-tx-ingestion/templates/risk-tx-ingestion.yaml` (12:09:30 PM)**: A new Helm template was introduced for `risk-tx-ingestion`. It mirrors the structure of `tx-ingestion.yaml` but is enabled by `Values.riskTxIngestionAppEnabled`, sets a lower default `workers` count (3), hardcodes `systemTargets`, and unconditionally mounts the `datadog-agent` volume.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-risk-tx-ingestion/values/oxford-staging.yaml` (12:11:43 PM)**: Provided values for `oxford-staging`'s risk ingestion, enabling `txIngestionAppEnabled`, `riskTxIngestionAppEnabled`, `dataRetentionAppEnabled`, setting `dataRetentionTxReplicas/Workers` to 1, enabling Datadog APM, and defining `systemTargets` and a trusted `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-ica-tx-ingestion/values/oxford-staging-product001.yaml` (12:16:41 PM)**: Configured ICA (Interbank Clearing Adapter) transaction ingestion for `oxford-staging-product001`, defining specific `ica-ingest-tx` `sourceTopics` and `ica-muxer-quarantine` `quarantineTopic`. It enabled a wider range of applications (`txIngestion`, `riskTxIngestion`, `anPostTxIngestion`, `abnTxIngestion`, `icaTxIngestion`, `dataRetention`) and set `dataRetentionTxWorkers` to 2.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-ica-tx-ingestion/values/oxford-staging.yaml` (12:17:22 PM, 12:17:28 PM)**: These entries were identical, configuring `oxford-staging` for ICA ingestion with the same expanded set of enabled applications and `dataRetentionTxWorkers` set to 2.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-data-retention/values/jersey-staging.yaml` (12:34:39 PM)**: Enabled `txIngestionAppEnabled` and `dataRetentionAppEnabled` for `jersey-staging`, along with `dataRetentionTxReplicas/Workers` at 1, Datadog APM, and a trusted AWS Private CA Issuer.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-data-retention/values/farnham-staging.yaml` (12:35:02 PM)**: Enabled `txIngestionAppEnabled` for `farnham-staging` (within the data retention chart context), setting resource limits, Datadog APM, and a trusted AWS Private CA Issuer. Notably, `dataRetentionAppEnabled` was not explicitly set to true here.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-data-retention/templates/data-retention-tx.yaml` (12:38:19 PM, 12:42:38 PM)**: Introduced a new Helm template for `data-retention-tx`, consistent across both timestamps. It defines a Kubernetes Deployment and PodDisruptionBudget. Key features include:
    *   Enabled by `Values.dataRetentionAppEnabled`.
    *   Kafka consumer configured with `initial-offset=newest` (unlike other ingesters that use `oldest`).
    *   Target path specific to data retention: `--target-path=v1/transactions/deletePeriod`.
    *   Defaults `dataRetentionTxWorkers` to 10.
    *   Uses `--ca-bundle` for certificate paths.

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-data-retention/values.yaml` (12:40:40 PM, 12:45:59 PM)**:
    *   The first entry (12:40:40 PM) set default `sourceTopics` and `quarantineTopic` for data retention. It also introduced extensive Datadog Kubernetes monitoring configurations (for CPU throttling, replica availability, pod issues, container restarts) and global Datadog metadata, along with `awsPrivateCAIssuer: trusted: true`.
    *   The second entry (12:45:59 PM) made a critical change by setting `awsPrivateCAIssuer: trusted: false`, disabling the trust for the AWS Private CA Issuer for data retention by default.

**Patterns and Recurring Elements:**

*   **Helm Templating**: Extensive use of Helm templating for Kubernetes resource definitions, allowing for dynamic configuration based on `Values`.
*   **Application Model**: A consistent pattern of defining Kubernetes Deployments and PodDisruptionBudgets for different event muxer applications (e.g., `tx-ingestion`, `risk-tx-ingestion`, `data-retention-tx`), each with its dedicated template and value files.
*   **Kafka-centric Processing**: All applications are Kafka consumers, configured with similar parameters for topic ingestion, quarantine, and consumer group management.
*   **Datadog Integration**: Deep integration with Datadog for APM, metrics scraping, and comprehensive Kubernetes monitoring, including common labels and annotations.
*   **Security Best Practices**: Consistent application of `runAsNonRoot: true`, `readOnlyRootFilesystem: true`, and specific `runAsUser`/`runAsGroup`/`fsGroup` for enhanced security.
*   **Vault for Secrets**: The `vaultify` initContainer is a standard component for fetching credentials and injecting them into application pods.
*   **TLS/CA Management**: A recurring conditional logic for handling TLS certificates, either from an AWS Private CA bundle or an internal Vault secret.
*   **Environment-Specific Overrides**: Widespread use of `values/<env-name>.yaml` files to provide environment-specific configurations (e.g., resource limits, AWS account IDs, enabled applications, topic names, `systemTargets`).
*   **`systemTargets`**: Often points to `transaction-processing-service.transaction-processing-service:443`, either as a default or explicitly configured.
*   **Rapid Iteration**: The multiple identical or near-identical timestamps for certain files suggest rapid iterations, saves, or deployment attempts within a short timeframe. The `oxford-staging-product001.yaml` file shows immediate changes and partial reverts.

## 10:29:38 AM
The changes primarily involve updates to Helm `values.yaml` files for a service named `reconciliation-storage-sink`, across different staging environments and a main configuration file. All changes occurred on February 4, 2026, within a span of minutes around 1 PM.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/values/barnsley-staging.yaml`**
    *   At 12:57:46 PM and 12:57:51 PM, the `awsAccountId` was set to `"768381140121"`, and `awsPrivateCAIssuer` was configured with `trusted: true`, `enabled: true`, and `used: false`. No functional change occurred between these two timestamps.

*   **`/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/values/farnham-staging.yaml`**
    *   At 12:57:58 PM, the `awsAccountId` was set to `"982426314530"`, and `awsPrivateCAIssuer` remained consistent with the `barnsley-staging` configuration (`trusted: true`, `enabled: true`, `used: false`).

*   **`/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/values/jersey-staging.yaml`**
    *   At 12:58:07 PM, the `awsAccountId` was set to `"357986230030"`, and `awsPrivateCAIssuer` was again set to `trusted: true`, `enabled: true`, `used: false`.

*   **`/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/values/oxford-staging.yaml`**
    *   At 12:58:13 PM, this file introduced more specific configurations in addition to the `awsAccountId` (`"232388878475"`) and `awsPrivateCAIssuer` settings (consistent with other staging environments).
    *   It enabled `s3OutputV2` with a `prefix: output-v2` and `appIds: - ALL`.
    *   It also enabled `unleash` with `"true"`.

*   **`/Users/mmuruts/projects/tink/reconciliation-storage-sink/.charts/reconciliation-storage-sink/values.yaml` (Main Configuration)**
    *   At 1:00:14 PM, a comprehensive set of configurations was applied, including:
        *   `useVault: false`
        *   `springProfile: production`
        *   `jvmRAMPercentage: "70.0"`
        *   `s3OutputV2: enable: false`
        *   Datadog APM enabled.
        *   Deployment strategies (rollingUpdate) and resource limits/requests (`cpu: "500m"`, `memory: "1Gi"`).
        *   HPA targets (`targetAverageCpuCoresUsed: 0.4`, `targetAverageMemoryUsed: 314572800.`, `podMinReplicas: 1`, `podMaxReplicas: 3`).
        *   Pod Disruption Budget (`maxUnavailable: "20%"`).
        *   Unleash disabled (`enabled: "false"`) with a specific API endpoint and fetch interval.
        *   Global settings for team, service, namespace, teamRoleId, Datadog base URL, and log level.
        *   Initial `awsPrivateCAIssuer` configuration was `trusted: true`, `enabled: false`, `used: false`.
    *   At 1:00:47 PM, the `awsPrivateCAIssuer` setting was updated, specifically changing `enabled: false` to `enabled: true`.
    *   At 1:02:29 PM, the `awsPrivateCAIssuer` setting was further modified, changing `trusted: true` to `trusted: false` and `enabled: true` back to `enabled: false`.

**Patterns and Recurring Elements:**

*   **`awsAccountId` and `awsPrivateCAIssuer` Configuration:** A consistent pattern across all staging environment `values.yaml` files is the presence of an `awsAccountId` (which is unique to each staging environment) and the `awsPrivateCAIssuer` block.
*   **Initial `awsPrivateCAIssuer` State:** For `barnsley`, `farnham`, `jersey`, and `oxford` staging environments, the `awsPrivateCAIssuer` configuration consistently starts as `trusted: true`, `enabled: true`, `used: false`.
*   **Main `values.yaml` `awsPrivateCAIssuer` Fluctuation:** The main `values.yaml` file shows a rapid sequence of changes to `awsPrivateCAIssuer` within a few minutes, first enabling it, then disabling it and untrusting it. This suggests a trial or debugging process related to this specific feature.
*   **Project Context:** All files belong to the `reconciliation-storage-sink` project, indicating a focus on deploying and configuring this specific service across various environments.
*   **Helm Configuration:** The file paths ending in `.yaml` and containing keys like `awsAccountId`, `config`, `deployment`, `resources`, `hpa`, `unleash`, and `global` suggest these are Helm chart `values` files used for Kubernetes deployments.