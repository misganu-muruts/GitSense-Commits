# Activity Summary for 2/25/2026

## 9:55:17 AM
The log entries show changes to a single file: `/Users/mmuruts/projects/tink/tink-backend/WORKSPACE`.

**File-specific updates and timestamps:**
The content of the `/Users/mmuruts/projects/tink/tink-backend/WORKSPACE` file remained identical across all three recorded timestamps: 2/24/2026, 4:07:54 PM; 2/24/2026, 4:08:07 PM; and 2/24/2026, 4:09:44 PM. No functional changes or modifications were introduced between these log entries, indicating the file was likely accessed or saved without alteration.

**Summary of file content and patterns:**
The `WORKSPACE` file is a Bazel configuration file for a virtual monorepository named "tink_backend". It's primarily responsible for defining external dependencies and toolchains.

Key patterns and recurring elements include:

*   **Extensive use of `http_archive` and `http_file`**: Numerous external dependencies are fetched via HTTP, including Bazel platforms, `libcrypto`, `bazel_skylib`, `rules_java`, `rules_pkg`, `rules_proto`, `rules_clojure`, `com_google_protobuf`, `buildtools`, `rules_go`, `bazel_gazelle`, and `go-containerregistry`. Each `http_archive` or `http_file` includes a `sha256` checksum for integrity verification.
*   **Bazel Rules and Toolchains**: The configuration loads and registers dependencies and toolchains for various languages and functionalities, such as Java (`rules_java`), package building (`rules_pkg`), Protocol Buffers (`rules_proto`), Clojure (`rules_clojure`), Go (`io_bazel_rules_go`, `bazel_gazelle`), and Docker (`io_bazel_rules_docker`).
*   **Version Management**: Specific versions are pinned for many dependencies, including Bazel (checked to be at least "4.2.2"), Protobuf ("3.11.3"), gRPC Java ("1.29.0"), Netty ("4.1.48.Final"), and various Docker image tags.
*   **Transactional Store Overrides**: Specific versions for `TX_STORE_GRPC_JAVA_VERSION` ("1.49.2") and `TX_STORE_IO_NETTY_BORINGSSL_VERSION` ("2.0.53.Final") are defined, explicitly noting they are "created in order to remove vulnerabilities provided in netty."
*   **Build Tools**: Dependencies for `buildifier` (for various OS and architectures like `darwin_x86_64`, `darwin_arm64`, `linux_x86_64`) and `unused_deps` are defined, indicating a focus on build system health and code quality.
*   **Docker Container Pulls (`container_pull`)**: A significant section is dedicated to pulling various Docker base images from `784217881480.dkr.ecr.eu-west-1.amazonaws.com`.
    *   There are references to `openjdk-jdk8` (JRE and JDK).
    *   A large number of `eclipse-temurin` images are pulled, primarily Java 8 versions (`8u345`, `8u352`, `8u362`), with both JRE and JDK variants, and across different Ubuntu bases like `focal` and `jammy`.
    *   Specific comments highlight past incidents related to `java 8u292` TLS changes, underscoring the importance of careful Java version management in Docker images.
*   **Go Dependencies**: Several `go_repository` entries define Go modules with `importpath`, `sum`, and `version` (e.g., `com_github_boltdb_bolt`, `io_gorm_plugin_opentracing`).
*   **Tink-Specific Repositories**: A `new_git_repository` for `dropwizard_jersey` points to a `tink-ab` GitHub repository, indicating internal or custom forks/dependencies.
*   **Comments**: The file contains numerous comments explaining the purpose of sections, version considerations (e.g., Netty compatibility, maven pinning), and historical context for critical choices (like the `java 8u292` TLS issue).

## 10:28:04 AM
This log details configuration changes across multiple `values.yaml` files within the `event-muxer` project, specifically concerning various transaction ingestion and data retention services. All changes occurred on February 25, 2026, between 9:48 AM and 9:50 AM, suggesting a coordinated update or deployment across these services.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-abn-tx-ingestion/values.yaml` (9:48:36 AM):** Configures `sourceTopics` as "abn-ingest-tx-hi,abn-ingest-tx-lo" and `quarantineTopic` as "abn-muxer-quarantine" for the `event-muxer-abn-tx-ingestion` service.
*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-an-post-tx-ingestion/values.yaml` (9:48:44 AM):** Sets `sourceTopics` to "an-post-ingest-tx-hi,an-post-ingest-tx-lo" and `quarantineTopic` to "an-post-muxer-quarantine" for the `event-muxer-an-post-tx-ingestion` service.
*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-data-retention/values.yaml` (9:48:53 AM):** Defines `sourceTopics` as "data-retention-tx" and `quarantineTopic` as "muxer-quarantine-1" for the `event-muxer-data-retention` service.
*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-seb-tx-ingestion/values.yaml` (9:49:15 AM):** Configures `sourceTopics` as "seb-ingest-tx-hi,seb-ingest-tx-lo" and `quarantineTopic` as "seb-muxer-quarantine" for the `event-muxer-seb-tx-ingestion` service.
*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tps-tx-ingestion/values.yaml` (9:49:22 AM):** Sets `sourceTopics` to "ingest-tps-tx-hi,ingest-tps-tx-lo" and `quarantineTopic` to "ingest-tps-tx-quarantine" for the `event-muxer-tps-tx-ingestion` service.
*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-tx-ingestion/values.yaml` (9:49:28 AM):** Defines `sourceTopics` as "ingest-tx-hi,ingest-tx-lo" and `quarantineTopic` as "muxer-quarantine-1" for the general `event-muxer-tx-ingestion` service.
*   **`/Users/mmuruts/projects/tink/event-muxer/.charts/event-muxer-risk-tx-ingestion/values.yaml` (9:50:05 AM):** Configures `sourceTopics` as "risk-ingest-tx-hi,risk-ingest-tx-lo" and `quarantineTopic` as "risk-muxer-quarantine" for the `event-muxer-risk-tx-ingestion` service.

**Patterns and Recurring Elements:**

*   **Helm Chart Configurations:** All files are `values.yaml` files for Helm charts, located under `.charts/event-muxer-*`.
*   **Kafka Topic Definitions:** Each file defines `sourceTopics` and a `quarantineTopic`. The `sourceTopics` are generally pairs (e.g., `*-hi,*-lo`) and are noted as "hard-coded in connector" and "ordered with priority from high to low." The specific topic names vary per service but follow a consistent naming convention (e.g., `[prefix]-ingest-tx-hi`, `[prefix]-muxer-quarantine`). Exceptions include `data-retention-tx` and `muxer-quarantine-1`.
*   **Identical Monitoring Setup:** All services share an identical Datadog monitoring configuration:
    *   `monitors: enabled: true`, `k8sMonitors: enabled: true`.
    *   `namespaceAlerts: false`, `deploymentAlerts: true`, `canaryDeploymentAlerts: false`.
    *   Consistent alert thresholds: `cpuThrottledSeconds: "1"`, `serviceDeploymentReplicasLow: "80"`, `containerRestartsOnDeployment: "5"`.
    *   All alerts (including `deploymentCompletelyUnavailable`, `deploymentNotUpToDate`, `podCrashLoopBackoff`, `podContainerOOMKilled`) are configured to notify `@slack-Tink-squad-transactions-oncall` with `priority: 2`.
*   **Common Global Settings:** Every file has the same `global` configuration:
    *   `team: transactions`
    *   `namespace: event-muxer`
    *   `teamRoleId: dd98bfda-c6a1-11ee-97d4-da7ad0900005`
    *   `baseUrl: https://app.datadoghq.eu`
    *   `awsPrivateCAIssuer: trusted: true`
    *   The `service` name within the `global` block dynamically matches the chart name in the file path.

In summary, these changes represent a standardized configuration rollout or update for various transaction ingestion and data retention components within the `event-muxer` system, primarily differentiating by Kafka topic names while maintaining uniform monitoring and global service settings.

## 11:55:25 AM
The provided log entries capture the state of the `/Users/mmuruts/projects/tink/tink-backend/WORKSPACE` file across three timestamps on 2/24/2026: 4:07:54 PM, 4:08:07 PM, and 4:09:44 PM. No code changes were detected between these specific log entries; the file's content remained identical across all recorded instances.

The `WORKSPACE` file configures the Bazel build system for the `tink_backend` project, detailing a wide range of external dependencies and build tools.

**File-specific details:**

*   **Bazel Core:** It defines the workspace name as "tink_backend" and loads essential Bazel rules for Git, HTTP archives, and files (`git_repository`, `new_git_repository`, `http_archive`, `http_file`, `http_jar`).
*   **Platform Support:** Includes `platforms` version 0.0.4 to resolve JVM issues on Apple Silicon/M1, and `libcrypto` version 4.4.18-4_amd64.deb.
*   **Bazel Utilities:** Uses `bazel_skylib` (v1.2.0) and enforces a minimum Bazel version of 4.2.2.
*   **Language Rules:** Integrates `rules_java` (v4.0.0), `rules_clojure`, `rules_go` (v0.22.11), and `bazel_gazelle` (v0.22.2) for their respective language ecosystems.
*   **Packaging:** Incorporates `rules_pkg` (v0.6.0) for package creation.
*   **Protobuf and gRPC:** Sets `PROTOBUF_VERSION` to "3.11.3" and includes `rules_proto`. It also defines `GRPC_JAVA_VERSION` (1.29.0), `GRPC_JAVA_NANO_VERSION` (1.21.1), `IO_NETTY_VERSION` (4.1.48.Final), and `IO_NETTY_BORINGSSL_VERSION` (2.0.30.Final), along with specific overrides for transaction store Netty versions (e.g., `TX_STORE_GRPC_JAVA_VERSION = "1.49.2"`). `zlib` (v1.3.1) is also included.
*   **Build Tools:** Defines `com_github_bazelbuild_buildtools` (a specific commit from 2020-11-02) and explicitly pulls `buildifier` and `bazel_unused_deps` binaries (from buildtools 5.1.0) for various architectures (darwin\_x86\_64, darwin\_arm64, linux\_x86\_64).
*   **Go Modules:** Manages Go dependencies using `go_repository` for `github.com/boltdb/bolt` (v1.3.1) and `gorm.io/plugin/opentracing` (v0.0.0-20210506132430-24a9caea7709).
*   **Custom Repositories:** Includes a `dropwizard_jersey` repository from `tink-ab/dropwizard.git`.
*   **Docker Images:** A significant section is dedicated to pulling Docker container images from `784217881480.dkr.ecr.eu-west-1.amazonaws.com/tink-containers`. These include `openjdk-jdk8`, `openjdk-jdk8-jdk`, `main-service-base-eclipse-temurin`, `user-service-base-eclipse-temurin`, and multiple specific versions of `eclipse-temurin` Java 8 images (e.g., `8u345`, `8u352`, `8u362` for JRE/JDK on 'focal' and 'jammy' distributions), all pinned with `sha256` digests.

**Patterns and Recurring Elements:**

*   **Dependency Management with Hashes:** Almost all `http_archive` and `http_file` declarations specify `sha256` hashes, ensuring dependency integrity and reproducibility.
*   **Version Pinning:** Specific versions are pinned for almost all external tools and libraries (e.g., Bazel, Protobuf, gRPC, Java, Go rules, Docker images).
*   **Multi-Platform Support:** The inclusion of `buildifier` and `bazel_unused_deps` for macOS (x86_64 and arm64) and Linux (x86_64) demonstrates an intent for cross-platform development/build support.
*   **Containerization Focus:** Extensive `container_pull` entries indicate a heavy reliance on Docker containers for deployment and development, with meticulous versioning of base images.
*   **Java 8 Dominance:** A large number of Docker images are variations of Java 8 (OpenJDK and Eclipse Temurin), with comments highlighting past issues related to Java 8u292's TLS changes, indicating a cautious approach to Java updates.
*   **Netty Compatibility Concerns:** Comments repeatedly warn about Netty version compatibility with gRPC-Java and the need for Maven pinning, suggesting this has been a point of concern in the past.