# Activity Summary for 2/23/2026

## 9:25:06 AM
The recent changes primarily revolve around refining certificate management, particularly in the context of integrating with AWS Private CA, and addressing related deployment configurations and runtime errors.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml`**
    *   **2/19/2026, 9:48:42 AM:** Initial or updated comprehensive configuration for `tink-backend-system`, detailing resource limits, HPA settings, keystore and certificate pinning with a placeholder `CERTSHA256:9B7F...`, and numerous service endpoints, all referencing `/internal-ca/ca.crt` for pinned certificates. It also includes database settings, Cassandra configuration, task queue, caching, logging, transaction processing, and various feature flags. Crucially, `awsPrivateCAIssuer` was initially set to `trusted: false, enabled: false, used: false`.
    *   **2/19/2026, 10:15:28 AM:** A significant update where `awsPrivateCAIssuer` was fully enabled by setting `trusted: true, enabled: true, used: true`. This change indicates a shift towards utilizing AWS Private CA for internal certificate trust.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/scripts/generate-pinned-certificates.sh`**
    *   **2/19/2026, 9:56:23 AM:** An initial script designed to extract certificates from `/ca-trust-bundle/bundle.pem`, calculate their SHA256 fingerprints, and inject these pins into `system-server.yaml` using `sed`.
    *   **2/19/2026, 10:31:43 AM - 10:40:46 AM:** The script saw several rapid iterations, switching between `sed` and `awk` for injecting certificate pins into the configuration file. The `awk` method was introduced for multi-line replacement, passing `PINS` as an environment variable.
    *   **2/19/2026, 11:03:32 AM - 11:10:27 AM:** A major refinement to the `awk` logic for splitting the CA bundle. The original simple `awk` command was replaced with a more robust pattern-matching approach to safely capture individual certificates between `-----BEGIN CERTIFICATE-----` and `-----END CERTIFICATE-----` markers, ensuring accurate extraction and handling of potential non-certificate content. It also added checks for empty certificate files.
    *   **2/19/2026, 12:45:56 PM - 1:42:18 PM:** Further, somewhat inconsistent, iterations occurred, including a temporary reversion of the robust `awk` certificate splitting logic (at 12:45:56 PM, then reverted back at 12:49:54 PM) and repeated changes to the `TARGET_CONFIG` path detection logic (switching between absolute paths like `/config-with-secrets/system-server.yaml` and relative `system-server.yaml`, with the final state reverting to absolute paths).
    *   **2/19/2026, 1:56:53 PM & 1:59:57 PM:** The method for passing the `PINS` variable to `awk` for injection was refined from `export PINS; awk 'BEGIN { pins = ENVIRON["PINS"] }'` to a cleaner `awk -v pins="$PINS" '...'` syntax.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml`**
    *   **2/19/2026, 10:26:40 AM:** Defines the main Kubernetes Deployment, including health annotations, Datadog monitoring, and a series of `initContainers`. These initContainers handle secret fetching (AWS Secret Manager, Vaultify), certificate pinning (calling `generate-pinned-certificates.sh` conditionally), Cassandra migrations, and building Java trust/keystores. The `build-truststore` and `fetch-credentials` containers' configurations dynamically adjust based on whether `awsPrivateCAIssuer.trusted` is enabled.
    *   **2/19/2026, 10:28:04 AM:** The volume mounting logic for the `fetch-credentials` initContainer was simplified to use `config-with-secrets` if either `useAwsSecretManager` or `awsPrivateCAIssuer.trusted` is true.
    *   **2/20/2026, 12:08:16 PM:** The `generate-pinned-certificates` initContainer's volume mounts for `config-with-secrets` were simplified to be unconditional. More importantly, the `build-keystore` initContainer's `volumeMounts` logic was fully defined, including `mountPath: /tls` when `Values.awsPrivateCAIssuer.used` is true, and an explicit `else` block for other scenarios.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/jobs/delete-users-v2.yaml`**
    *   **2/19/2026, 4:07:29 PM:** Defines a Kubernetes Job for deleting users, mirroring the deployment's initContainer structure for secrets and certificate management. It specifies Java system properties for the deletion process, including a `userIdsFile`.
    *   **2/20/2026, 11:33:58 AM:** The `build-truststore` initContainer's `keytool -importkeystore` command was corrected to include missing `srcstorepass` and `deststorepass` arguments when `awsPrivateCAIssuer.trusted` is enabled. Additionally, a `scripts` ConfigMap volume was introduced.
    *   **2/20/2026, 11:58:46 AM:** The `scripts` ConfigMap volume definition was completed.
    *   **2/20/2026, 12:08:28 PM:** A minor adjustment in the `fetch-credentials` initContainer volume mounts logic, reverting a previous change regarding `config-with-secrets` usage.

*   **`/Users/mmuruts/projects/tink/tink-backend/log` and `/Users/mmuruts/projects/tink/tink-backend/log_1`**
    *   **2/19/2026, 3:11:51 PM - 3:43:01 PM (and recurring until 2/20/2026, 9:06:01 AM):** Multiple instances of `io.grpc.StatusRuntimeException: UNAVAILABLE: io exception` are logged, specifically caused by `javax.net.ssl.SSLHandshakeException: General OpenSslEngine problem` and `sun.security.provider.certpath.SunCertPathBuilderException: unable to find valid certification path to requested target`. This indicates a persistent issue with TLS/certificate trust in gRPC communication.
    *   **2/19/2026, 8:44:35 PM:** A `java.lang.NoSuchMethodError: Method io.netty.channel.epoll.NativeStaticallyReferencedJniMethods.tcpFastopenMode()I not found` error appeared once, pointing to a potential incompatibility or missing method in Netty's native Epoll transport library, likely due to versioning issues.

**Patterns and Recurring Elements:**

*   **Extensive Certificate Management:** A clear and consistent focus on handling and trusting certificates is evident across the codebase. This includes explicit certificate pinning, the use of `keytool` for building Java truststores and keystores, and a dedicated script to generate certificate pins.
*   **AWS Private CA Integration:** The enablement of `awsPrivateCAIssuer` in `values.yaml` is a central theme, driving conditional logic in deployment and job configurations to use AWS-specific certificate bundles (e.g., `.p12` files) for trust.
*   **Initialization Container Pattern:** Both the main deployment and the `delete-users-v2` job utilize a sequence of initContainers for a standardized setup process: fetching static secrets, templating them, generating certificate pins, fetching dynamic credentials from Vault, and building necessary Java trust/keystores.
*   **Persistent SSL/TLS Issues:** The repeated `SSLHandshakeException` entries in the logs directly correlate with the extensive certificate management changes, suggesting that despite the configuration efforts, the application is struggling to establish a valid trust path for its TLS connections. This points to ongoing debugging and refinement needed in the certificate setup.
*   **Inconsistent Script Pathing:** The `generate-pinned-certificates.sh` script shows some instability in its logic for determining the target configuration file path, frequently switching between absolute and relative paths, possibly indicating a struggle to adapt to different execution environments.
*   **`notAsecret` Password:** The literal string `notAsecret` is used consistently as a password for keystores and truststores across multiple configurations, which is likely a placeholder for secrets managed by other systems.

## 10:25:07 AM
The changes log details updates to the Tink backend system's Helm charts and identifies recurring runtime errors, particularly concerning certificate management and network communication.

**File-specific Updates:**

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml**:
    *   **2/19/2026, 9:48:42 AM**: This file initialized comprehensive configuration settings for the `tink-backend-system` Helm chart, covering resource allocation, HPA, numerous service endpoints with `pinnedCertificates` (many pointing to `/internal-ca/ca.crt`), database settings for Cassandra and Flyway, transaction processing, Redis, Tracing, Notifications, and feature flags. Notably, `awsPrivateCAIssuer` was initially disabled (`trusted: false`, `enabled: false`, `used: false`).
    *   **2/19/2026, 10:15:28 AM**: A significant configuration change was introduced, enabling the `awsPrivateCAIssuer` by setting `trusted: true`, `enabled: true`, and `used: true`. This indicates a strategic shift towards using AWS Private Certificate Authority for internal TLS.

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/scripts/generate-pinned-certificates.sh**:
    *   **2/19/2026, 9:56:23 AM**: The initial version of this shell script focused on generating SHA256 certificate pins from a CA bundle (`/ca-trust-bundle/bundle.pem`) using `awk` and `openssl`, then injecting these into `system-server.yaml` via `sed`.
    *   **2/19/2026, 10:31:43 AM - 10:40:46 AM**: There was a series of changes and reverts concerning the method of injecting pins into the config file. The script transitioned from using `sed` to `awk` (exporting PINS as an environment variable for multiline replacement), then temporarily reverted to `sed`, and finally settled back on the `awk` approach.
    *   **2/19/2026, 11:03:32 AM - 11:04:47 AM**: The `awk` logic for splitting the CA bundle (`bundle.pem`) into individual certificates was substantially refined to use a pattern range for safer and more accurate extraction of certificate blocks. An empty file check for generated certificate files was also added.
    *   **2/19/2026, 12:45:56 PM - 1:42:18 PM**: Further churn occurred in the script. The CA bundle splitting logic reverted to a simpler `awk` command, then reverted back to the more robust range-based `awk` method. The target configuration file path within the script was also changed multiple times between direct `system-server.yaml` and `/config-with-secrets/system-server.yaml` before finally settling on the latter.
    *   **2/19/2026, 1:56:53 PM - 1:59:57 PM**: The method for passing the `PINS` variable to the `awk` command for injection was updated to use the standard `awk -v pins="$PINS"` syntax, providing better clarity and robustness.

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml**:
    *   **2/19/2026, 10:26:40 AM**: This Kubernetes Deployment manifest defined the `tink-backend-system` deployment, including readiness/liveness probes, resource limits, and several `initContainers` for secret management (`secretman`, `secretman-template`), credential fetching (`fetch-credentials`), Cassandra migrations, and trust/keystore building. Conditional logic based on `awsPrivateCAIssuer.trusted` was already present for `fetch-credentials` and `build-truststore`. The `generate-pinned-certificates` init container's volume mounts were conditional based on `useAwsSecretManager`.
    *   **2/19/2026, 10:27:57 AM - 10:28:04 AM**: Conditional volume mounts for `generate-pinned-certificates` and `fetch-credentials` init containers were simplified. Specifically, `generate-pinned-certificates` now always mounts `config-with-secrets` for its output, and the `fetch-credentials` container's input config path uses `config-with-secrets` if either `useAwsSecretManager` or `awsPrivateCAIssuer.trusted` is true.
    *   **2/20/2026, 12:08:16 PM**: The `generate-pinned-certificates` init container's volume mount was further simplified to unconditionally mount `config-with-secrets`. A new conditional volume mount was added to the `build-keystore` init container, selecting between `grpc-certificate-aws` and `grpc-certificate` based on `.Values.awsPrivateCAIssuer.used`.

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/jobs/delete-users-v2.yaml**:
    *   **2/19/2026, 4:07:29 PM**: This Helm template defines a Kubernetes Job for `delete-users-v2`, with similar `initContainers` for secret handling, credential fetching, and truststore building as the main deployment, but initially excluded the `generate-pinned-certificates` container.
    *   **2/20/2026, 11:33:58 AM**: The `generate-pinned-certificates` init container was explicitly added to this job template, conditional on `awsPrivateCAIssuer.trusted`, alongside a new `scripts` ConfigMap volume to support it.
    *   **2/20/2026, 11:58:46 AM**: The `ca-trust-bundle` ConfigMap definition was expanded to include `bundle.p12`, which is likely required by the `build-truststore` init container when using the AWS Private CA Issuer.

*   **/Users/mmuruts/projects/tink/tink-backend/src/aggregation-controller/lib/src/main/java/se/tink/backend/aggregationcontroller/identityaggregator/client/grpc/IdentityAggregatorServiceStorageChannelProvider.java**:
    *   **2/20/2026, 2:33:18 PM**: This Java file shows explicit implementation for building an `SslContext` for gRPC client connections using `pinnedCertificates` provided as file paths. It reads certificate data, parses them, and configures the `NettyChannelBuilder` for transport security if pinned certificates are present. This functionality is marked with "GENAI" comments. Subsequent entries for this file are identical, suggesting minor saves without functional changes.

*   **/Users/mmuruts/projects/tink/tink-backend/log** and **/Users/mmuruts/projects/tink/tink-backend/log_1**:
    *   **2/19/2026, 3:11:51 PM - 2/20/2026, 1:38:20 PM**: Multiple log entries consistently report `io.grpc.StatusRuntimeException: UNAVAILABLE: io exception` with `javax.net.ssl.SSLHandshakeException: General OpenSslEngine problem` and `sun.security.provider.certpath.SunCertPathBuilderException: unable to find valid certification path to requested target`. These errors specifically occur during `deleteUser` operations via `IdentityServiceFetchingChannelGrpc`, indicating persistent SSL trust issues when the `tink-backend-system` attempts to communicate with the `identity-aggregator` service.
    *   **2/19/2026, 8:44:35 PM**: A new error `java.lang.NoSuchMethodError: Method io.netty.channel.epoll.NativeStaticallyReferencedJniMethods.tcpFastopenMode()I not found` appeared during Cassandra initialization, suggesting a potential runtime incompatibility with Netty's native transport libraries. This error is distinct from the SSL handshake failures.

**Patterns and Recurring Elements:**

The changes demonstrate a clear effort to transition and solidify the use of **AWS Private CA for internal TLS/SSL certificate management**. This involves modifying Helm `values.yaml` to enable it, adapting Kubernetes deployment and job manifests (`deployment.yaml`, `delete-users-v2.yaml`) to use AWS-specific certificate bundles and `keytool` commands, and implementing `generate-pinned-certificates.sh` to dynamically inject certificate pins.

Despite these efforts, there is a **persistent pattern of `SSLHandshakeException` errors** in the logs, indicating that the certificate pinning and trust chain establishment for gRPC communication, particularly with the `IdentityAggregatorService`, is a challenging and ongoing issue. The development of `IdentityAggregatorServiceStorageChannelProvider.java` explicitly for handling pinned certificates confirms the direct focus on resolving these SSL problems.

The `generate-pinned-certificates.sh` script, in particular, shows **frequent iterative refinements and occasional reverts**, highlighting the complexity and trial-and-error nature of safely and correctly extracting and injecting certificate fingerprints into configuration files within the Kubernetes environment.

A separate **`NoSuchMethodError` related to Netty epoll** emerged, suggesting a potential underlying library version mismatch or environment configuration issue impacting Cassandra connectivity, adding another dimension to the system's operational challenges.