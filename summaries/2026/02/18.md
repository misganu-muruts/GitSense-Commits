# Activity Summary for 2/18/2026

## 9:06:14 AM
The logs show changes across several Helm chart configuration files for a `tink-backend` project, primarily focusing on `tink-backend-system` and related services.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml`**
    *   This file, dating from 2/17/2026, defines comprehensive configuration parameters for the `tink-backend-system` Helm chart. It includes settings for resource allocation (`cpu: 2000m`, `memory: 4Gi`), horizontal pod autoscaling (HPA), keystores, and a multitude of internal service endpoints (e.g., `aggregationController`, `identityAggregatorServiceConfiguration`, `oauth`, `account`, `credentials`, `aggregationEngine`, `transactionAccess`).
    *   Significant changes within this file occurred around the `awsPrivateCAIssuer` configuration. Initially, at 1:35:25 PM, it was set to `trusted: true`, `enabled: true`, `used: false`. This was then changed to `trusted: false`, `enabled: false` at 1:39:25 PM. The configuration reverted to `trusted: true`, `enabled: true` at 1:46:28 PM, and then had its `used` flag set to `true` at 1:46:37 PM. Finally, all three `awsPrivateCAIssuer` flags (`trusted`, `enabled`, `used`) were reset to `false` at 1:51:30 PM. These multiple toggles suggest active testing or reconfiguration related to AWS Private CA integration.
    *   Other configurations, such as database pool size (`maxPoolSize: 20`), Cassandra settings, task queue parameters, feature flags (e.g., `enableAggJourneyTrackingEvents: true`, `useAggregationController: true`), and specific job settings (e.g., `tempUserDeletionEnabled: true`), remained consistent across all logged timestamps for this file.
    *   A notable placeholder is `keystore.password: notAsecret`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/config.yaml`**
    *   This Kubernetes ConfigMap template, logged five times between 1:44:43 PM and 2:03:44 PM on 2/17/2026, defines the `system-server.yaml` content. It uses Helm templating to dynamically inject values from `values.yaml`.
    *   It configures two HTTPS application connectors (ports 8443, 8444) and one HTTP admin connector (port 8081).
    *   Keystore and truststore paths are configured with conditional logic, allowing for retrieval of `keyStorePassword` and `accessToken` from AWS Secret Manager or Vault, or using direct values. It also sets `trustStorePath` to either `/var/lib/kubernetes/ca.jks` (if `awsPrivateCAIssuer.trusted` is true) or `data/security/TinkCA.truststore`. Certificate validation is explicitly disabled (`validateCerts: false`).
    *   Various service endpoints are configured, mirroring the structure in `values.yaml`, with conditional `pinnedCertificates` based on `awsPrivateCAIssuer.trusted` or specific `/internal-ca/ca.crt` paths.
    *   No content changes were observed across the multiple logged entries for this file.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/oxford-staging-product001.yaml`**
    *   Logged at 1:51:00 PM on 2/17/2026, this environment-specific `values.yaml` file for `oxford-staging-product001` sets the `taskQueue.topicPrefix` to `oxford-staging-product001`. This indicates environment-specific overrides for the base `account-processing` chart.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/change-certain-date-job.yaml`**
    *   This Kubernetes Job template, logged at 1:51:09 PM on 2/17/2026, defines a job named `change-certain-date-job` within the `account` namespace.
    *   It's designed to run a script (`change-certain-date.sh`) from a `net-tools` container image.
    *   The job is configured to run `Never` (restartPolicy), has a `ttlSecondsAfterFinished` of 300 seconds, and sets environment variables `CERTAIN_DATE` to `"2025-10-01T00:00:00Z"` and `DRY_RUN` to `"false"`, indicating it is intended to perform actual updates.
    *   It requests and limits `500m` CPU and `1Gi` memory.
    *   Volumes include a persistent volume claim for `large-data` and a config map for scripts.

**Patterns and Recurring Elements:**

*   **Helm Chart Usage**: The log showcases extensive use of Helm charts for managing Kubernetes deployments, with `values.yaml` for configuration and `templates/*.yaml` for defining Kubernetes resources based on those values.
*   **Dynamic Configuration**: There's a strong pattern of dynamic configuration through Helm templating, allowing parameters like `keyStorePassword` and `accessToken` to be fetched conditionally from Vault or AWS Secret Manager.
*   **Microservice Architecture**: The presence of numerous `endpoints` configurations for various services (e.g., `aggregationController`, `identityAggregator`, `oauth`, `account`, `credentials`) indicates an interconnected microservice architecture.
*   **Certificate Management**: A recurring theme is the configuration of TLS, including `pinnedCertificates` for various internal endpoints and explicit truststore definitions, often with conditional logic to handle different certificate authority setups (e.g., internal CA vs. AWS Private CA).
*   **Environment-Specific Overrides**: The `oxford-staging-product001.yaml` demonstrates the practice of overriding default chart values for specific deployment environments.
*   **Temporary Jobs**: The `change-certain-date-job.yaml` illustrates the use of Kubernetes Jobs for one-off tasks, with safeguards like `DRY_RUN` and `ttlSecondsAfterFinished`.
*   **Consistent Resource Definitions**: Resource limits and requests are consistently defined for services and jobs, showing standardized resource management.

## 9:06:18 AM
The changes primarily affect two Go files related to an internal gRPC transaction service: `internal_server_test.go` and `internal_server.go`.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server_test.go`**
    *   **Timestamp: 2/17/2026, 9:55:59 AM:** This file contains comprehensive unit tests for the gRPC internal transaction service.
        *   It defines `mockCassandraRepo` and `mockSearchClient` to simulate interactions with Cassandra and an Elasticsearch search client, respectively. These mocks cover various transaction operations like `GetTransactionsByUserIdAndIdsWithPeriods`, `FindByUserIDAndPeriodWithCursor`, `Search`, `SaveBatch`, and delete operations.
        *   Tests are structured using `t.Run` for `Test_GetTransaction` and `Test_ListTransactions`.
        *   `Test_GetTransaction` includes test cases for valid requests, missing user/transaction IDs (validation errors), scenarios where a transaction is not found in Cassandra, and cases where Cassandra returns an error.
        *   `Test_ListTransactions` (partially shown) includes tests for valid requests, validation errors (missing user ID), and scenarios where no transactions are found in Cassandra.
        *   Feature toggles, specifically `fetch-v1-from-cassandra-for-v2`, are enabled within the tests using a fake Unleash client, indicating that the service's behavior under feature flags is being tested.
        *   The `github.com/stretchr/testify/assert` and `require` packages are used for assertions.
    *   **Timestamp: 2/17/2026, 2:39:18 PM & 2/17/2026, 2:40:10 PM:** No functional code changes were observed in this file at these timestamps; the content is identical to the earlier entry. This suggests minor save operations without modifications.

*   **`/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server.go`**
    *   **Timestamp: 2/17/2026, 10:34:19 AM:** This is the core implementation file for the internal transactions gRPC service.
        *   It introduces a `cassandraRepository` interface defining methods like `GetTransactionsByUserIdAndIdsWithPeriods`, `FindPeriodsByUserIDV2`, and `GetTransactionsByUserIdAndIdsV2` for Cassandra operations.
        *   The `InternalTransactionsServiceServerImpl` struct is introduced, holding dependencies for search, Cassandra, account service, user repository, logger, and a configuration struct.
        *   The `NewInternalTransactionsServiceServer` constructor initializes a `Config` struct, populating `ProviderMarkets` from an environment variable "provider-markets".
        *   Key methods `GetTransaction` and `ListTransactions` implement logic for fetching transactions. This logic is heavily dependent on feature toggles:
            *   If `featuretoggle.CompareEsAndCassandraTransactionResponsesToggle` is enabled, the service first fetches data synchronously from Elasticsearch, then asynchronously fetches from Cassandra and compares the results (`compareTransactionAsync`/`compareTransactionListAsync`), logging any mismatches. The Elasticsearch data is returned immediately to the user.
            *   If the user is a `TemporaryUser`, data is fetched directly from Elasticsearch.
            *   If `featuretoggle.FetchV1FromCassandraForV2Toggle` is enabled, data is fetched from Cassandra (for `GetTransaction` and for `ListTransactions` when `TransactionIdIn` is specified).
            *   Otherwise, data is fetched from Elasticsearch.
        *   New `compareTransactionAsync` and `compareTransactionListAsync` functions are added. These functions run in separate goroutines with timeouts and include panic recovery. They log the start of the comparison, any errors during Cassandra fetch, and the match/mismatch status of the data between Elasticsearch and Cassandra.
    *   **Timestamp: 2/17/2026, 2:20:48 PM:** No functional code changes were observed; the content is identical to the earlier entry.
    *   **Timestamp: 2/17/2026, 2:27:49 PM:** The order of conditional checks within the `GetTransaction` method was modified. Specifically, the check for `userType == elasticsearch.TemporaryUser` was moved to occur *before* the `featuretoggle.CompareEsAndCassandraTransactionResponsesToggle` check.
    *   **Timestamp: 2/17/2026, 2:28:24 PM:** Similarly, the order of conditional checks within the `ListTransactions` method was modified. The check for `userType == elasticsearch.TemporaryUser` was moved to occur *before* the `featuretoggle.CompareEsAndCassandraTransactionResponsesToggle` check.
    *   **Timestamp: 2/17/2026, 2:28:50 PM & 2/17/2026, 2:29:09 PM:** No functional code changes were observed in this file at these timestamps; the content is identical to the 2:28:24 PM entry.

**Timestamps of Significant Changes:**

*   **2/17/2026, 9:55:59 AM:** Initial comprehensive unit tests for the gRPC service, setting up mocking and covering various success and error scenarios.
*   **2/17/2026, 10:34:19 AM:** Introduction of the core gRPC server logic, including:
    *   Mechanism for configuring provider markets via environment variables.
    *   Logic for routing transaction fetches based on feature toggles (Cassandra vs. Elasticsearch).
    *   Implementation of asynchronous data comparison between Elasticsearch and Cassandra results, indicating a data migration or consistency monitoring effort.
*   **2/17/2026, 2:27:49 PM and 2:28:24 PM:** Reordering of conditional logic in both `GetTransaction` and `ListTransactions` methods to prioritize handling `TemporaryUser` requests by fetching exclusively from Elasticsearch before considering the asynchronous comparison logic.

**Patterns or Recurring Elements:**

*   **Feature Toggles:** The `internal_server.go` file heavily relies on feature toggles (`CompareEsAndCassandraTransactionResponsesToggle`, `FetchV1FromCassandraForV2Toggle`) to control the data source (Elasticsearch or Cassandra) and activate asynchronous data comparison.
*   **Dual Data Storage and Migration:** The presence of both Elasticsearch and Cassandra as data sources, coupled with the asynchronous comparison logic, strongly suggests a strategy for data migration, data consistency validation, or a dual-read approach.
*   **Asynchronous Processing:** The `compareTransactionAsync` and `compareTransactionListAsync` functions highlight a pattern of offloading non-critical operations to background goroutines to maintain low latency for user-facing API calls.
*   **Extensive Logging:** The service includes detailed logging statements indicating the data source, application ID, user ID, transaction ID, and the outcome of data comparisons (match/mismatch), which is crucial for monitoring data consistency during migration or dual-write phases.
*   **Robust Error Handling:** Both the main service methods and asynchronous comparison functions incorporate error handling, including panic recovery for background goroutines.
*   **Repeated Saves/Commits:** Multiple consecutive identical entries in the log for the same file, especially for `internal_server.go` after the reordering, indicate frequent save or commit actions without additional code modifications.