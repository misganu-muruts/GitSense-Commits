# Activity Summary for 2/3/2026

## 9:35:12 AM
The changes primarily involve Helm `values.yaml` configuration files for a `transaction-processing-service` within a `tink` project, specifically managing environment-specific settings for staging deployments.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml`**

*   **Timestamps:** Updates occurred at 2/2/2026, 1:18:43 PM; 1:27:29 PM; 1:43:49 PM. There were also identical entries at 1:27:39 PM and 1:45:35 PM, suggesting re-saves without content changes.
*   **Key Updates:**
    *   The `awsPrivateCAIssuer` configuration saw several modifications. Initially, it was set with `trusted: true`, `enabled: false`, `used: false`.
    *   At 1:27:29 PM, `awsPrivateCAIssuer.used` was changed from `false` to `true`.
    *   Later, at 1:43:49 PM, `awsPrivateCAIssuer.trusted` was changed from `true` to `false`. The `enabled` flag remained `false` throughout these changes.
    *   This file represents the default settings, with most observability, Redis, categorization, and Fasttext features disabled, and `canaryDeployments` set to `DISABLED`.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/barnsley-staging.yaml`**

*   **Timestamps:** Changes were recorded at 2/2/2026, 1:42:30 PM; 1:49:05 PM; 1:49:40 PM.
*   **Key Updates:**
    *   This file defines staging-specific overrides. It enables `datadog` observability (log injection and profiling), `useVault`, `useSecretsManager`, `categorization.userLearningEnabled`, `redisConfiguration`, `cassandra`, and `fasttext`.
    *   It specifies a unique `database.url` and `awsAccountId`.
    *   `processing.workersMaxQueueSize` is increased to `1000` from the default `200`.
    *   `canaryDeployments.phase` is set to `COMPLETE`.
    *   The `awsPrivateCAIssuer.enabled` flag was briefly set to `true` at 1:49:05 PM but then reverted back to `false` at 1:49:40 PM, indicating a short-lived experimental change or a quick rollback.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/oxford-staging.yaml`**

*   **Timestamps:** Entries from 2/2/2026, 1:43:21 PM to 1:49:28 PM show no changes, indicating a stable configuration within the provided log.
*   **Key Updates:**
    *   Similar to `barnsley-staging`, this environment enables `datadog`, `useVault`, `useSecretsManager`, `userLearningEnabled`, `redisConfiguration`, `cassandra`, and `fasttext`, and sets `canaryDeployments.phase: COMPLETE`.
    *   It has its own specific `database.url` and `awsAccountId`.
    *   **Unique to Oxford-staging:** `bfmEnabled: true`, `bfmUserLearningEnabled: true`, and explicit `bfmAppIds` are defined, indicating active use of Bank Financial Management features.
    *   It sets `tasksQueue.topicPrefix: 'oxford-staging-product001'`.
    *   `processing.reconcileUsingProviderExternalId` is set to `true`, differing from the default and `barnsley-staging`.
    *   The `awsPrivateCAIssuer` configuration (`trusted: true`, `enabled: false`, `used: false`) remained consistent.

**File: `/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/jersey-staging.yaml`**

*   **Timestamps:** A single entry at 2/2/2026, 1:45:19 PM.
*   **Key Updates:**
    *   This configuration also enables `datadog`, `useVault`, `useSecretsManager`, `redisConfiguration`, `cassandra`, and sets `canaryDeployments.phase: COMPLETE`.
    *   It provides a unique `database.url` and `awsAccountId`.
    *   **Unique to Jersey-staging:** Categorization features are explicitly disabled (`enabled: false`, `mxCategoryMappingEnabled: false`, `userLearningEnabled: false`, `bfmUserLearningEnabled: false`, `bfmAppIds: null`), and `fasttext` is also disabled. This contrasts with other staging environments that enable categorization or Fasttext.
    *   Notably, `awsPrivateCAIssuer.enabled` is set to `true` in this configuration, which is different from the default and the consistent state of `oxford-staging`.

**Patterns and Recurring Elements:**

*   **Staging Environment Overrides:** All specific staging environment `values` files (`barnsley`, `oxford`, `jersey`) consistently override the base `values.yaml` to enable core infrastructure components like Datadog for observability, Vault, Secrets Manager, Redis, and Cassandra, and set `canaryDeployments.phase` to `COMPLETE`.
*   **Environment-Specific Database and AWS Account IDs:** Each staging environment configures a unique `database.url` and `awsAccountId`.
*   **`springProfile: staging`:** This parameter is consistently set to `staging` across all configurations, confirming their purpose.
*   **`awsPrivateCAIssuer` Fluctuations:** The `awsPrivateCAIssuer` configuration is a recurring point of change or variation. Its `trusted` and `used` flags are toggled in the default `values.yaml`, and its `enabled` flag is briefly toggled in `barnsley-staging` and is uniquely `true` in `jersey-staging`.
*   **Categorization Feature Differentiation:** There's a clear pattern of varying categorization and BFM (Bank Financial Management) feature enablement across environments: disabled in `jersey-staging`, partially enabled (`userLearningEnabled`) in `barnsley-staging`, and fully enabled with specific `bfmAppIds` in `oxford-staging`. The default `values.yaml` has categorization enabled but BFM/user learning disabled.
*   **Fasttext Usage:** `fasttext` is enabled in `barnsley-staging` and `oxford-staging`, but disabled in the default `values.yaml` and `jersey-staging`.
*   **Processing Queue Size:** The `processing.workersMaxQueueSize` is consistently `1000` in staging environments, significantly higher than the default `200`.
*   **Reconciliation Logic:** `reconcileUsingProviderExternalId` is `true` only in `oxford-staging`, while it remains `false` in the default, `barnsley-staging`, and `jersey-staging` configurations.

## 9:35:15 AM
The provided log details changes primarily within the `transaction-service` and `transaction-service-reconciliation-worker` Helm charts, focusing on `values.yaml` files for various staging and production environments, and `config.yaml` and `deployment.yaml` templates. The timestamps range from January 30, 2026, to February 3, 2026, indicating active development and configuration adjustments within a short period.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/*.yaml` (e.g., `jersey-staging.yaml`, `oxford-staging-product001.yaml`, `barnsley-staging.yaml`, `oxford-staging.yaml`, `farnham-production.yaml`, `oxford-production.yaml`):**
    *   These files define environment-specific configurations.
    *   **Elasticsearch:** Hosts are unique to each environment (e.g., `vpc-search-jersey-staging`, `vpc-search-oxford-staging`, `vpc-search-barnsley-staging`, `vpc-search-farnham-production`, `vpc-search-oxford-production`). `startIndexYear` and `endIndexYear` are set, typically 2017-2028 or 2022-2028. `nshards` for `common` index vary (e.g., 10, 50, 200). Some `oxford` environments (staging, production, product001) configure multiple Elasticsearch clusters (`common`, `nordea`, `seb`) with specific `appIds`, `syncbehaviour`, and `sniffing`/`healthCheck`/`retry` settings. `tempUserGracePeriodEnabled` is consistently set.
    *   **Cassandra:** Hosts are also environment-specific (e.g., `bigdb-0.internal.staging.jersey.tink.network`). `useTLS` is enabled for `oxford-staging` and `oxford-production`.
    *   **CassandraV2:** `oxford-staging` and `oxford-production` enable CassandraV2 with `keyspace: tink_transactions` and `username: app_tink_transactions`.
    *   **Reconciliation:** `enabled: true` and `dryRun: false` are common for most environments, with `sqs.batchSize` typically 5. `reconWorkerMinReplicas` and `reconWorkerMaxReplicas` vary (e.g., 2-4, 2-3, 3-6).
    *   **Data Retention:** `dataretention` is enabled for `farnham-production`, `oxford-staging`, `oxford-production`, `farnham-staging` (with `dryRun: true` in staging and `dryRun: false` in production), and `cronJob` defined in `farnham-production`.
    *   **Service Communication:** `userSvc` is frequently enabled across environments, with a default `addr` and `ca` path. `accountSvc` is enabled for `oxford-staging` and `oxford-production`.
    *   **Redis:** Enabled for `oxford-staging` and `oxford-production` with specific hosts and `ttl`.
    *   **Resource Requests:** `podMemoryRequest`, `podCpuRequest`, `podTargetCPU`, `podMinReplicas`, `podMaxReplicas` are configured, varying by environment (e.g., `2Gi`/`1.25` for `oxford-staging-product001`, `1Gi`/`1` for `farnham-staging`, `3Gi`/`2.25` for `oxford-production`).
    *   **Canary Deployments:** `phase: COMPLETE` is consistently set across most staging and production environments, while `values.yaml` in the `reconciliation-worker` chart defaults to `DISABLED`.
    *   **AWS Account IDs:** Unique `awsAccountId` values are present in several environment-specific `values.yaml` files.
    *   **AWS Private CA Issuer:** The `awsPrivateCAIssuer` settings (trusted, enabled, used) are changed in various `values.yaml` files, fluctuating between `true` and `false` values, indicating active adjustments related to certificate management. `oxford-staging-product001.yaml` had `trusted: false` then `trusted: true` within minutes. `awsPrivateCAIssuer.enabled` also sees changes from `false` to `true` in later updates.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml` (Multiple entries around 1/30/2026, 11:53 AM - 12:02 PM):**
    *   This Helm template generates a Kubernetes ConfigMap for the `transaction-service`.
    *   It dynamically sets `log-level`, service addresses (`user-svc-addr`, `account-svc-addr`), data retention, Elasticsearch cluster configurations (common, nordea, seb), SNS, Kafka topics, Cassandra, and Redis settings based on values passed from the `values.yaml` files.
    *   **Certificate Authority (CA) Paths:** A notable pattern is the conditional logic for `user-svc-ca` and `account-svc-ca`, and `cassandra-ca-path`, switching between `"/ca-trust-bundle/bundle.pem"` (if `awsPrivateCAIssuer.trusted` is true) and `"/tls/ca.crt"` (otherwise). This indicates a move towards or support for AWS Private CA for trusted certificates.
    *   **Cassandra/Main DB Credentials:** Utilizes `vault` for fetching credentials for `cassandra-username`, `cassandra-password`, `main-db-user`, and `main-db-password` when `useVault` is true, otherwise uses values from `$cassandra` and `$maindb` config.
    *   **Kafka:** Configuration for `userDeletionListener` and `accountDeletionListener` (hosts, topics, groups) is present, with TLS enabled if either listener is active.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml` (Multiple entries around 2/2/2026, 9:52 AM - 11:13 AM, and 2/3/2026, 9:11 AM - 9:23 AM):**
    *   This is the default `values.yaml` for the reconciliation worker.
    *   **Reconciliation:** `reconciliation.enabled` is `false` by default, `dryRun: true`, `sqs.batchSize: 1`, `shutdownTimeout: 60` (later updated to `110` with a warning about message timeout). `maxConcurrency: -1`.
    *   **AWS Private CA Issuer:** Initially `trusted: false, enabled: false, used: false`, then `trusted: true, enabled: false, used: false`, and eventually `trusted: true, enabled: true, used: false`.
    *   **Datadog Monitors:** Extensive Datadog monitoring is enabled by default (`monitors: enabled: true`, `k8sMonitors: enabled: true`) with specific alert thresholds and Slack notifications configured for CPU throttling, low replicas, unavailable deployments, OOMKilled pods, CrashLoopBackoff, and container restarts.
    *   **Tags Updater:** `tagsUpdater: enabled: false` by default, but the `config.yaml` template suggests its parameters (`shutdownTimeout`).

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/config.yaml` (Multiple entries around 1/30/2026, 12:33 PM - 12:37 PM, and 2/2/2026, 10:52 AM - 11:01 AM, and 2/3/2026, 9:15 AM):**
    *   This Helm template generates a ConfigMap specifically for the `reconciliation-worker`.
    *   Similar to the main service's `config.yaml`, it uses values from `values.yaml` to set `log-level`, `user-svc-enabled`, Elasticsearch configurations, `secret-manager-enabled`, Cassandra settings, Kafka hosts, and processing callback URLs.
    *   **CA Paths:** Conditional logic for `user-svc-ca`, `cassandra-ca-path`, `cassandra-v2-ca-path`, and `cert-path` depending on `awsPrivateCAIssuer.trusted` is present, consistently pointing to `/ca-trust-bundle/bundle.pem` when trusted.
    *   **Reconciliation Specifics:** Includes `reconciliation-queue-name`, `reconciliation-dry-run`, `firehose-transactions-topic`, `reconciliation-sqs-batch-size`, `reconciliation-shutdown-timeout`, and `reconciliation-max-concurrency`.
    *   **New `tags-updater-shutdown-timeout` parameter:** Added on 2/3/2026, 9:15:38 AM, defaulting to 30.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml` (Multiple entries around 1/30/2026, 12:05 PM and 2/2/2026, 9:59 AM - 11:44 AM):**
    *   This Helm template defines the Deployment for the `transaction-service-reconciliation-worker`.
    *   **Init Containers:** Includes an `initContainer` named `fetch-credentials` using `vaultify` for secret retrieval if `useVault` is enabled. The `VAULT_CACERT` path in this init container also became conditional based on `awsPrivateCAIssuer.trusted` around 2/2/2026, 11:41:55 AM.
    *   **Containers:** Defines the `transaction-service-reconciliation-worker` container and an optional `transaction-service-tags-updater` container (if `tagsUpdater.enabled`).
    *   **Environment Variables:** Sets `FEATURE_TOGGLE_ENABLED`, `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD` (from secrets), Datadog APM settings (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`), and `GOMAXPROCS`.
    *   **Volume Mounts:** Mounts for certificates (`tls`, `internal-ca`, `ca-trust-bundle`), configuration (`config`), credentials (`credentials`), and Datadog agent (`datadog-agent`). The mount paths for CA bundles were frequently adjusted between `/ca-trust-bundle` and `/vault_ca`/`/tls/internal-ca` based on `awsPrivateCAIssuer.trusted`.
    *   **Liveness/Readiness Probes:** Standard HTTP GET probes are configured.
    *   **Resource Requests/Limits:** `podMemoryRequest` and `podCpuRequest` are dynamically set.
    *   **Affinity:** Configures pod anti-affinity to ensure worker distribution across nodes/zones in non-local clusters.
    *   **Volume Definitions:** Defines volumes for secrets (`grpc-certificate`, `internal-ca`), config maps (`reconciliation-worker-configuration`, `ca-trust-bundle`), empty directory for credentials, and host path for datadog agent. The CA bundle volume was refactored based on `awsPrivateCAIssuer.trusted` status. A change on 2/2/2026, 11:35:54 AM, modified the conditional for `vault-tls` volume to specifically check `ne .Values.Cluster "dev"` instead of `"local"`.

*   **`/Users/mmuruts/projects/tink/transaction-service/master_render_7.yaml` (1 entry on 2/2/2026, 11:37:01 AM):**
    *   This appears to be a rendered Helm chart output, combining `PodDisruptionBudget` and `ConfigMap` for the `reconciliation-worker`, and several `DatadogMonitor` definitions.
    *   **PodDisruptionBudget:** Ensures high availability with `maxUnavailable: 1`.
    *   **ConfigMap:** Shows the compiled reconciliation worker configuration with default or interpolated values. Notably, cluster and environment variables (`{{ .Values.Cluster }}` and `{{ .Values.Environment }}`) are empty in the rendered file, suggesting an issue with the rendering context or placeholder values.
    *   **DatadogMonitors:** Defines various alerts for the `transaction-service-reconciliation-worker` focusing on Kubernetes state metrics: `container-restarted`, `cpu-throttled-seconds`, `deployment-completely-unavailable`, `deployment-not-up-to-date`, `pod-container-oom-killed`, and `pod-crash-loop-backoff`, and `service-deployment-replicas-low`. All monitors specify `priority: 2` and notify `@slack-Tink-squad-transactions-oncall`.

**Timestamps of Significant Changes:**

*   **January 30, 2026:**
    *   **11:46 AM - 11:48 AM:** Initial configuration of `values.yaml` for various staging and production environments (`jersey-staging`, `oxford-staging-product001`, `barnsley-staging`, `oxford-staging`). These set up basic Elasticsearch, Cassandra, and reconciliation parameters, and define `awsAccountId` and `canaryDeployments.phase`.
    *   **11:53 AM - 12:02 PM:** Multiple, very frequent changes to `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml`. These changes are mostly identical, possibly reflecting repeated saves or minor formatting adjustments, indicating active iteration on the core service configuration template. The key aspect here is the introduction of conditional logic for CA paths based on `awsPrivateCAIssuer.trusted`.
    *   **12:05 PM:** First appearance of `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`, setting up the deployment for the reconciliation worker.
    *   **12:29 PM - 12:30 PM:** Several identical updates to `farnham-production.yaml`, suggesting minor non-content changes or re-saves.
    *   **12:33 PM:** First appearance of `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/config.yaml`.

*   **February 2, 2026:**
    *   **9:52 AM - 11:13 AM:** Multiple, frequent identical updates to `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml`, again likely re-saves or minor non-content changes. A change around 9:54 AM adds `used: false` to `awsPrivateCAIssuer`.
    *   **9:59 AM - 11:35 AM:** Extremely frequent, largely identical updates to `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`. This indicates very active development or debugging on the deployment configuration. A significant change at **11:35:54 AM** modified the `vault-tls` volume condition from `ne .Values.Cluster "local"` to `ne .Values.Cluster "dev"`.
    *   **11:37 AM:** A full rendered Helm chart (`master_render_7.yaml`) is generated, showing the combined configuration and Datadog monitors. This indicates a build or deployment step. It reveals potential issues where `{{ .Values.Cluster }}` and `{{ .Values.Environment }}` placeholders are not being filled, leading to incomplete paths like `reconciliation-output-queue--` and `-/transaction-service/HMacToken`.
    *   **11:41 AM - 11:44 AM:** Further frequent, mostly identical updates to `transaction-service-reconciliation-worker/templates/deployment.yaml`.
    *   **11:56 AM - 11:59 AM:** Updates to environment-specific `values.yaml` files for the reconciliation worker (`barnsley-production`, `jersey-staging`, `farnham-staging`, `oxford-staging`, `oxford-staging-product001`), mostly copying values from the main service. `oxford-staging-product001.yaml` specifically sets `firehoseTransactionsTopic`.
    *   **3:29 PM - 3:37 PM:** Updates to `oxford-staging.yaml`, `farnham-staging.yaml`, `barnsley-staging.yaml`, `oxford-staging-product001.yaml` within the main `transaction-service` chart, and also to the global `values.yaml` for the main service. A notable change is setting `awsPrivateCAIssuer.enabled: true` in several environment-specific `values.yaml` files. The global `values.yaml` for the main service also sees `awsPrivateCAIssuer.trusted` changed to `false` and `enabled: false`.

*   **February 3, 2026:**
    *   **9:11 AM:** Update to `transaction-service-reconciliation-worker/values.yaml`, changing `reconciliation.sqs.shutdownTimeout` to `110` with a new comment explaining its impact on message timeout.
    *   **9:13 AM:** Update to `oxford-production.yaml` for the main service, setting specific resource requests/limits, enabling CassandraV2, user/account deletion listeners, and feature toggles, and defining multiple Elasticsearch clusters (common, nordea, seb) with specific shard counts and `appIds`.
    *   **9:15 AM:** Significant update to `transaction-service-reconciliation-worker/templates/config.yaml`, introducing a new variable `$tagsUpdater` and adding `tags-updater-shutdown-timeout` to the configuration.
    *   **9:23 AM - 9:24 AM:** Updates to `values.yaml` files for the reconciliation worker in `jersey-staging` and `barnsley-staging`, setting `awsPrivateCAIssuer.enabled: true`. Also, the global `values.yaml` for reconciliation worker is updated, setting `awsPrivateCAIssuer.trusted: true, enabled: true, used: false`.
    *   **9:29 AM - 9:30 AM:** Introduction of `certificate-grpc-certificate-aws.yaml` templates in both `transaction-service` and `transaction-service-reconciliation-worker` charts. These define `Certificate` resources for `grpc-certificate-aws` using an `AWSPCAClusterIssuer`, conditionally enabled by `awsPrivateCAIssuer.enabled`. The reconciliation worker's certificate is given a significantly shorter `duration` (7 days) and `renewBefore` (2 days), compared to the main service's (90 and 30 days, respectively), suggesting a short-lived certificate strategy for the worker.

**Patterns and Recurring Elements:**

1.  **Helm Chart Configuration:** The changes consistently involve `.charts` directories, indicating heavy use of Helm for deploying and managing the `transaction-service` and `transaction-service-reconciliation-worker`.
2.  **Environment-Specific `values.yaml`:** There are multiple `values.yaml` files (e.g., `jersey-staging.yaml`, `oxford-staging.yaml`, `farnham-production.yaml`), highlighting a practice of defining configurations per environment (staging, production) and potentially per product within an environment (e.g., `oxford-staging-product001`).
3.  **Elasticsearch Configuration:** Almost all `values.yaml` files configure Elasticsearch, with `hosts`, `startIndexYear`, `endIndexYear`, and `nshards` being common parameters. Multi-cluster setups (common, nordea, seb) are present in more complex environments like Oxford.
4.  **Cassandra and CassandraV2:** Both database versions are configured, with `CassandraV2` being explicitly enabled in some staging and production environments, along with specific `keyspace` and `username`.
5.  **Reconciliation Worker:** The `reconciliation` section is consistently configured across `values.yaml` files and its own dedicated Helm chart (`transaction-service-reconciliation-worker`). It manages SQS batching, dry-run settings, and worker replicas.
6.  **Secret Management:** Frequent mentions of `useVault` and `secretManagerEnabled`, along with placeholders for AWS Secrets Manager paths (`page-token-hmac-secret-path`, `system-access-token-secret-path`, `cassandra-v2-secret-name`), indicate a robust secret management strategy.
7.  **Certificate Management (AWS Private CA):** A strong recurring theme is the integration and conditional enabling of `awsPrivateCAIssuer`. This involves dynamically setting CA paths for services (user, account, Cassandra) and creating `Certificate` resources using `AWSPCAClusterIssuer`. The reconciliation worker uses short-lived certificates.
8.  **Re-saves/Minor Edits:** Many consecutive log entries for the same file (especially `config.yaml` and `deployment.yaml` templates) show identical content, suggesting frequent saving during editing sessions or automated formatting/linting.
9.  **Monitoring (Datadog):** The `transaction-service-reconciliation-worker` chart includes detailed Datadog monitors for Kubernetes health, with specific alert queries, messages, priorities, and Slack notification channels. This points to a comprehensive observability strategy.
10. **Kubernetes Resources:** The changes involve Helm templates generating Kubernetes resources like `ConfigMap`, `Deployment`, `PodDisruptionBudget`, and `DatadogMonitor` custom resources.
11. **Resource Allocation:** `podMemoryRequest` and `podCpuRequest` settings are prevalent across environment values, indicating careful resource management for pods.
12. **Deletion Listeners:** Kafka-based user and account deletion listeners are configured in the main service, with specific topics and groups.

## 10:35:08 AM
The log details configuration changes within the `transaction-processing-service` Helm charts, specifically impacting `values.yaml` and environment-specific `values/*.yaml` files. The primary timestamps for changes cluster around 1:18 PM and 1:49 PM on February 2, 2026.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values.yaml`**
    This file, acting as the base configuration, saw several minor updates.
    *   At **2/2/2026, 1:18:43 PM**, the `awsPrivateCAIssuer` block was initially `trusted: true`, `enabled: false`, `used: false`.
    *   By **2/2/2026, 1:27:29 PM**, the `awsPrivateCAIssuer.used` flag was toggled to `true`.
    *   Subsequent entries at **2/2/2026, 1:27:39 PM** and **2/2/2026, 1:45:35 PM** show no changes from their immediate predecessors.
    *   A more significant change occurred at **2/2/2026, 1:43:49 PM**, where `awsPrivateCAIssuer.trusted` was switched from `true` to `false`, and `awsPrivateCAIssuer.used` was reverted from `true` back to `false`. Throughout these changes, `awsPrivateCAIssuer.enabled` consistently remained `false`.
    *   Other notable settings in this default file include `datadog: enabled: false`, `redisConfiguration: enabled: false`, `canaryDeployments: phase: DISABLED`, and `fasttext: enabled: false`.

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/barnsley-staging.yaml`**
    This environment-specific configuration was initially captured at **2/2/2026, 1:42:30 PM**.
    *   It enabled `datadog`, `useVault`, `useSecretsManager`, and `fasttext`. It configured a specific `database.url` and `redisConfiguration` with a `clusteredHost` and `timeout: 5000`. `canaryDeployments.phase` was `COMPLETE`. The `awsPrivateCAIssuer` was set to `trusted: true`, `enabled: false`, `used: false`.
    *   At **2/2/2026, 1:49:05 PM**, `awsPrivateCAIssuer.enabled` was changed from `false` to `true`.
    *   Shortly after, at **2/2/2026, 1:49:40 PM**, this change was reverted, and `awsPrivateCAIssuer.enabled` was set back to `false`.

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/oxford-staging.yaml`**
    Multiple entries for this file at **2/2/2026, 1:43:21 PM**, **1:43:27 PM**, **1:43:34 PM**, **1:43:56 PM**, and **1:49:28 PM** show identical content.
    *   This configuration enables `datadog`, `useVault`, `useSecretsManager`, `fasttext`, and specific BFM (Budgeting and Financial Management) categorization features (`bfmEnabled: true`, `bfmUserLearningEnabled: true`) with defined `bfmAppIds`. It also sets `processing.reconcileUsingProviderExternalId: true` and a `tasksQueue.topicPrefix`. The `awsPrivateCAIssuer` configuration consistently remained `trusted: true`, `enabled: false`, `used: false`.

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/values/jersey-staging.yaml`**
    This configuration was logged once at **2/2/2026, 1:45:19 PM**.
    *   It enables `datadog`, `useVault`, `useSecretsManager`, and sets `canaryDeployments.phase` to `COMPLETE`. However, unlike other staging environments, `categorization: enabled` is `false` and `fasttext: enabled` is `false`. The `awsPrivateCAIssuer` here is uniquely configured as `trusted: true`, `enabled: true`, `used: false`.

**Patterns and Recurring Elements:**

*   **Environment-Specific Overrides:** There is a clear pattern of specific staging environment files (`barnsley-staging.yaml`, `oxford-staging.yaml`, `jersey-staging.yaml`) overriding the default `values.yaml`. These overrides typically involve:
    *   Enabling `datadog` observability (`enabled: true`, `logInjection: true`, `profiling: true`) which is `false` in the base `values.yaml`.
    *   Setting both `useVault` and `useSecretsManager` to `true` (base `values.yaml` has `useSecretsManager: false`).
    *   Configuring specific `database.url` values.
    *   Enabling `redisConfiguration` with environment-specific `clusteredHost` values and a higher `timeout` (5000ms vs. 3000ms).
    *   Setting `canaryDeployments.phase` to `COMPLETE` (base `values.yaml` has `DISABLED`).
    *   Increased `processing.workersMaxQueueSize` to `1000` (base `values.yaml` is `200`).
*   **Consistent Settings:** `springProfile: staging` is universal across all files. `cassandra: useTls: true` and `useCustomDriver: true` are also consistent.
*   **`awsPrivateCAIssuer` Volatility:** This particular setting shows frequent changes in the default `values.yaml` and variations across staging environments (e.g., `enabled: true` in `jersey-staging.yaml`, `enabled: false` in `oxford-staging.yaml`, and a brief toggle in `barnsley-staging.yaml`). This suggests active management or experimentation around AWS Private CA.
*   **Categorization and FastText Differences:** The enabling and disabling of `categorization`, `bfm` features, and `fasttext` vary significantly across the different staging environments, indicating tailored configurations for each.

## 10:35:33 AM
The provided logs detail changes across Helm chart configuration files for a `transaction-service` and its associated `reconciliation-worker`. The updates span from January 30, 2026, to February 3, 2026, indicating active development and refinement of deployment and configuration parameters.

**File-Specific Updates:**

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml**:
    *   Between January 30, 2026, 12:01:43 PM and 12:02:17 PM, multiple entries show identical content. This file defines a Kubernetes ConfigMap for the main `transaction-service`. Key configurations include `log-level`, user and account service endpoints, data retention settings (batch size, workers, dry run), extensive Elasticsearch configurations (common, Nordea, and SEB profiles, with hosts, sniffing, health checks, refresh intervals, start/end index years, shards, and app IDs), SNS enablement, AWS Secrets Manager paths for page tokens and system access tokens, Cassandra (v1 and v2) connection details (hosts, keyspace, port, TLS, CA paths, and conditional Vault integration for credentials), Kafka listener settings for user and account deletion, and Redis configurations. Certificate Authority (CA) paths are conditionally set based on `Values.awsPrivateCAIssuer.trusted`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml**:
    *   Initial detailed entry on January 30, 2026, 12:05:04 PM, outlines a Kubernetes Deployment for the `transaction-service-reconciliation-worker`. It includes Datadog APM integration, security contexts, and an optional `initContainer` for Vault credential fetching. The main `recon-worker` container defines resource limits, image, arguments, environment variables (including feature toggles, various tokens from secrets, and Datadog APM settings), liveness/readiness probes, and volume mounts for TLS certificates and configuration. An optional `tags-updater` container is also present.
    *   A notable update on February 2, 2026, 12:41:54 PM, revised the `VAULT_CACERT` environment variable logic within `initContainers` and `renew-leases` to be conditional on `Values.awsPrivateCAIssuer.trusted`, using either `/ca-trust-bundle/bundle.pem` or `/vault_ca/ca.crt`. Similar conditional logic was introduced for volume mounts related to certificates.
    *   Subsequent entries for this file show minor adjustments and apparent consolidations around the conditional certificate handling logic, especially between February 2, 2026, 11:18:02 AM and 11:44:58 AM, stabilizing the use of `ca-trust-bundle` for AWS Private CA and `internal-ca`/`vault-tls` otherwise.
    *   On February 3, 2026, 10:11:00 AM, the `terminationGracePeriodSeconds` was increased from 90 to 120 seconds, indicating a need for longer graceful shutdown for the worker.

*   **/Users/mmuruts/projects/tink/transaction-service/master_render_7.yaml**:
    *   A single entry on February 2, 2026, 11:37:01 AM, presents a rendered Kubernetes configuration. It defines a `PodDisruptionBudget` for the reconciliation worker (maxUnavailable: 1) and a `ConfigMap` (`reconciliation-worker-configuration`) which shows resolved values (e.g., log level `info`, dry run `true` for reconciliation, conditional CA paths pointing to `/ca-trust-bundle/bundle.pem`).
    *   This file also defines multiple `DatadogMonitor` resources for the `transaction-service-reconciliation-worker`, covering metrics like container restarts, CPU throttled seconds, deployment unavailability, out-of-memory (OOM) kills, crash loop backoffs, and service available replicas. All monitors are set with priority 2 and configured to notify a Slack channel.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/config.yaml**:
    *   From January 30, 2026, 12:33:45 PM, to February 2, 2026, 11:16:08 AM, this ConfigMap template for the reconciliation worker underwent iterative changes primarily focused on conditional CA pathing.
    *   Specifically, on February 2, 2026, 10:51:03 AM, `main-db-cert` was made conditional on `awsPrivateCAIssuer.trusted`. This change was quickly reverted at 10:52:48 AM, where several CA paths were simplified (removing the `if trusted` block). However, by 10:53:41 AM, the `if trusted` blocks for `user-svc-ca`, `cassandra-ca-path`, `cassandra-v2-ca-path`, and `cert-path` were re-introduced, stabilizing the conditional logic for certificate source.
    *   On February 3, 2026, 9:15:38 AM, a `tags-updater-shutdown-timeout` variable was added, with a default value of 30, suggesting new configuration options for the tags updater component.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values.yaml**:
    *   On February 2, 2026, 9:52:54 AM, this default values file had a comprehensive set of configurations for the reconciliation worker, including general service settings, Elasticsearch, Cassandra (v1 and v2, initially disabled for v2), data retention, reconciliation parameters (disabled by default, dry run true), user/account services (disabled), Redis (disabled), pod resources, Vault usage (`true`), feature toggles, Datadog APM, canary deployments (`DISABLED`), and extensive Kubernetes monitoring definitions. `awsPrivateCAIssuer` was initially `trusted: false, enabled: false`.
    *   Between February 2, 2026, 9:54:41 AM and 11:07:22 AM, the `awsPrivateCAIssuer.trusted` and `enabled` fields were frequently toggled between `true` and `false`, indicating testing or uncertainty around this feature.
    *   On February 3, 2026, 9:11:59 AM, a `shutdownTimeout` was added to `reconciliation.sqs` (set to 110) with a warning about its impact on message timeout. A root-level `tagsUpdater.shutdownTimeout` was also introduced, later refined to be nested under `tagsUpdater` or removed at the root.
    *   By February 3, 2026, 10:06:46 AM, `awsPrivateCAIssuer.trusted` and `enabled` were both definitively set to `true`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/farnham-production.yaml**:
    *   Multiple identical entries on January 30, 2026, between 12:29:16 PM and 12:30:48 PM. This environment-specific values file configures Elasticsearch (common hosts, index years, refresh interval, shards), Cassandra hosts, data retention (enabled, dry run false, specific batch size and cron job), reconciliation (enabled, dry run false), user service enabled, pod replica counts, canary deployments phase `COMPLETE`, and `awsAccountId`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml**:
    *   On February 2, 2026, 11:59:06 AM, initially only defined `firehoseTransactionsTopic` for reconciliation.
    *   On February 2, 2026, 3:37:32 PM, it was significantly expanded to override numerous default settings, including `logLevel: "debug"`, enabling multi-Elasticsearch clusters, setting a `kafkaTopicPrefix`, detailing Elasticsearch, Cassandra, and Cassandra v2 configurations, enabling user/account deletion listeners, setting data retention and reconciliation to `enabled: true`, enabling user/account services and Redis with specific hosts, enabling feature toggles, increasing pod resource requests/limits, setting canary deployment phase to `COMPLETE`, adding `awsAccountId`, disabling `serviceLevels`, and explicitly setting `awsPrivateCAIssuer` to `trusted: true, enabled: true`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/farnham-staging.yaml**:
    *   On February 2, 2026, 11:56:40 AM and 3:35:46 PM, this file maintained configurations for Elasticsearch, Cassandra, user service, data retention (enabled, dry run true), reconciliation (enabled, dry run false), pod/recon worker resources and replicas, and canary deployments. The later timestamp added `awsAccountId` and explicitly enabled `awsPrivateCAIssuer` (trusted: true, enabled: true).

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/barnsley-staging.yaml**:
    *   On February 2, 2026, 3:35:55 PM, it defined Elasticsearch, Cassandra, user service, reconciliation settings, pod replica counts, canary deployments, `awsAccountId`, and `awsPrivateCAIssuer` (trusted: true, enabled: true).

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging.yaml**:
    *   On February 2, 2026, 11:57:09 AM, this file provided extensive staging configurations, enabling multi-Elasticsearch clusters, specific Elasticsearch profiles (common, Nordea), Cassandra (v1 and v2), data retention, reconciliation, user/account services, Redis, feature toggles, and `tagsUpdater`. `awsPrivateCAIssuer` was `trusted: true, enabled: false`.
    *   On February 2, 2026, 3:36:19 PM, `userDeletionListener` and `accountDeletionListener` were explicitly enabled, and `awsAccountId` was added. `secretManagerEnabled: true` was removed from explicit declaration, likely indicating it's now inherited as a default, and `awsPrivateCAIssuer` was set to `trusted: true, enabled: true`.
    *   On February 3, 2026, 9:17:29 AM, `awsPrivateCAIssuer.enabled` was changed from `false` to `true`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values.yaml**:
    *   On February 2, 2026, 3:36:58 PM and 3:37:06 PM (identical content), this default values file provided base configurations, including `logLevel`, multi-Elasticsearch clusters (disabled), SNS enabled, `secretManagerEnabled`, `kafkaTopicPrefix`, `transactionMutationTopicName`, `providerMarkets`, default Elasticsearch and Cassandra settings (v1 and v2 disabled), `userDeletionListener` and `accountDeletionListener` (disabled by default), data retention, reconciliation (disabled), user/account services (disabled), Redis (disabled), pod resource requests/limits and replica counts, Vault usage, service levels, feature toggles (disabled), Datadog APM, canary deployments (DISABLED), and monitoring configurations. `awsPrivateCAIssuer` was `trusted: true, enabled: false` in the 3:36:58 PM entry, then `trusted: false, enabled: false` in the 3:37:06 PM entry.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-production.yaml**:
    *   On February 3, 2026, 9:13:23 AM, this production configuration enables multi-Elasticsearch clusters with common, Nordea, and SEB profiles (each with specific hosts, refresh intervals, start/end index years, total hits as int, sniffing, health check, retry, common flag, appIds, and shards). Cassandra v1 and v2 are enabled with specific hosts, keyspace, username, and TLS. User/account deletion listeners are enabled, as are user/account services and Redis. Reconciliation is enabled, with SQS batch size. Pod and recon worker resource requests/limits and replica counts are set. Feature toggles and canary deployments are enabled (`COMPLETE`). It also specifies an `awsAccountId`.

*   **/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/certificate-grpc-certificate-aws.yaml**:
    *   On February 3, 2026, 9:30:38 AM and 10:00:27 AM (identical content), this template defines a Kubernetes Certificate resource specific to the reconciliation worker. It's conditional on `awsPrivateCAIssuer.enabled` and requests a short-lived certificate (7 days duration, 2 days renewBefore) from `aws-private-ca-short-lived-issuer`, configured for server and client authentication for various transaction-service DNS names.

**Patterns and Recurring Elements:**

1.  **Helm Chart Structure:** The changes consistently reflect a Helm chart structure, with base templates (`templates/*.yaml`) and environment-specific overrides (`values/*.yaml`).
2.  **Kubernetes Resource Management:** The primary focus is on configuring Kubernetes `ConfigMap` and `Deployment` resources, along with `PodDisruptionBudget` and `DatadogMonitor` definitions.
3.  **Templated Configuration:** Extensive use of templating (e.g., `{{ .Values.config | default dict }}`) allows for dynamic, environment-specific configurations for services, databases, and monitoring.
4.  **Certificate Authority (CA) Handling:** A prominent pattern is the conditional logic for certificate paths, often switching between a global `/ca-trust-bundle/bundle.pem` (presumably for AWS Private CA) and `/tls/ca.crt` or `/vault_ca/ca.crt`. This indicates a migration towards or dynamic selection of CA sources. The `awsPrivateCAIssuer.trusted` and `enabled` flags are frequently adjusted in `values.yaml` files.
5.  **Database Integration:** Both Elasticsearch and Cassandra are heavily configured, supporting multiple instances/profiles (common, Nordea, SEB for ES) and versions (Cassandra v1 and v2).
6.  **Secret Management:** Integration with AWS Secrets Manager (specifying secret paths) and Vault (conditional credential fetching) is consistently implemented for sensitive data.
7.  **Inter-Service Communication:** Configuration for `user-svc-addr`, `account-svc-addr`, and Kafka topics (`kafka-hosts`, `kafka-topic-user-deletion`, `kafka-topic-account-deletion`) is present across configurations.
8.  **Monitoring with Datadog:** Standardized Datadog monitors are defined for the `reconciliation-worker`, covering common Kubernetes operational issues like container restarts, CPU throttling, deployment availability, OOM kills, and crash loops, all configured with Slack notifications and specific priorities.
9.  **Worker Component (Reconciliation Worker):** A dedicated worker for reconciliation tasks is clearly defined, with its own specific configurations for queues, SQS batch sizes, and shutdown timeouts, indicating a microservices or distributed architecture.
10. **Configuration Granularity:** Configurations range from general (e.g., `logLevel`) to very specific (e.g., Elasticsearch `startIndexYear`, `refreshinterval`, Cassandra `keyspace`), and are often overridden in environment-specific `values` files.
11. **Refinement of Shutdown Behavior:** The repeated adjustment and warning comments around `shutdownTimeout` (e.g., for reconciliation SQS and tags updater) suggest ongoing efforts to ensure graceful service termination and prevent message loss.