# Activity Summary for 5/9/2026

## 9:46:42 PM
The file `/Users/mmuruts/projects/tink/transaction-processing-service/opus_benchmark.py` was updated on 5/9/2026, 9:08:56 PM.

This Python script implements a benchmark to compare the performance and cost of Claude Opus 4.6 and Opus 4.7 models.

Key aspects of the changes include:

*   **Benchmarking Objective**: The script is designed to measure latency (time-to-first-token, total wall time), throughput (output tokens/sec), and cost for both Claude Opus 4.6 and Opus 4.7. It also captures raw model outputs for qualitative review.
*   **Model and Pricing Configuration**: It explicitly defines the two models, `claude-opus-4-6` and `claude-opus-4-7`, and their published pricing per 1M input/output tokens, which are identical for both tiers.
*   **Caching Strategy**: A `SYSTEM_PROMPT` is padded with filler content to exceed the 4096-token minimum-cacheable-prefix threshold, enabling testing of Anthropic's caching mechanisms. Cache read costs are set at 10% and cache write costs at 125% of normal input pricing. The system prompt is marked with `cache_control={"type": "ephemeral"}`.
*   **Prompt Suite**: A diverse set of predefined `Prompt` objects are used, categorized into "coding" (e.g., Fibonacci, SQL query), "reasoning" (e.g., three boxes puzzle, train problem), and "summarization" (e.g., French Revolution, prompt caching explanation).
*   **Data Structures and Metrics**:
    *   `dataclass`es `Prompt` and `RunResult` are used to structure prompt definitions and capture detailed results for each execution.
    *   `RunResult` calculates `tokens_per_sec` and `cost_usd` based on API usage, including separate accounting for cache read and write tokens.
*   **Execution Flow**: The `main` function iterates through each model and each prompt, calling `run_one` to execute the query via the Anthropic streaming API. It captures `ttft_s` (time-to-first-token), `total_s` (total wall time), and token usage statistics (`input_tokens`, `output_tokens`, `cache_read_input_tokens`, `cache_creation_input_tokens`).
*   **Reporting and Output**:
    *   `print_per_prompt` displays detailed results for each individual model-prompt combination.
    *   `print_aggregate` provides summary statistics (average TTFT, total time, tokens/sec, summed tokens, total cost) per model.
    *   `save_outputs` generates markdown files (one per model) containing the prompt and the model's response for manual review, and also creates a `metrics.json` file with all collected data for downstream analysis.
*   **Environment Requirement**: The script requires the `ANTHROPIC_AUTH_TOKEN` environment variable to be set for API authentication.

The overall pattern is a structured benchmark execution, data collection, and multi-format reporting for comparative analysis of language models, with a specific focus on Anthropic's Opus models and their caching features.

## 10:46:41 PM
The file `/Users/mmuruts/projects/tink/transaction-processing-service/opus_benchmark.py` was updated on May 9, 2026, at 9:08:56 PM.

This Python script is designed to benchmark Anthropic's Claude Opus 4.6 and Opus 4.7 models. It measures key performance indicators such as latency (time-to-first-token and total wall time), throughput (output tokens per second), and cost, while also capturing raw outputs for qualitative review.

**Key updates and functionalities include:**

*   **Model Comparison:** Explicitly defines `claude-opus-4-6` and `claude-opus-4-7` as the models under comparison, with identical input/output pricing configured for both.
*   **Prompt Suite:** Utilizes a predefined set of prompts categorized into "coding," "reasoning," and "summarization" to ensure consistent evaluation.
*   **Caching Mechanism:** Implements specific logic for prompt caching, including padding the `SYSTEM_PROMPT` to exceed the 4096-token minimum cacheable prefix for Opus, using `cache_control: {"type": "ephemeral"}`, and accounting for cache read (0.1x normal cost) and write (1.25x normal cost) multipliers in cost calculations.
*   **Data Structures:** Uses `dataclass` for `Prompt` to structure prompt details and `RunResult` to store comprehensive benchmark metrics for each model's interaction with a prompt, including input/output tokens, cache usage, and calculated cost.
*   **Execution Flow:**
    *   The `run_one` function handles the API call, streaming responses, and capturing detailed timing and token usage information.
    *   `print_per_prompt` and `print_aggregate` functions are responsible for displaying formatted results to the console, showing individual prompt performance and overall model aggregates, respectively.
    *   `save_outputs` stores the raw model responses in Markdown files (`benchmark_outputs/<model_name>.md`) for manual quality review and exports all quantitative metrics into a `metrics.json` file.
*   **Dependency:** The script requires the `ANTHROPIC_AUTH_TOKEN` environment variable to be set for authentication.

The core pattern involves iterating through defined models and prompts, executing API calls, collecting performance and cost metrics, and then presenting these results in both structured console outputs and persistent files for further analysis.