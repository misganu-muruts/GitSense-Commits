# Activity Summary for 5/19/2026

## 12:31:49 PM
The provided log details activities within a Kubernetes environment and a Buildkite CI/CD pipeline, all recorded in the `/Users/mmuruts/projects/tink/tink-backend/logggg` file.

**Timestamp: 5/19/2026, 11:35:00 AM**
At this timestamp, the logs indicate a critical issue with the `account-processing-service` within a Kubernetes cluster. The `pod/account-processing-service-6b96c74bd9-rdszk` is in a `CrashLoopBackOff` state, having restarted 7 times within 15 minutes, with a `Last State: Terminated Reason: Error Exit Code: 1`.
Further examination of the pod's description reveals:
*   It's running on the `tink-backend-test-control-plane` node.
*   It's configured with specific CPU and memory requests/limits (1 CPU, 1480Mi memory requested; 4 CPU, 1480Mi memory limited).
*   The container uses the `bazel/src/account-processing-service:docker` image and executes a Java command with various JVM arguments, including setting `spring.config.location` and `logging.config`.
*   It mounts configuration from `account-processing-service-configuration-latest` ConfigMap, and refers to secrets for `UNLEASH_API_KEY`.
*   Liveness, Readiness, and Startup probes are configured, all pointing to `/actuator/health` endpoints on port 10142.
The node itself (`tink-backend-test-control-plane`) appears healthy with sufficient resources.

**Timestamp: 5/19/2026, 12:07:53 PM**
Approximately half an hour later, the log shifts to detailing a Buildkite pipeline execution related to the `tink-backend` project.
The pipeline's key steps include:
*   Downloading a 564 MiB artifact: `bazel-kindtest-out/src/account-processing-service/docker.tar`.
*   Running a Docker command within a `kind` (Kubernetes in Docker) container, indicating an attempt to set up a local Kubernetes cluster for testing or deployment.
*   The `kind` cluster setup involves using a `kubernetes-generator` image to generate Kubernetes manifests for the `account-processing` chart with specific parameters (chart=account-processing, cluster=local, environment=development, mode=generate, vars=kind=True).
*   Applying these generated manifests via `kubectl apply -f-`. While several standard Kubernetes resources were successfully created (network policy, pod disruption budget, service account, config maps, role binding, services, deployment, horizontal pod autoscaler), there were significant `resource mapping not found` errors. These errors specifically indicate that Custom Resource Definitions (CRDs) for `AnalysisTemplate`, `Certificate`, `DatadogMetric`, and `Rollout` (associated with `argoproj.io`, `cert-manager.io`, and `datadoghq.com` respectively) were not installed or available in the `kind` cluster during the `kubectl apply` phase.

**Patterns and Recurring Elements:**
*   **Focus on `account-processing-service`:** This service is consistently the subject of the logs, first showing a runtime failure and then being the target of a deployment pipeline.
*   **Kubernetes Environment:** Both entries are deeply intertwined with Kubernetes operations, using `kubectl` commands and describing cluster resources, nodes, and pod states. The `kind` cluster is specifically mentioned for deployment.
*   **Persistent Failure:** The `CrashLoopBackOff` status with `Error Exit Code: 1` for the `account-processing-service` pod points to a recurring application-level or configuration issue preventing it from starting successfully.
*   **Deployment Challenges:** The Buildkite log highlights issues during deployment, specifically the absence of necessary CRDs in the `kind` cluster, which prevents the full application of the generated Kubernetes manifests.
*   **Buildkite Warnings:** Repetitive warnings about `polyglot-hooks` and `job-api` experiments being promoted to stable features indicate that the Buildkite agent used might be running slightly older configurations.