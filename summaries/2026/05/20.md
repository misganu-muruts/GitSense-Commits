# Activity Summary for 5/20/2026

## 8:25:01 AM
The provided log details a sequence of events and operations primarily focused on the `tink-backend` project, specifically concerning the `account-processing-service`, its deployment to a Kubernetes `kind` cluster, and associated Buildkite CI/CD pipeline activities. All observed changes are recorded within the file `/Users/mmuruts/projects/tink/tink-backend/logggg`.

**Summary of Key Changes:**

**5/19/2026, 11:35:00 AM - Kubernetes Deployment Issues**
At this timestamp, the logs indicate a critical issue with the `account-processing-service` pod, which is in a `CrashLoopBackOff` state after 7 restarts within 15 minutes.
*   **Pod Status:** The pod `account-processing-service-6b96c74bd9-rdszk` is not ready, with its last state being `Terminated` due to an `Error` (exit code 1).
*   **Deployment and Services:** While Kubernetes services (ClusterIP, NodePort) and the deployment for `account-processing-service` are present, the deployment shows 0/1 ready pods.
*   **Node Information:** The `tink-backend-test-control-plane` node appears healthy and ready, with ample CPU (16) and memory (32GB) resources. Resource requests for the problematic pod (1 CPU, 1480Mi memory) are well within the node's capacity.
*   **Container Details:** The `tink-backend-account` container runs a Java application, configured with specific memory settings (`-Xms500m -Xmx500m`) and Spring configuration pointing to `/etc/tink/server.yml`.
*   **Probes and Environment:** Liveness, readiness, and startup probes are configured to `/actuator/health/*` endpoints on port 10142. Environment variables like `DD_GRPC_SERVER_ERROR_STATUSES` and `UNLEASH_CLUSTER_NAME` are set, with `UNLEASH_API_KEY` being sourced from a secret.
*   **Volumes:** Several volumes are mounted, including `config-volume` from `account-processing-service-configuration-latest` and volumes for JKS, TLS CA, and scripts.
*   **Pod Conditions:** The pod is scheduled, but `Ready` and `ContainersReady` conditions are false.

**5/19/2026, 12:07:53 PM - Buildkite Deployment & CRD Errors**
Later, the log captures a Buildkite agent process attempting to deploy or test the `account-processing-service` using a `kind` Kubernetes cluster.
*   **Artifact Download:** A 564 MiB Docker image artifact (`bazel-kindtest-out/src/account-processing-service/docker.tar`) was successfully downloaded.
*   **Kubernetes Setup:** A Docker container is executed to set up Kubernetes with `kind`, and a `kubernetes-generator` tool is used to generate manifests for the `account-processing` chart in a "local" "development" environment.
*   **Manifest Application:** `kubectl apply` successfully created standard Kubernetes resources such as `networkpolicy`, `poddisruptionbudget`, `serviceaccount`, `configmap` (for `scripts-aps-latest` and `account-processing-service-configuration-latest`), `rolebinding`, `service` (main and headless), `deployment`, and `horizontalpodautoscaler`.
*   **Critical Errors:** However, the `kubectl apply` command reported multiple "resource mapping not found" errors for custom resource kinds including `AnalysisTemplate` (argoproj.io/v1alpha1), `Certificate` (cert-manager.io/v1), `DatadogMetric` (datadoghq.com/v1alpha1), and `Rollout` (argoproj.io/v1alpha1). This indicates that the necessary Custom Resource Definitions (CRDs) for these resources were not installed in the `kind` cluster, leading to incomplete deployment of the `account-processing-service`'s extended capabilities.

**5/19/2026, 1:02:40 PM - Buildkite Environment Setup and Git Operations**
This entry details the Buildkite agent's environment initialization and source code retrieval.
*   **Environment Configuration:** Buildkite agent environment variables are set, including `BUILDKITE_AGENTS_PER_INSTANCE`, `AWS_DEFAULT_REGION`, `DOCKER_VERSION`, and `PLUGINS_ENABLED`. Buildkite plugins for secrets, ECR, and Docker login are enabled and configured.
*   **Secrets Loading:** SSH keys and environment files (`env`, `build-tink-backend/env`) are loaded from the `tink-buildkite-secrets` S3 bucket. Several environment variables like `GITHUB_PACKAGE_USERNAME`, `JFROG_WRITE_TOKEN`, and `UNLEASH_API_TOKEN` are noted as having been added to the environment.
*   **Git Operations:** The agent performs standard git operations, including `git clean`, `git fetch` for a pull request (`refs/pull/67438/head`), and `git checkout` to commit `fada1db0b651b2a1ad9f08c12736cb70584a019d`.
*   **Commit Message:** The checked-out commit message is "chore: Removed account-processing local-development cluster", suggesting a recent change to remove local development cluster configurations related to `account-processing`. This specific commit might be related to the cause of, or an attempted fix for, the issues seen in the earlier log entry.

**Patterns and Recurring Elements:**
*   **`account-processing-service` focus:** The `account-processing-service` is the central component under review across all log entries, experiencing deployment and operational issues.
*   **Kubernetes `kind` environment:** The use of `kind` for local Kubernetes testing/development is a consistent theme.
*   **Buildkite CI/CD:** Buildkite is the automation platform, responsible for artifact handling, Docker execution, and deploying to Kubernetes.
*   **Recurring Warnings:** All Buildkite-related entries repeatedly log warnings about "The polyglot-hooks experiment" and "The job-api experiment" having been promoted to stable features, advising to remove the experiment flags.
*   **CRD Dependency:** The critical "resource mapping not found" errors highlight a dependency on specific Custom Resource Definitions (CRDs) (from ArgoCD, Cert-Manager, Datadog) that were not present in the Kubernetes environment during manifest application.
*   **Java Application:** The `account-processing-service` is identified as a Java application.

## 11:24:55 AM
The file `/Users/mmuruts/projects/tink/transaction-service/K8S_CLUSTER_PRUNING.md`, last updated on 5/20/2026, 8:26:50 AM, details comprehensive procedures for pruning Kubernetes cluster resources and deleting AWS infrastructure provisioned by Terraform.

Key updates include:

*   **Kubernetes Resource Deletion:**
    *   Instructions for enabling pruning via Buildkite (setting `PRUNE="True"`) and Github Actions reusable workflows (setting `prune_k8s_objects: "true"` or `prune: "true"`).
    *   Guidance on deploying "almost empty" charts (e.g., a single harmless ConfigMap) as a workaround for pruning zero objects, including referencing a `delete-chart.sh` script.
    *   Clarification on what resources are removed by pruning (resource type and label `generator-chart={chart_name}`).
    *   A multi-step process for full cleanup, involving deleting service code and chart directories after pruning.
*   **AWS Resource Deletion (Terraform):**
    *   Emphasis on deploying destructive changes rather than wholesale code removal.
    *   Specific considerations for database deletion, highlighting the need to disable `deletion_protection` first and providing a workaround for issues with DB option group removal due to AWS Backup policies.
    *   Introduces an automated script, `delete-terraform-namespace.sh`, for easy deletion.
    *   Detailed manual processes for deleting AWS resources:
        *   For a specific environment: using Terragrunt's `generate` function to create empty `main.tf` and `outputs.tf` files, followed by deleting the `terragrunt.hcl` file.
        *   For an entire namespace: deleting all `.tf` files except `providers.tf`, ensuring at least one dummy file remains for Terraform to detect state changes, and finally deleting the parent directory.
*   **Recurring Patterns:** The document consistently offers both automated script-based solutions and detailed manual steps for resource deletion. It frequently highlights the need for explicit `true` flags or specific configurations to activate pruning or destructive changes. There's a recurring theme of managing state and ensuring proper cleanup across different phases (e.g., initial deletion, subsequent PRs for code removal). It also provides troubleshooting steps for common issues, such as database option group deletion failures.

## 12:25:05 PM
The file `/Users/mmuruts/projects/tink/transaction-service/K8S_CLUSTER_PRUNING.md`, last updated on 5/20/2026, 8:26:50 AM, details procedures for deleting resources from Kubernetes clusters and AWS environments.

**Key Information by File:**

The document `K8S_CLUSTER_PRUNING.md` outlines two primary resource deletion strategies:

1.  **Deleting Kubernetes Resources:**
    *   For moving from a monorepo to a polyrepo, the recommendation is to **not** remove resources but rather delete the chart directory and clean up manually, aiming to retain in-cluster resources with the same names and namespaces.
    *   To actually remove resources, pruning must be explicitly enabled. This involves:
        *   Passing `PRUNE="True"` as an environment variable to the `release-train` trigger in Buildkite.
        *   For Github Actions reusable workflows (`kubernetes-service-deploy`, `kubernetes-chartonly-deploy`, etc.), setting `prune_k8s_objects: "true"`.
        *   For the `release-train` action, setting `prune: "true"`.
    *   A method for activating pruning is to "deploy almost empty chart" as zero objects cannot be deployed for pruning. This involves either using a script (`delete-chart.sh` from `tink-infrastructure`) or manually editing charts to leave only one harmless manifest (e.g., `templates/empty-configmap.yaml`).
    *   Pruning removes resources matching a specific type list and the label `generator-chart={chart_name}`. Subsequent cleanup involves deleting service code, `catalog-info.yaml`, and the chart directory in a separate PR.

2.  **Deleting AWS Resources:**
    *   This requires deploying a "destructive change" rather than removing all Terraform code at once.
    *   **Database concerns:** `deletion_protection` must be set to `false` before attempting deletion. A common issue is that DB option groups cannot be removed due to AWS Backup policies. The suggested workaround is to remove the database from Terraform code, let the build pipeline fail (which still cleans up most resources), and then manually adjust option groups for existing snapshots.
    *   An "easy mode" script `delete-terraform-namespace.sh` from `tink-infrastructure` is provided.
    *   **Manual modification** involves:
        *   For a single environment (e.g., `oxford-preprod`), adding `generate "main_tf" { ... contents = "" }` to `terragrunt.hcl` (and similarly for `outputs.tf`) to create empty configuration files, triggering destructive changes. The `terragrunt.hcl` file is then deleted in a subsequent PR.
        *   To delete all AWS resources in a namespace, all Terraform files (`*.tf`) except `providers.tf` should be deleted. It's crucial to leave at least one empty file (like `providers.tf` or environment-specific `terragrunt.hcl`) for Terraform to detect state changes. The parent directory is deleted in a separate PR.
    *   Users are advised to confirm that AWS resources are not used by other services/teams before deletion and to create a ticket to infrastructure for questions, including namespace names and PRs used.

**Patterns and Recurring Elements:**

*   **Scripted vs. Manual:** For both Kubernetes and AWS, the document consistently provides both convenient scripts (e.g., `delete-chart.sh`, `delete-terraform-namespace.sh` from `tink-infrastructure`) and detailed manual instructions for resource deletion.
*   **Phased Deletion:** The process often involves multiple steps or subsequent PRs (e.g., deleting resource manifests first, then service code or directories).
*   **"Emptying" for Deletion:** A recurring theme is the strategy of deploying "empty" configurations (e.g., an empty ConfigMap for Kubernetes, empty `main.tf` files for AWS) to trigger pruning or destructive Terraform changes.
*   **CI/CD Integration:** Specific instructions are provided for integrating pruning into Buildkite and Github Actions workflows, highlighting the need for explicit `PRUNE` or `prune_k8s_objects` flags.
*   **Database Specifics:** Databases are repeatedly called out as having special considerations, particularly regarding `deletion_protection` and the persistent issue of DB option groups failing to delete.
*   **Infrastructure Repository:** The `tink-infrastructure` repository is frequently mentioned as the source for helper scripts and examples.

## 1:24:54 PM
**File:** `/Users/mmuruts/projects/tink/transaction-service/K8S_CLUSTER_PRUNING.md`
**Timestamp:** 5/20/2026, 8:26:50 AM

This document details comprehensive strategies for deleting resources from Kubernetes clusters and AWS infrastructure, primarily focusing on managing deployments previously handled by `tink-ab` systems.

**Kubernetes Cluster Pruning:**
*   **Context:** Provides guidelines for removing resources, especially when moving from a monorepo to a polyrepo structure, emphasizing that resources should often remain in-cluster with the same names and namespaces unless explicit deletion is intended.
*   **Enabling Pruning:**
    *   **Buildkite:** Requires setting the environment variable `PRUNE="True"` within the `release-train` trigger.
    *   **GitHub Actions:** For reusable workflows (e.g., `kubernetes-service-deploy`, `kubernetes-chartonly-deploy`), `prune_k8s_objects: "true"` must be explicitly set. For the `release-train` action, `prune: "true"` is used.
*   **Method for Deletion:** Since deploying zero objects isn't feasible for pruning, the recommended approach is to "Deploy an almost empty chart" by replacing all manifests with a single, harmless `empty-configmap.yaml`.
*   **Automation:** References the `tink-infrastructure/scripts/delete-chart.sh` script for automating the empty chart deployment.
*   **Pruning Scope:** The pipeline removes resources matching specific types and the label `generator-chart={chart_name}`.
*   **Post-Pruning Cleanup:** After successful pruning, a subsequent PR is needed to delete service code, `catalog-info.yaml`, and the chart directory.

**AWS Resource Deletion (Terraform):**
*   **Destructive Changes:** Deleting AWS resources provisioned by Terraform requires deploying a "destructive change" rather than simply removing all Terraform code at once.
*   **Database Concerns:** For databases, `deletion_protection` must be set to `false` *before* attempting deletion. A common issue of "DB option group cannot be removed" is addressed by allowing an initial apply to fail (which cleans up most resources), then manually changing the option group for existing snapshots.
*   **Automation:** The `tink-infrastructure/scripts/delete-terraform-namespace.sh` script is provided for easy deletion of an entire Terraform namespace.
*   **Manual Deletion for Specific Environments:** To remove resources from a single AWS environment (e.g., `oxford-preprod`), the `terragrunt.hcl` file for that environment needs `generate "main_tf"` (and optionally `generate "outputs_tf"`) blocks added, setting `contents = ""`. This generates empty `.tf` files, triggering destructive changes. The `terragrunt.hcl` file is then deleted in a subsequent PR.
*   **Manual Deletion for All Namespace Resources:** To delete all AWS resources in a namespace, all `*.tf` files except `providers.tf` should be removed. It's crucial to leave at least one empty file initially so Terraform can detect state changes.
*   **Final Steps:** After AWS resources are deleted, the parent directory for the service's Terraform configuration should be deleted in a separate PR.
*   **Support:** Users are advised to create an infrastructure ticket if they have questions, including namespace names and relevant PRs.

**Key Patterns/Recurring Elements:**
*   The document consistently offers both automated script-based solutions (e.g., `delete-chart.sh`, `delete-terraform-namespace.sh` from `tink-infrastructure`) and detailed manual steps for resource deletion.
*   It highlights specific configurations for different deployment tools (Buildkite, GitHub Actions).
*   Emphasis is placed on handling specific edge cases and potential issues during deletion, such as database `deletion_protection` and option group failures.
*   The overall process encourages a multi-step approach, often involving separate PRs for different phases of cleanup to manage the state changes effectively.

## 10:04:54 PM
The file `/Users/mmuruts/projects/tink/transaction-service/K8S_CLUSTER_PRUNING.md`, last updated on **5/20/2026, 8:26:50 AM**, provides a comprehensive guide for deleting Kubernetes (K8S) and AWS resources, specifically when moving from a monorepo to a polyrepo or performing general cleanup.

**Key updates and instructions include:**

*   **Kubernetes Resource Deletion:**
    *   Instructions for enabling pruning through environment variables (`PRUNE="True"`) for Buildkite release-train triggers or `prune_k8s_objects: "true"` for GitHub Actions reusable workflows (e.g., `kubernetes-service-deploy`).
    *   The primary method involves deploying an "almost empty chart" (as zero objects cannot be deployed to activate pruning) using a `delete-chart.sh` script or by manually editing manifests to leave a single harmless file like `empty-configmap.yaml`.
    *   Details what resources get removed: those matching specific types and a `generator-chart={chart_name}` label.
    *   Subsequent cleanup steps include deleting service code, `catalog-info.yaml`, and the chart directory.

*   **AWS Resource Deletion (Terraform):**
    *   Emphasizes that direct deletion of Terraform code is not feasible; instead, "destructive changes" must be deployed.
    *   **Database-specific concerns:** Highlights checking and disabling `deletion_protection` before removal and provides a workaround for persistent DB option group issues due to AWS Backup policies.
    *   Offers an "easy mode" using a `delete-terraform-namespace.sh` script for full namespace cleanup.
    *   **Manual deletion for specific environments:** Involves adding `generate "main_tf" { ... contents = "" }` (and `outputs.tf` if applicable) to the `terragrunt.hcl` file for the target environment, then deleting the `terragrunt.hcl` file in a separate PR after the destructive change is applied.
    *   **Manual deletion for all AWS resources in a namespace:** Requires deleting all `.tf` files except `providers.tf` and ensuring `terragrunt.hcl` files for environments remain. The parent directory should be deleted in a subsequent, separate PR.

**Recurring elements and patterns:**

*   The document consistently stresses the need for **"destructive changes"** when dealing with Terraform.
*   It highlights the importance of **separate PRs** for different stages of cleanup (e.g., deploying destructive changes first, then deleting configuration files/directories).
*   The use of **helper scripts** (e.g., `delete-chart.sh`, `delete-terraform-namespace.sh`) is encouraged for common cleanup tasks.
*   Specific files like `terragrunt.hcl` and `providers.tf` are frequently mentioned in the context of Terraform configuration.
*   There's an underlying theme of **caution and staged deletion** to prevent unintended consequences or state mismatches.