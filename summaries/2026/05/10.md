# Activity Summary for 5/10/2026

## 1:46:42 AM
The file `/Users/mmuruts/projects/tink/transaction-processing-service/opus_benchmark.py`, last modified on 5/9/2026, 9:08:56 PM, introduces a comprehensive benchmarking script designed to compare Anthropic's Claude Opus 4.6 and Opus 4.7 models.

The script's primary objective is to measure:
*   **Latency:** Time-to-first-token (TTFT) and total wall time.
*   **Throughput:** Output tokens per second.
*   **Cost:** Calculated based on published input/output token pricing for both models, which are identical.
*   **Quality:** Raw outputs are captured for manual review.

A key focus of this benchmark is the interaction with Anthropic's **prompt caching mechanism**. It incorporates a padded system prompt (`_FILLER` and `SYSTEM_PROMPT`) marked with `cache_control: {"type": "ephemeral"}` to test caching behavior. The script explicitly defines `CACHE_READ_MULT` (0.1x normal input pricing) and `CACHE_WRITE_MULT` (1.25x normal input pricing) to reflect the cost implications of cache usage. It notes that the first call per model incurs the write premium, while subsequent calls within the same run benefit from cheaper cache reads for the shared prefix.

The benchmark suite (`PROMPTS`) consists of a variety of tasks categorized as:
*   **Coding:** Fibonacci function, SQL query for top customers.
*   **Reasoning:** Three boxes puzzle, train meeting problem.
*   **Summarization:** French Revolution, explanation of prompt caching.

The script defines a `RunResult` dataclass to store detailed metrics for each prompt execution, including model, prompt details, TTFT, total time, input/output tokens, cache read/write tokens, output text, and calculated tokens per second and cost in USD.

Execution involves iterating through each model and prompt, streaming responses via `anthropic.Anthropic().messages.stream` to capture TTFT, and then collecting final usage statistics. The results are presented in two ways:
1.  **Per-Prompt Summary:** A detailed table showing metrics for each individual prompt across models.
2.  **Per-Model Aggregate:** Average performance and total cost metrics for each model.

Finally, the script saves all results to a `benchmark_outputs` directory, generating Markdown files (`.md`) per model containing prompts and their raw responses for quality review, and a `metrics.json` file for structured, programmatic analysis of all numerical metrics. The script requires the `ANTHROPIC_API_KEY` environment variable to run.

## 2:46:41 AM
The file `/Users/mmuruts/projects/tink/transaction-processing-service/opus_benchmark.py` was last updated on 5/9/2026, 9:08:56 PM.

This Python script is designed to benchmark and compare the performance of Anthropic's Claude Opus 4.6 and Opus 4.7 models. It measures several key metrics including:
*   **Latency:** Time-to-first-token (TTFT) and total wall time.
*   **Throughput:** Output tokens per second.
*   **Cost:** Calculated based on input and output token pricing, including cache read/write costs.
*   **Quality:** Raw outputs are captured for manual review.

**Key Features and Patterns:**
*   **Model Comparison:** The benchmark explicitly defines two models, "claude-opus-4-6" and "claude-opus-4-7," and iterates through them for each test.
*   **Pricing Structure:** Detailed pricing for input and output tokens is hardcoded, with cache read operations costing 10% of normal input pricing and cache write operations costing 125%.
*   **Prompt Caching Strategy:** A `SYSTEM_PROMPT` is padded with filler content to exceed the 4096-token minimum-cacheable-prefix threshold, ensuring that repeated calls within a run benefit from cached prefixes to simulate real-world cost savings. The system prompt also includes `cache_control` set to `ephemeral`.
*   **Prompt Suite:** A structured set of prompts is used, categorized into "coding" (e.g., Fibonacci, SQL query), "reasoning" (e.g., three boxes, train problem), and "summarization" (e.g., French Revolution, caching concepts). Each prompt has a specific `category`, `name`, and `content`.
*   **Data Collection and Reporting:** The script defines `Prompt` and `RunResult` dataclasses to store test configurations and outcomes. It includes functions to print detailed per-prompt results, aggregate results per model (showing average TTFT, total time, tokens/sec, and total cost), and save all outputs.
*   **Output Saving:** Results are saved to a `benchmark_outputs` directory. Individual model responses are written to markdown files for manual quality review, and all collected metrics are dumped into a `metrics.json` file for further analysis.
*   **External Dependency:** The script uses the `anthropic` client library and requires an `ANTHROPIC_AUTH_TOKEN` to be set in the environment for API authentication.