# Activity Summary for 1/29/2026

## 9:13:29 AM
The log details changes across three `terragrunt.hcl` configuration files for different production environments within the `tink-infrastructure/terraform/namespace/transaction` project. All changes occurred on `1/22/2026` between 2:35 PM and 2:37 PM, indicating a coordinated update or generation process.

**File-specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-infrastructure/terraform/namespace/transaction/barnsley-production/terragrunt.hcl`** (Timestamp: 1/22/2026, 2:35:49 PM): This file defines the `iam_role` as `arn:aws:iam::330892627570:role/TinkTransactionsOncaller`. It configures specific inputs to enable a transaction service Redis cluster (`create_transaction_service_redis_cluster = true`) and sets its node type to `cache.t3.micro`.
*   **`/Users/mmuruts/projects/tink/tink-infrastructure/terraform/namespace/transaction/houston-production/terragrunt.hcl`** (Timestamp: 1/22/2026, 2:36:46 PM): The `iam_role` is set to `arn:aws:iam::861808244446:role/TinkTransactionsOncaller`. Its inputs define `recon_storage_bucket_retention_days` as "185".
*   **`/Users/mmuruts/projects/tink/tink-infrastructure/terraform/namespace/transaction/jersey-production/terragrunt.hcl`** (Timestamp: 1/22/2026, 2:37:22 PM): This file specifies the `iam_role` as `arn:aws:iam::920227896204:role/TinkTransactionsOncaller`. Notably, its `inputs` block is empty, suggesting no environment-specific variables are being overridden for this particular deployment.

**Patterns and Recurring Elements:**

*   **Autogenerated Files**: All `terragrunt.hcl` files are explicitly marked with `# Autogenerated file`, indicating they are likely generated by a script or CI/CD pipeline rather than being manually authored for each environment.
*   **Terragrunt Configuration**: Each file follows a standard Terragrunt structure, including comments explaining its purpose and the use of `include { path = find_in_parent_folders() }` to inherit common configurations.
*   **IAM Role Definition**: All environments define an `iam_role` with a common suffix `/TinkTransactionsOncaller`, but each role is associated with a distinct AWS account ID (e.g., `330892627570`, `861808244446`, `920227896204`), reflecting deployment to separate AWS accounts per environment.
*   **Environment-Specific Inputs**: The `inputs` block is used to customize environment variables, but the specific variables and their values differ greatly across environments, showing tailored configurations for each production instance. Some environments (Barnsley, Houston) have specific overrides, while others (Jersey) do not.

## 11:24:14 AM
The provided log details a single, comprehensive update to a Kubernetes Deployment manifest for a `transaction-service` application. This file, located at `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/deployment.yaml`, was last modified on `1/29/2026, 10:44:57 AM`.

**File-Specific Updates (`deployment.yaml`):**

The `deployment.yaml` defines a Helm template for the `transaction-service` Kubernetes Deployment. Key aspects include:

*   **Deployment Strategy**: Configures a `RollingUpdate` strategy with `maxUnavailable: 0` and `maxSurge: 1`. It dynamically sets `replicas` based on `canaryDeployments.phase`, supporting partial (default 3 replicas) and complete (0 replicas) canary deployments.
*   **Datadog Integration**: Extensive conditional logic (enabled by `.Values.datadog.apmEnabled`) is present for Datadog APM. This includes setting `tags` on both the Deployment and Pod metadata, defining `tink-health` annotations with environment-specific `monitor_id`s, and injecting Datadog-specific environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`). A conditional volume mount for `/var/run/datadog` is also included.
*   **Vault Integration**: If `.Values.useVault` is true, an `initContainer` named `fetch-credentials` (using `vaultify:0.4.2`) is added to retrieve secrets from Vault into a `/credentials` volume. A sidecar container named `renew-leases` (also using `vaultify:0.4.2`) is included to manage secret lease renewals, exposing metrics on port 9105. Both Vault-related containers are configured with `readOnlyRootFilesystem: true`.
*   **Main `transaction-service` Container**:
    *   Specifies CPU and memory resource requests and limits.
    *   Image is dynamically retrieved from an ECR repository, potentially overridden by `tink-versions/tink-transaction-service.txt` or `.Values.Version`.
    *   Runs as non-root user (`65535`) and has `readOnlyRootFilesystem: true`.
    *   Numerous environment variables are set, including conditional feature toggle settings (from `.Values.featureToggles`) and sensitive credentials fetched from Kubernetes Secrets (`PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`).
    *   **Health Probes**: `readinessProbe`, `livenessProbe`, and `startupProbe` are configured using `grpc-health-probe` targeting port 8443, with a `60` second `initialDelaySeconds` for all.
    *   **Lifecycle Hook**: A `preStop` hook waits for 30 seconds before termination.
    *   Mounts certificates (`/tls`), internal CA, configuration, and credentials.
*   **Volumes**: Defines volumes for secrets (certificates, internal CA), a ConfigMap for service configuration, an `emptyDir` for credentials, and conditional hostPath for Datadog.
*   **Pod Anti-Affinity**: Configures `podAntiAffinity` (required) based on `kubernetes.io/hostname` and `podAffinity` (preferred) based on `failure-domain.beta.kubernetes.io/zone`, both conditionally applied for non-"local" clusters.
*   **Local Cluster Specifics**: For "local" clusters, it additionally defines a `Namespace` resource named `transaction`.

**Patterns and Recurring Elements:**

*   **Helm Templating**: The entire file extensively uses Helm templating (`{{ .Values... }}`, `{{- if ... }}`, `{{- end }}`) for dynamic configuration based on release values and cluster environments.
*   **Conditional Logic**: Many sections are guarded by `if` statements, allowing configurations to be applied based on feature flags (e.g., `datadog.apmEnabled`, `useVault`, `featureToggles.enabled`) or cluster type (`.Values.Cluster`).
*   **Security Contexts**: `readOnlyRootFilesystem: true` is consistently applied to containers (main service, init container, sidecar) to enhance security.
*   **Secret Management**: Secrets are managed through a combination of Kubernetes `secretKeyRef` for direct access and `vaultify` init/sidecar containers for more dynamic Vault-based credential fetching and renewal.
*   **Observability**: Strong emphasis on observability with deep Datadog integration and gRPC health probes for robust service monitoring.
*   **Environment-Specific Configuration**: Many parameters (e.g., `monitor_id`, `VAULT_CACERT`, `affinity` rules) are adapted based on the `Cluster` or `Environment` values.