# Activity Summary for 3/27/2026

## 11:55:39 AM
The `deployment.yaml` file located at `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml` was logged twice on 3/27/2026, first at 11:24:34 AM and again at 11:26:02 AM. The content of the file remained identical across both timestamps, indicating no functional changes were introduced in this specific log sequence.

The file itself is a Kubernetes Deployment Helm chart template for a `transaction-service-reconciliation-worker`. Key aspects of its configuration include:

*   **Conditional Deployment:** The entire deployment is conditional on `.Values.config.reconciliation.enabled`.
*   **Datadog APM Integration:** Extensive conditional logic exists to enable Datadog APM, adding labels, annotations, and specific environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`).
*   **Vault Integration:** When `.Values.useVault` is enabled, an `initContainer` named `fetch-credentials` is included to retrieve configuration and secrets from Vault using `vaultify:0.4.2`. A `renew-leases` sidecar container is also added to manage Vault lease renewals.
*   **Multiple Containers:** The deployment can include several containers:
    *   `transaction-service-reconciliation-worker`: The main application container, running `/bin/transaction-service recon-worker`.
    *   `transaction-service-tags-updater`: An optional container (enabled by `.Values.tagsUpdater.enabled`) running `/bin/transaction-service tags-consumer`.
    *   `fetch-credentials` (init container) and `renew-leases` (sidecar container) for Vault integration.
*   **Secret Management:** Various sensitive values such as `FEATURE_TOGGLE_API_KEY`, `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, and `CASSANDRA_V2_PASSWORD` are retrieved from Kubernetes secrets using `secretKeyRef`.
*   **Configuration Source:** Application configuration (`reconciliation-config.yaml`) is mounted either from a `ConfigMap` (named `reconciliation-worker-configuration`) or from the Vault-managed `credentials` volume if Vault is enabled.
*   **Resource Allocation:** Container memory and CPU requests/limits are dynamically set using Helm values (`.Values.podMemoryRequest`, `.Values.podCpuRequest`). The `GOMAXPROCS` environment variable is set to the CPU limit.
*   **Security Context:** Containers typically run as non-root users (65535) and have `readOnlyRootFilesystem: true`.
*   **Probes:** Liveness and readiness probes are configured for all application containers.
*   **Affinity Rules:** Pod anti-affinity rules are defined to distribute replicas across different nodes and availability zones, unless the cluster is identified as "local".
*   **Image Sources:** Common image repositories include `784217881480.dkr.ecr.eu-west-1.amazonaws.com/transaction-service` and `tink-containers/vaultify`. The application version is derived from `tink-versions/tink-transaction-service.txt` or `.Values.Version`.
*   **Certificate Management:** Mounts certificates from either a `grpc-certificate-aws` secret (if `awsPrivateCAIssuer.used`) or a generic secret named by `.Values.certificateName`.