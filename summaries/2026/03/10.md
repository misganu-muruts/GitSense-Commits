# Activity Summary for 3/10/2026

## 8:43:54 AM
The provided log details a series of configuration updates to Helm chart values within the `tink-backend` project, primarily impacting `account`, `account-processing`, and `tink-backend-system` services across different staging environments (Barnsley, Farnham, Jersey, Oxford). These configuration changes occurred on **March 9, 2026, between 1:55 PM and 1:58 PM**. Following these updates, two error logs were recorded on **March 9, 2026, at 4:24 PM** and **March 10, 2026, at 8:39 AM**, indicating critical application startup failures.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/barnsley-staging.yaml` (3/9/2026, 1:55:20 PM):** Updated the `maindb` host to `main-2020020412433091760000003c.cxq3zl9dmblk.eu-west-1.rds.amazonaws.com`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/farnham-staging.yaml` (3/9/2026, 1:55:41 PM):** Updated the `maindb` host to `main-20180215095414751400000018.c3cclha086ww.eu-west-1.rds.amazonaws.com`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/oxford-staging.yaml` (3/9/2026, 1:56:11 PM and 3/9/2026, 1:58:31 PM):** This file was updated twice with identical content, indicating a significant and comprehensive configuration for the `account` service in the Oxford staging environment. It includes:
    *   Settings for `taskqueue` (TLS enabled) and `cassandra`.
    *   `maindb` host set to `main-20250326090413424800000001.cbzwvtiqht4m.eu-west-1.rds.amazonaws.com`.
    *   Extensive endpoint configurations for various internal services like `credentials`, `identity`, `statistics`, `riskInsights`, `incomeCheck`, `holderNameCheck`, `accountService`, `authService`, `user`, and `providersSearchApi`, all specifying URLs (many `dns:///`) and `pinnedCertificates` to `/tls/ca.crt`.
    *   Numerous feature flags enabled, including `accountVerificationEnabled`, `accountPartyServiceEnabled`, `accountDataRepositoryEnabled`, `enableContinuesAccountDataEndpoints`, `enableBalanceApiEndpoints`, `accountCheckValueAddedServices`.
    *   Specific `appIdFeatures` for test accounts (`c848e83078bd4bb287b127b2e3010223`, `985d1ac5660b44189efcd66c6a28845e`, `36a2ebad2efe4787a0b7c6997be3d3ff`, `4e84d0b93eb44178ab45afd374ac2f55`).
    *   Health check `tinkHealth.tryhardSuccessRates`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/farnham-staging.yaml` (3/9/2026, 1:56:25 PM):** Enabled the `enableAccountEvents` feature flag.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/jersey-staging.yaml` (3/9/2026, 1:56:31 PM):** This file only contained the `awsPrivateCAIssuer` settings.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/oxford-staging.yaml` (3/9/2026, 1:56:36 PM):** Configured `taskQueue` endpoints to be enabled. Enabled multiple feature flags: `enableAccountEvents`, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, `balanceCheckEnabled`, `enableBalanceCheckWebhooks`. It also specified `accountDeduplication` settings with specific `appIds` for simplified deduplication and PSD2 migration disablement.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/barnsley-staging.yaml` (3/9/2026, 1:56:52 PM):** Configured `clusterName` as "BARNSLEY". Enabled `userInternalClient`, set `taskQueue.streamFetcherMaxBytes`, and updated `cache.host`. Defined `transactionAccess`, `transactions.processingUserLockSuffixBits`, `redisConfiguration`, `categorization` settings (`userLearningEnabled`, `overrideDeprecatedEventClient`, `useIdentityAggregatorService: false`, `capabilityFilteringEnabled: false`). Enabled `aggregationApplicationPolicyService`. Enabled feature flags `enableAccountEvents` and `enableTransactionEvents`. Updated `database.url` and set `useAwsSecretManager` and `awsAccountId`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/oxford-staging.yaml` (3/9/2026, 1:57:11 PM):** This is the most extensive configuration. It updated `cache.host`. Enhanced `categorization` with `bfmEnabled`, `bfmUserLearningEnabled`, `bfmAppIds`, and `tinkAppId`. Configured `aggregationApplicationPolicyService`. Enabled many feature flags including `resetSupplementalInformation`, `deletePeriodDryRun`, `enableAccountEvents`, `enableTransactionEvents`, `enableUpdateAccountEvents`, `enablePaymentAmlMonitoring`. Detailed `locales` for various countries. Defined `dataRetention` policies with `periodicity`, `batchSize`, `sleepMs`, `defaultRetentionDays`, and `appRetentionDays` with user-specific retention periods. Enabled `datadog` profiling and specified various `logging` levels. Configured `investments` and `loans` endpoints with specific URLs and `pinnedCertificates` (`/internal-ca/ca.crt`). Defined various `transactions` settings related to firehose messages, reconciliation, and provider-based settings. Updated `redisConfiguration`. Listed several `jobs` (e.g., `deleteAccountsAndTransactionsJob`, `maskAccountNumberOnAccountsJob`) as `false` and `deleteUsersV2: false`. Updated `database.url` and `awsAccountId`.

**Patterns and Recurring Elements:**

*   **`awsPrivateCAIssuer: trusted: true enabled: true used: true`:** This setting appears consistently across almost all `.yaml` files, indicating a standardized approach to AWS Private CA usage for trusted certificates across environments and services.
*   **AWS RDS Main Database Hosts:** Each environment's `account` and `tink-backend-system` charts specify distinct `maindb: host` values, pointing to different AWS RDS instances.
*   **Feature Flags:** There's a strong emphasis on managing service behavior through various `featureFlags`, which are frequently enabled or explicitly configured (e.g., `enableAccountEvents`, `enableTransactionEvents`, `accountVerificationEnabled`).
*   **Oxford Staging Extensiveness:** The `oxford-staging.yaml` files consistently contain the most detailed and comprehensive configurations across all service categories (`account`, `account-processing`, `tink-backend-system`), suggesting it's a primary environment for testing or advanced features. This includes numerous service endpoints, granular data retention policies, and specific application IDs for various features.
*   **Certificate Pinned to `/tls/ca.crt` or `/internal-ca/ca.crt`:** Many gRPC service endpoints list `pinnedCertificates` pointing to a CA certificate, indicating a reliance on mTLS or secure communication with internal services.

**Significant Error Logs:**

*   **`/Users/mmuruts/projects/tink/tink-backend/logggggg` (3/9/2026, 4:24:08 PM and 3/10/2026, 8:39:26 AM):** Both log entries report a `java.lang.IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString`.
    *   The first log (3/9/2026) points to this error originating within `se.tink.account.app.grpc.GrpcAccountService.<init>` while trying to call `ensurePkcs8`, suggesting an issue with parsing a PKCS#8 private key, likely related to TLS/SSL setup for the gRPC server.
    *   The second, more detailed log (3/10/2026) shows the same Bouncy Castle error cascading through Spring bean initialization, specifically affecting `AccountDeduplicationService` and `ApplicationConfigurationClient`. This error occurs during the `internalApplicationConfigurationClient` factory method call, again within `GrpcTlsFileReaderShaded.ensurePkcs8`, indicating a failure to correctly build an SSL context due to an invalid private key parameter.

The recurring `IllegalArgumentException` from Bouncy Castle regarding `org.bouncycastle.asn1.DEROctetString` strongly suggests an issue with the cryptographic key or certificate files being used by the application for TLS/SSL communication, possibly in a `PKCS#8` format, after the configuration updates on March 9th. This issue affects multiple services (`GrpcAccountService`, `AccountDeduplicationService`) and prevents them from starting correctly due to failures in secure communication setup.

## 9:18:57 AM
The log details configuration changes across two YAML files related to the `transaction-history` service's Helm chart values.

**File-specific Updates:**

1.  **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/houston-staging.yaml`** (Timestamp: 3/9/2026, 12:25:00 PM)
    This file defines staging-specific configurations.
    *   **Service Clients:** `account_service_client`, `transaction_service_client`, `event_producer_service_client`, and `credential_service_client` are all configured with `round_robin: true`.
    *   **Tracing:** Explicitly `enabled: false` with a `sample_rate: 0.5`.
    *   **HPA:** Horizontal Pod Autoscaling is `enabled: true`.
    *   **Datadog:** Configured for `cluster: us`.
    *   **Canary Deployments:** The `phase` is set to `COMPLETE`, indicating HPA points to the rollout, and the rollout scales up while the deployment scales to 0.
    *   An `awsAccountId` of "276026349597" is specified.

2.  **`/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values.yaml`** (Timestamp: 3/9/2026, 12:25:30 PM)
    This is a more comprehensive default values file.
    *   **Auth:** `enabled: true` for authentication.
    *   **Service Clients:** Unlike staging, `round_robin: false` is set for `account_service_client`, `transaction_service_client`, `event_producer_service_client`, and `credential_service_client`, with all being `enabled: true`.
    *   **Transactions API:** `page_size_limit` is 100. `provider_markets` include `UK`, `GB`, and `SE-OXFORD-ABSTRACT`. `category_enabled_for_search` and `enable_get_single_transaction` are both `false`.
    *   **Reports API:** `msg_size_limit` is set to 20971520 (20MiB) and `snapshot_days_limit` to 540.
    *   **Storage:** `enabled: true` and `useVault: true`.
    *   **HPA:** Detailed HPA configuration is present, including `minReplicas: 3`, `maxReplicas: 4`, `targetAverageCpuCoresUsed: 0.6`, and `targetAverageMemoryUsed: 104857600` (100MiB).
    *   **Service Levels:** `enabled: true`.
    *   **Datadog:** `apmEnabled: true` and `cluster: eu`.
    *   **Feature Toggles:** `url` is provided (`https://tink.edge.getunleash.io/api/`), but `enabled: false`.
    *   **Canary Deployments:** The `phase` is `DISABLED`.
    *   **Monitors:** Extensive monitoring is configured, with `monitors: enabled: true` and `k8sMonitors: enabled: true`. It includes various alerts for CPU throttled seconds, low replicas, deployment unavailability, pod crash loop backoff, OOMKilled containers, container restarts, and namespace CPU/Memory quota exceeded. Each alert specifies a threshold, priority, and a Slack notification channel (`@slack-Tink-squad-transactions-oncall`).
    *   **Global Settings:** Defines `team: transactions`, `service: transaction-history`, `namespace: transaction-history`, `teamRoleId`, `baseUrl: https://app.datadoghq.eu`, and `awsPrivateCAIssuer: used: false`.

**Patterns and Recurring Elements:**

*   **Timestamps:** The changes occurred in quick succession on 3/9/2026, within 30 seconds of each other (12:25:00 PM and 12:25:30 PM), suggesting a batch of related configuration updates or a deployment process.
*   **Service Configuration:** Both files configure `config` parameters for multiple microservice clients (account, transaction, event_producer, credential), though their `round_robin` settings differ between the staging and general values.
*   **Tracing:** `tracing` is consistently `enabled: false` with a `sample_rate: 0.5` across both configurations.
*   **Canary Deployments:** Both files define `canaryDeployments` with detailed comments explaining the different phases (DISABLED, PARTIAL, COMPLETE), highlighting a standardized approach to managing canary deployments, even if the active phase differs.
*   **Datadog:** Datadog integration is a recurring theme, with cluster and APM settings being defined.
*   **Helm Chart Structure:** The file paths indicate these are Helm chart `values.yaml` files, common for defining application configurations in Kubernetes deployments.

## 9:44:02 AM
On March 9th and 10th, 2026, a series of configuration changes and critical errors were logged across the `tink-backend` project.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/`**:
    *   **`barnsley-staging.yaml` and `farnham-staging.yaml`**: Initially configured with `awsPrivateCAIssuer.used: true` on March 9th. On March 10th, this setting was updated to `false`.
    *   **`oxford-staging.yaml`**: On March 9th, this file contained extensive configurations for `maindb` host, `taskqueue` TLS, `cassandra` settings, numerous service endpoints (credentials, identity, statistics, riskInsights, incomeCheck, holderNameCheck, accountService, authService, user, providersSearchApi), various feature flags (e.g., `accountVerificationEnabled`, `accountPartyServiceEnabled`, `accountDataRepositoryEnabled`, `enableContinuesAccountDataEndpoints`, `enableBalanceApiEndpoints`), specific `appIdFeatures`, and `tinkHealth` metrics. It also had `awsPrivateCAIssuer.used: true`. An identical update was logged shortly after. On March 10th, only `awsPrivateCAIssuer.used` was changed to `false`.
    *   **`jersey-staging.yaml`**: This file was first introduced on March 10th with a comprehensive configuration including `useAccountdb`, `databaseMigrationsEnabled`, `accountDataRepositoryEnabled`, `maximumNumberOfAccountToRetrieve`, `cassandra` settings, `accountdb` host, and a distinct set of enabled/disabled endpoints and feature flags. It was configured with `awsPrivateCAIssuer.used: false` upon its first appearance.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/`**:
    *   **`farnham-staging.yaml`**: On March 9th, it was configured with `featureFlags.enableAccountEvents: True` and `awsPrivateCAIssuer.used: true`. No further updates logged for this file.
    *   **`jersey-staging.yaml`**: Initially configured with `awsPrivateCAIssuer.used: true` on March 9th. On March 10th, this setting was updated to `false`.
    *   **`oxford-staging.yaml`**: On March 9th, this file included configurations for `taskQueue`, `featureFlags` (e.g., `enableAccountEvents`, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, `balanceCheckEnabled`, `enableBalanceCheckWebhooks`), and `accountDeduplication` settings with specific `appIds`. It had `awsPrivateCAIssuer.used: true`. On March 10th, `awsPrivateCAIssuer.used` was updated to `false`.
    *   **`barnsley-staging.yaml`**: This file was first introduced on March 10th, configuring `taskQueue`, `featureFlags` (`enableAccountEvents: true`, `enableBalanceCheckWebhooks: true`), and `awsPrivateCAIssuer.used: false`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/`**:
    *   **`barnsley-staging.yaml`**: On March 9th, it received an extensive configuration detailing `clusterName`, various endpoints, cache, transaction processing, Redis, categorization, aggregation policy, feature flags (`enableAccountEvents`, `enableTransactionEvents`), and database settings, with `awsPrivateCAIssuer.used: true`. On March 10th, `awsPrivateCAIssuer.used` was changed to `false`, followed by two redundant saves of the same content.
    *   **`oxford-staging.yaml`**: On March 9th, this file received a very comprehensive configuration including cache settings, detailed categorization (BFM, app IDs), aggregation policy, numerous feature flags (e.g., `enablePaymentAmlMonitoring`, data retention, tracing, logging, investments, loans, enrichment), transaction processing settings, Redis, and various cleanup jobs. It had `awsPrivateCAIssuer.used: true`. On March 10th, `awsPrivateCAIssuer.used` was updated to `false`.
    *   **`farnham-staging.yaml` and `jersey-staging.yaml`**: Both files were first introduced on March 10th with detailed, environment-specific configurations for their respective cluster names, endpoints (including oauth settings), cache, transaction processing, categorization, aggregation policy, feature flags, locales, data retention, Redis, and database settings. Both were configured with `awsPrivateCAIssuer.used: false`.

*   **`/Users/mmuruts/projects/tink/tink-backend/logggggg` (Log Files)**:
    *   **March 9th, 4:24:08 PM**: A `com.google.inject.ProvisionException` occurred during the initialization of `GrpcAccountService`, specifically an `IllegalArgumentException` related to "unknown object in getInstance: org.bouncycastle.asn1.DEROctetString", suggesting an issue with private key parsing during TLS setup.
    *   **March 10th, 8:39:26 AM**: A similar `java.lang.IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString` error was observed. This time, it caused an `org.springframework.beans.factory.UnsatisfiedDependencyException` during the initialization of beans like `accountDeduplicationService` and `aggregationAppConfigClient` within the `account-processing` service, again pointing to an issue with private key parsing when building an SSL context for gRPC clients.

**Timestamps of Significant Changes:**

*   **March 9th, 1:55 PM - 1:57 PM**: Initial widespread deployment or major configuration updates for various staging environments across `account`, `account-processing`, and `tink-backend-system` charts.
*   **March 9th, 4:24:08 PM**: First occurrence of the `org.bouncycastle.asn1.DEROctetString` error in the `GrpcAccountService`.
*   **March 10th, 8:39:26 AM**: Second occurrence of the `org.bouncycastle.asn1.DEROctetString` error, this time affecting the `account-processing` service.
*   **March 10th, 8:45 AM - 8:47 AM**: A concentrated series of updates across nearly all `values/*.yaml` files, explicitly setting `awsPrivateCAIssuer.used: false`. This timestamp also marks the introduction of several new staging environment configurations (`account/values/jersey-staging.yaml`, `account-processing/values/barnsley-staging.yaml`, `tink-backend-system/values/farnham-staging.yaml`, `tink-backend-system/values/jersey-staging.yaml`).

**Patterns and Recurring Elements:**

*   **`awsPrivateCAIssuer.used` setting**: The most prominent pattern is the widespread change of `awsPrivateCAIssuer.used` from `true` to `false` across almost all `.charts/*/values/*.yaml` files on March 10th. This indicates a system-wide adjustment to how AWS Private CA Issuers are utilized.
*   **Database and Cache Hostnames**: Many configurations specify AWS RDS database URLs and AWS ElastiCache hostnames, following consistent naming conventions (e.g., `main-YYYYMMDD...rds.amazonaws.com`, `main.xxxxxx.cache.amazonaws.com`).
*   **Internal Service Endpoints**: Numerous internal service endpoints are configured across charts using `dns:///` URLs within the `cluster.local` domain, often accompanied by `pinnedCertificates` pointing to `/tls/ca.crt` or `/internal-ca/ca.crt`.
*   **Extensive Feature Flags**: All services rely heavily on `featureFlags` to control functionality, with common flags like `enableAccountEvents` and `enableTransactionEvents` appearing in multiple configurations.
*   **Recurring TLS/PKI Error**: The `IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString` error is a critical recurring issue on both March 9th and 10th, indicating a persistent problem with private key parsing or SSL context initialization in different Tink services. This suggests a systemic problem related to certificate handling or underlying cryptographic libraries.
*   **Boolean Casing Inconsistency**: Some `featureFlags` use `True`/`False` while others use `true`/`false`, indicating a lack of consistent style in the YAML configurations.
*   **Environment-Specific Configurations**: Each staging environment (Barnsley, Farnham, Oxford, Jersey) has its own `values.yaml` file, often with tailored settings for endpoints, feature flags, and database connections.