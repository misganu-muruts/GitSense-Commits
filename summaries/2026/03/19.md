# Activity Summary for 3/19/2026

## 10:51:35 AM
The provided log details changes to Kubernetes/Helm configuration files for a backend system, all occurring on 3/19/2026 within a short timeframe (9:51 AM to 9:58 AM). The updates primarily focus on enhancing security and configuration around TLS, certificates, and various microservices.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (Timestamp: 3/19/2026, 9:51:34 AM)**
    This file defines a Kubernetes `Secret` named `cassandra-client-certificate-keystore-secret` in the `tink-system` namespace. Its creation is conditional on `.Values.cassandra.privateCA.enabled` and it is designed to store a password for a keystore.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (Timestamp: 3/19/2026, 9:51:46 AM)**
    This template introduces a `cert-manager.io/v1` `Certificate` resource, `cassandra-client-certificate`, for Cassandra client authentication within the `tink-system` namespace. It's also conditional on `.Values.cassandra.privateCA.enabled`. The certificate is configured to:
    *   Generate a JKS keystore, referencing the `cassandra-client-certificate-keystore-secret` for its password.
    *   Be issued by an `AWSPCAClusterIssuer`.
    *   Have subject organization "tink-ab" and include client authentication key usages.
    *   Use an RSA 2048-bit private key with an `Always` rotation policy.
    *   Include SPIFFE URI extensions for identity (e.g., `spiffe://<cluster-id>.<cluster>-<environment>.tink.network/ns/<namespace>/sa/<service>`).

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (Timestamp: 3/19/2026, 9:58:00 AM)**
    This comprehensive Helm `values.yaml` file contains numerous configuration parameters. Key updates and settings include:
    *   **Cassandra:** The `privateCA.enabled` flag is set to `true`, activating the certificate management described in the other two files.
    *   **Keystore Configuration:** Default `newPath`, `password`, `newCertAlias`, `path`, and `certAlias` are specified for keystores.
    *   **Service Endpoints:** An extensive list of internal microservices (e.g., `aggregationController`, `identityAggregatorServiceConfiguration`, `oauth`, `account`, `credentials`, `transactionAccess`) are configured with their URLs and `pinnedCertificates`. The most common pinned certificate path is `/internal-ca/ca.crt`.
    *   **Database & Distributed Database:** `sslMode=VERIFY_IDENTITY` is set for the database, and `tlsEnabled: true` with `pinnedCertificates: - /internal-ca/ca.crt` is configured for the distributed database.
    *   **Observability:** Datadog APM and profiling are enabled (`apmEnabled: true`, `profilingEnabled: true`), while tracing is disabled (`tracing: false`).
    *   **Feature Flags:** A large number of boolean feature flags are present, controlling various system behaviors like `enableAggJourneyTrackingEvents`, `useAggregationController`, and `useIdentityAggregatorService`.
    *   **Task Queue:** A `truststore` path of `/var/lib/kubernetes/tink-internal-ca.jks` is specified.
    *   **Redis:** Redis configuration is present but `enabled` is set to `false`, though `useRedisLocks` is `true`.
    *   **Notifications:** Notifications are explicitly disabled (`enabled: false`).
    *   **Scheduler:** `tempUserDeletionEnabled` is set to `true`.
    *   **Kafka & AWS Private CA Issuer:** Private CA functionality for Kafka and a general AWS Private CA Issuer are explicitly disabled (`enabled: false`) in their respective sections, contrasting with the Cassandra setup.

**Patterns and Recurring Elements:**

*   **TLS and Certificate Management:** A prominent theme across all changes is the strong focus on securing inter-service communication through TLS, certificates, and keystores. This includes specific configurations for Cassandra client certificates, extensive use of pinned certificates for internal service endpoints, and explicit truststore/keystore paths.
*   **`cassandra.privateCA.enabled`:** This boolean value acts as a central toggle, enabling or disabling the Cassandra Private CA certificate infrastructure.
*   **`pinnedCertificates: - /internal-ca/ca.crt`:** This configuration appears repeatedly across many service endpoint definitions within `values.yaml`, indicating a reliance on a common internal Certificate Authority for trust.
*   **"notAsecret" Placeholder:** The string "notAsecret" is used as a placeholder password in both the `cassandra-secret-keystore.yaml` definition and the `keystore.password` field within `values.yaml`.
*   **Cert-Manager and AWS PCA Integration:** The system leverages `cert-manager` for automated certificate provisioning and `AWSPCAClusterIssuer` for issuing certificates from AWS Private CA, highlighting an integrated cloud-native certificate solution.
*   **SPIFFE Identity:** The use of SPIFFE URIs for identifying Cassandra clients indicates an adherence to modern workload identity standards.
*   **Microservice Architecture:** The extensive list of internal service endpoints in `values.yaml` with their individual configurations strongly suggests a microservice-based architecture.
*   **Helm for Kubernetes Deployment:** The file structure and content (e.g., `.charts`, `templates`, `values.yaml`, `{{- if .Values... }}`) are characteristic of a Kubernetes deployment managed by Helm.

## 1:08:36 PM
The file `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml` was updated on `3/19/2026, 12:28:41 PM`.

This file defines a Kubernetes ConfigMap named `transaction-service-configuration-{{ .Values.Version }}`. It acts as a Helm template to dynamically generate the `transaction-service-server.yaml` configuration.

Key updates and configurations include:

*   **Core Service Settings:** Configuration for `log-level`, `user-svc`, `account-svc`, `data-retention` (including batch size, workers, and dry run), and `internal-v2-enabled`.
*   **Elasticsearch (ES) Integration:** Extensive configuration for Elasticsearch clusters, including a `common` cluster and conditional support for `nordea` and `seb` specific ES clusters if `enableMultiElasticSearchClusters` is true. Each ES configuration specifies hosts, sniffing, health checks, refresh intervals, index year ranges, shards, `temp-user-grace-period-enabled`, `is-common`, and `appids`.
*   **SNS and Provider Markets:** Settings for `sns-enabled`, `transaction-mutation-topic-name`, and `provider-markets` (defaulting to "GB,UK").
*   **Secret Management:**
    *   An `secret-manager-enabled` flag.
    *   Specific AWS Secrets Manager paths are defined for `page-token-hmac-secret-path` and `system-access-token-secret-path`, following a pattern of `{{ $cluster }}-{{ $environment }}/transaction-service/...`. This section is explicitly marked `START GENAI` and `END GENAI`.
    *   Conditional retrieval of Cassandra and main-db credentials from Vault if `.Values.useVault` is true, otherwise from `$cassandra` or `$maindb` variables.
*   **Cassandra Database Configuration:**
    *   Both a `cassandra-v2` (enabled, hosts, keyspace, port, username, a secret name derived from cluster/environment, TLS, CA path, start/end years) and a legacy `cassandra` (keyspace, port, TLS, CA path) are configured.
    *   Default Cassandra hosts are provided if not explicitly set.
*   **Main Database:** `main-db-cert` and `main-db-address` are configured, with a default address based on environment and cluster.
*   **Queue Configuration:** Sets `queue-name`, with a default that includes the cluster and environment.
*   **Server Settings:** `server-max-concurrency` and `listen-https` can be configured.
*   **Kafka Listeners:**
    *   `kafka-use-tls` and `cert-path` are conditionally enabled for Kafka.
    *   Specific listeners for `user-deletion-listener-enabled` and `account-deletion-listener-enabled`.
    *   Kafka hosts default to `queue.internal.{{ $environment }}.{{ $cluster }}.tink.network:9093`.
    *   Includes configurations for Kafka topics (e.g., `users.deleted`, `users.deleted.dlt`, `firehose-v1-accounts`), consumer groups, and `max-message-size` for user deletion.
*   **Redis Cache:** Configuration for `redis-hosts`, `redis-enabled`, `redis-ttl`, and `redis-min-response-transactions-number`.
*   **Build Metadata:** Includes `build-date` and `commit` if available, and `run-id` if specified.

**Patterns and Recurring Elements:**

*   **Helm Templating with Defaults:** Extensive use of `{{ .Values.config | default dict }}` and `{{ $var.field | default "value" }}` syntax demonstrates a strong reliance on Helm for injecting configuration values and providing robust default settings.
*   **Environment/Cluster Specificity:** Many service addresses (user-svc, account-svc, Cassandra, MainDB, Kafka, AWS Secrets Manager paths) are dynamically constructed using `{{ $environment }}` and `{{ $cluster }}` variables, indicating a multi-environment, multi-cluster deployment strategy.
*   **Modular Configuration:** The configuration is broken down into logical sections (Elasticsearch, Cassandra, Kafka, Redis, etc.), often with dedicated variables (`$common_es`, `$nordea_es`, `$seb_es`) for clarity and reusability.
*   **Data Retention and Deletion Focus:** Specific configurations for data retention and Kafka listeners for user and account deletion events highlight a focus on data lifecycle management.
*   **Security Practices:** The use of TLS for various services (user-svc, account-svc, Cassandra, Kafka), CA paths, and the integration with AWS Secrets Manager and Vault for credentials reflect a strong emphasis on secure configuration.

## 6:51:35 PM
The log details a series of code changes, primarily focusing on Kubernetes configurations and Helm chart values for a `tink-backend-system`, with all changes timestamped on 3/19/2026.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (Timestamp: 3/19/2026, 9:51:34 AM)**
    A Kubernetes Secret resource is defined to store the password for a Cassandra client certificate keystore. This secret is conditionally enabled based on whether a private CA for Cassandra is active.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (Timestamp: 3/19/2026, 9:51:46 AM)**
    This file defines a `cert-manager` Certificate resource for the Cassandra client certificate. It's configured for a 90-day duration with 30-day renewal attempts, uses an AWS Private CA issuer (`AWSPCAClusterIssuer`), and specifies "tink-ab" as the organization. The certificate supports client authentication, digital signature, and key encipherment, with an RSA 2048-bit private key and an "Always" rotation policy. Notably, it includes a dynamically generated SPIFFE URI for identity in the SAN extension. The entire resource is also conditionally enabled based on the Cassandra private CA setting.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (Timestamp: 3/19/2026, 9:58:00 AM)**
    This is a comprehensive Helm `values.yaml` file for the `tink-backend-system`, configuring a wide array of services and features:
    *   **General Configuration:** `useVault` is true, `clusterName` is "TINK".
    *   **Cassandra:** Private CA is `enabled: true`, matching the conditional logic in the certificate templates. `runCassandraMigrations` is false.
    *   **Resources & HPA:** Defines CPU (2000m) and memory (4Gi) limits, with HPA enabled (min 8, max 25 pods) targeting 60% CPU usage.
    *   **Certificates & Keystores:** A specific SHA256 pinned certificate is listed. A keystore path `/jks/tls.jks` and a placeholder password are set. Many service configurations include `pinnedCertificates` pointing to `/internal-ca/ca.crt`. A `truststore` path `/var/lib/kubernetes/tink-internal-ca.jks` is also specified.
    *   **Service Endpoints:** Numerous internal service endpoints (e.g., `aggregationController`, `identityAggregatorServiceConfiguration`, `oauth`, `account`, `credentials`) are configured with their URLs, mostly enabled, and frequently include certificate pinning.
    *   **Database:** `maxPoolSize` 20, `runFlywayMigrations: true`, and `sslMode=VERIFY_IDENTITY` are configured.
    *   **Distributed Database (Cassandra):** `tlsEnabled: true` and `pinnedCertificates` are specified.
    *   **Transactions:** Extensive configuration for transaction processing, including S3 bucket and SNS topic names for reconciliation, and an `snsFlowCallbackUrl`.
    *   **Redis:** `enabled: false` but `useRedisLocks: true` and `snsFlowEnabled: true`.
    *   **Tracing & Notifications:** Both are `enabled: false`.
    *   **Feature Flags:** Many flags are set to true, such as `enableAggJourneyTrackingEvents`, `useAggregationController`, `useIdentityAggregatorService`, and `useCredentialBackgroundRefresh`.
    *   **Scheduler:** `tempUserDeletionEnabled: true`.
    *   **Datadog:** `apmEnabled: true`, `profilingEnabled: false`.
    *   **AWS Integration:** `useAwsSecretManager` is false. Kafka and AWS Private CA Issuer settings are `enabled: false`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/farnham-staging.yaml` (Timestamp: 3/19/2026, 10:56:11 AM)**
    This file specifies environment-specific overrides for `farnham-staging`. It configures the `maindb` host to an AWS RDS instance (`eu-west-1.rds.amazonaws.com`). It also explicitly enables and trusts the `awsPrivateCAIssuer` for this environment (`trusted: true, enabled: true, used: true`).

**Timestamps of Significant Changes:**
All recorded changes occurred on March 19, 2026, within a span of approximately one hour, starting from 9:51:34 AM and concluding by 10:56:11 AM. This indicates a concentrated set of updates or a single deployment/configuration push.

**Patterns or Recurring Elements:**

*   **Certificate and TLS Management:** There is a strong emphasis on secure communication. This includes:
    *   Extensive use of `cert-manager` and `AWSPCAClusterIssuer` for certificate lifecycle management.
    *   Conditional enabling of private CAs (e.g., `cassandra.privateCA.enabled`, `awsPrivateCAIssuer`).
    *   Frequent configuration of `pinnedCertificates`, with `/internal-ca/ca.crt` being a common path, and specific `truststore` and `keystore` paths.
    *   Explicit TLS/SSL settings like `sslMode=VERIFY_IDENTITY` and `tlsEnabled: true` for databases and distributed systems.
    *   SPIFFE URIs are used for workload identity, demonstrating a modern approach to secure inter-service communication.
*   **Service-Oriented Architecture Configuration:** A large number of distinct internal service endpoints are defined with their URLs and often include service-specific certificate pinning, indicating a complex microservices environment.
*   **Conditional Feature Toggles:** Many configurations across `values.yaml` are controlled by `enabled: true/false` flags, allowing for flexible deployment and feature rollout.
*   **AWS Integration:** Multiple components reference AWS services, including RDS for the database host, `AWSPCAClusterIssuer` for certificates, and S3/SNS for transaction reconciliation, suggesting a significant reliance on the AWS cloud ecosystem.
*   **Placeholder Secrets:** The string `notAsecret` appears as a placeholder password in both the Cassandra keystore secret and the general keystore configuration in `values.yaml`.
*   **Consistency in `tink-system` Context:** The namespace `tink-system`, organization `tink-ab`, and domain `tink.network` are consistently used across the configurations, tying them to a specific system within the Tink platform.

## 10:51:34 PM
The changes primarily occurred on March 19, 2026, within a span of approximately one hour, indicating a concentrated effort on infrastructure, security, and service configuration updates.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (3/19/2026, 9:51:46 AM):**
    This template defines a `cert-manager.io/v1` Certificate resource named `cassandra-client-certificate`. It's configured to issue Cassandra client certificates with a 90-day duration, renewing 30 days before expiry. It specifies the creation of a JKS keystore, with its password sourced from a Kubernetes Secret. The certificate issuer is an `AWSPCAClusterIssuer`, intended for client authentication (digital signature, key encipherment) for the "tink-ab" organization. The private key uses RSA 2048-bit with an "Always" rotation policy. A notable feature is the inclusion of a dynamic SPIFFE identity URI in the Subject Alternative Name (SAN) extension. This certificate generation is conditional on `cassandra.privateCA.enabled`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (3/19/2026, 9:58:00 AM):**
    This extensive configuration file for the `tink-backend-system` defines numerous settings:
    *   It enables `useVault` and sets the `clusterName` to "TINK".
    *   Cassandra's private CA is enabled, and resource limits (2000m CPU, 4Gi memory) with Horizontal Pod Autoscaler (HPA) settings are defined (8-25 replicas).
    *   Many internal service endpoints (e.g., `aggregationController`, `identityAggregatorServiceConfiguration`, `oauth`, `account`, `credentials`, `connectedService`, `authenticationEngine`, `transactionAccess`, `aggregationApplicationPolicyService`) are configured. Most are `enabled: true`, and a recurring pattern is the use of `pinnedCertificates: - /internal-ca/ca.crt` for secure communication.
    *   Database settings include `maxPoolSize: 20`, `runFlywayMigrations: true`, `alwaysUseJdbi: true`, and `sslConfig: sslMode=VERIFY_IDENTITY`.
    *   Distributed database (Cassandra) configurations specify `tlsEnabled: true` also with `/internal-ca/ca.crt` pinned certificate, 3 seed nodes, and no keyspace creation on start.
    *   Task queue settings indicate `rateLimitEnabled: false`, `mode: enabled`, and `hasLoadBalancer: true`.
    *   Transaction processing configurations define `processingWorkers: 10`, `topicWorkers: 12`, and specify AWS S3 bucket and SNS topic names for reconciliation. `snsFlowEnabled` is true.
    *   Several feature flags are enabled, such as `enableAggJourneyTrackingEvents`, `useAggregationController`, `useIdentityAggregatorService`, and `useCredentialBackgroundRefresh`. `developmentMode` is false.
    *   Datadog APM and profiling are enabled.
    *   Kafka's private CA and AWS Private CA Issuer are explicitly disabled.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/farnham-staging.yaml` (3/19/2026, 10:56:11 AM):**
    This environment-specific `values.yaml` for `farnham-staging` configures the `maindb` host to an AWS RDS instance located in `eu-west-1`. Significantly, it explicitly sets `awsPrivateCAIssuer: trusted: true enabled: true used: true`, indicating that the AWS Private CA Issuer is trusted and enabled for this specific staging environment.

**Patterns and Recurring Elements:**

1.  **Strong Emphasis on TLS/SSL and Certificate Management:** There is a pervasive configuration for secure communication across services. This includes `cassandra.privateCA.enabled`, `sslMode=VERIFY_IDENTITY` for the database, `tlsEnabled: true` for the distributed database, and the widespread use of `pinnedCertificates: - /internal-ca/ca.crt` across almost all internal service endpoints. This indicates a consistent strategy for mutual TLS or robust server identity verification within the cluster, relying on a shared internal Certificate Authority. The use of `cert-manager` and `AWSPCAClusterIssuer` points to an automated and integrated approach to certificate lifecycle management.
2.  **AWS Integration:** Several configurations directly reference AWS services. The `AWSPCAClusterIssuer` is key for certificate issuance, a `maindb` host is an AWS RDS instance, and transaction reconciliation uses AWS S3 and SNS.
3.  **Microservices Architecture:** The `values.yaml` details configurations for numerous distinct internal services, each with its own `enabled` flag, URL, and security settings, demonstrating a microservices-based system design.
4.  **Configuration-Driven Deployment:** The `values.yaml` file serves as a central hub for enabling/disabling features, defining connectivity, and setting resource allocations, highlighting a flexible, configuration-based approach to deploying and managing the backend system.