# Activity Summary for 3/12/2026

## 10:05:59 AM
Between March 9th and March 11th, 2026, the codebase saw a series of configuration updates across multiple staging environments and critical Java code changes addressing private key handling for gRPC services.

**Timestamps of Significant Changes:**

*   **March 9, 2026, 4:24 PM:** The first error log indicated a critical `java.lang.IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString` during private key processing within the `GrpcAccountService`.
*   **March 10, 2026, 8:39 AM:** A similar `IllegalArgumentException` reappeared in the logs, highlighting a persistent issue with private key parsing, this time originating from `GrpcTlsFileReaderShaded`.
*   **March 10, 2026, 8:45 AM - 8:47 AM:** A broad configuration update changed the `awsPrivateCAIssuer.used` flag from `true` to `false` across almost all staging environments, potentially as an immediate mitigation or in preparation for a fix related to certificate issuance/usage.
*   **March 10, 2026, 9:53 AM - 1:22 PM:** Core Java libraries were updated to explicitly support EC private keys, followed by the addition of new tests to validate this functionality.
*   **March 11, 2026, 9:34 AM - 9:58 AM:** A new Kubernetes certificate template was introduced specifying ECDSA for private keys, and the EC private key parsing logic in Java was further refined for robustness.

**File-Specific Updates:**

**YAML Configuration Files (`.charts/*/values/*.yaml`):**

*   **Initial Configuration (March 9, 2026, 1:55 PM - 1:57 PM):**
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/barnsley-staging.yaml`, `/farnham-staging.yaml`: Contained basic `maindb` host and `awsPrivateCAIssuer` set to `used: true`.
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/oxford-staging.yaml`: Included extensive settings for `taskqueue`, `cassandra`, `maindb`, numerous internal `endpoints` with `pinnedCertificates`, and various `FeatureFlags` (`accountVerificationEnabled`, `appIdFeatures` for specific test accounts), with `awsPrivateCAIssuer: used: true`. A subsequent timestamp (3/9, 1:58 PM) showed identical content for this file.
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/farnham-staging.yaml`: Enabled `enableAccountEvents` feature flag and `awsPrivateCAIssuer: used: true`.
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/jersey-staging.yaml`: Only contained `awsPrivateCAIssuer: used: true`.
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/oxford-staging.yaml`: Configured `taskQueue` and `FeatureFlags` like `enableAccountEvents`, `balanceCheckEnabled`, and `accountDeduplication` with specific app IDs, with `awsPrivateCAIssuer: used: true`.
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/barnsley-staging.yaml`: Defined `clusterName`, `endpoints`, `cache`, `transactionAccess` settings, `redisConfiguration`, `categorization` settings, `aggregationApplicationPolicyService`, various `featureFlags`, `database` URL, and `awsAccountId`, with `awsPrivateCAIssuer: used: true`.
    *   `/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/oxford-staging.yaml`: Contained similar system-wide configurations, with more extensive `categorization` (BFM), `dataRetention` policies, `tracing`, `logging` levels, and `jobs` for data management, with `awsPrivateCAIssuer: used: true`.

*   **Subsequent Configuration Updates (March 10, 2026, 8:45 AM - 8:47 AM):**
    *   **Universal Change:** All existing `.yaml` files (Barnsley, Farnham, Oxford across `account`, `account-processing`, `tink-backend-system` charts) were updated to set `awsPrivateCAIssuer: used: false`.
    *   **New Environment Introductions:**
        *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/jersey-staging.yaml`: Introduced with `accountdb` host, specific `endpoints` and `FeatureFlags` for Jersey staging, and `awsPrivateCAIssuer: used: false`.
        *   `/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/barnsley-staging.yaml`: Introduced with `taskQueue` and `FeatureFlags` and `awsPrivateCAIssuer: used: false`.
        *   `/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/farnham-staging.yaml`: Introduced with a full set of system configurations for `FARNHAM` cluster, including `endpoints`, `cache`, `categorization`, `featureFlags`, `locales`, `persistentVolume`, `transactions` settings, `database` URL, and `awsAccountId`, with `awsPrivateCAIssuer: used: false`.
        *   `/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/jersey-staging.yaml`: Introduced with similar system configurations for `JERSEY` cluster, with `awsPrivateCAIssuer: used: false`.
    *   Several subsequent timestamps for `/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/barnsley-staging.yaml` (March 10, 8:52 AM and 8:53 AM) showed no change in content.

**Java Code Files:**

*   **`logggggg` (Error Logs - March 9, 4:24 PM & March 10, 8:39 AM):**
    *   Recorded repeated `java.lang.IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString` errors during private key parsing (specifically involving `org.bouncycastle.asn1.ASN1Sequence.getInstance` and `org.bouncycastle.crypto.util.PrivateKeyFactory.createKey`), highlighting a critical issue in how private keys were being handled, particularly in `GrpcServer.ensurePkcs8` and `GrpcTlsFileReaderShaded.getPrivateKeyParameter`.

*   **`src/libraries/grpc_server/src/main/java/se/tink.libraries.grpc_server_shaded/GrpcServer.java` (March 10, 9:53 AM & March 11, 9:57 AM):**
    *   The `ensurePkcs8` method was modified to improve private key parsing. Initially, it was updated to explicitly handle `EC PRIVATE KEY` types (SEC1 EC keys) using `ECPrivateKey.getInstance` and `CustomNamedCurves.getByOID`.
    *   Later, the EC key parsing logic was refined to use `ASN1ObjectIdentifier` for the curve and include a fallback to `org.bouncycastle.asn1.x9.ECNamedCurveTable.getByOID` if `CustomNamedCurves` did not find the curve, and `ECNamedDomainParameters` for more robust EC key handling. These changes were explicitly marked with `/* START GENAI */` and `/* END GENAI */` comments.

*   **`src/libraries/grpc_utils/GrpcTlsFileReaderShaded.java` (March 10, 9:56 AM & 9:57 AM):**
    *   The `getPrivateKeyParameter` method was updated to add support for parsing `EC PRIVATE KEY` (SEC1 EC keys) using `ECPrivateKey.getInstance` and `CustomNamedCurves.getByOID`, similar to `GrpcServer.java`. These additions were also marked with `/* START GENAI */` and `/* END GENAI */` comments. No functional change between the two timestamps.

*   **`src/libraries/grpc_client_utils/shaded/GrpcClientUtilsTest.java` (March 10, 1:22 PM):**
    *   Expanded `setup` method to include generation of EC server and client certificates and keys.
    *   Added two new test methods, `buildTlsChannelWithEcKey` and `buildMTlsChannelWithEcKey`, to verify TLS and mTLS channel construction using EC keys, demonstrating new functionality and testing related to the code changes in `GrpcServer` and `GrpcTlsFileReaderShaded`. These additions were also marked with `/* START GENAI */` and `/* END GENAI */` comments.

*   **`src/libraries/grpc_server/src/main/java/se/tink.libraries.grpc_server_shaded_test/TestGrpcServer.java` (March 11, 9:58 AM):**
    *   The `ensurePkcs8` method in this test-focused gRPC server utility was updated with the same refined EC private key parsing logic as seen in the production `GrpcServer.java` to ensure consistency and correctness in test environments. This also includes the `/* START GENAI */` and `/* END GENAI */` markers.

**Kubernetes/Helm Chart Template:**

*   **`.charts/account/templates/certificate-service-certificate-aws.yaml` (March 11, 9:34 AM & 9:58 AM):**
    *   A new `Certificate` resource was defined, conditionally enabled by `awsPrivateCAIssuer.enabled`.
    *   It explicitly sets `privateKey: algorithm: ECDSA` and `rotationPolicy: Always`, indicating a shift towards ECDSA certificates.
    *   It uses an `AWSPCAClusterIssuer` and specifies numerous `dnsNames` for `account` and `account-headless` services across various network configurations, including `localhost`. No functional change between the two timestamps.

**Patterns and Recurring Elements:**

*   **Shift in AWS Private CA Usage:** A notable pattern is the change of `awsPrivateCAIssuer: used: true` to `used: false` across nearly all staging environment configurations on March 10th. This suggests a systemic change or a temporary disabling of the AWS Private CA issuer.
*   **EC Key Adoption:** The recurring theme in the Java code is the implementation and refinement of Elliptic Curve (EC) private key parsing. This was driven by `IllegalArgumentException` errors encountered during runtime, leading to explicit code additions (marked `/* START GENAI */`) in `GrpcServer.java` and `GrpcTlsFileReaderShaded.java` and corresponding test coverage in `GrpcClientUtilsTest.java`. The subsequent Kubernetes certificate template specifying `ECDSA` further solidifies this shift.
*   **Staging Environment Expansion:** The introduction of `jersey-staging.yaml` and additional `farnham-staging.yaml` files indicates the expansion of deployment targets or new environments being configured.
*   **Consistency in `values.yaml` Structure:** The `values.yaml` files consistently define `Connections`, `Endpoints` (often with `pinnedCertificates`), `FeatureFlags`, `Database` URLs, and `awsAccountId`, demonstrating a standardized approach to service configuration across environments.
*   **Internal Service Communication:** Many `endpoints` configurations use `dns:///service-headless.service.svc.cluster.local` URLs with `pinnedCertificates`, indicating a robust internal mTLS communication setup within the Kubernetes cluster.

## 10:18:33 AM
The file `/Users/mmuruts/projects/tink/identity-aggregator/.charts/identity-aggregator/values/jersey-staging.yaml` was updated on 3/12/2026, 9:35:02 AM. This update primarily configures service connections and security settings for the `jersey-staging` environment.

Key changes include:
*   **Database Host Configuration**: The `db: host` setting was updated to point to a specific AWS RDS endpoint: `identitydata-20240828104440949900000007.cqzul1kb5khg.eu-west-1.rds.amazonaws.com`.
*   **Providers Service Endpoint**: The `endpoints: providers` section was configured:
    *   `enabled` is set to `true`.
    *   The `url` specifies an internal Kubernetes service: `dns:///providers-app-search-api-headless.providers-app-search-api.svc.cluster.local:8443/`.
    *   `pinnedCertificates` includes `/tls/ca.crt`, indicating a certificate pinning configuration.
    *   `awsPrivateCAIssuer` is explicitly set to `used: false`, meaning an AWS Private CA is not utilized for this service.

## 8:22:36 PM
The log details changes to Kubernetes configuration files within the `transaction-processing-service` Helm chart, focusing on certificate management and the main application deployment.

### File-Specific Updates:

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/templates/certificate-service-certificate-aws.yaml`**
    *   **Timestamp: 3/12/2026, 3:59:56 PM** - Initial creation of a `Certificate` resource named `service-certificate-aws`. This certificate is configured for `server auth` and `client auth` for the `transaction-processing-service`, issued by `aws-private-ca-issuer` (an `AWSPCAClusterIssuer`). It specifies a 90-day duration with renewal attempted 30 days prior to expiry, storing the private key in `ingress-tls-aws` secret with `ECDSA` algorithm and `PKCS8` encoding.
    *   **Timestamp: 3/12/2026, 4:00:31 PM** - A minor modification occurred where the `encoding: PKCS8` specification for the private key was removed. All other settings remained identical.

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/templates/certificate-service-certificate-aws-pcks.yaml`**
    *   **Timestamp: 3/12/2026, 4:02:21 PM** - Introduction of a new `Certificate` resource named `service-certificate-aws-pkcs`. This certificate is largely identical in its specifications to `service-certificate-aws`, including the issuer, durations, usages, and DNS names. The key difference is that it explicitly creates a secret named `ingress-tls-aws-pkcs` and *retains* the `privateKey.encoding: PKCS8` setting, which was removed from the other AWS certificate in a subsequent commit.
    *   **Timestamp: 3/12/2026, 4:08:26 PM** - No functional changes were observed in this entry; the content is identical to the previous timestamp for this file.

*   **`/Users/mmuruts/projects/tink/transaction-processing-service/.charts/transaction-processing-service/templates/deployment.yaml`**
    *   **Timestamp: 3/12/2026, 4:06:15 PM** - A comprehensive update to the `Deployment` configuration for `transaction-processing-service`. Key aspects include:
        *   **Dynamic Replicas:** Number of replicas is controlled by `canaryDeployments.phase` and `hpa.podMinReplicas`.
        *   **Rolling Updates:** Configured with `maxSurge` and `maxUnavailable` for graceful rollouts.
        *   **Annotations:** Includes `kube-score` ignore, a `checksum/config` annotation to force rollouts on configuration changes, and extensive Datadog observability tags (env, service, version, git commit SHA, repo URL) and configuration if Datadog is enabled.
        *   **Security Context:** Enforces `runAsNonRoot: true`, specific `runAsUser`/`runAsGroup`/`fsGroup`, and `readOnlyRootFilesystem: true` for containers, enhancing security posture.
        *   **Vault Integration:** If `useVault` is enabled, two init containers (`fetch-credentials` using `vaultify` and `build-truststore` using OpenJDK) are added to fetch secrets from Vault, template configurations, and build a JKS truststore from an internal CA bundle. A `renew-leases` sidecar container (also `vaultify`) is included to manage secret lease renewals.
        *   **Main Service Container:** Specifies JVM arguments for memory management, logging, and Spring profiles. It supports conditional Cassandra driver configuration. Environment variables include `UNLEASH_API_KEY` (fetched from a secret) and Datadog logging/profiling settings.
        *   **Probes and Lifecycle:** Standard `readinessProbe`, `livenessProbe`, and `startupProbe` are configured for health checks. A `preStop` hook introduces a sleep to allow graceful termination.
        *   **Volume Management:** Defines various volumes for configuration (`config-volume`), temporary data (`tmp`, `credentials`, `tink-internal-ca-truststore-volume` as `emptyDir`), TLS certificates (`tls-aws` or `tls` secrets, and `service-certificate-volume-aws` or `service-certificate` secrets), and CA trust bundles (`ca-trust-bundle` ConfigMap).
        *   **Pod Anti-Affinity:** Configured to spread pods across hostnames and availability zones for high availability.

### Patterns and Recurring Elements:

*   **Kubernetes and Helm Centric:** All changes are to Kubernetes resource definitions, heavily leveraging Helm templating (`.Values`, `.Chart`) for dynamic configuration.
*   **Cert-Manager Integration:** `cert-manager.io/v1` is consistently used for managing X.509 certificates, with a strong reliance on `AWSPCAClusterIssuer` (`aws-private-ca-issuer`) to obtain certificates from AWS Private Certificate Authority.
*   **Service Identity and Trust:** There's a clear emphasis on establishing service identity through certificates for `server auth` and `client auth`. The presence of `ca-trust-bundle` and the `build-truststore` init container indicates a robust internal CA trust mechanism.
*   **Security Best Practices:** Repeated use of `readOnlyRootFilesystem: true`, `runAsNonRoot: true`, and integration with Vault for secret management highlights a focus on secure container configurations.
*   **Observability:** Datadog integration is a recurring pattern for monitoring and tracing, with specific annotations and environment variables to enable it.
*   **Consistent Naming:** The service name `transaction-processing-service` is used uniformly across namespaces, labels, DNS entries, and secret references, indicating a standardized naming convention.
*   **Private Key Configuration:** `ECDSA` is the preferred algorithm for private keys. The difference in `PKCS8` encoding specification between `certificate-service-certificate-aws.yaml` (removed) and `certificate-service-certificate-aws-pcks.yaml` (present) suggests specific requirements for different certificate secrets, possibly for interoperability or compliance.