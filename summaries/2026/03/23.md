# Activity Summary for 3/23/2026

## 8:14:58 AM
The log details changes primarily related to Kubernetes Helm charts for the `tink-backend-system` and `account` services, focusing on certificate management, service configurations across different staging environments, and deployment settings.

### File-Specific Updates:

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (3/19/2026, 9:51:34 AM)**:
    *   Introduces a Kubernetes Secret `cassandra-client-certificate-keystore-secret` of type `Opaque`.
    *   This secret is conditionally enabled based on `.Values.cassandra.privateCA.enabled` and stores a password.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (3/19/2026, 9:51:46 AM)**:
    *   Defines a `cert-manager.io/v1` Certificate resource for the Cassandra client certificate, also conditional on `.Values.cassandra.privateCA.enabled`.
    *   It specifies a 90-day duration with 30-day renewal, creates a JKS keystore, and references the `cassandra-client-certificate-keystore-secret` for its password.
    *   Uses `aws-private-ca-issuer` and sets `subject.organizations` to "tink-ab" with `client auth`, `digital signature`, and `key encipherment` usages.
    *   Includes SPIFFE identity in SAN URI extensions, dynamically configured with global cluster, environment, namespace, and service values.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (3/19/2026, 9:58:00 AM)**:
    *   This is a comprehensive update to the main `values.yaml` for `tink-backend-system`.
    *   **Enabled Features**: `useVault`, `cassandra.privateCA.enabled`, HPA for `tink-backend-system`, `fasttext`, `oauth`, `account`, `credentials`, `connectedService`, `aggregationEngine`, `authenticationEngine`, `oauthService`, `providersSearchApiClientConfiguration`, `runFlywayMigrations`, `alwaysUseJdbi`, `tlsEnabled` for distributed database, `hasLoadBalancer` for taskQueue, `firehoseEnabled`, `categorization`, `readFromTransactionService`, `snsFlowEnabled`, `useAggregationController`, `isAggregationControllerOnK8s`, `useIdentityAggregatorService`, `useCredentialBackgroundRefresh`, `enableAggJourneyTrackingEvents`, `datadog.apmEnabled`.
    *   **Disabled Features**: `cassandra.privateCA.used`, `cassandra.runCassandraMigrations`, `investments`, `loans`, `enrichmentEventTransmitterService`, `userInternalClient`, `plainText` for `providersSearchApiClientConfiguration`, `runFlywayMigrations`, `createKeyspaceOnStart`, `rateLimitEnabled` for taskQueue, `idMask`, `elasticsearchCluster`, `backoffice`, `writeToTransactionService`, `compareSearchResponses`, `notifications`, `aggregationApplicationPolicyService`, `useForkedContext`, `useClientPolicy`, `whitelistingEnabledOnCluster`, `whitelistingEnabledOnAppDefault`, `mxCategoryMappingEnabled`, `userLearningEnabled`, `bfmUserLearningEnabled`, `developmentMode`, `useRealProviderData`, `overrideDeprecatedEventClient`, `capabilityFilteringEnabled`, `tempUserDeletionEnabled`, `maskBankIdOnAccountsJob`, `modifyUserFlags`, `allowAllUsers`, `deleteUsersV2`, `persistentVolume`, `createConsentsByBgrExplicitly`, `datadog.profilingEnabled`, `useAwsSecretManager`, `kafka.privateCA.enabled`, `kafka.privateCA.used`, `awsPrivateCAIssuer.trusted`, `awsPrivateCAIssuer.enabled`, `awsPrivateCAIssuer.used`.
    *   **Key Configurations**: Sets default resources, HPA targets, `notAsecret` for keystore password, defines numerous service endpoints with `pinnedCertificates` (many pointing to `/internal-ca/ca.crt`), database connection details including `sslMode=VERIFY_IDENTITY`, Cassandra distributed database settings, Redis configuration, transaction processing settings, locales, and feature flags. Notably, `awsPrivateCAIssuer` is initially disabled globally.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/*.yaml` (3/19/2026, 10:56:11 AM - 3/20/2026, 9:12:08 AM)**:
    *   **`farnham-staging.yaml`**: Configures `maindb.host` and sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.
    *   **`barnsley-staging.yaml`**: Similar to farnham, updates `maindb.host` and enables `awsPrivateCAIssuer`.
    *   **`jersey-staging.yaml`**: Enables `useAccountdb`, `databaseMigrationsEnabled`, `accountDataRepositoryEnabled`, `cassandra.enabled`, specifies `accountdb.host`, various endpoints (credentials, identity, accountService, authService, user, providersSearchApi) with `/tls/ca.crt` pinned certificates, and multiple feature flags (e.g., `accountVerificationEnabled`, `enableContinuesAccountDataEndpoints`). Also sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.
    *   **`oxford-staging.yaml`**: Enables `taskqueue.tlsEnabled`, `cassandra.enabled`, `avrByUserTableWriteEnabled: false`. Updates `maindb.host` and configures numerous endpoints (credentials, identity, statistics, riskInsights, incomeCheck, holderNameCheck, accountService, authService, user, providersSearchApi, als) with `/tls/ca.crt` pinned certificates. Includes specific `appIdFeatures` for different test accounts and sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.
    *   A consistent pattern across these environment-specific files is the explicit enabling of `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/*.yaml` (3/20/2026, 9:12:38 AM - 3/20/2026, 12:11:27 PM)**:
    *   **`farnham-staging.yaml` (initial)**: Enables `featureFlags.enableAccountEvents` and sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.
    *   **`jersey-staging.yaml` (initial)**: Simply sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.
    *   **`oxford-staging.yaml` (initial)**: Enables `endpoints.taskQueue`, several `featureFlags` related to account and balance events, `balanceCheckEnabled`, `accountDeduplication` settings, and sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`.
    *   **`barnsley-staging.yaml`**: Enables `endpoints.taskQueue`, `featureFlags.enableAccountEvents`, `enableBalanceCheckWebhooks`, and sets `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: false`.
    *   **`farnham-staging.yaml` (update)**: Changes `awsPrivateCAIssuer.used` from `true` to `false`.
    *   **`oxford-staging.yaml` (update)**: Changes `awsPrivateCAIssuer.used` from `true` to `false`.
    *   **`jersey-staging.yaml` (update)**: Changes `awsPrivateCAIssuer.used` from `true` to `false`.
    *   The `account-processing` staging files consistently enable `awsPrivateCAIssuer` as trusted and enabled, but an update on 3/20/2026 around midday specifically changes `awsPrivateCAIssuer.used` to `false` across all `barnsley`, `farnham`, `oxford`, and `jersey` staging environments for `account-processing`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/*.yaml` (3/20/2026, 9:13:27 AM - 3/20/2026, 12:12:09 PM)**:
    *   **`barnsley-staging.yaml` (initial & update)**: Sets `clusterName: "BARNSLEY"`, disables `identityAggregatorServiceConfiguration`, enables `userInternalClient`, customizes `taskQueue.streamFetcherMaxBytes`, `cache.host`, `transactionAccess.writeToTransactionService`, `transactions.processingUserLockSuffixBits`, `redisConfiguration`, `categorization.userLearningEnabled`, and specific `featureFlags` (e.g., `enableAccountEvents`, `enableTransactionEvents`). Crucially, enables `aggregationApplicationPolicyService` with `useForkedContext` and `useClientPolicy`. Sets `database.url`, `useAwsSecretManager: true`, `awsAccountId`, and `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`. An update later on 3/20/2026 changes `awsPrivateCAIssuer.used` to `false`.
    *   **`farnham-staging.yaml` (initial & multiple updates)**: Sets `clusterName: "FARNHAM"`, disables `identityAggregatorServiceConfiguration` and `oauth`, enables `userInternalClient` and `oauthService`. Configures `cache.host`, `transactionAccess.writeToTransactionService`, `categorization.userLearningEnabled`, `aggregationApplicationPolicyService` with `useForkedContext` and `useClientPolicy`. Enables various `featureFlags`. Defines specific `locales`. Enables `persistentVolume`. Configures `transactions.redisConfiguration` with `clusteredHost` and `timeout`. Sets `database.url`, `useAwsSecretManager: true`, `awsAccountId`, and `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`. Multiple subsequent updates to this file are identical, indicating re-commits or re-saves without content changes, until the 3/20/2026, 12:11:57 PM update where `awsPrivateCAIssuer.used` is changed to `false`.
    *   **`jersey-staging.yaml`**: Sets `clusterName: "JERSEY"`, enables `userInternalClient`, configures `cache.host`, `transactionAccess.writeToTransactionService`, disables `categorization` and `userLearningEnabled`, enables `aggregationApplicationPolicyService`. Sets `featureFlags`, `locales`, `persistentVolume`, `transactions.redisConfiguration`, `database.url`, `useAwsSecretManager`, `awsAccountId`, and `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`. An update later on 3/20/2026 changes `awsPrivateCAIssuer.used` to `false`.
    *   **`oxford-staging.yaml` (initial & update)**: Configures `cache.host`, enables `categorization.userLearningEnabled`, `bfmEnabled`, `bfmUserLearningEnabled`, `aggregationApplicationPolicyService`. Sets numerous `featureFlags` (e.g., `enableAccountEvents`, `enableTransactionEvents`, `enableUpdateAccountEvents`). Configures `locales`, `dataRetention` policies, `tracing` and `logging` levels. Enables `investments`, `loans`, `enrichmentEventTransmitterService`, `userInternalClient`. Updates `transactions` settings including `redisConfiguration`. Disables several jobs related to account/transaction data modification/deletion. Sets `database.url`, `useAwsSecretManager: true`, `awsAccountId`, and `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true`. An update later on 3/20/2026 changes `awsPrivateCAIssuer.used` to `false`.
    *   The `tink-backend-system` staging files show a consistent pattern of enabling `awsPrivateCAIssuer` as trusted and enabled initially, similar to `account` and `account-processing`. However, around midday on 3/20/2026, all `tink-backend-system` staging environments (`barnsley`, `farnham`, `jersey`, `oxford`) also change `awsPrivateCAIssuer.used` to `false`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml` (3/20/2026, 12:23:48 PM & 3/20/2026, 12:28:28 PM)**:
    *   Defines a `cert-manager.io/v1` Certificate resource `grpc-certificate-aws` for `tink-system` namespace.
    *   Conditionally enabled by `.Values.awsPrivateCAIssuer.enabled`.
    *   Specifies 90-day duration, 30-day renewal, RSA private key with `PKCS8` encoding, and `Always` rotation policy.
    *   Uses `aws-private-ca-issuer` with `tink ab` organization and `server auth`, `client auth` usages.
    *   Lists several `dnsNames` for the `tink-backend-system` service.
    *   The two timestamps show identical content, indicating a re-commit.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml` (3/20/2026, 12:28:42 PM)**:
    *   Defines a `cert-manager.io/v1` Certificate resource `grpc-certificate-aws` for `account-processing` namespace.
    *   Conditionally enabled by `.Values.awsPrivateCAIssuer.enabled`.
    *   Similar certificate properties (duration, renewal, private key, issuerRef, subject, usages) to the `tink-backend-system` GRPC certificate.
    *   Dynamically includes `additionalCertNames` and hardcoded DNS names for `account-processing-service` and its headless variant, including `localhost`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml` (3/20/2026, 3:12:37 PM - 3/20/2026, 3:15:30 PM)**:
    *   Multiple, nearly identical updates to the `tink-backend-system` deployment manifest.
    *   Includes `tink-health` annotations with Prometheus queries and Datadog monitor IDs for error rates and HTTP healthy 200 ratios, with specific overrides for the "cornwall" cluster and "oxford-staging" environment.
    *   Configures deployment strategy (`RollingUpdate`) and replica settings (conditional on `hpa.enabled` and `disabled`).
    *   Adds Datadog APM annotations for environment, version, and service.
    *   Defines several `initContainers` if `useVault` is true:
        *   `secretman`: Retrieves secrets from AWS Secret Manager based on cluster and environment.
        *   `secretman-template`: Uses `sed` to replace placeholders in `system-server.yaml` with values from AWS secrets, if `useAwsSecretManager` is enabled.
        *   `generate-pinned-certificates`: Runs a script to generate pinned certificates, conditional on `awsPrivateCAIssuer.trusted`.
        *   `fetch-credentials`: Uses `vaultify` to template configuration with Vault secrets.
        *   `cassandra-migrations`: Runs Cassandra schema migrations if `runCassandraMigrations` is true.
        *   `build-truststore`: Creates a JKS truststore, conditionally importing `bundle.p12` from `ca-trust-bundle` (if `awsPrivateCAIssuer.trusted`) or `ca.crt` from `vault_ca`.
        *   `build-keystore`: Starts to define a keystore build, which is conditional on `awsPrivateCAIssuer.used`.
    *   The `deployment.yaml` content remains virtually identical across the three timestamps on 3/20/2026 afternoon, suggesting re-commits or re-saves without substantive changes.

### Timestamps of Significant Changes:

*   **3/19/2026, 9:51 AM - 9:58 AM**: Initial setup for Cassandra private CA certificate management (secret and certificate definitions) and a comprehensive update to the global `tink-backend-system/values.yaml` configuring numerous service settings and feature flags.
*   **3/19/2026, 10:56 AM - 3/20/2026, 9:12 AM**: Initial configuration of `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true` across all `account` and `account-processing` staging environments (`farnham`, `barnsley`, `jersey`, `oxford`).
*   **3/20/2026, 9:13 AM - 9:14 AM**: Initial configuration of `awsPrivateCAIssuer` to `trusted: true`, `enabled: true`, `used: true` across all `tink-backend-system` staging environments.
*   **3/20/2026, 12:11 PM - 12:12 PM**: A wave of changes where `awsPrivateCAIssuer.used` is specifically set to `false` across all `account-processing` and `tink-backend-system` staging environments (`barnsley`, `farnham`, `jersey`, `oxford`). This is a notable reversal for the `used` flag.
*   **3/20/2026, 12:23 PM - 12:28 PM**: Introduction of GRPC certificate definitions for `tink-backend-system` and `account-processing`, leveraging `aws-private-ca-issuer`.
*   **3/20/2026, 3:12 PM - 3:15 PM**: Multiple re-commits of the `tink-backend-system` deployment manifest, detailing various init containers for secret management (AWS Secret Manager, Vault), certificate generation, and Cassandra migrations.

### Patterns or Recurring Elements:

1.  **AWS Private CA Integration**: There's a strong recurring theme of integrating with AWS Private CA for certificate management. Files like `certificate-cassandra.yaml`, `certificate-grpc-certificate-aws.yaml`, and various `values.yaml` files frequently refer to `awsPrivateCAIssuer`.
2.  **Conditional Configuration**: Many Kubernetes resources and value settings are conditional using `{{- if .Values.someFlag }}`. This indicates a flexible deployment system where features can be enabled/disabled per environment or specific requirements.
3.  **Environment-Specific Overrides**: A clear pattern is the use of `values/<env-name>-staging.yaml` files to override global settings for specific staging environments (Farnham, Barnsley, Jersey, Oxford). These often include unique database hosts, cache hosts, `awsAccountId`, and specific feature flag toggles for each environment.
4.  **Secret Management**: The `deployment.yaml` reveals a sophisticated secret management strategy involving both AWS Secret Manager (`secretman` init container) and Vault (`vaultify` init container) to inject configuration and credentials.
5.  **Certificate Punning**: Many service endpoints in `values.yaml` specify `pinnedCertificates` (e.g., `/internal-ca/ca.crt`, `/tls/ca.crt`), suggesting a strong emphasis on mTLS or secure communication within the microservice architecture.
6.  **`awsPrivateCAIssuer.used` Toggle**: A very specific pattern emerged on 3/20/2026 where `awsPrivateCAIssuer.trusted` and `awsPrivateCAIssuer.enabled` are consistently set to `true` in environment-specific `values` files, but then `awsPrivateCAIssuer.used` is explicitly changed from `true` to `false` across multiple `account-processing` and `tink-backend-system` staging environments. This suggests a decision to prepare for or enable the AWS Private CA for issuance but perhaps temporarily or permanently disable its *usage* in certain contexts.
7.  **Helm Chart Structure**: The file paths clearly indicate a Helm chart structure (`.charts/<service-name>/templates/` and `.charts/<service-name>/values/`), which is a common way to manage Kubernetes applications.
8.  **Repeated Commits**: Several files (e.g., `tink-backend-system/values/farnham-staging.yaml`, `tink-backend-system/templates/deployment.yaml`, `tink-backend-system/templates/certificate-grpc-certificate-aws.yaml`) were committed multiple times in quick succession with identical content, possibly indicating formatting changes, re-saves, or automatic tooling committing.

## 9:14:55 AM
The changes primarily revolve around Kubernetes configurations for the `tink-backend-system` and `account-processing` services, specifically managing certificates, secrets, and environment-specific values.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (3/19/2026, 9:51:34 AM):**
    *   Introduces a Kubernetes Secret named `cassandra-client-certificate-keystore-secret` of type `Opaque`.
    *   This secret stores a placeholder password "notAsecret" and is conditionally enabled based on `.Values.cassandra.privateCA.enabled`.
    *   This is likely part of a setup for managing Cassandra client certificates via a private Certificate Authority.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (3/19/2026, 9:51:46 AM):**
    *   Defines a `cert-manager` Certificate resource for the Cassandra client.
    *   Configures a 90-day duration with 30-day renewal attempts.
    *   Specifies JKS keystore creation with `cassandra-client-certificate-keystore-secret` for the password.
    *   Uses an `AWSPCAClusterIssuer` named `aws-private-ca-issuer` for certificate issuance.
    *   Includes `client auth`, `digital signature`, and `key encipherment` as key usages.
    *   Sets a SPIFFE URI in the SAN extension, dynamically generated using global cluster, environment, namespace, and service values.
    *   This resource is also conditionally enabled by `.Values.cassandra.privateCA.enabled`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (3/19/2026, 9:58:00 AM):**
    *   A comprehensive `values.yaml` file for the `tink-backend-system` chart.
    *   Sets `useVault: true`, `clusterName: "TINK"`, `cassandra.privateCA.enabled: true`.
    *   Defines resources for CPU/memory, HPA settings, nodePort, and various pinned certificates for multiple internal endpoints (e.g., `aggregationController`, `identityAggregatorServiceConfiguration`, `oauth`, `account`, `credentials`, `connectedService`, `aggregationEngine`, `authenticationEngine`, `investments`, `loans`, `enrichmentEventTransmitterService`, `userInternalClient`, `oauthService`, `providersSearchApiClientConfiguration`, `transactionAccess`).
    *   Configures database settings, distributed database for Cassandra, task queue, caching, logging, transaction processing, Redis, notifications, categorization, feature flags, scheduler jobs, persistent volume, Datadog integration, Kafka, and `awsPrivateCAIssuer`.
    *   Notably, `cassandra.privateCA.enabled` is `true`, while `awsPrivateCAIssuer.enabled` is `false` and `used` is `false`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/*.yaml` (3/19/2026, 10:56:11 AM - 3/20/2026, 9:12:08 AM):**
    *   **farnham-staging.yaml:** Configures `maindb.host` and sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}`.
    *   **barnsley-staging.yaml:** Configures `maindb.host` and sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}`.
    *   **jersey-staging.yaml:** Enables `useAccountdb`, `databaseMigrationsEnabled`, `accountDataRepositoryEnabled`, `cassandra.enabled`. Configures `accountdb.host` and various endpoints (`credentials`, `identity`, `accountService`, `authService`, `user`, `providersSearchApi`) with pinned certificates (`/tls/ca.crt`). Sets feature flags like `accountVerificationEnabled`, `accountPartyServiceEnabled`, `enableContinuesAccountDataEndpoints`, `enableBalanceApiEndpoints`. Also sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}`.
    *   **oxford-staging.yaml:** Enables `taskqueue.tlsEnabled`, `cassandra.enabled`. Configures `maindb.host` and various endpoints with pinned certificates. Sets numerous feature flags, including application-specific feature flags (`appIdFeatures`) and health checks. Also sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/*.yaml` (3/20/2026, 9:12:38 AM - 3/20/2026, 12:11:27 PM):**
    *   **farnham-staging.yaml:** Enables `featureFlags.enableAccountEvents: True` and sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}` (later changed to `used: false` at 12:11:08 PM).
    *   **jersey-staging.yaml:** Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}` (later changed to `used: false` at 12:11:27 PM).
    *   **oxford-staging.yaml:** Configures `taskQueue.enabled: true` and several feature flags (`enableAccountEvents`, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, `balanceCheckEnabled`, `enableBalanceCheckWebhooks`, `accountDeduplication`). Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}` (later changed to `used: false` at 12:11:14 PM).
    *   **barnsley-staging.yaml:** Configures `taskQueue.enabled: true` and feature flags (`enableAccountEvents`, `enableBalanceCheckWebhooks`). Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: false}`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/*.yaml` (3/20/2026, 9:13:27 AM - 3/20/2026, 12:12:09 PM):**
    *   **barnsley-staging.yaml:** Sets `clusterName: "BARNSLEY"`, disables `identityAggregatorServiceConfiguration`, enables `userInternalClient`. Configures cache host, transaction access, Redis, categorization (enables `userLearningEnabled`), aggregation policy, and several feature flags. Configures `database.url` and `useAwsSecretManager: true` with `awsAccountId`. Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}` (later changed to `used: false` at 12:11:52 PM).
    *   **farnham-staging.yaml:** Similar to Barnsley, sets `clusterName: "FARNHAM"`, disables `identityAggregatorServiceConfiguration` and `oauth`, enables `userInternalClient` and `oauthService`. Configures cache host, transaction access, Redis, categorization (enables `userLearningEnabled`), aggregation policy, and feature flags. Defines multiple locales. Enables `persistentVolume`. Configures `database.url` and `useAwsSecretManager: true` with `awsAccountId`. Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}` (later changed to `used: false` at 12:11:57 PM). Note: The same file was logged twice with identical content within seconds.
    *   **jersey-staging.yaml:** Sets `clusterName: "JERSEY"`, enables `userInternalClient`. Configures cache host, transaction access, Redis, disables categorization, enables aggregation policy. Sets feature flags and locales. Enables `persistentVolume`. Configures `database.url` and `useAwsSecretManager: true` with `awsAccountId`. Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}`.
    *   **oxford-staging.yaml:** Configures cache host, categorization (enables BFM features), aggregation policy. Sets extensive feature flags, data retention policies, tracing, Datadog logging levels. Enables `investments`, `loans`, `enrichmentEventTransmitterService`, `userInternalClient` endpoints. Configures transaction processing and Redis. Disables several jobs. Configures `database.url` and `useAwsSecretManager: true` with `awsAccountId`. Sets `awsPrivateCAIssuer: {trusted: true, enabled: true, used: true}` (later changed to `used: false` at 12:12:09 PM).

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml` (3/20/2026, 12:23:48 PM and 3/20/2026, 12:28:28 PM):**
    *   Defines a `cert-manager` Certificate resource for AWS Private CA issued gRPC certificates.
    *   Sets duration to 90 days, renewal to 30 days. Uses RSA private key.
    *   Refers to `aws-private-ca-issuer`.
    *   Subject organization is "tink ab", with `server auth` and `client auth` usages.
    *   Includes a list of `dnsNames` for the `tink-backend-system` service.
    *   Conditionally enabled by `.Values.awsPrivateCAIssuer.enabled`.
    *   The content is identical in both timestamps, indicating a re-save or minor unnoticed change.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml` (3/20/2026, 12:28:42 PM):**
    *   Defines a similar `cert-manager` Certificate resource, but for the `account-processing` service.
    *   The secret name is `grpc-certificate-aws` but it's in the `account-processing` namespace.
    *   Includes dynamic `dnsNames` based on `additionalCertNames`, environment, cluster, and service names for `account-processing-service`.
    *   Also conditionally enabled by `.Values.awsPrivateCAIssuer.enabled`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml` (3/20/2026, 3:12:37 PM - 3/20/2026, 3:15:30 PM):**
    *   Defines a Kubernetes Deployment for `tink-backend-system`.
    *   Includes `tink-health` annotations with Prometheus and Datadog queries for error rates and healthy HTTP ratios, with specific monitor IDs for EU/US Datadog clusters.
    *   Conditional `replicas` based on `disabled` and `hpa.enabled` values.
    *   Rolling update strategy.
    *   Datadog APM annotations for environment, version, and service.
    *   `initContainers` for:
        *   `secretman`: fetches AWS secrets (accessToken, serverTokens, idMaskLength/Masking, keyStorePassword, abnAmroInternetBankingAuthorizationToken, applePasswords, googleApiKeys, aggregationControllerAccessToken) into `/secrets`. This is enabled if `useAwsSecretManager` is true.
        *   `secretman-template`: uses `sed` to substitute template strings in `system-server.yaml` with the fetched AWS secrets, creating `system-server.yaml` in `/config-with-secrets`. This is also enabled if `useAwsSecretManager` is true.
        *   `generate-pinned-certificates`: an OpenJDK container running a script to generate pinned certificates, conditional on `awsPrivateCAIssuer.trusted`.
        *   `fetch-credentials`: uses `vaultify` to template configurations with Vault secrets, mounts `/config` (or `/config-with-secrets` if AWS secrets are used) and outputs to `/credentials`. Configures `VAULT_CACERT` based on `awsPrivateCAIssuer.trusted`.
        *   `cassandra-migrations`: runs Cassandra schema migrations, conditional on `runCassandraMigrations`. Uses an OpenJDK image and mounts credentials.
        *   `build-truststore`: builds a Java Keystore (`ca.jks` or `tink-internal-ca.jks`) from either a PKCS12 bundle from `ca-trust-bundle` (if `awsPrivateCAIssuer.trusted`) or a CRT file from `vault-tls`.
        *   `build-keystore`: partial configuration shown, but intended to build a keystore, specifically referencing `grpc-certificate-aws` volume if `awsPrivateCAIssuer.used` is true.
    *   Security context: `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, `fsGroup: 65535`.
    *   Multiple identical logs for this file within seconds, suggesting repeated saves or automated updates.

**Timestamps of Significant Changes:**

*   **March 19, 2026, 9:51 AM:** Initial setup of Cassandra secrets and certificate management.
*   **March 19, 2026, 9:58 AM:** Comprehensive `tink-backend-system` chart `values.yaml` defined, including global settings and numerous service configurations.
*   **March 20, 2026, ~9:12 AM - 9:14 AM:** Initial configuration of environment-specific `values.yaml` files for `account` and `tink-backend-system` charts across "farnham-staging", "barnsley-staging", "jersey-staging", and "oxford-staging" clusters/environments. These consistently enable `awsPrivateCAIssuer` and set it to `trusted: true, enabled: true, used: true`.
*   **March 20, 2026, ~12:11 PM - 12:12 PM:** A pattern emerges across `account-processing` and `tink-backend-system` staging `values.yaml` files (barnsley, farnham, oxford, jersey) where `awsPrivateCAIssuer.used` is explicitly changed from `true` to `false`. This indicates a specific decision or change in how AWS Private CA is utilized or referenced in these environments.
*   **March 20, 2026, 12:23 PM - 12:28 PM:** Introduction and slight duplication of `cert-manager` Certificate definitions for gRPC, specifically `grpc-certificate-aws` for both `tink-backend-system` and `account-processing` services.
*   **March 20, 2026, 3:12 PM - 3:15 PM:** Detailed `tink-backend-system` deployment configuration, outlining various init containers for secret management (AWS Secret Manager, Vault), certificate generation, and database migrations. Repeated identical logs for this file indicate potential automated updates or re-saves without content changes.

**Patterns and Recurring Elements:**

1.  **Kubernetes Chart Structure:** The log entries show a consistent pattern of `tink-backend/.charts/<service>/templates/*.yaml` for Kubernetes resource definitions and `tink-backend/.charts/<service>/values/*.yaml` for environment-specific configurations.
2.  **Certificate Management:** A significant focus on certificate management using `cert-manager.io/v1` Certificates, `AWSPCAClusterIssuer`, and related secrets/keystores. Both Cassandra client certificates and general gRPC service certificates are defined.
3.  **AWS Private CA Integration:** Frequent appearance of `awsPrivateCAIssuer` in `values.yaml` files across different services and environments. It is consistently configured with `trusted: true` and `enabled: true`. There is a notable pattern of setting `awsPrivateCAIssuer.used` from `true` to `false` in several staging `values.yaml` files on March 20th.
4.  **Templating and Secret Management:** The `tink-backend-system/templates/deployment.yaml` heavily utilizes init containers for fetching secrets from AWS Secret Manager (`secretman`) and Vault (`vaultify`), and then templating configurations. It also includes steps for building Java truststores and keystores, often relying on the `awsPrivateCAIssuer.trusted` or `awsPrivateCAIssuer.used` flags.
5.  **Environment-Specific Configurations:** The presence of `values/<env>-staging.yaml` files (e.g., `farnham-staging`, `barnsley-staging`, `jersey-staging`, `oxford-staging`) indicates a robust environment-specific deployment strategy, allowing for distinct database hosts, cache settings, feature flags, and AWS account IDs per staging environment.
6.  **Feature Flags:** Numerous feature flags are defined and enabled/disabled across the `values.yaml` files, allowing fine-grained control over application behavior in different environments. Examples include `enableAccountEvents`, `enableTransactionEvents`, `userLearningEnabled`, `enableBalanceApiEndpoints`, etc.
7.  **Service Endpoints:** Many services within the `tink-backend-system` and `account` charts have their internal endpoints configured with specific URLs and `pinnedCertificates`, indicating a strong emphasis on secure internal communication within the microservices architecture.
8.  **Datadog Integration:** Datadog is used for monitoring, with APM and health checks configured in the deployment annotations and values.
9.  **Java Runtime Configuration:** Init containers that run Java applications (e.g., `cassandra-migrations`, `build-truststore`, `build-keystore`) consistently use OpenJDK 11 and configure JVM options like `user.timezone`, `java.security.egd`, and memory settings (`-XX:InitialRAMPercentage`, etc.).
10. **"notAsecret" Password:** The string "notAsecret" appears in multiple locations for keystore/truststore passwords, indicating placeholder values in templates that might be replaced by actual secrets in production or are for non-sensitive internal uses.

## 10:14:54 AM
The provided log details several changes across Helm charts for a `tink-backend` system, primarily focusing on certificate management, database configurations, and environment-specific settings. The changes span two days: March 19-20, 2026.

### File-Specific Updates:

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (March 19, 2026, 9:51:34 AM)**:
    *   Introduced a Kubernetes `Secret` named `cassandra-client-certificate-keystore-secret` in the `tink-system` namespace.
    *   This secret conditionally creates a `stringData` entry for a `password` with a placeholder value "notAsecret" if `cassandra.privateCA.enabled` is true.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (March 19, 2026, 9:51:46 AM)**:
    *   Defined a `cert-manager.io/v1` `Certificate` resource for Cassandra client certificates, also conditional on `cassandra.privateCA.enabled`.
    *   This certificate, `cassandra-client-certificate`, has a duration of 90 days and renews 30 days prior to expiry.
    *   It's configured to create a JKS keystore with the password from `cassandra-client-certificate-keystore-secret`.
    *   The issuer is an `AWSPCAClusterIssuer`, with subject organization "tink-ab" and specific key usages (client auth, digital signature, key encipherment).
    *   It also includes SPIFFE URI SAN extensions, indicating an identity management integration.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (March 19, 2026, 9:58:00 AM)**:
    *   A comprehensive update to the main `values.yaml` file for `tink-backend-system`.
    *   Key changes include:
        *   Enabling `useVault` and `cassandra.privateCA`.
        *   Setting `clusterName` to "TINK".
        *   Defining Cassandra resource limits, HPA settings, and a long list of pinned certificates for various internal service endpoints (e.g., `aggregationController`, `oauth`, `account`, `credentials`).
        *   Configuring `keystore` paths and passwords.
        *   Details for `database` (maxPoolSize, flyway migrations, SSL mode), `distributedDatabase` (Cassandra seed nodes, TLS), `taskQueue`, `cache`, `elasticsearchCluster`, `logging`, `backoffice`, `transactionAccess`, `transactions` processing, `redisConfiguration`, `snsFlow`, `tracing`, `notifications`, `aggregationApplicationPolicyService`, `categorization`, `service`, `featureFlags` (numerous flags like `enableAggJourneyTrackingEvents`, `useAggregationController`), `scheduler`, `jobs`, and `persistentVolume`.
        *   Introducing `datadog` APM and profiling.
        *   Setting `awsPrivateCAIssuer.enabled` to `false` and `used` to `false`.

*   **Environment-Specific `values.yaml` Files (March 19-20, 2026):**
    These files, located under `.charts/<service>/values/<environment>-staging.yaml` (e.g., `account/values/farnham-staging.yaml`, `tink-backend-system/values/barnsley-staging.yaml`), show a consistent pattern of configuration overrides for different staging environments.

    *   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/farnham-staging.yaml` (March 19, 2026, 10:56:11 AM)**:
        *   Configured `maindb.host` and enabled `awsPrivateCAIssuer` (`trusted: true`, `enabled: true`, `used: true`).

    *   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/barnsley-staging.yaml` (March 20, 2026, 9:11:53 AM)**:
        *   Similar to farnham, configured `maindb.host` and enabled `awsPrivateCAIssuer`.

    *   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/jersey-staging.yaml` (March 20, 2026, 9:11:58 AM)**:
        *   Enabled `useAccountdb`, `databaseMigrationsEnabled`, `accountDataRepositoryEnabled`, `cassandra`.
        *   Configured `accountdb.host` and various endpoint URLs (credentials, identity, authService, user, etc.) with `/tls/ca.crt` pinned certificates.
        *   Numerous `FeatureFlags` enabled (e.g., `accountVerificationEnabled`, `enableBalanceApiEndpoints`).
        *   Enabled `awsPrivateCAIssuer`.

    *   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/oxford-staging.yaml` (March 20, 2026, 9:12:08 AM)**:
        *   Enabled `taskqueue.tlsEnabled`, `cassandra`.
        *   Configured `maindb.host` and various endpoints similar to Jersey, with `/tls/ca.crt` pinned certificates.
        *   Extensive `FeatureFlags` and `appIdFeatures` for specific test accounts.
        *   Enabled `awsPrivateCAIssuer`.

    *   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/farnham-staging.yaml`, `/jersey-staging.yaml`, `/oxford-staging.yaml` (March 20, 2026, between 9:12:38 AM and 9:12:54 AM, and again between 12:11:08 PM and 12:11:27 PM)**:
        *   These files consistently enable `awsPrivateCAIssuer` (`trusted: true`, `enabled: true`).
        *   The `used` flag for `awsPrivateCAIssuer` was initially `true` but was changed to `false` in the later updates on March 20th around noon.
        *   `farnham-staging.yaml` enabled `enableAccountEvents`.
        *   `oxford-staging.yaml` enabled `enableAccountEvents`, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, `balanceCheckEnabled`, `enableBalanceCheckWebhooks`, and configured `accountDeduplication`.
        *   `barnsley-staging.yaml` (March 20, 2026, 12:11:00 PM) also showed `awsPrivateCAIssuer.used: false`.

    *   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/barnsley-staging.yaml`, `/farnham-staging.yaml`, `/jersey-staging.yaml`, `/oxford-staging.yaml` (March 20, 2026, between 9:13:27 AM and 9:14:01 AM, with some re-commits later)**:
        *   Each environment sets its `clusterName`.
        *   Configured specific `cache.host` values for Redis/Memcached.
        *   Database URLs (`url`) are unique to each staging environment.
        *   `useAwsSecretManager` and `awsAccountId` are specified for each.
        *   `awsPrivateCAIssuer` is consistently enabled (`trusted: true`, `enabled: true`, `used: true`) across these backend system values files in the morning.
        *   Similar to `account-processing`, the `awsPrivateCAIssuer.used` flag was changed from `true` to `false` in the later updates on March 20th around noon (e.g., `barnsley-staging.yaml`, `farnham-staging.yaml`, `oxford-staging.yaml` at 12:11:52 PM, 12:11:57 PM, 12:12:09 PM respectively).
        *   Feature flags like `enableAccountEvents` and `enableTransactionEvents` are frequently enabled.
        *   `aggregationApplicationPolicyService` is enabled with `useForkedContext` and `useClientPolicy` set to `true`.
        *   `redisConfiguration` is enabled with `clusteredHost` and `timeout`.
        *   `categorization.userLearningEnabled` is often `true`.
        *   Oxford staging includes more specific logging, data retention policies, and additional `jobs` configurations.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml` (March 20, 2026, 12:23:48 PM and 12:28:28 PM)**:
    *   Defined a `cert-manager.io/v1` `Certificate` resource named `grpc-certificate-aws` in the `tink-system` namespace, conditional on `awsPrivateCAIssuer.enabled`.
    *   Configures a 90-day duration, 30-day renewal, RSA private key, and specifies `AWSPCAClusterIssuer`.
    *   Includes multiple `dnsNames` for the `tink-backend-system` service for both server and client authentication.
    *   The content is identical across both timestamps, suggesting a re-commit or minor metadata update not reflected in the provided content.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml` (March 20, 2026, 12:28:42 PM)**:
    *   Defined a `cert-manager.io/v1` `Certificate` resource named `grpc-certificate-aws` in the `account-processing` namespace, conditional on `awsPrivateCAIssuer.enabled`.
    *   Similar certificate settings to `tink-backend-system`, but with `dnsNames` including `account-processing-service` specific entries and also allowing for `additionalCertNames`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml` (March 20, 2026, 3:12:37 PM, 3:15:22 PM, 3:15:30 PM)**:
    *   Updates to the Kubernetes Deployment for `tink-backend-system`.
    *   Includes `tink-health` annotations for Prometheus and Datadog monitoring (error rates, HTTP healthy ratio). Oxford staging has an additional "Tryhard Success Rate" monitor.
    *   Conditional `replicas` based on `disabled` and `hpa.enabled` values.
    *   Datadog APM annotations are included if `apmEnabled` is true.
    *   Introduces multiple `initContainers` for:
        *   `secretman`: Retrieves AWS secrets from `Secret Manager` if `useAwsSecretManager` is enabled.
        *   `secretman-template`: Populates a config file with AWS static secrets, running shell commands to `sed` template strings.
        *   `generate-pinned-certificates`: Executes a script to generate pinned certificates, conditional on `awsPrivateCAIssuer.trusted`.
        *   `fetch-credentials`: Uses `vaultify` to fetch credentials from Vault, conditional on `awsPrivateCAIssuer.trusted` for `VAULT_CACERT`.
        *   `cassandra-migrations`: Runs Cassandra schema migrations if `runCassandraMigrations` is true.
        *   `build-truststore`: Creates a JKS truststore, with logic varying based on `awsPrivateCAIssuer.trusted` (PKCS12 bundle vs. .crt file).
        *   `build-keystore`: Initiated with `grpc-certificate-aws` volume mount if `awsPrivateCAIssuer.used`.
    *   Security contexts are set to `runAsNonRoot: true`, `readOnlyRootFilesystem: true` for most init containers.
    *   Resource `limits` and `requests` are defined for init containers.
    *   The content across the three timestamps is identical, suggesting re-commits without functional changes.

### Timestamps of Significant Changes:

*   **March 19, 2026, 9:51 AM - 9:58 AM**: Initial setup or update of Cassandra private CA, certificate management, and a major `values.yaml` configuration for `tink-backend-system`. This appears to be a foundational set of changes for enabling/configuring private CA for Cassandra.
*   **March 20, 2026, 9:11 AM - 9:14 AM**: A concentrated block of updates to environment-specific `values.yaml` files across `account` and `tink-backend-system` charts. This phase primarily involves setting database hosts, enabling/disabling various services/features, and configuring `awsPrivateCAIssuer` to `used: true`.
*   **March 20, 2026, 12:11 PM - 12:12 PM**: A rapid series of updates to `account-processing` and `tink-backend-system` environment-specific `values.yaml` files, specifically changing `awsPrivateCAIssuer.used` from `true` to `false`. This indicates a potential rollback or re-evaluation of how AWS Private CA is utilized in these specific configurations.
*   **March 20, 2026, 12:23 PM - 12:28 PM**: Introduction and refinement of GRPC certificate definitions (`certificate-grpc-certificate-aws.yaml` and `certificate-service-certificate-aws.yaml`).
*   **March 20, 2026, 3:12 PM - 3:15 PM**: Major updates to `tink-backend-system` deployment configuration, introducing complex init container logic for secret management, certificate generation, database migrations, and trust/keystore building. The identical content in three closely spaced commits suggests minor non-functional changes or re-application of the same configuration.

### Patterns and Recurring Elements:

*   **AWS Private CA Integration**: A dominant theme is the extensive integration with AWS Private CA, controlled by `awsPrivateCAIssuer.enabled`, `trusted`, and `used` flags. This indicates a shift towards centralized certificate management.
*   **Environment-Specific Overrides**: Many `values.yaml` files are tailored for specific staging environments (Barnsley, Farnham, Jersey, Oxford), showcasing granular control over database hosts, cache configurations, feature flags, and service endpoints per environment.
*   **Secret Management**: The use of `useVault` and `useAwsSecretManager` alongside init containers (`secretman`, `secretman-template`, `fetch-credentials`) demonstrates a robust secret management strategy involving both AWS Secret Manager and Vault.
*   **Certificate Management**: Repeated creation of `cert-manager.io/v1` `Certificate` resources for various services (Cassandra, GRPC) with consistent settings like `duration`, `renewBefore`, `privateKey` algorithm, and `issuerRef` to `AWSPCAClusterIssuer`.
*   **Health and Monitoring**: `tink-backend-system/templates/deployment.yaml` shows integration with Prometheus and Datadog for monitoring application health, error rates, and HTTP response ratios.
*   **Pinned Certificates**: Numerous service endpoints in `values.yaml` files specify `pinnedCertificates` (e.g., `/internal-ca/ca.crt`, `/tls/ca.crt`), indicating a strong emphasis on mTLS or secure communication within the microservices architecture.
*   **"notAsecret"**: The use of "notAsecret" as a placeholder password in `cassandra-secret-keystore.yaml` and `values.yaml`'s `keystore.password` suggests these are to be replaced by actual secrets at deployment time, or are dummy values for templating.
*   **Feature Flags**: A multitude of `featureFlags` are present across `values.yaml` files, allowing for fine-grained control over application behavior and rollout of new functionalities.
*   **Consistency in Init Container Images**: Many init containers across `tink-backend-system/templates/deployment.yaml` use images from `784217881480.dkr.ecr.eu-west-1.amazonaws.com/tink-containers/`.
*   **Change in `awsPrivateCAIssuer.used`**: A notable pattern is the change of `awsPrivateCAIssuer.used` from `true` to `false` in several `account-processing` and `tink-backend-system` environment-specific `values.yaml` files around noon on March 20th. This indicates a coordinated decision or adjustment regarding the actual operational use of the AWS Private CA for specific components, despite it remaining `enabled` and `trusted`.

## 4:15:18 PM
The log details changes across various Helm chart configurations and Kubernetes resource definitions, primarily focusing on certificate management, service configurations, and feature flags within a "tink-backend" project.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml`** (3/19/2026, 9:51:46 AM): This file defines a `cert-manager` Certificate resource for Cassandra client certificates. It configures the certificate with a 90-day duration, 30-day renewal, creates a JKS keystore, and uses an `AWSPCAClusterIssuer`. It also specifies `client auth`, `digital signature`, and `key encipherment` usages, an RSA 2048 private key, and includes SPIFFE URI extensions for identity.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml`** (3/19/2026, 9:58:00 AM): This is a comprehensive configuration file for the `tink-backend-system`. Key configurations include enabling Vault, setting cluster resources and Horizontal Pod Autoscaler (HPA), defining numerous service endpoints with `pinnedCertificates` (many pointing to `/internal-ca/ca.crt`), and configuring database, distributed database (Cassandra), task queue, caching, transaction processing, and Redis settings. It also includes extensive feature flags, datadog integration settings, and initially sets `awsPrivateCAIssuer: trusted: false, enabled: false, used: false`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/farnham-staging.yaml`** (3/19/2026, 10:56:11 AM): Configures the main database host for the Farnham staging environment and explicitly enables and trusts the `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/barnsley-staging.yaml`** (3/20/2026, 9:11:53 AM): Sets the main database host for the Barnsley staging environment and also enables and trusts the `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/jersey-staging.yaml`** (3/20/2026, 9:11:58 AM): Contains detailed settings for the `account` service in Jersey staging. It enables account database usage, migrations, Cassandra, and configures various service endpoints (credentials, identity, statistics, risk insights, etc.) with `pinnedCertificates` (using `/tls/ca.crt`). Numerous account-related feature flags are enabled, and `awsPrivateCAIssuer` is set to be trusted, enabled, and used.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/oxford-staging.yaml`** (3/20/2026, 9:12:08 AM): Provides extensive configurations for the `account` service in Oxford staging, including `taskqueue` TLS, Cassandra, main database host, and numerous endpoints similar to Jersey staging, but also explicitly enabling statistics, risk insights, income check, and holder name check. It features detailed `appIdFeatures`, health checks, and enables and trusts `awsPrivateCAIssuer`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/farnham-staging.yaml`** (3/20/2026, 9:12:38 AM and 3/20/2026, 12:11:08 PM): Enables the `enableAccountEvents` feature flag. The `awsPrivateCAIssuer.used` setting changed from `true` to `false` at the later timestamp.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/jersey-staging.yaml`** (3/20/2026, 9:12:47 AM and 3/20/2026, 12:11:27 PM): The `awsPrivateCAIssuer.used` setting changed from `true` to `false` at the later timestamp.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/oxford-staging.yaml`** (3/20/2026, 9:12:54 AM and 3/20/2026, 12:11:14 PM): Configures `taskQueue` endpoints, and enables several account and balance event-related feature flags (e.g., `enableAccountEvents`, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, `balanceCheckEnabled`, `enableBalanceCheckWebhooks`). The `awsPrivateCAIssuer.used` setting changed from `true` to `false` at the later timestamp.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/barnsley-staging.yaml`** (3/20/2026, 9:13:27 AM, 3/20/2026, 10:13:02 AM, and 3/20/2026, 12:11:52 PM): Defines specific settings for the Barnsley staging cluster, including `clusterName`, disabled `identityAggregatorServiceConfiguration`, enabled `userInternalClient`, `taskQueue` limits, cache host, `transactionAccess` for writing, Redis configuration, categorization settings, aggregation policy service, and various feature flags for events. It sets the database URL, enables AWS Secret Manager, specifies `awsAccountId`, and initially sets `awsPrivateCAIssuer: trusted: true, enabled: true, used: true`. The `awsPrivateCAIssuer.used` setting changed from `true` to `false` between the 10:13:02 AM and 12:11:52 PM entries.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/farnham-staging.yaml`** (3/20/2026, 9:13:35 AM, 3/20/2026, 9:13:41 AM, and 3/20/2026, 12:11:57 PM): Configures the Farnham staging cluster with disabled `identityAggregatorServiceConfiguration` and `oauth` endpoints, enabled `userInternalClient` and `oauthService`. It also defines cache, transaction access, categorization, aggregation policy, feature flags for events, locales, persistent volume, Redis configuration, database URL, AWS Secret Manager, `awsAccountId`, and initially sets `awsPrivateCAIssuer: trusted: true, enabled: true, used: true`. The `awsPrivateCAIssuer.used` setting changed from `true` to `false` between the 9:13:41 AM and 12:11:57 PM entries.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/jersey-staging.yaml`** (3/20/2026, 9:13:56 AM): Configures the Jersey staging cluster, enabling `userInternalClient`, defining cache, transaction access, and aggregation policy settings. Notably, categorization is disabled. It includes event-related feature flags, locales, persistent volume, Redis configuration, database URL, AWS Secret Manager, `awsAccountId`, and sets `awsPrivateCAIssuer: trusted: true, enabled: true, used: true`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/oxford-staging.yaml`** (3/20/2026, 9:14:01 AM and 3/20/2026, 12:12:09 PM): Provides extensive configuration for the Oxford staging cluster, including advanced categorization with BFM (Behavioral Financial Management) settings and specific `bfmAppIds`. It details data retention policies (daily, default retention days, app/user retention with different periods), tracing, Datadog profiling, and detailed logging levels. Various endpoints (investments, loans, enrichment) are enabled. Transaction processing includes `reconcileUsingProviderExternalId`, various provider/app ID allow lists, and Redis configuration. Several jobs for account/transaction deletion are defined as disabled. It also sets the database URL, enables AWS Secret Manager, specifies `awsAccountId`, and initially sets `awsPrivateCAIssuer: trusted: true, enabled: true, used: true`. The `awsPrivateCAIssuer.used` setting changed from `true` to `false` at the later timestamp.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml`** (3/20/2026, 12:23:48 PM, 3/20/2026, 12:28:28 PM, and 3/23/2026, 4:07:31 PM): Defines a `cert-manager` Certificate resource for GRPC communication within the `tink-system` namespace. It uses `awsPrivateCAIssuer`, sets a 90-day duration, 30-day renewal, RSA PKCS8 private key, and specifies `server auth` and `client auth` usages with a predefined list of DNS names for the `tink-backend-system` service. The content remained identical across these timestamps.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml`** (3/20/2026, 12:28:42 PM): Defines a `cert-manager` Certificate resource for GRPC communication in the `account-processing` namespace. Similar to the system GRPC certificate, it uses `awsPrivateCAIssuer`, but includes dynamic DNS names based on `.Values.additionalCertNames` and service-specific names for `account-processing-service`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml`** (3/20/2026, 3:12:37 PM, 3/20/2026, 3:15:22 PM, and 3/20/2026, 3:15:30 PM): This Kubernetes Deployment manifest for `tink-backend-system` includes `tink-health` annotations with Prometheus and Datadog monitoring queries. It defines several `initContainers` responsible for: fetching secrets from AWS Secret Manager, templating config with these secrets, generating pinned certificates (conditional on `awsPrivateCAIssuer.trusted`), fetching credentials using Vault, running Cassandra migrations (conditional on `runCassandraMigrations`), and building Java truststores and keystores. The `build-truststore` and `build-keystore` steps show conditional logic based on `awsPrivateCAIssuer.trusted` and `awsPrivateCAIssuer.used`. The content remained identical across these timestamps.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/certificate-service-certificate-aws.yaml`** (3/23/2026, 4:06:52 PM): Defines a `cert-manager` Certificate resource for GRPC communication in the `account` namespace. This is similar to the `account-processing` GRPC certificate, using `awsPrivateCAIssuer` and templated DNS names for the `account` service.

**Patterns and Recurring Elements:**

*   **AWS Private CA Integration:** A prominent and recurring theme is the integration and configuration of AWS Private CA (`AWSPCAClusterIssuer`). `cert-manager` is consistently used to provision GRPC and Cassandra client certificates, leveraging AWS Private CA. Many `values.yaml` files for specific staging environments (`farnham`, `barnsley`, `jersey`, `oxford`) explicitly set `awsPrivateCAIssuer: trusted: true, enabled: true, used: true` initially, indicating a widespread adoption of this certificate management strategy.
*   **Staging Environment Customization:** There are distinct `values.yaml` files for different staging clusters (Farnham, Barnsley, Jersey, Oxford) under both `account` and `tink-backend-system` services. These files customize database hosts, cache settings, `awsAccountId`, and enable or disable various service endpoints and feature flags unique to each environment.
*   **`awsPrivateCAIssuer.used` Flag Changes:** A notable pattern on 3/20/2026 (between 9:12 AM and 12:12 PM) is the consistent change of the `awsPrivateCAIssuer.used` flag from `true` to `false` across multiple `account-processing` and `tink-backend-system` staging `values.yaml` files (Farnham, Jersey, Oxford, Barnsley). This suggests a bulk modification or a specific operational adjustment related to how AWS Private CA certificates are utilized in these staging environments.
*   **Certificate Management with `cert-manager`:** All certificate definitions (`certificate-cassandra.yaml`, `certificate-grpc-certificate-aws.yaml`, `certificate-service-certificate-aws.yaml` for `account` and `account-processing`) consistently use `cert-manager.io/v1` Certificate resources, defining similar parameters like duration, renewal, private key algorithm, usages (`server auth`, `client auth`), and issuer references.
*   **Init Containers in Deployments:** The `tink-backend-system/deployment.yaml` heavily utilizes Kubernetes `initContainers` for a sequential setup process, including secret fetching (AWS Secret Manager, Vault), configuration templating, and Java keystore/truststore creation, often with conditional logic based on `awsPrivateCAIssuer` flags.
*   **Pinned Certificates:** Many service endpoint configurations across `values.yaml` files explicitly define `pinnedCertificates`, indicating a strong emphasis on mTLS or certificate pinning for secure inter-service communication. These often point to `/internal-ca/ca.crt` or `/tls/ca.crt`.
*   **Feature Flag Management:** Numerous boolean feature flags (`enabled`, `use...Service`, `enable...Events`) are configured across the `values.yaml` files, allowing fine-grained control over application behavior in different environments.
*   **Timestamp Recurrence without Content Change:** Several files, particularly the `deployment.yaml` and `certificate-grpc-certificate-aws.yaml`, show multiple entries with identical content but different timestamps on the same day. This might indicate commits or deployments that did not alter the file's content but were logged.

## 9:15:21 PM
The log details configuration and infrastructure changes across the Tink backend system, primarily focusing on Helm charts for Kubernetes deployments in various staging environments. The changes span from March 19 to March 23, 2026.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (3/19/2026, 9:51:34 AM)**: A Kubernetes Secret named `cassandra-client-certificate-keystore-secret` was introduced. This secret, conditionally enabled, is designed to store the password (`notAsecret`) for a Cassandra client certificate keystore.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (3/19/2026, 9:51:46 AM)**: A `cert-manager` `Certificate` resource for Cassandra client certificates was added. It configures a 90-day validity, 30-day renewal, uses an `AWSPCAClusterIssuer`, and includes SPIFFE URI SAN for identity, also conditional on Cassandra private CA being enabled.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (3/19/2026, 9:58:00 AM)**: A comprehensive base `values` file for the `tink-backend-system` was updated. Key elements include enabling Vault, initial settings for Cassandra private CA (`enabled: true`) and AWS Private CA Issuer (`trusted: false, enabled: false, used: false`), resource allocations, HPA settings, certificate pinning for numerous internal service endpoints, database SSL configuration (`sslMode=VERIFY_IDENTITY`), Redis configuration, extensive feature flags, Datadog integration, and various scheduled jobs.
*   **Staging `values.yaml` files (`/Users/mmuruts/projects/tink/tink-backend/.charts/{account,account-processing,tink-backend-system}/values/*-staging.yaml`)**:
    *   **Initial updates (3/19/2026, 10:56:11 AM - 3/20/2026, 9:14:01 AM)**: Multiple staging environments (`farnham`, `barnsley`, `jersey`, `oxford`) received specific configurations. A consistent pattern was the explicit enablement and trust of the AWS Private CA Issuer (`awsPrivateCAIssuer: trusted: true, enabled: true, used: true`). Each environment defined its unique database hosts, cache hosts, and a wide array of feature flags. Notable differences included `jersey-staging` disabling general categorization, and `oxford-staging` having detailed BFM (Budgeting and Financial Management) settings, advanced data retention policies, and specific logging levels.
    *   **Subsequent updates (3/20/2026, 12:11:00 PM - 12:12:09 PM)**: Across all reviewed staging `values.yaml` files for `account`, `account-processing`, and `tink-backend-system` charts, the `awsPrivateCAIssuer.used` property was consistently changed from `true` to `false`. This indicates a potential shift in how the AWS Private CA is *utilized* by these services, even while it remains enabled and trusted.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml` (3/20/2026, 12:23:48 PM)**: A `cert-manager` `Certificate` for gRPC communication within the `tink-system` namespace was introduced. It also uses the `AWSPCAClusterIssuer` and defines multiple DNS names for the backend system, conditioned on `awsPrivateCAIssuer.enabled`. This file saw identical content updates at 3/20/2026, 12:28:28 PM and 3/23/2026, 4:07:31 PM.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml` (3/20/2026, 12:28:42 PM)**: A similar `cert-manager` `Certificate` for gRPC services was added for the `account-processing` namespace, supporting environment-specific DNS names and additional custom certificate names.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml` (3/20/2026, 3:12:37 PM)**: This significant update to the main `tink-backend-system` deployment manifest introduced a complex `initContainers` pipeline. This pipeline manages secrets via AWS Secret Manager (`secretman` and `secretman-template`) and Vault (`fetch-credentials`), generates pinned certificates, runs Cassandra schema migrations, and builds Java truststores and keystores for TLS. The deployment also includes detailed `tink-health` annotations for Prometheus and Datadog monitoring. This file saw identical content updates at 3/20/2026, 3:15:22 PM and 3/20/2026, 3:15:30 PM.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/certificate-service-certificate-aws.yaml` (3/23/2026, 4:06:52 PM)**: A `cert-manager` `Certificate` for gRPC services was added specifically for the `account` namespace, mirroring the pattern seen in `tink-backend-system` and `account-processing`.

**Timestamps of Significant Changes:**

*   **March 19, 2026, around 9:51 AM**: Initial setup of Cassandra client certificate management and the foundational `tink-backend-system` base configuration.
*   **March 20, 2026, between 9:11 AM and 9:14 AM**: A concentrated series of updates across multiple staging environments (`farnham`, `barnsley`, `jersey`, `oxford`) for both `account` and `tink-backend-system` charts, consistently enabling `awsPrivateCAIssuer` and tailoring environment-specific settings.
*   **March 20, 2026, between 12:11 PM and 12:12 PM**: A notable change across various staging `values.yaml` files, where `awsPrivateCAIssuer.used` was uniformly switched from `true` to `false`, indicating a policy adjustment in its application.
*   **March 20, 2026, between 12:23 PM and 12:28 PM**: Introduction of `cert-manager` `Certificate` resources for gRPC services in `tink-backend-system` and `account-processing`, standardizing TLS for internal communication.
*   **March 20, 2026, around 3:12 PM**: The `tink-backend-system` deployment underwent a major refactor to its `initContainers`, implementing a robust multi-source secret and certificate management pipeline.

**Patterns and Recurring Elements:**

*   **Conditional Configuration (`{{- if .Values... }}`):** A pervasive use of Helm templating to enable or disable features and resources based on environment-specific values, demonstrating a flexible deployment strategy.
*   **AWS Private CA Integration:** A strong and evolving pattern of leveraging `AWSPCAClusterIssuer` for TLS certificates across different services (Cassandra, gRPC). The consistent initial enablement (`trusted: true, enabled: true, used: true`) in staging environments, followed by the uniform change of `used: false`, suggests a refined approach to how this issuer's capabilities are exposed or consumed by applications.
*   **Environment-Specific Overrides:** Each staging environment (`farnham`, `barnsley`, `jersey`, `oxford`) maintains distinct configurations for critical components like database URLs, cache hosts, and feature flags, highlighting tailored deployments.
*   **Secure Inter-Service Communication:** Widespread configuration of internal service endpoints with `pinnedCertificates` (e.g., `/internal-ca/ca.crt` or `/tls/ca.crt`) underscores a commitment to secure communication channels within the backend system.
*   **Multi-Source Secret Management:** The sophisticated `initContainers` in `tink-backend-system`'s deployment, integrating AWS Secret Manager and Vault, illustrates a mature and robust strategy for handling both static and dynamic secrets.
*   **Standardized Monitoring:** Consistent application of `tink-health` annotations for Prometheus and Datadog across deployments indicates a unified approach to observability and health checks.
*   **Repeated Log Entries:** Several log entries for the same file at slightly different timestamps with identical content suggest either automated saves without material changes or very rapid, minor, unlogged modifications.