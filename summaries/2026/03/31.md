# Activity Summary for 3/31/2026

## 9:26:12 AM
The `deployment.yaml` for `transaction-service-reconciliation-worker` was updated consistently at `3/27/2026, 11:24:34 AM` and `3/27/2026, 11:26:02 AM`. This Kubernetes Helm template defines a deployment with a `transaction-service-reconciliation-worker` container and optionally a `transaction-service-tags-updater` container. It incorporates conditional features such as Datadog APM integration (with specific tags and environment variables like `DD_SERVICE_MAPPING`), Vault integration for secret fetching (`fetch-credentials` initContainer) and lease renewal (`renew-leases` sidecar container), and Go runtime debugging (`GODEBUG`). Key security settings include `runAsNonRoot: true` and `readOnlyRootFilesystem: true` for containers. Environment variables for the main application and tags updater are sourced from Helm values and Kubernetes secrets, including `FEATURE_TOGGLE_API_KEY`, `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, and `CASSANDRA_V2_PASSWORD`. Images are pulled from `784217881480.dkr.ecr.eu-west-1.amazonaws.com` with versions derived from `tink-versions/tink-transaction-service.txt` or `.Values.Version`. The deployment also configures volume mounts for TLS certificates, a CA trust bundle, configuration, and credentials, alongside pod anti-affinity rules for non-local clusters.

The `hpa.yaml` for `transaction-service` was updated at `3/27/2026, 11:58:17 AM`. This file defines a Horizontal Pod Autoscaler (`HPA`) that dynamically scales the `transaction-service`. The `scaleTargetRef` is conditionally set to either a Kubernetes `Rollout` or `Deployment` based on the `.Values.canaryDeployments.phase`. It configures `minReplicas` (default 1) and `maxReplicas` (default 3) and sets a `scaleDown` stabilization window of 600 seconds. Scaling metrics are based on two external Datadog metrics: `cpu-usage` (targeting an `averageValue` from `.Values.podTargetCPU`, derived from `kubernetes.cpu.usage.total` nanocores) and `memory-usage` (targeting a `value` of `250Mi`, derived from aggregated container memory usage of `transaction-service` and `renew-leases` containers).

**Patterns and Recurring Elements:**
*   **Helm Templating:** Extensive use of Helm's `{{- if .Values... }}` for conditional logic and configuration, making the deployments highly configurable.
*   **Datadog Integration:** A strong pattern of integrating with Datadog for APM, monitoring, and horizontal pod autoscaling metrics. This includes specific Datadog tags, environment variables, and `DatadogMetric` custom resources.
*   **Vault Integration:** Conditional integration with Vault via the `vaultify` container image (`784217881480.dkr.ecr.eu-west-1.amazonaws.com/tink-containers/vaultify:0.4.2`) for secure secret management and lease renewal.
*   **Centralized Image Registry:** All container images specified (`tink-containers/vaultify`, `transaction-service`) are sourced from the same ECR registry: `784217881480.dkr.ecr.eu-west-1.amazonaws.com`.
*   **Secret Management:** Multiple environment variables derive their values from Kubernetes secrets (`secretKeyRef`), often marked as `optional: true`.
*   **Standard Security Practices:** Consistent application of security contexts such as `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, and `readOnlyRootFilesystem: true`.
*   **Health Probes:** Liveness and readiness probes are consistently defined for application containers.