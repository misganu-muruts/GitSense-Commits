# Activity Summary for 3/31/2026

## 9:26:12 AM
The `deployment.yaml` for `transaction-service-reconciliation-worker` was updated consistently at `3/27/2026, 11:24:34 AM` and `3/27/2026, 11:26:02 AM`. This Kubernetes Helm template defines a deployment with a `transaction-service-reconciliation-worker` container and optionally a `transaction-service-tags-updater` container. It incorporates conditional features such as Datadog APM integration (with specific tags and environment variables like `DD_SERVICE_MAPPING`), Vault integration for secret fetching (`fetch-credentials` initContainer) and lease renewal (`renew-leases` sidecar container), and Go runtime debugging (`GODEBUG`). Key security settings include `runAsNonRoot: true` and `readOnlyRootFilesystem: true` for containers. Environment variables for the main application and tags updater are sourced from Helm values and Kubernetes secrets, including `FEATURE_TOGGLE_API_KEY`, `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, and `CASSANDRA_V2_PASSWORD`. Images are pulled from `784217881480.dkr.ecr.eu-west-1.amazonaws.com` with versions derived from `tink-versions/tink-transaction-service.txt` or `.Values.Version`. The deployment also configures volume mounts for TLS certificates, a CA trust bundle, configuration, and credentials, alongside pod anti-affinity rules for non-local clusters.

The `hpa.yaml` for `transaction-service` was updated at `3/27/2026, 11:58:17 AM`. This file defines a Horizontal Pod Autoscaler (`HPA`) that dynamically scales the `transaction-service`. The `scaleTargetRef` is conditionally set to either a Kubernetes `Rollout` or `Deployment` based on the `.Values.canaryDeployments.phase`. It configures `minReplicas` (default 1) and `maxReplicas` (default 3) and sets a `scaleDown` stabilization window of 600 seconds. Scaling metrics are based on two external Datadog metrics: `cpu-usage` (targeting an `averageValue` from `.Values.podTargetCPU`, derived from `kubernetes.cpu.usage.total` nanocores) and `memory-usage` (targeting a `value` of `250Mi`, derived from aggregated container memory usage of `transaction-service` and `renew-leases` containers).

**Patterns and Recurring Elements:**
*   **Helm Templating:** Extensive use of Helm's `{{- if .Values... }}` for conditional logic and configuration, making the deployments highly configurable.
*   **Datadog Integration:** A strong pattern of integrating with Datadog for APM, monitoring, and horizontal pod autoscaling metrics. This includes specific Datadog tags, environment variables, and `DatadogMetric` custom resources.
*   **Vault Integration:** Conditional integration with Vault via the `vaultify` container image (`784217881480.dkr.ecr.eu-west-1.amazonaws.com/tink-containers/vaultify:0.4.2`) for secure secret management and lease renewal.
*   **Centralized Image Registry:** All container images specified (`tink-containers/vaultify`, `transaction-service`) are sourced from the same ECR registry: `784217881480.dkr.ecr.eu-west-1.amazonaws.com`.
*   **Secret Management:** Multiple environment variables derive their values from Kubernetes secrets (`secretKeyRef`), often marked as `optional: true`.
*   **Standard Security Practices:** Consistent application of security contexts such as `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, and `readOnlyRootFilesystem: true`.
*   **Health Probes:** Liveness and readiness probes are consistently defined for application containers.

## 11:26:18 AM
The provided code change log outlines updates to Kubernetes Helm charts for a `transaction-service`, specifically focusing on a reconciliation worker, Horizontal Pod Autoscaling (HPA), and environment-specific configurations.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
    *   **Timestamp: 3/27/2026, 11:24:34 AM & 3/27/2026, 11:26:02 AM:** These two entries represent the initial, stable configuration of the `transaction-service-reconciliation-worker` Kubernetes Deployment. It defines a `Deployment` with standard metadata, labels, and strategies (RollingUpdate). Key aspects include:
        *   Conditional Datadog APM integration (labels and annotations).
        *   Security context settings (`runAsNonRoot`, `readOnlyRootFilesystem`).
        *   Optional Vault integration via `vaultify` init and renew-leases containers for credential management, including arguments for fetching and renewing secrets.
        *   A `transaction-service-reconciliation-worker` container configured with resource requests/limits, image from AWS ECR, command arguments (`recon-worker`), and various environment variables sourced from `.Values` or Kubernetes secrets (e.g., `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`).
        *   Datadog APM environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`).
        *   Conditional inclusion of a `transaction-service-tags-updater` container, acting as a `tags-consumer`, if `tagsUpdater.enabled` is true.
        *   Standard liveness and readiness probes.
        *   Volume definitions for certificates (conditional on `awsPrivateCAIssuer.used`), CA trust bundle, configuration, and credentials (if using Vault).
        *   Pod anti-affinity rules for non-local clusters.
    *   **Timestamp: 3/31/2026, 11:24:58 AM:** This update introduced redundant conditional logic within the `volumeMounts` section for the `transaction-service-reconciliation-worker` container and the `volumes` definition block at the end of the file. Specifically, an `{{- if .Values.awsPrivateCAIssuer.used }}` block was nested unnecessarily inside an `{{- else }}` branch, effectively repeating the same conditional check for certificate volume mounting and secret naming.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`**
    *   **Timestamp: 3/27/2026, 11:58:17 AM:** This file defines a Horizontal Pod Autoscaler (HPA) for the main `transaction-service`.
        *   It can target either a Kubernetes `Deployment` or an Argo Rollout (`Rollout`) resource, based on the `canaryDeployments.phase` value.
        *   Configures `minReplicas` (default 1) and `maxReplicas` (default 3, but overridden to 8 by `oxford-staging-product001.yaml`).
        *   Includes a `scaleDown` stabilization window of 600 seconds (10 minutes) and a policy to scale down by 1 pod every 60 seconds.
        *   Defines two `External` metrics for scaling, both sourced from Datadog:
            *   CPU usage: targets `datadogmetric@transaction:cpu-usage` with an `AverageValue` based on `.Values.podTargetCPU`. The underlying DatadogMetric queries `sum:kubernetes.cpu.usage.total` converted from nanocores.
            *   Memory usage: targets `datadogmetric@transaction:memory-usage` with a fixed `Value` of 250Mi. The underlying DatadogMetric queries the average `container.memory.usage` for both `transaction-service` and `renew-leases` containers.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml`**
    *   **Timestamp: 3/31/2026, 11:16:49 AM:** This file provides specific configuration values for the `oxford-staging-product001` environment.
        *   Sets `logLevel` to "debug", enables `MultiElasticSearchClusters`, `snsEnabled`, and defines a `kafkaTopicPrefix`.
        *   Configures two Elasticsearch clusters (`common` and `nordea`) with distinct hosts, synchronization behavior (`wait_for`), index years, `totalHitsAsInt`, and shard counts (`nshards: 10`). The `nordea` cluster also specifies multiple `appIds`.
        *   Enables `dataretention` and `reconciliation` features, both with `dryRun: false`.
        *   Configures SQS batching and concurrency, and enables `userSvc` and `accountSvc` with their respective addresses and CA paths.
        *   Enables Redis caching with a TTL of "2m" and lists two Redis host endpoints.
        *   Enables `featureToggles`.
        *   Sets resource requests and limits for pods: `podMemoryRequest: "2Gi"`, `podCpuRequest: "1.25"`, `podTargetCPU: "0.6"`.
        *   Specifies replica counts: `reconWorkerMinReplicas: 2`, `reconWorkerMaxReplicas: 3`, `podMinReplicas: 2`, `podMaxReplicas: 8`.
        *   Sets `canaryDeployments.phase` to "COMPLETE", indicating the HPA targets a Rollout, which is scaled up, while the underlying deployment scales to zero.
        *   Specifies an `awsAccountId` ("232388878475") and enables `awsPrivateCAIssuer.used`.

**Patterns and Recurring Elements:**

*   **Helm Templating for Kubernetes Deployments:** Heavy reliance on Helm's templating features (`{{- if .Values.prop }}`) for conditional configuration and dynamic value injection from `.Values` and `.Files`.
*   **Datadog Integration:** Extensive use of Datadog for APM (tags, annotations, specific environment variables) and for metrics-driven Horizontal Pod Autoscaling. This indicates a strong observability and auto-scaling strategy.
*   **Vault for Secrets Management:** A consistent pattern for securely injecting and renewing credentials using `vaultify` init containers, demonstrating a commitment to secure credential handling.
*   **Security Best Practices:** Regular application of `readOnlyRootFilesystem: true` and `runAsNonRoot` security contexts for containers, and specific user/group IDs (65535).
*   **Modular Application Structure:** The `transaction-service` binary is reused for different worker roles (`recon-worker`, `tags-consumer`) within the same deployment, configured by specific arguments.
*   **AWS ECR for Container Images:** All container images are sourced from a specific AWS ECR registry (`784217881480.dkr.ecr.eu-west-1.amazonaws.com`).
*   **Service Communication:** Configuration for internal service communication (e.g., `userSvc`, `accountSvc`) and external integrations (Elasticsearch, Kafka, SQS, Cassandra, Redis).
*   **Reconciliation and Data Retention:** Features like `dataretention` and `reconciliation` are explicitly enabled, suggesting a focus on data consistency and lifecycle management.
*   **Canary Deployments:** The HPA configuration, combined with the `canaryDeployments.phase` in `values.yaml`, indicates a strategy for phased rollouts using Argo Rollouts.

## 1:26:18 PM
The changes log details updates across a "transaction-service" project, primarily concerning Kubernetes deployments, autoscaling, and environment-specific configurations, with a strong emphasis on Datadog and Vault integration.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
    *   **Initial state (3/27/2026, 11:24:34 AM):** This Kubernetes Helm template defines the deployment for a `transaction-service-reconciliation-worker`. Key features include:
        *   Conditional deployment of a `tags-updater` container and Vault-related `initContainers` and `renew-leases` sidecar container.
        *   Extensive Datadog APM integration for service tagging, compute stats, and peer tags aggregation.
        *   Robust security contexts, including `runAsNonRoot`, `readOnlyRootFilesystem`, and specific user/group IDs.
        *   Resource requests and limits set for all containers.
        *   Dynamic image versioning, falling back to "latest".
        *   Configuration of environment variables for feature toggles, HMAC keys, access tokens, and Cassandra V2 passwords, often fetched from Kubernetes secrets.
        *   Volume mounts for configuration, credentials, CA bundles, and certificates, with conditional logic for AWS Private CA issuer.
        *   Affinity rules configured for non-local clusters.
    *   **Minor change (3/27/2026, 11:26:02 AM):** The content appears functionally identical to the previous entry, suggesting a re-save or minor, non-functional alteration.
    *   **Structural template change (3/31/2026, 11:24:58 AM):** An update introduced redundant conditional logic within the `volumeMounts` and `volumes` sections. Specifically, the `{{- if .Values.awsPrivateCAIssuer.used }}` block was duplicated inside its `{{- else }}` branch, making the certificate volume mount selection unnecessarily nested. While this change is structural, it likely doesn't alter the rendered output's functionality given consistent `.Values.awsPrivateCAIssuer.used` evaluation.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml` (3/27/2026, 11:58:17 AM)**
    *   This file defines a Kubernetes Horizontal Pod Autoscaler (HPA) for the main `transaction-service`.
    *   The HPA dynamically targets either a `Rollout` or a `Deployment` based on `canaryDeployments.phase` values.
    *   It specifies `minReplicas` and `maxReplicas` configurable via Helm values, with defaults of 1 and 3 respectively.
    *   Scaling down behavior is configured with a 10-minute stabilization window and a policy to remove 1 pod every 60 seconds.
    *   Scaling metrics are driven by external Datadog metrics: `cpu-usage` (targeting average CPU) and `memory-usage` (targeting a value of 250Mi, summing memory from both `transaction-service` and `renew-leases` containers).
    *   Two `DatadogMetric` Custom Resources are defined to query specific Datadog metrics (`kubernetes.cpu.usage.total` and `container.memory.usage`) filtered by cluster name and deployment.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml` (3/31/2026, 11:16:49 AM)**
    *   This is a Helm values file for the `oxford-staging-product001` environment, providing specific configuration for the `transaction-service`.
    *   It sets `logLevel` to "debug" and enables `enableMultiElasticSearchClusters` and `snsEnabled`.
    *   A `kafkaTopicPrefix` specific to this environment is defined.
    *   Detailed Elasticsearch configurations are provided for both common and "nordea" clusters, including hosts, sync behavior, index years, and shard counts.
    *   Cassandra is configured with multiple hosts and TLS enabled.
    *   `dataretention` and `reconciliation` are enabled with `dryRun: false`.
    *   SQS, User Service, Account Service, and Redis caching (with specific hosts and TTL) are configured and enabled.
    *   `featureToggles` are enabled.
    *   Resource and replica settings for the reconciliation worker are specified (`reconWorkerMinReplicas: 2`, `reconWorkerMaxReplicas: 3`).
    *   General pod resource requests (`podMemoryRequest: "2Gi"`, `podCpuRequest: "1.25"`) and HPA target CPU (`podTargetCPU: "0.6"`) are set.
    *   The `canaryDeployments.phase` is set to `COMPLETE`.
    *   `awsAccountId` is "232388878475". `serviceLevels` are disabled, and `awsPrivateCAIssuer.used` is enabled.

**Patterns and Recurring Elements:**

*   **Datadog Observability:** A consistent pattern of integrating Datadog for APM, metrics, and autoscaling is evident across the `deployment.yaml` and `hpa.yaml` files, indicating a standardized monitoring strategy.
*   **Kubernetes and Helm for Deployment:** All changes are within Helm charts and Kubernetes manifests, highlighting a mature container orchestration strategy.
*   **Secrets Management with Vault:** The `deployment.yaml` for the reconciliation worker explicitly uses `vaultify` for fetching and renewing secrets, demonstrating a focus on secure credentials handling.
*   **Conditional Configuration:** Helm's templating capabilities are extensively used with `{{- if .Values.<param> }}` blocks to conditionally enable or disable features (e.g., Datadog APM, Vault, Tags Updater, AWS Private CA).
*   **Resource Management:** CPU and memory `requests` and `limits` are consistently defined for containers, ensuring predictable resource allocation.
*   **Security Best Practices:** The use of `runAsNonRoot: true`, `readOnlyRootFilesystem: true`, and specific `runAsUser`/`runAsGroup` in security contexts is a recurring pattern for enhanced security.
*   **External Service Integration:** The `values.yaml` demonstrates extensive configuration for various external services like Elasticsearch, Cassandra, SQS, Redis, and internal services (User Service, Account Service), showing a microservices architecture.
*   **Environment-Specific Tuning:** The `values/oxford-staging-product001.yaml` file exemplifies the use of Helm values for environment-specific overrides, such as log levels, Kafka prefixes, and Elasticsearch hosts.
*   **Reconciliation Worker Component:** The reconciliation worker is a prominent component, with its own deployment configuration and dedicated replica and resource settings.

## 3:26:44 PM
The log captures changes across a Kubernetes Helm chart configuration for a `transaction-service`.

**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
This Kubernetes Deployment manifest for the `transaction-service-reconciliation-worker` underwent a notable change on **3/31/2026, 11:24:58 AM**.
*   The entries from **3/27/2026, 11:24:34 AM** and **3/27/2026, 11:26:02 AM** were identical, establishing the base configuration. This configuration defines a `Deployment` for a reconciliation worker, optionally including a `tags-updater` container and a `vaultify` `renew-leases` container. It extensively uses Helm templating for dynamic configuration, including Datadog APM integration, security contexts (e.g., `runAsNonRoot: true`, `readOnlyRootFilesystem: true`), resource requests/limits, and volume mounts for certificates, CA bundles, and configuration. Secret references are handled via `secretKeyRef` for various tokens and passwords (e.g., Unleash, Page Token, System Access, Cassandra).
*   The update on **3/31/2026, 11:24:58 AM** introduced a redundant conditional block within the `volumeMounts` section of the `transaction-service-reconciliation-worker` container and also in the main `volumes` definition. Specifically, the logic for mounting the `/tls` path and defining the corresponding secret now contains a nested `if-else` statement that re-evaluates the `awsPrivateCAIssuer.used` condition, leading to duplicated and potentially erroneous template output for these volume definitions.

**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`**
Recorded on **3/27/2026, 11:58:17 AM**, this file defines a Horizontal Pod Autoscaler (`HPA`) for the `transaction-service`.
*   The HPA is configured to dynamically target either a `Rollout` or a `Deployment` based on `canaryDeployments.phase`.
*   It sets flexible `minReplicas` (default 1) and `maxReplicas` (default 3) and specifies a scale-down stabilization window of 600 seconds.
*   Scaling metrics are external Datadog metrics: `cpu-usage` (targeting an average CPU value) and `memory-usage` (targeting a value of 250Mi).
*   Accompanying `DatadogMetric` resources are defined to query `kubernetes.cpu.usage.total` and `container.memory.usage` for the `transaction-service` deployment, with the memory metric also including the `renew-leases` container's usage.

**File: `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/oxford-staging-product001.yaml`**
This file, logged on **3/31/2026, 11:16:49 AM**, provides environment-specific configuration values for the `oxford-staging-product001` cluster.
*   Key configurations include setting `logLevel` to "debug", enabling `snsEnabled`, and defining a `kafkaTopicPrefix`.
*   It specifies detailed configurations for two Elasticsearch clusters (`common` and `nordea`), including their respective host URLs, `syncbehaviour`, index year ranges, and `nshards`. The `nordea` configuration also includes specific `appIds`.
*   Cassandra hosts are listed with TLS enabled.
*   `dataretention` and `reconciliation` features are enabled with `dryRun: false`.
*   SQS, User Service, Account Service, and Redis are configured with their respective settings, including Redis hosts and TTL.
*   `featureToggles` are enabled.
*   Resource requests and limits are defined for various pods, including `reconWorkerMinReplicas`, `podMemoryRequest`, `podCpuRequest`, and `podTargetCPU`.
*   The `canaryDeployments.phase` is set to "COMPLETE".
*   An `awsAccountId` is specified, `serviceLevels` are disabled, and `awsPrivateCAIssuer` is enabled.

**Patterns and Recurring Elements:**
*   **Extensive Helm Templating:** All files demonstrate heavy reliance on Helm's templating capabilities (`{{- if .Values.someProperty }}`) to create flexible and environment-aware Kubernetes manifests.
*   **Datadog Integration:** Datadog is a prominent feature, used for APM tagging in deployments and as an external metrics source for Horizontal Pod Autoscaling.
*   **Security Best Practices:** Containers consistently implement `readOnlyRootFilesystem: true` and `runAsNonRoot: true` with specific user/group IDs (65535). Secret management is centralized via Vault (`vaultify` containers) and Kubernetes `secretKeyRef`.
*   **AWS Infrastructure:** The use of AWS ECR for container images (`784217881480.dkr.ecr.eu-west-1.amazonaws.com`), SQS, and references to AWS-specific CA issuers indicates a strong AWS cloud presence.
*   **Microservice Architecture:** The configuration mentions several services (transaction-service, reconciliation-worker, tags-updater, userSvc, accountSvc), indicating a microservice-based application.
*   **Environment-Specific Configuration:** The `values` file clearly shows how different settings are tailored for a specific staging environment, highlighting the use of Helm for managing configuration variations.