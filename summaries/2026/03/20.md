# Activity Summary for 3/20/2026

## 12:51:25 AM
The following changes were observed in the codebase:

**File: `/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml`**
*   **Timestamp:** 3/19/2026, 9:51:46 AM
*   **Key Update:** This file introduces a `cert-manager.io/v1` Certificate resource, `cassandra-client-certificate`, for managing Cassandra client certificates within the `tink-system` namespace. It's designed to be conditionally enabled by `cassandra.privateCA.enabled`. The certificate has a 90-day duration with renewal attempted 30 days prior to expiry. It configures JKS keystore creation, with the password retrieved from `cassandra-client-certificate-keystore-secret`. The certificate is issued by an AWS Private CA Cluster Issuer, `aws-private-ca-issuer`, for the `tink-ab` organization, supporting client authentication, digital signature, and key encipherment. It specifies an RSA 2048-bit private key with an 'Always' rotation policy and includes a dynamically generated SPIFFE URI for identity in the SAN extension.

## 3:08:37 AM
The file `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml`, last modified on `3/19/2026, 12:28:41 PM`, defines a Kubernetes ConfigMap template for a `transaction-service`. This ConfigMap dynamically configures various aspects of the service based on Helm values.

Key updates and configurations include:

*   **Service Dependencies:** Configuration for `user-svc` and `account-svc` enablement, addresses, and CA certificate paths, defaulting to internal cluster DNS.
*   **Data Retention:** Parameters for enabling data retention, batch size, worker count, and a dry-run flag.
*   **Elasticsearch (ES) Setup:** Extensive configuration for common Elasticsearch clusters, including verbose logging, retry settings, host details, sniffing, health checks, sync behavior, refresh intervals, index year ranges, shards, and `appids`. It also supports conditional configuration for multiple ES clusters, specifically for "nordea" and "seb", which mirror the common ES settings.
*   **Messaging and Integrations:** Settings for SNS enablement and topic names, and default `provider-markets` (`GB,UK`).
*   **Secret Management:** Introduction of `secret-manager-enabled` and specific AWS Secrets Manager paths for `page-token-hmac-secret-path` and `system-access-token-secret-path`, which are dynamically constructed using `{{ $cluster }}-{{ $environment }}` and noted with `GENAI` comments.
*   **Cassandra Database:** Detailed configuration for both `cassandra-v2` (enabled, hosts, keyspace, port, username, a secret name following the `{{ $cluster }}-{{ $environment }}/transaction-service/TransactionsCassandraPassword` pattern, TLS, CA path, and transaction store year ranges) and an older `cassandra` version (keyspace, port, TLS, CA path, username/password from config or Vault, and hosts).
*   **Main Database:** Configuration for `main-db-cert` and `main-db-address`, with the option to fetch credentials from Vault if `useVault` is enabled.
*   **Queueing:** A configurable `queue-name` defaulting to `data-retention-worker-queue-{{ $cluster }}-{{ $environment }}.fifo`.
*   **Kafka Listeners:** Configuration for `user-deletion-listener` and `account-deletion-listener`, including Kafka hosts (defaulting to internal Tink network), topics, DLQ topics, consumer groups, and message size limits. Kafka TLS is explicitly enabled with a CA path.
*   **Redis Cache:** Settings for `redis-hosts`, enablement, TTL, and minimum response transactions number.

**Patterns and Recurring Elements:**

*   **Extensive Use of Default Values:** Almost all parameters leverage `| default` to provide fallback values, ensuring robustness even if specific values are not provided.
*   **Conditional Configuration:** Many sections, especially for Elasticsearch, Cassandra credentials, version information, and Kafka listeners, are enclosed in `{{ if }}` blocks, allowing for flexible feature toggling and environment-specific adjustments.
*   **Environment and Cluster Parameterization:** The `{{ $environment }}` and `{{ $cluster }}` variables are frequently used to construct dynamic hostnames (e.g., for Cassandra, Main DB, Kafka), queue names, and secret paths, indicating a highly adaptable deployment strategy across different environments.
*   **Internal Network Defaults:** Several services, including Cassandra (both versions), Main DB, and Kafka, default to hostnames ending with `internal.{{ $environment }}.{{ $cluster }}.tink.network`, suggesting a consistent internal networking setup within the Tink infrastructure.
*   **CA Trust Bundle Consistency:** The path `/ca-trust-bundle/bundle.pem` is consistently used for various services (User, Account, Cassandra, Kafka) requiring CA certificates for TLS, promoting a standardized trust mechanism.
*   **Secrets Management Strategy:** There's a clear pattern of supporting either direct configuration of sensitive data (like Cassandra username/password) or fetching them from a Vault, with specific Vault paths for different credentials.

## 4:08:37 AM
The `config.yaml` file, updated on 3/19/2026, 12:28:41 PM, defines a Kubernetes ConfigMap for the `transaction-service`. This ConfigMap is heavily templated using Helm values, drawing configuration from `.Values.config`, `.Values.Environment`, and `.Values.Cluster` to support dynamic deployments across different environments and clusters. It includes a label indicating its use for canary deployments.

Key configurations defined include:

*   **Core Service Settings:** Log level, enablement and addresses for user and account services, and parameters for data retention (batch size, workers, dry run).
*   **Elasticsearch (ES) Clusters:** A `es-config` block defines settings for multiple Elasticsearch clusters. A "common" cluster is always configured, detailing hostnames, sniffing, health checks, refresh intervals, index year ranges, and shard numbers. Optionally, "nordea" and "seb" specific ES clusters can be enabled, sharing a similar configuration structure, contingent on `enableMultiElasticSearchClusters` being set.
*   **Messaging and Markets:** SNS enablement, transaction mutation topic names, and configurable provider markets (defaulting to "GB,UK").
*   **Secret Management Integration:** The configuration includes flags for `secret-manager-enabled` and uses dynamic paths for secrets from AWS Secrets Manager based on the current cluster and environment. It also shows conditional integration with Vault for database credentials (Cassandra and main database), falling back to direct value retrieval if Vault is not used.
*   **Database Connectivity:** Detailed configurations for both an original Cassandra instance and a newer Cassandra V2 setup, including hosts, keyspaces, ports, TLS settings, CA paths, and secret names for credentials. Main database connection details, certificate paths, and address are also specified.
*   **Queueing:** Configuration for a queue name, defaulting to a pattern that includes the cluster and environment.
*   **Server Parameters:** Settings for maximum concurrency and HTTPS listening.
*   **Kafka Listeners:** Configuration for Kafka-based user and account deletion listeners, including TLS settings, CA paths, hostnames (dynamically constructed), topic names, and consumer group IDs.
*   **Redis Cache:** Redis hostnames, enablement, time-to-live (TTL), and minimum response transaction numbers.
*   **Metadata:** Conditional inclusion of build date, commit, and run ID information.

**Recurring Patterns:**

*   **Extensive Helm Templating:** The file makes widespread use of Helm's `{{ .Values.key | default "default_value" }}` pattern to provide flexible configuration with sensible defaults.
*   **Dynamic Hostname and Path Generation:** The `{{ $environment }}` and `{{ $cluster }}` variables are consistently used to construct hostnames for internal services (e.g., databases, Kafka) and paths for secrets, facilitating multi-environment and multi-cluster deployments.
*   **Conditional Configuration Blocks:** Many sections are conditionally rendered based on other configuration values or feature flags (e.g., enabling specific Elasticsearch clusters, Vault integration, data retention, or Kafka listeners).
*   **CA Trust Bundle:** The path `/ca-trust-bundle/bundle.pem` is repeatedly referenced for CA certificates across multiple service integrations requiring TLS, such as user service, account service, Cassandra, and Kafka.

## 6:51:33 AM
The code changes primarily focus on certificate management, secure inter-service communication, and extensive configuration for a `tink-backend-system`.

**File-Specific Updates:**

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml (Timestamp: 3/19/2026, 9:51:34 AM)**
    This file defines a Kubernetes `Secret` named `cassandra-client-certificate-keystore-secret` for the `tink-system` namespace. It's conditionally created based on `.Values.cassandra.privateCA.enabled` and includes a `password` field with the placeholder value "notAsecret". This secret is intended to hold the keystore password for Cassandra client certificates.

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml (Timestamp: 3/19/2026, 9:51:46 AM)**
    This template defines a `cert-manager` `Certificate` resource for Cassandra client authentication, also conditional on `.Values.cassandra.privateCA.enabled`. It creates a certificate named `cassandra-client-certificate` in the `tink-system` namespace, configured for a 90-day duration with 30-day renewal. It specifies JKS keystore creation, references the `cassandra-client-certificate-keystore-secret` for the password, and uses an `AWSPCAClusterIssuer` for issuance. Key usages include `client auth`, `digital signature`, and `key encipherment`. A critical feature is the inclusion of a dynamic SPIFFE identity URI in the Subject Alternative Name (SAN) extension, constructed from global Helm values.

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml (Timestamp: 3/19/2026, 9:58:00 AM)**
    This large configuration file for the `tink-backend-system` enables `cassandra.privateCA` and sets `useVault: true`. It defines a placeholder `keystore.password` as "notAsecret". The file extensively configures numerous internal service endpoints (e.g., `aggregationController`, `oauth`, `credentials`, `authenticationEngine`, `transactions`), specifying their `url` and consistently pinning certificates to `/internal-ca/ca.crt`. It also sets up database connection details, `runFlywayMigrations: true`, and `sslMode=VERIFY_IDENTITY` for SSL. Distributed database settings for Cassandra include `tlsEnabled: true` and `pinnedCertificates`. Many feature flags are set, such as `enableAggJourneyTrackingEvents: true`, `useAggregationController: true`, and `categorization: enabled: true`. Datadog APM and profiling are enabled. Kafka and `awsPrivateCAIssuer` are explicitly disabled in this general configuration.

*   **/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/farnham-staging.yaml (Timestamp: 3/19/2026, 10:56:11 AM)**
    This is an environment-specific `values` file for the `account` service in the `farnham-staging` environment. It configures the `maindb.host` to a specific AWS RDS endpoint. Notably, it explicitly enables and trusts the `awsPrivateCAIssuer` (`trusted: true, enabled: true, used: true`), which overrides the general disabling of this issuer in the `tink-backend-system/values.yaml` for this specific environment.

**Patterns and Recurring Elements:**

*   **Certificate Management and Private CA:** There's a strong emphasis on securing inter-service communication using certificates. The Cassandra setup leverages a private CA for client certificates, integrated with `cert-manager` and potentially AWS Private CA.
*   **Consistent `pinnedCertificates`:** Almost all internal service endpoints configured in `tink-backend-system/values.yaml` utilize `pinnedCertificates: - /internal-ca/ca.crt`, indicating a reliance on a centralized internal Certificate Authority for trust within the system.
*   **Placeholder Passwords:** The recurring use of `"notAsecret"` for keystore passwords in both template files and `values.yaml` suggests these are default or placeholder values within Helm charts, designed to be replaced by a secure secret management solution in production.
*   **Conditional Deployment:** Helm's `if` statements are used to conditionally deploy resources (e.g., Cassandra secret and certificate) based on feature flags like `cassandra.privateCA.enabled`.
*   **SPIFFE Identity:** The `certificate-cassandra.yaml` explicitly configures SPIFFE identity in SAN URIs, reflecting a modern approach to workload identity management.
*   **Extensive Microservice Configuration:** The `tink-backend-system/values.yaml` file shows a highly interconnected system with numerous internal services, each meticulously configured with URLs and security settings.
*   **Environment-Specific Overrides:** The `farnham-staging.yaml` demonstrates how general configurations can be overridden for specific environments, particularly regarding the enablement and trust of the AWS Private CA issuer.

The timestamps, all occurring on March 19, 2026, within approximately an hour, suggest a concentrated effort to configure or refine the system's security, service connectivity, and certificate infrastructure, with a subsequent environment-specific adjustment.

## 8:08:37 AM
The file `/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml` was updated on `3/19/2026, 12:28:41 PM`.

This log details changes to a Helm chart template that defines a Kubernetes `ConfigMap` for a `transaction-service`. The primary purpose of this update is to centralize and manage various service configurations, often pulling values from `.Values.config` and setting default fallbacks.

**File-Specific Updates:**

*   **Extensive Configuration**: The `ConfigMap` `transaction-service-configuration-{{ .Values.Version }}` defines numerous service parameters within `transaction-service-server.yaml`.
*   **Service Connectivity**: Configurations for `user-svc` and `account-svc` are present, including `enabled` flags, addresses (e.g., `dns:///user2-headless.user.svc.cluster.local:9443`), and CA certificate paths (`/ca-trust-bundle/bundle.pem`).
*   **Data Retention**: Settings for `data-retention-enabled`, `data-retention-batch-size`, `data-retention-workers`, and `dry-run` are introduced.
*   **Elasticsearch Configuration**: The `es-config` section is significantly expanded to support multiple Elasticsearch clusters. It now includes `common`, `nordea`, and `seb` specific configurations, conditionally rendered based on `$config.enableMultiElasticSearchClusters`. Each cluster defines hosts, sniffing, health check, sync behavior, refresh interval, index year ranges, shards, and `appids`.
*   **Secret Management Integration**:
    *   A `secret-manager-enabled` flag is added.
    *   Specific AWS Secrets Manager paths (`page-token-hmac-secret-path`, `system-access-token-secret-path`) are introduced, dynamically constructed using `{{ $cluster }}-{{ $environment }}/transaction-service/...`. This section is explicitly marked with `START GENAI` and `END GENAI` comments.
    *   Vault integration (`.Values.useVault`) is used for fetching `cassandra-username`/`password` and `main-db-user`/`password` when applicable.
*   **Cassandra V2**: New configurations for `enable-cassandra-v2`, `cassandra-v2-hosts`, `cassandra-v2-keyspace`, `cassandra-v2-port`, `cassandra-v2-username`, `cassandra-v2-secret-name` (also constructed with `{{ $cluster }}-{{ $environment }}/transaction-service/...`), `cassandra-v2-use-tls`, `cassandra-v2-ca-path`, and transaction store year ranges are added.
*   **Kafka Listeners**: Configurations for `userDeletionListener` and `accountDeletionListener` are introduced, including `kafka-use-tls`, `cert-path`, `kafka-hosts`, specific topic names (prefixed with `$config.kafkaTopicPrefix`), and consumer group IDs.
*   **Redis Caching**: Settings for `redis-hosts`, `redis-enabled`, `redis-ttl`, and `redis-min-response-transactions-number` are included.
*   **General Service Parameters**: Updates also include `sns-enabled`, `transaction-mutation-topic-name`, `provider-markets`, and optional `build-date`, `commit`, and `run-id` values.

**Patterns and Recurring Elements:**

*   **Helm Templating**: The code heavily uses Helm templating syntax (`{{- ... }}`) for defining variables, setting default values (e.g., `| default "info"`), and implementing conditional logic (`{{ if ... }}`).
*   **Environment and Cluster Specificity**: Many configurations, particularly hostnames and secret paths (e.g., `bigdbt-0.internal.{{ $environment }}.{{ $cluster }}.tink.network`, `{{ $cluster }}-{{ $environment }}/transaction-service/HMacToken`), leverage the `{{ $environment }}` and `{{ $cluster }}` variables for dynamic deployment across different environments.
*   **CA Certificate Path**: The path `/ca-trust-bundle/bundle.pem` is consistently used for CA certificates in several service configurations (user-svc, account-svc, cassandra-v2, kafka).
*   **Default Values**: A strong pattern of providing default values for most parameters is evident, ensuring a fallback if values are not explicitly defined in the Helm `.Values`.
*   **Service Naming Conventions**: Internal service addresses often follow a pattern like `dns:///service-headless.namespace.svc.cluster.local:port`.