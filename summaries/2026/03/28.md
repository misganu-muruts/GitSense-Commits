# Activity Summary for 3/28/2026

## 8:17:40 AM
The logs show changes across two distinct Kubernetes Helm chart template files within the `transaction-service` project.

**File-specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
    *   **Timestamp:** Two entries at `3/27/2026, 11:24:34 AM` and `3/27/2026, 11:26:02 AM`. The content of both entries is identical, suggesting a re-save or a non-functional change within a short timeframe.
    *   This file defines a Kubernetes Deployment for the `transaction-service-reconciliation-worker`.
    *   It includes a main `transaction-service-reconciliation-worker` container, and conditionally, a `transaction-service-tags-updater` container and a `renew-leases` container.
    *   **Datadog Integration:** Extensive Datadog APM configuration is present, including service tags, environment variables for APM computation, and an annotation for team tagging (`ad.datadoghq.com/tags: '{"team": "transactions"}'`).
    *   **Vault Integration:** If `.Values.useVault` is enabled, an `initContainer` named `fetch-credentials` (using `vaultify:0.4.2`) is configured to retrieve configuration and secrets from Vault, and a `renew-leases` container is added to manage Vault lease renewals.
    *   **Configuration & Secrets:** The worker's configuration (`reconciliation-config.yaml`) is mounted either from a `configMap` or from the `credentials` volume (if Vault is used). Several environment variables fetch values from Kubernetes Secrets, such as `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`, and `FEATURE_TOGGLE_API_KEY`.
    *   **Security Contexts:** Pods and containers are configured with `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, `fsGroup: 65535`, and `readOnlyRootFilesystem: true` for enhanced security.
    *   **Liveness/Readiness Probes:** Standard HTTP GET probes are defined for both the main worker and the tags updater.
    *   **Affinity:** Pod anti-affinity is configured to spread pods across different Kubernetes nodes and zones, unless the cluster is "local".
    *   **GODEBUG:** Conditional `GODEBUG` environment variable setup is included.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`**
    *   **Timestamp:** `3/27/2026, 11:58:17 AM`.
    *   This file defines a Horizontal Pod Autoscaler (HPA) for the `transaction-service` deployment.
    *   **Dynamic Target:** The HPA can target either a standard Kubernetes `Deployment` or an Argo CD `Rollout` based on the `.Values.canaryDeployments.phase`, supporting canary deployment strategies.
    *   **Replicas:** Configures `minReplicas` (default 1) and `maxReplicas` (default 3).
    *   **Scale-down Behavior:** A `scaleDown` stabilization window of 600 seconds (10 minutes) is set, with a policy to reduce 1 pod every 60 seconds.
    *   **Datadog Metrics:** The HPA scales based on external Datadog metrics:
        *   `cpu-usage`: Targets an `AverageValue` defined by `.Values.podTargetCPU`.
        *   `memory-usage`: Targets a `Value` of `250Mi`.
    *   **DatadogMetric Definitions:** Includes two `DatadogMetric` custom resource definitions that specify the exact Datadog queries for CPU (`sum:kubernetes.cpu.usage.total`) and memory (`avg:container.memory.usage`) for the `transaction-service` deployment and its `renew-leases` sidecar.

**Patterns and Recurring Elements:**

*   **Helm Templating:** Both files heavily utilize Helm templating (`{{ .Values... }}`) to dynamically configure Kubernetes resources, allowing for flexible deployments based on chart values.
*   **Datadog Ecosystem Integration:** A strong pattern of integrating with Datadog is evident for both APM/tracing (via labels and environment variables in the deployment) and horizontal scaling (via `DatadogMetric` CRDs and external HPA metrics).
*   **Vault Integration:** Conditional logic for Vault (`.Values.useVault`) for secure secret management and automatic lease renewal is a common feature across the service's components, using the `tink-containers/vaultify` image.
*   **Standardized Security Practices:** Consistent application of security contexts such as `runAsNonRoot`, `readOnlyRootFilesystem`, and specifying user/group IDs indicates a focus on minimizing attack surface.
*   **Container Registry:** All custom container images referenced (e.g., `transaction-service`, `vaultify`) are pulled from `784217881480.dkr.ecr.eu-west-1.amazonaws.com`, indicating a centralized image repository.
*   **Health Checks:** Liveness and readiness probes are consistently defined for service health monitoring.

## 9:17:42 AM
The provided log details changes primarily within Kubernetes Helm chart templates for a `transaction-service` project.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`** (Timestamp: 3/27/2026, 11:24:34 AM and 3/27/2026, 11:26:02 AM):
    This file defines a Kubernetes Deployment for a `transaction-service-reconciliation-worker`.
    *   It includes comprehensive configuration for the worker pod, featuring conditional logic based on Helm values for enabling features like reconciliation, Datadog APM, Vault integration, a tags updater, AWS Private CA, and Go debugging.
    *   The deployment specifies resource requests and limits, security contexts (`runAsNonRoot`, `readOnlyRootFilesystem`), liveness and readiness probes.
    *   **Vault Integration:** An `initContainer` named `fetch-credentials` uses `vaultify` to retrieve configuration and secrets if `.Values.useVault` is true. A `renew-leases` sidecar container is also included under this condition to manage Vault lease renewals.
    *   **Datadog APM:** Extensive Datadog labels and annotations are applied to the deployment and pods if `.Values.datadog.apmEnabled` is true, facilitating monitoring. Datadog-specific environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`) are configured.
    *   **Containers:**
        *   The primary `transaction-service-reconciliation-worker` container runs the `recon-worker` command.
        *   An optional `transaction-service-tags-updater` container runs the `tags-consumer` command if `.Values.tagsUpdater.enabled` is true.
    *   **Secret Management:** Environment variables for `FEATURE_TOGGLE_API_KEY`, `PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, and `CASSANDRA_V2_PASSWORD` are pulled from Kubernetes secrets.
    *   **Volume Mounts:** Configuration and credentials are mounted, with conditional logic for Vault-provided credentials versus standard ConfigMap mounts. Certificate bundles (for TLS) are also mounted.
    *   **Affinity:** Pod anti-affinity is configured for non-local clusters to ensure worker pods are spread across different nodes and availability zones.
    *   **Identical Content:** Notably, the code for this file is identical across both timestamps (3/27/2026, 11:24:34 AM and 3/27/2026, 11:26:02 AM), indicating no functional change occurred between these two log entries.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/hpa.yaml`** (Timestamp: 3/27/2026, 11:58:17 AM):
    This file defines a Kubernetes Horizontal Pod Autoscaler (HPA) for the main `transaction-service`.
    *   **Scaling Target:** The HPA targets either a Kubernetes `Rollout` resource (if `canaryDeployments.phase` is "PARTIAL" or "COMPLETE") or a standard `Deployment`, both named `transaction-service`. This indicates support for progressive delivery (e.g., canary deployments).
    *   **Replicas:** Configures minimum (`.Values.podMinReplicas`, default 1) and maximum (`.Values.podMaxReplicas`, default 3) replicas.
    *   **Scaling Behavior:** Includes a scale-down stabilization window of 600 seconds and a policy to scale down by 1 pod every 60 seconds.
    *   **Metrics:** The HPA uses external Datadog metrics for autoscaling:
        *   **CPU Usage:** Targets an average CPU usage value defined by `.Values.podTargetCPU`. The associated `DatadogMetric` queries `sum:kubernetes.cpu.usage.total` for the `transaction-service` deployment.
        *   **Memory Usage:** Targets a fixed memory value of `250Mi`. The associated `DatadogMetric` queries the average memory usage for both the `transaction-service` container and the `renew-leases` container (likely the Vault lease renewal sidecar).

**Timestamps of Significant Changes:**

The timestamps indicate three distinct operations related to Kubernetes resource definitions within a relatively short period on March 27, 2026:
*   **11:24:34 AM and 11:26:02 AM:** The `transaction-service-reconciliation-worker` deployment definition was recorded twice consecutively without modification, suggesting a commit or save event rather than a code change.
*   **11:58:17 AM:** A new or updated Horizontal Pod Autoscaler definition for the main `transaction-service` was recorded, approximately 30 minutes after the last `deployment.yaml` entry.

**Patterns or Recurring Elements in the Content:**

*   **Kubernetes and Helm Templates:** All changes involve Kubernetes resource definitions written as Helm chart templates, heavily relying on `.Values` for dynamic configuration and conditional logic (`{{- if .Values.someProperty }}`).
*   **Datadog Integration:** Datadog is a pervasive component, used for application performance monitoring (APM) through labels/annotations and for driving Horizontal Pod Autoscaling via custom `DatadogMetric` resources.
*   **Vault Integration:** Secret management through Vault is a recurring pattern, implemented with `vaultify` init and sidecar containers to fetch and renew credentials.
*   **Image Management:** Container images are consistently sourced from `784217881480.dkr.ecr.eu-west-1.amazonaws.com`, with versions dynamically resolved from `.Files.Get "tink-versions/*.txt"` or `.Values.Version`.
*   **Security Best Practices:** There's a consistent application of security contexts such as `runAsNonRoot: true`, `runAsUser: 65535`, `runAsGroup: 65535`, `fsGroup: 65535`, and `readOnlyRootFilesystem: true` across containers, promoting secure execution environments.
*   **Secret Injection:** Environment variables often retrieve sensitive data (API keys, tokens, passwords) using `valueFrom: secretKeyRef`, indicating a standard approach for injecting secrets.
*   **Health Checks:** Liveness and readiness probes are consistently defined for HTTP endpoints, ensuring service reliability and proper rollout behavior.
*   **Modular Microservices:** The presence of a `reconciliation-worker` and a `tags-updater` alongside a main `transaction-service` (implied by the HPA) suggests a microservice architecture.

## 10:17:40 AM
The `deployment.yaml` file for `transaction-service-reconciliation-worker` was updated at `3/27/2026, 11:24:34 AM`. This Kubernetes Helm template defines a deployment for a reconciliation worker, including comprehensive Datadog APM integration (labels, annotations, environment variables), robust security contexts like `runAsNonRoot` and `readOnlyRootFilesystem`, and resource management. It features conditional Vault integration via an `initContainer` (`fetch-credentials`) and a `renew-leases` sidecar for secret management. The main container, `transaction-service-reconciliation-worker`, executes `recon-worker` and incorporates various environment variables for feature toggles and database credentials, often sourced from Kubernetes secrets. An optional `transaction-service-tags-updater` container, running as `tags-consumer` and on a different port (8181), is also defined if enabled. Both containers use images from `784217881480.dkr.ecr.eu-west-1.amazonaws.com/transaction-service`, with the version determined from a file or Helm values. The manifest also configures liveness and readiness probes, volume mounts for certificates, CA bundles, configuration, and credentials, along with pod anti-affinity rules for non-local clusters. A subsequent log entry for the same file at `3/27/2026, 11:26:02 AM` shows identical content, indicating no functional changes were introduced in that specific commit/save.

The `hpa.yaml` file for the main `transaction-service` was updated at `3/27/2026, 11:58:17 AM`. This HorizontalPodAutoscaler (HPA) manifest uses `autoscaling/v2` and is configured to scale the `transaction-service` based on a configurable `minReplicas` and `maxReplicas`. It intelligently targets either a `Rollout` (for canary deployments) or a standard `Deployment` based on the `.Values.canaryDeployments.phase`. The HPA includes a `scaleDown` stabilization window of 600 seconds (10 minutes) and a policy to reduce 1 pod every 60 seconds. Scaling metrics are defined as external Datadog metrics: `cpu-usage` targets an average CPU value from `.Values.podTargetCPU`, and `memory-usage` targets a fixed `250Mi`. The file also defines the `DatadogMetric` custom resources for both `cpu-usage` and `memory-usage`, specifying their respective Datadog queries.

**Recurring elements and patterns:**
*   Extensive use of Helm templating (`{{- if .Values.someProperty -}}`, `{{ .Values.someValue }}`) is evident across both files for dynamic configuration and conditional feature enablement.
*   A strong emphasis on observability is present, with detailed Datadog integration (`tags.datadoghq.com` labels, `DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`, and `DatadogMetric` resources) configured in both the deployment and HPA.
*   Security best practices are consistently applied, including `runAsNonRoot`, `readOnlyRootFilesystem` in container security contexts, and referencing Kubernetes secrets for sensitive environment variables.
*   Common Kubernetes deployment patterns such as resource requests and limits, liveness and readiness probes, and volume mounts for configuration and credentials are utilized.
*   The ECR image repository `784217881480.dkr.ecr.eu-west-1.amazonaws.com` is a recurring source for container images.
*   Conditional logic is frequently employed for enabling features like Vault integration, AWS Private CA, and Datadog APM, making the configurations highly flexible.