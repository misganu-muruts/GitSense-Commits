# Activity Summary for 3/25/2026

## 8:58:18 AM
The logged changes span from March 19-23, 2026, primarily focusing on Kubernetes configurations for the `tink-backend-system`, `account`, and `account-processing` services, with a strong emphasis on certificate management, AWS Private CA integration, and environment-specific overrides.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml`** (3/19/2026, 9:51:46 AM): This template configures a `cert-manager` `Certificate` resource for Cassandra client certificates. It enables JKS keystore creation, references an AWS Private CA issuer, and defines specific subject organizations (`tink-ab`), client authentication key usages, and a SPIFFE URI for identity in SAN extension, conditional on `cassandra.privateCA.enabled`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml`** (3/19/2026, 9:58:00 AM): This global `values` file sets up core configurations including:
    *   Enabling `useVault` and `cassandra.privateCA`.
    *   Defining resource limits, HPA settings, and pinned certificates.
    *   Extensive endpoint configurations for various internal services (e.g., aggregation controller, identity aggregator, OAuth, account, credentials), many pointing to `/internal-ca/ca.crt` for TLS.
    *   Database (Postgres and Cassandra) settings, including SSL/TLS.
    *   Redis, cache, logging, and Datadog APM/profiling configurations.
    *   Numerous feature flags (e.g., `enableAggJourneyTrackingEvents`, `useIdentityAggregatorService`).
    *   Notably, `awsPrivateCAIssuer` is initially set to `trusted: false, enabled: false, used: false` in this global file.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/{farnham,barnsley,jersey,oxford}-staging.yaml`** (3/19/2026, 10:56:11 AM - 3/20/2026, 9:12:08 AM): These staging environment-specific `values` files for the `account` service consistently enable and trust the AWS Private CA Issuer (`awsPrivateCAIssuer: trusted: true, enabled: true, used: true`). They also configure specific database hosts, various service endpoints with pinned certificates (often to `/tls/ca.crt`), and several feature flags related to account data, verification, and integrations. The `jersey-staging.yaml` and `oxford-staging.yaml` are particularly detailed, outlining numerous feature flags, endpoint URLs, and in Oxford's case, specific `appIdFeatures` and data retention policies.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/{farnham,jersey,oxford}-staging.yaml`** (3/20/2026, 9:12:38 AM - 9:12:54 AM): These initial entries for `account-processing` staging environments also enable and trust the AWS Private CA Issuer (`awsPrivateCAIssuer: trusted: true, enabled: true, used: true`) and define various feature flags like `enableAccountEvents` and `enableBalanceCheckWebhooks`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/{barnsley,farnham,jersey,oxford}-staging.yaml`** (3/20/2026, 9:13:27 AM - 12:12:09 PM): These files, with several identical repeated entries across timestamps, define the `tink-backend-system` configurations for specific staging clusters. Key aspects include:
    *   Setting `clusterName`.
    *   Configuring specific `cache.host` and database URLs for each environment.
    *   Customizing endpoint enablement (e.g., `identityAggregatorServiceConfiguration: enabled: false` for Barnsley/Farnham, but `userInternalClient: enabled: true` across all).
    *   Detailed transaction processing, Redis, and categorization settings.
    *   Extensive feature flags tailored for each staging environment.
    *   Crucially, they consistently set `awsPrivateCAIssuer: trusted: true, enabled: true, used: true`, overriding the global default.
    *   Specific AWS Account IDs are defined for `useAwsSecretManager`.
    *   Oxford staging also includes detailed `dataRetention` policies and logging levels.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/{barnsley,farnham,oxford,jersey}-staging.yaml`** (3/20/2026, 12:11:00 PM - 12:11:27 PM): These updates to `account-processing` staging `values` files show a consistent change: `awsPrivateCAIssuer.used` is updated from `true` to `false`, while `trusted` and `enabled` remain `true`. This suggests a specific adjustment to how the AWS Private CA is utilized within the `account-processing` service for these environments.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml`** (3/20/2026, 12:23:48 PM - 3/23/2026, 4:07:31 PM): This template defines a `cert-manager` `Certificate` for gRPC communication within the `tink-backend-system`. It is conditional on `awsPrivateCAIssuer.enabled`, specifies standard certificate durations, RSA private key rotation, `tink ab` as the organization, and `server auth`/`client auth` usages. It lists a fixed set of DNS names for the `tink-backend-system` service.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml`** (3/20/2026, 12:28:42 PM): This template sets up a gRPC certificate for the `account-processing` service, similar in structure to the `tink-backend-system` gRPC certificate, but with DNS names specific to `account-processing-service` and supporting dynamic `additionalCertNames`.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml`** (3/20/2026, 3:12:37 PM - 3:15:30 PM): These entries, also identical across several timestamps, detail the Kubernetes Deployment for `tink-backend-system`. It includes:
    *   Datadog and conditional Prometheus health annotations.
    *   RollingUpdate strategy and Datadog APM integration.
    *   Multiple `initContainers` that manage secrets and certificates: `secretman` (for AWS Secret Manager), `secretman-template` (for config templating with AWS secrets), `generate-pinned-certificates` (for AWS Private CA), `fetch-credentials` (using Vaultify), `cassandra-migrations`, and `build-truststore`/`build-keystore` containers for TLS setup.
    *   Security contexts are configured for non-root and read-only filesystems.

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/certificate-service-certificate-aws.yaml`** (3/23/2026, 4:06:52 PM): Similar to the `account-processing` certificate, this template configures a gRPC certificate for the `account` service, conditional on `awsPrivateCAIssuer.enabled`, with DNS names specific to the `account` service and support for `additionalCertNames`.

**Patterns and Recurring Elements:**

1.  **AWS Private CA as a Central Identity/Trust Mechanism:** The most prominent pattern is the widespread adoption and configuration of the `awsPrivateCAIssuer`. While globally disabled in the main `values.yaml`, it's explicitly enabled, trusted, and used (`trusted: true, enabled: true, used: true`) across almost all specific staging environments (`farnham`, `barnsley`, `jersey`, `oxford`) for both `account` and `tink-backend-system` charts. This highlights a shift towards using AWS PCA for internal certificate management.
2.  **Standardized Certificate Management:** There's a consistent approach to generating `cert-manager` `Certificate` resources for various services (Cassandra, gRPC for `tink-backend-system`, `account-processing`, `account`). These certificates share common attributes like 90-day duration, 30-day renewal, RSA private keys, and server/client authentication usages, all issued by `AWSPCAClusterIssuer`.
3.  **Environment-Specific Overrides:** Configurations are heavily tailored per staging environment (`{cluster}-staging.yaml`). These environment-specific `values` files override global defaults for database connections, cache hosts, enabled services, feature flags, and AWS Private CA settings, indicating a fine-grained deployment strategy.
4.  **Multi-Layered Secret Management:** The `tink-backend-system` deployment leverages both AWS Secret Manager (`secretman` init containers) for static secrets and Vault (`vaultify` init container) for dynamic secrets, alongside explicit `keytool` commands within init containers to build truststores and keystores from fetched certificates.
5.  **Feature Flag Driven Development:** Numerous feature flags are present across various `values.yaml` files, allowing for granular control over new functionalities or operational changes (e.g., `enableAccountEvents`, `enableTransactionEvents`, `userLearningEnabled`, `balanceCheckEnabled`).
6.  **Consistency in Repeated Log Entries:** Several files appear multiple times with identical content within a short timeframe (e.g., `tink-backend-system/values/barnsley-staging.yaml`, `tink-backend-system/templates/deployment.yaml`), suggesting re-commits without functional changes or automated system updates.
7.  **Specific Change for `account-processing` `awsPrivateCAIssuer.used`**: A notable specific pattern is the change in all `account-processing/values/{env}-staging.yaml` files where `awsPrivateCAIssuer.used` was explicitly set from `true` to `false` around noon on March 20, while `trusted` and `enabled` remained `true`. This indicates a deliberate decision regarding the active utilization of the AWS Private CA issuer for this specific service's value configurations, possibly a step in a staged rollout or a service-specific requirement.

## 9:58:17 AM
The code changes primarily revolve around the configuration and management of certificates, secrets, and environment-specific settings for the `tink-backend` system, particularly focusing on AWS Private Certificate Authority (PCA) integration.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/cassandra-secret-keystore.yaml` (3/19/2026, 9:51:34 AM):** A Kubernetes Secret named `cassandra-client-certificate-keystore-secret` was introduced. This secret is intended to store the password for the Cassandra client certificate keystore, set to a placeholder "notAsecret".
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-cassandra.yaml` (3/19/2026, 9:51:46 AM):** A Cert-Manager Certificate resource, `cassandra-client-certificate`, was added. This certificate is configured to be issued by `aws-private-ca-issuer`, has a 90-day duration with 30-day renewal, and uses the previously defined secret for its JKS keystore password. It specifies client authentication usages, an RSA 2048 private key, and a SPIFFE URI for identity. Both this and the secret are conditionally enabled by `cassandra.privateCA.enabled`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values.yaml` (3/19/2026, 9:58:00 AM):** This core configuration file saw updates, enabling `cassandra.privateCA.enabled: true` and including a wide array of endpoint configurations, many utilizing `pinnedCertificates: /internal-ca/ca.crt`. It defines `sslConfig: sslMode=VERIFY_IDENTITY` for databases and sets `tlsEnabled: true` for the distributed Cassandra database. Initially, `awsPrivateCAIssuer` was globally set to `trusted: false, enabled: false, used: false`.
*   **Multiple `values/*.yaml` files for `account` and `account-processing` services (3/19-3/20):**
    *   Files like `farnham-staging.yaml`, `barnsley-staging.yaml`, `jersey-staging.yaml`, and `oxford-staging.yaml` within both `account` and `account-processing` charts were updated. These files primarily configure environment-specific database hosts, cache endpoints, AWS account IDs, and a multitude of feature flags (e.g., `enableAccountEvents`, `userLearningEnabled`).
    *   A significant initial change was setting `awsPrivateCAIssuer: trusted: true, enabled: true, used: true` across all these staging environment configurations.
    *   Later on March 20th, between 12:11 PM and 12:12 PM, a series of updates to all `account-processing/values/*-staging.yaml` and `tink-backend-system/values/*-staging.yaml` files changed `awsPrivateCAIssuer.used` from `true` to `false`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/certificate-grpc-certificate-aws.yaml` (3/20/2026, 12:23:48 PM and subsequent identical entries):** This file was introduced to define a `grpc-certificate-aws` Cert-Manager Certificate for the `tink-system` namespace. It uses the `aws-private-ca-issuer` to issue certificates for gRPC communication (both server and client authentication) with specific DNS names for `tink-backend-system` services, conditioned on `awsPrivateCAIssuer.enabled`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/templates/certificate-service-certificate-aws.yaml` (3/20/2026, 12:28:42 PM):** A similar `grpc-certificate-aws` Cert-Manager Certificate was introduced for the `account-processing` namespace, containing service-specific DNS names for its headless and regular services, also conditional on `awsPrivateCAIssuer.enabled`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/templates/deployment.yaml` (3/20/2026, 3:12:37 PM and subsequent identical entries):** This is a major update. The deployment now includes several `initContainers` for robust secret and certificate management:
    *   `secretman`: Retrieves secrets from AWS Secret Manager.
    *   `secretman-template`: Populates configuration files with secrets obtained from AWS.
    *   `generate-pinned-certificates`: Executes a script for certificate pinning, conditional on `awsPrivateCAIssuer.trusted`.
    *   `fetch-credentials`: Uses `vaultify` to retrieve dynamic secrets from Vault, with its CA certificate path conditional on `awsPrivateCAIssuer.trusted`.
    *   `cassandra-migrations`: Runs Cassandra schema migrations, conditional on `runCassandraMigrations` and `awsPrivateCAIssuer.trusted`.
    *   `build-truststore` and `build-keystore`: Creates Java keystores and truststores, with their commands and volume mounts adapted based on `awsPrivateCAIssuer.trusted` and `awsPrivateCAIssuer.used`.
    The deployment also includes detailed health check annotations for Datadog and Prometheus.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/certificate-service-certificate-aws.yaml` (3/23/2026, 4:06:52 PM):** A `grpc-certificate-aws` Cert-Manager Certificate for the `account` namespace was added, similar to the `account-processing` version but tailored with DNS names specific to the `account` service.

**Timestamps of Significant Changes:**

*   **March 19, 2026, 9:51 AM - 9:58 AM:** Initial setup of Cassandra private CA infrastructure (secret, certificate, and global enablement in `values.yaml`).
*   **March 20, 2026, 9:11 AM - 9:14 AM:** A broad rollout enabling AWS Private CA (`awsPrivateCAIssuer: trusted: true, enabled: true, used: true`) across various staging environments for `account` and `tink-backend-system` charts.
*   **March 20, 2026, 12:11 PM - 12:12 PM:** A subsequent, rapid update disabling the *usage* of the AWS Private CA (`awsPrivateCAIssuer.used: false`) for `account-processing` and `tink-backend-system` staging environments.
*   **March 20, 2026, 12:23 PM - 12:28 PM:** Introduction of gRPC certificate definitions for `tink-backend-system` and `account-processing`, leveraging AWS PCA.
*   **March 20, 2026, 3:12 PM:** Major overhaul of the `tink-backend-system` deployment, integrating complex init containers for secret management and certificate trust/keystore building, heavily relying on AWS PCA configuration.
*   **March 23, 2026, 4:06 PM:** Introduction of gRPC certificate definition for the `account` service, completing the set of gRPC certificates across key backend services.

**Patterns and Recurring Elements:**

*   **AWS Private CA Integration:** There's a strong recurring pattern of integrating and configuring AWS PCA using `cert-manager.io/v1` Certificates. This involves setting `trusted`, `enabled`, and `used` flags for `awsPrivateCAIssuer` in various `values.yaml` files and referencing `aws-private-ca-issuer` in certificate definitions.
*   **Environment-Specific Configuration:** The extensive use of `values/<env>-staging.yaml` files highlights a pattern of granular, environment-specific overrides for database connections, cache hosts, feature flags, and security settings.
*   **Certificate Management Standardization:** The repeated structure of `cert-manager.io/v1/Certificate` resources for different services (`cassandra`, `tink-backend-system` gRPC, `account-processing` gRPC, `account` gRPC) indicates an effort to standardize certificate issuance and management across the backend.
*   **Advanced Initialization Logic:** The `tink-backend-system` deployment demonstrates a pattern of using multiple chained `initContainers` for a sophisticated startup process involving fetching secrets from different sources (AWS Secret Manager, Vault) and dynamically preparing certificate stores (JKS trust/keystores).
*   **Conditional Logic in Templates:** Helm's conditional logic (`{{- if .Values.someFlag }}`) is heavily used throughout the templates to enable or disable resources and configure init container steps based on various feature flags and AWS PCA settings.
*   **Placeholder Passwords:** The string "notAsecret" appears in multiple locations (e.g., `cassandra-secret-keystore.yaml`, `values.yaml` for keystore password, `build-truststore` init container command), indicating a pattern of using temporary or placeholder values within templates that would typically be replaced by more secure mechanisms in a production context.
*   **Health and Monitoring Annotations:** The `tink-backend-system` deployment consistently includes `tink-health` annotations for Prometheus and Datadog, signifying a standard approach to monitoring application health and errors.
*   **Rapid Adjustment of `awsPrivateCAIssuer.used`:** The immediate change from `awsPrivateCAIssuer.used: true` to `false` for several services on March 20th suggests an initial attempt to enable full AWS PCA usage, followed by a quick re-evaluation or refinement, perhaps to control which components actively utilize the issued certificates versus just trusting the CA.
*   **Identical Code Commits:** Several files appear multiple times with identical content at different timestamps, suggesting re-saves, re-formatting, or automated system commits that didn't introduce functional code changes.

## 3:34:34 PM
The `/Users/mmuruts/projects/tink/transaction-service/configmap_farnham` file was updated on March 25, 2026, at 10:48:42 AM.

This file appears to be a Kubernetes ConfigMap definition. Key aspects of the change include:

*   **Initial Warning**: The log begins with a warning regarding AGPL-licensed client tools updates being disabled and suggests setting the `TELEPORT_CDN_BASE_URL` environment variable for community or custom binaries.
*   **ConfigMap Details**: It defines a ConfigMap named `ca-trust-bundle` within the `transaction` namespace.
    *   It carries labels: `trust.cert-manager.io/bundle=ca-trust-bundle`.
    *   An annotation `trust.cert-manager.io/hash` is present, indicating a hash of the content.
*   **Certificate Bundles**: The primary content consists of certificate bundles:
    *   `bundle.pem`: This contains three concatenated PEM-encoded X.509 certificates.
        *   The first certificate is from "Visa Corporate Root CA - G2", valid from 2016 to 2036.
        *   The second certificate is a self-signed CA named "Tink Kubernetes Self Signed CA farnham-production", valid from 2019 to 2029.
        *   The third certificate is from "Visa Infrastructure Root CA", valid from 2021 to 2041.
    *   `bundle.p12`: Binary data of 4259 bytes is indicated for a PKCS#12 bundle, likely containing additional certificates or keys.
*   **No Events**: The log explicitly states that there are no associated events (`Events: <none>`).

The update primarily concerns the configuration of trusted root certificates and a self-signed CA within a Kubernetes environment, likely for establishing trust relationships for the `transaction-service`.