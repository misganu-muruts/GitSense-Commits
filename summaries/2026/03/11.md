# Activity Summary for 3/11/2026

## 9:01:01 AM
The changes primarily involve Helm chart configurations for a `transaction-service-reconciliation-worker` and Go application logic for an internal gRPC server.

### File-Specific Updates:

1.  **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/values/houston-staging.yaml`**
    *   **Timestamp:** 3/9/2026, 12:48:27 PM and 3/9/2026, 12:53:52 PM.
    *   **Updates:** This file defines the configuration for the `transaction-service-reconciliation-worker` in the Houston staging environment. Key configurations include setting `logLevel` to "debug", specifying Elasticsearch hosts, index years (2022-2028), and shard count (10). Cassandra hosts are explicitly listed for `bigdb-0/1/2.internal.staging.houston.tink.network`. Reconciliation is `enabled: true` and `dryRun: false`, SQS batch size is 5, and Datadog cluster is set to "us". Worker replica scaling limits (`reconWorkerMinReplicas: 2`, `reconWorkerMaxReplicas: 4`) are defined, and `canaryDeployments` phase is set to `COMPLETE`. The content is identical across both timestamps, suggesting no functional change.

2.  **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service-reconciliation-worker/templates/deployment.yaml`**
    *   **Timestamps:** 3/9/2026, 12:51:43 PM; 3/9/2026, 12:54:41 PM; 3/9/2026, 12:54:55 PM; 3/9/2026, 1:21:03 PM.
    *   **Updates:** This Kubernetes Deployment template for the reconciliation worker includes extensive configurations. It uses Helm templating to dynamically set values.
        *   **Metadata & Labels:** Sets `app: transaction-service-reconciliation-worker` and integrates Datadog APM tags and admission annotations if `apmEnabled` is true.
        *   **Strategy:** Configures a `RollingUpdate` strategy with `maxUnavailable: 0` and `maxSurge: 1`.
        *   **Security Context:** Specifies `runAsNonRoot: true` with `runAsUser`/`runAsGroup`/`fsGroup` set to `65535` and `readOnlyRootFilesystem: true` for containers.
        *   **Init Containers:** An optional `fetch-credentials` init container is included if `useVault` is enabled, using the `vaultify` image to fetch and template credentials.
        *   **Main Container (`transaction-service-reconciliation-worker`):**
            *   Resource requests and limits are dynamically set.
            *   Image is pulled from ECR, with version determined from a file or `.Values.Version`.
            *   It runs the `recon-worker` command with a specific config path.
            *   Numerous environment variables are configured, including feature toggle settings, HMAC keys, system access tokens, and Cassandra V2 password (all sourced from Kubernetes secrets).
            *   Datadog tracing environment variables (`DD_SERVICE_MAPPING`, `DD_APM_COMPUTE_STATS_BY_SPAN_KIND`, `DD_APM_PEER_TAGS_AGGREGATION`) are present.
            *   Volume mounts for TLS certificates, CA trust bundle, and reconciliation configuration are defined.
            *   Liveness and Readiness probes are configured.
        *   **Sidecar Containers:**
            *   An optional `transaction-service-tags-updater` sidecar runs a `tags-consumer` if `tagsUpdater.enabled`.
            *   An optional `renew-leases` sidecar uses `vaultify` to renew Vault leases if `useVault` is enabled.
        *   **Volumes:** Defines various volumes, including secrets for certificates, configMaps for CA bundles and configuration, and an emptyDir for credentials.
        *   **Affinity:** Configures `podAntiAffinity` for non-local clusters to ensure worker pods are spread across hostnames and zones.
        *   The content is identical across all four timestamps, indicating no functional changes within this time window.

3.  **`/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server.go`**
    *   **Timestamp:** 3/10/2026, 2:07:32 PM.
    *   **Updates:** This file, implementing the internal gRPC transaction service, introduces significant changes related to data fetching and consistency.
        *   **New Structs:**
            *   `Config`: Now includes a `ProviderMarkets` map for efficient lookup of markets supporting ISO8601 timestamps.
            *   `FilterContext`: A new struct for holding pre-processed filter data, aiming for efficient filtering for both V1 and V2 APIs.
        *   **New Interface:** `cassandraRepository`: Defines Cassandra operations, explicitly adding `GetTransactionsByUserIdAndIdsV2`, `FindAllByUserIDAndAccountsAndPeriodsV2`, and `FindPeriodsByUserIDV2` alongside their V1 counterparts, indicating a shift towards or support for a V2 API.
        *   **`NewInternalTransactionsServiceServer`:** Modified to populate the `ProviderMarkets` map from an environment variable named "provider-markets".
        *   **`GetTransaction` and `ListTransactions` Methods:** These core methods now include conditional logic:
            *   If `featuretoggle.CompareEsAndCassandraTransactionResponsesToggle` is enabled, they fetch data from Elasticsearch first, then **asynchronously** fetch from Cassandra and perform a deep comparison. The Elasticsearch data is returned immediately to avoid latency impact.
            *   If `featuretoggle.FetchV1FromCassandraForV2Toggle` is enabled, they prioritize fetching data from Cassandra. Otherwise, Elasticsearch is the default source.
        *   **New Asynchronous Comparison Functions:**
            *   `compareTransactionAsync`: Performs a background comparison for a single transaction.
            *   `compareTransactionListAsync`: Performs a background comparison for a list of transactions.
            *   Both use a background context with a timeout, include panic recovery, and log the comparison results (match or mismatch with details).

### Timestamps of Significant Changes:

*   **3/9/2026, 12:48:27 PM:** Initial state of `houston-staging.yaml` configuration.
*   **3/9/2026, 12:51:43 PM:** Initial state of `deployment.yaml` template, configuring the Kubernetes deployment.
*   **3/10/2026, 2:07:32 PM:** Major application logic changes in `internal_server.go`, introducing V2 Cassandra integration, data comparison, and feature toggle-driven data source selection.

### Patterns and Recurring Elements:

*   **Helm Chart Configuration:** The Kubernetes deployment for the reconciliation worker is managed via Helm charts, indicated by the `.charts` directory and the use of `.Values` in the template files.
*   **Datadog Integration:** Consistent integration of Datadog for APM, service mapping, and tracing across the deployment configuration.
*   **Vault Integration:** Strong reliance on Vault for secure credential management, visible through the `vaultify` init and renew-leases containers in the deployment manifest.
*   **Feature Toggles:** Extensive use of feature toggles (e.g., `featuretoggle.CompareEsAndCassandraTransactionResponsesToggle`, `featuretoggle.FetchV1FromCassandraForV2Toggle`) to control application behavior and data source preferences, suggesting an A/B testing or gradual rollout strategy.
*   **Elasticsearch and Cassandra Coexistence/Migration:** The Go code reveals an ongoing strategy for handling transaction data, involving both Elasticsearch and Cassandra. The comparison and conditional fetching logic indicate efforts to ensure data consistency or to transition data sources, especially concerning V1 and V2 API implementations.
*   **Reconciliation Focus:** All changes pertain to a "reconciliation worker" for a "transaction-service", implying a critical function for maintaining data integrity and consistency within the transaction system.
*   **Repetitive Content Logs:** Several log entries for `values/houston-staging.yaml` and `templates/deployment.yaml` show identical content at different timestamps, which might indicate saving without changes or automated updates rather than functional modifications.

## 9:49:18 AM
The file `/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values/houston-staging.yaml` was updated on 3/9/2026, 12:25:00 PM. This configuration primarily defines settings for a staging environment. It enables round-robin for various service clients (`account_service_client`, `transaction_service_client`, `event_producer_service_client`, `credential_service_client`) and enables Horizontal Pod Autoscaling (HPA). Tracing is disabled with a sample rate of 0.5. Datadog is configured for the `us` cluster. Canary deployments are set to a `COMPLETE` phase, and an AWS Account ID "276026349597" is specified.

Shortly after, on 3/9/2026, 12:25:30 PM, the main configuration file `/Users/mmuruts/projects/tink/transaction-history/.charts/transaction-history/values.yaml` was updated. This file contains a more comprehensive set of default configurations. It enables authentication and all service clients (`account_service_client`, etc.) but disables round-robin for them. Tracing settings are identical to the staging file (`enabled: false`, `sample_rate: 0.5`). The `transactions_api` is configured with a page size limit of 100, specific `provider_markets` (UK, GB, SE-OXFORD-ABSTRACT), and disables category search and single transaction retrieval. The `reports_api` sets a message size limit and snapshot days limit. Storage is enabled and configured to use Vault. HPA settings define `minReplicas` at 3, `maxReplicas` at 4, and targets for CPU and memory usage. Service levels are enabled. Datadog APM is enabled for the `eu` cluster. Feature toggles are disabled, pointing to a specific Unleash URL. Canary deployments are `DISABLED` in this default configuration, contrasting with the staging environment. An extensive `monitors` section is included, enabling various Kubernetes monitors and defining alert thresholds and `@slack-Tink-squad-transactions-oncall` as the notification channel for a range of issues including CPU throttling, low replica counts, deployment unavailability, pod crashes, OOMKilled containers, container restarts, and namespace CPU/memory quota exceedances. Global settings define the team, service, namespace, team role ID, and Datadog base URL, with `awsPrivateCAIssuer` set to `used: false`.

A recurring pattern observed across both configuration files is the presence of `config`, `tracing`, `hpa`, `datadog`, and `canaryDeployments` sections. `tracing` settings (`enabled: false, sample_rate: 0.5`) are consistently identical. However, `canaryDeployments` phases differ significantly between the staging (`COMPLETE`) and default (`DISABLED`) environments, as do the `datadog` cluster regions (`us` for staging, `eu` for default). Service clients are present in both but with varying `round_robin` settings.

## 9:49:49 AM
The log details changes across several configuration files for various staging environments (Barnsley, Farnham, Oxford, Jersey) of the Tink backend, alongside logs of critical application errors and updates to Java source code.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/values/*.yaml` (Barnsley, Farnham, Oxford):**
    *   **March 9, 2026, ~1:55 PM:** Initial configurations were set, including database hostnames (e.g., `main-20200204...rds.amazonaws.com`), various service endpoint URLs with pinned certificates, and feature flags such as `accountVerificationEnabled` and `accountDataRepositoryEnabled`. The `awsPrivateCAIssuer` was initially set to `trusted: true, enabled: true, used: true`. The Oxford environment received the most extensive initial configuration, detailing numerous internal service endpoints and application-specific feature flags.
    *   **March 10, 2026, ~8:45 AM:** Across all these account service configuration files, the `awsPrivateCAIssuer.used` setting was changed from `true` to `false`.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account-processing/values/*.yaml` (Farnham, Jersey, Oxford, Barnsley):**
    *   **March 9, 2026, ~1:56 PM:** Initial configurations for account processing were set, enabling feature flags like `enableAccountEvents` and, for Oxford, `enableUpdateAccountEvents`, `enableUpdateBalanceEvents`, and `balanceCheckEnabled`. Deduplication settings were also present in the Oxford config. The `awsPrivateCAIssuer` was initially set to `trusted: true, enabled: true, used: true`.
    *   **March 10, 2026, ~8:46 AM:** The `awsPrivateCAIssuer.used` setting was consistently updated from `true` to `false` in these files.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/tink-backend-system/values/*.yaml` (Barnsley, Oxford, Farnham, Jersey):**
    *   **March 9, 2026, ~1:56 PM:** Extensive system-level configurations were established, including cluster names, cache and database hostnames, Redis configurations for transaction processing locks, categorization settings (e.g., `userLearningEnabled`), and various feature flags like `enableAccountEvents` and `enableTransactionEvents`. Data retention policies and logging levels were specified for Oxford. The `awsPrivateCAIssuer` was initially set to `trusted: true, enabled: true, used: true`.
    *   **March 10, 2026, ~8:46 AM:** The `awsPrivateCAIssuer.used` setting was changed from `true` to `false` across these system configuration files.
*   **`/Users/mmuruts/projects/tink/tink-backend/logggggg`:**
    *   **March 9, 2026, 4:24:08 PM** and **March 10, 2026, 8:39:26 AM:** These logs contain critical Java exceptions (`com.google.inject.ProvisionException` and `org.springframework.beans.factory.UnsatisfiedDependencyException`) whose root cause is an `java.lang.IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString`. This error consistently points to issues during the parsing of private keys within the BouncyCastle library, specifically in `se.tink.backend.libraries.grpc_server_shaded.GrpcServer.ensurePkcs8` and `se.tink.libraries.grpc_utils.GrpcTlsFileReaderShaded.ensurePkcs8`, affecting both the `account` and `account-processing` services.
*   **`/Users/mmuruts/projects/tink/tink-backend/src/libraries/grpc_server/src/main/java/se/tink/libraries/grpc_server_shaded/GrpcServer.java`:**
    *   **March 10, 2026, 9:53:40 AM:** The `ensurePkcs8` method was modified, including `/* START GENAI */` and `/* END GENAI */` markers. This change likely refines or introduces logic to correctly parse different private key formats, including "EC PRIVATE KEY" (SEC1 EC keys), into `AsymmetricKeyParameter` objects using BouncyCastle, presumably in response to the observed parsing errors.
*   **`/Users/mmuruts/projects/tink/tink-backend/src/libraries/grpc_utils/GrpcTlsFileReaderShaded.java`:**
    *   **March 10, 2026, 9:56:13 AM:** This file was significantly updated, introducing helper methods (`readPemObject`, `getPrivateKeyParameter`) to centralize private key parsing logic. Similar to `GrpcServer.java`, the `getPrivateKeyParameter` method was updated (within `/* START GENAI */` and `/* END GENAI */` markers) to explicitly handle "EC PRIVATE KEY" types. This refactoring aims to provide a robust utility for secure gRPC client configuration.
*   **`/Users/mmuruts/projects/tink/tink-backend/src/libraries/grpc_client_utils/shaded/GrpcClientUtilsTest.java`:**
    *   **March 10, 2026, 1:22:32 PM:** New test cases (`buildTlsChannelWithEcKey`, `buildMTlsChannelWithEcKey`) were added, and the `setup` method was expanded to generate Elliptic Curve (EC) server and client certificates and keys using `ECGenParameterSpec("secp256r1")`. This indicates an active effort to validate and ensure proper handling of EC keys in TLS/mTLS configurations for gRPC clients.
*   **`/Users/mmuruts/projects/tink/tink-backend/.charts/account/templates/certificate-service-certificate-aws.yaml`:**
    *   **March 11, 2026, 9:34:54 AM:** A new Kubernetes `Certificate` resource was defined, explicitly configuring `privateKey: algorithm: ECDSA` and `rotationPolicy: Always` for `grpc-certificate-aws`. This certificate is intended for internal `account` services, confirming the adoption or explicit support for ECDSA key algorithms.

**Timestamps of Significant Changes:**

*   **March 9, 2026, ~1:55 PM - 1:57 PM:** Initial comprehensive configuration deployments across various staging environments for multiple services.
*   **March 9, 2026, 4:24:08 PM to March 10, 2026, 8:39:26 AM:** Period during which critical `IllegalArgumentException` errors related to BouncyCastle private key parsing were observed in application logs.
*   **March 10, 2026, ~8:45 AM - 8:47 AM:** Rapid, widespread update across numerous staging environment configuration files to set `awsPrivateCAIssuer.used` to `false`, likely a temporary measure to bypass the certificate/key parsing issue.
*   **March 10, 2026, ~9:53 AM - 9:56 AM:** Direct code modifications to `GrpcServer.java` and `GrpcTlsFileReaderShaded.java` to address private key parsing, specifically introducing explicit handling for EC private keys. These changes include `/* START GENAI */` and `/* END GENAI */` markers.
*   **March 10, 2026, 1:22:32 PM:** Introduction of new tests in `GrpcClientUtilsTest.java` to validate TLS/mTLS with EC keys, complementing the code changes.
*   **March 11, 2026, 9:34:54 AM:** Deployment of a new Kubernetes certificate configuration explicitly specifying ECDSA for private keys, indicating a move towards or solidifying support for this algorithm.

**Patterns and Recurring Elements:**

*   **`awsPrivateCAIssuer.used` Toggle:** A clear pattern of changing `awsPrivateCAIssuer.used` from `true` to `false` is observed across nearly all `.yaml` configuration files on March 10, 2026. This suggests a systemic response to a platform-wide issue related to AWS Private CA, possibly to temporarily disable its usage.
*   **BouncyCastle Private Key Parsing Issues:** The recurring `IllegalArgumentException: unknown object in getInstance: org.bouncycastle.asn1.DEROctetString` in the logs is a strong indicator of a critical problem with how private keys were being read or interpreted by the BouncyCastle library, likely affecting services using TLS/mTLS.
*   **Focus on EC Key Handling:** The Java code changes, the new test cases for EC keys, and the subsequent explicit `privateKey: algorithm: ECDSA` configuration in the Kubernetes certificate manifest collectively demonstrate a targeted effort to implement and verify correct handling of Elliptic Curve Digital Signature Algorithm (ECDSA) private keys. This suggests the parsing errors were specifically related to EC keys.
*   **`/* START GENAI */` Markers:** These markers in the Java code indicate sections that were either generated, heavily refactored, or otherwise highlighted, possibly for review or documentation purposes, in connection with addressing complex cryptographic parsing logic.