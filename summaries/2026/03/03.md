# Activity Summary for 3/3/2026

## 9:35:43 AM
The provided log details recent changes across a Go-based transaction service, focusing on Cassandra integration, asynchronous data comparison, and Kubernetes deployment configurations.

**File-Specific Updates:**

*   **`/Users/mmuruts/projects/tink/transaction-service/internal/grpc/internal_server.go` (Timestamp: 3/2/2026, 2:08:36 PM, and subsequent identical saves until 2:42:46 PM):**
    *   Introduced a `Config` struct to manage provider markets, loaded from environment variables.
    *   Defined a new `cassandraRepository` interface specifically for Cassandra v2 operations, including methods to retrieve transactions by user and IDs, and to find periods by user.
    *   The `NewInternalTransactionsServiceServer` constructor was updated to initialize the `Config` struct with provider markets parsed from an environment variable.
    *   Significant logic was added to the `GetTransaction` and `ListTransactions` gRPC methods. These now support:
        *   Fetching data from Elasticsearch for temporary users.
        *   A "comparison mode" enabled by a feature toggle (`CompareEsAndCassandraTransactionResponsesToggle`). In this mode, data is fetched synchronously from Elasticsearch and then asynchronously from Cassandra. The results are compared in separate goroutines (`compareTransactionAsync`, `compareTransactionListAsync`) to avoid impacting API response latency, and any mismatches are logged.
        *   Conditional fetching from Cassandra if `FetchV1FromCassandraForV2Toggle` is enabled, otherwise defaulting to Elasticsearch.
    *   New `compareTransactionAsync` and `compareTransactionListAsync` functions were implemented to perform the asynchronous comparison, including context management with timeouts and panic recovery.

*   **`/Users/mmuruts/projects/tink/transaction-service/cassandra/txs_store_v2.go` (Timestamp: 3/2/2026, 2:21:44 PM, and subsequent identical save at 2:32:17 PM):**
    *   This file outlines the Cassandra v2 data store, managing transaction-related tables.
    *   The `newTransactionStoreV2` function ensures the existence of several Cassandra tables: `transactions_external_id`, `period_by_userid`, and quarterly-partitioned `transactions_by_userid_period_YYYYQ`.
    *   Detailed schema for the `transactions_by_userid_period_YYYYQ` table was defined, including fields for transaction IDs, user IDs, account IDs, amounts, descriptions, various date/time stamps (booked, value, transaction, internal), payee/payer details, merchant information, and status.
    *   Functions for batch insertion (`insertIntoTransactionsByUseridPeriodsV2`, `updateTransactionExternalIdV2`) and deletion (`deleteFromTransactionsByUseridPeriodV2`) of CassandraTransactionV2 objects were present, leveraging speculative execution for batches.
    *   New functions `deleteAccountExternalTxIds` (to remove external transaction IDs for a specific account) and `findPeriodsByUserID` (to retrieve all periods for a user) were introduced.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/certificate-grpc-certificate-aws.yaml` (Timestamp: 3/3/2026, 9:16:39 AM and 9:17:02 AM):**
    *   Defines a Kubernetes `Certificate` resource for `grpc-certificate-aws` in the `transaction` namespace.
    *   It uses an `AWSPCAClusterIssuer` and specifies `tink ab` as the organization, with usages for both server and client authentication.
    *   The certificate covers multiple DNS names for `transaction-service-api` and `transaction-service-headless` across internal, service, and cluster local domains, incorporating Helm variables for environment and cluster.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/config.yaml` (Timestamp: 3/3/2026, 9:17:57 AM to 9:22:30 AM):**
    *   This Kubernetes ConfigMap template defines extensive service configurations, including log level, user and account service endpoints, data retention settings (enabled, batch size, workers, dry run), and an `internal-v2-enabled` flag.
    *   It configures multiple Elasticsearch clusters (common, nordea, seb) with distinct settings for hosts, sniffing, health checks, retry behavior, index years, and shard numbers.
    *   AWS Secrets Manager paths are referenced for fetching sensitive information like page token HMAC keys and system access tokens.
    *   Detailed configurations for Cassandra v2 (hosts, keyspace, port, username, secret, TLS settings, and transaction store year range) and Cassandra v1 are provided.
    *   Kafka listeners for user and account deletion events are configured, including topics, groups, and TLS settings.
    *   Redis caching parameters (hosts, enabled status, TTL, minimum response transaction number) are also defined.
    *   Several minor changes occurred during this period, primarily refining the conditional inclusion of Cassandra and Kafka CA certificate paths based on the `awsPrivateCAIssuer.trusted` Helm value, aiming to standardize CA bundle usage.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/templates/deployment.yaml` (Timestamp: 3/3/2026, 9:28:49 AM to 9:32:39 AM):**
    *   Outlines the Kubernetes `Deployment` for `transaction-service`, including pod labels, Datadog APM annotations, and `tink-health` monitoring configuration.
    *   Includes `revisionHistoryLimit` and `RollingUpdate` strategy, with `replicas` configurable based on `canaryDeployments.phase`.
    *   Specifies pod security contexts for non-root execution.
    *   If Vault integration is enabled, an `initContainer` (`fetch-credentials`) uses `vaultify` to retrieve and template credentials, and a `renew-leases` sidecar is configured to manage Vault leases.
    *   The main `transaction-service` container defines resource limits, image, arguments, and environment variables. Sensitive environment variables (e.g., HMAC keys, access tokens, Cassandra passwords) are sourced from Kubernetes Secrets.
    *   Datadog APM environment variables are set for tracing.
    *   `grpc-health-probe` is used for `readinessProbe`, `livenessProbe`, and `startupProbe`.
    *   A `preStop` hook is included with a 30-second sleep for graceful shutdown.
    *   Volume mounts for TLS certificates, CA trust bundles, configuration, and Datadog agent are defined, with conditional logic for CA bundles based on `awsPrivateCAIssuer.trusted` and Vault usage.
    *   Changes throughout these timestamps indicate a simplification and standardization of CA certificate volume mounts, moving towards consistent use of a `ca-trust-bundle` when `awsPrivateCAIssuer.trusted` is enabled, and removing older conditional paths for `internal-ca` and `vault-tls`.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values.yaml` (Timestamp: 3/3/2026, 9:33:11 AM and 9:33:35 AM):**
    *   Provides default Helm values for the transaction-service, covering general service settings, Elasticsearch, Cassandra (v1 and v2), deletion listeners, data retention, reconciliation, Redis, pod resources, Vault usage, service levels, feature toggles, Datadog integration, canary deployments, and SLO definitions. No functional changes occurred between these two timestamps.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/values/*.yaml` (Timestamps: 3/3/2026, 9:33:42 AM to 9:34:29 AM):**
    *   Multiple environment-specific `values.yaml` files (`barnsley-staging`, `farnham-staging`, `houston-staging`, `jersey-staging`, `oxford-staging-product001`, `oxford-staging`) customize the Helm chart defaults. These overrides include:
        *   Specific Elasticsearch hosts and index years.
        *   Cassandra hosts.
        *   Enabling or disabling features like `userSvc`, `dataretention`, `reconciliation`, `cassandraV2`, `userDeletionListener`, `accountDeletionListener`, and `redis`.
        *   Adjusting resource requests and limits for pods.
        *   Setting `canaryDeployments.phase` to `COMPLETE` for most staging environments.
        *   Configuring `enableMultiElasticSearchClusters` for `oxford-staging` and `oxford-staging-product001`, along with `nordea` specific Elasticsearch details and `kafkaTopicPrefix`.
        *   `houston-staging` specifically sets `logLevel` to "debug" and `datadog.cluster` to "us".
        *   Most staging environments set `awsPrivateCAIssuer.used` to `false`.

*   **`/Users/mmuruts/projects/tink/transaction-service/.charts/transaction-service/Chart.yaml` (Timestamp: 3/3/2026, 9:34:37 AM):**
    *   The Helm chart's metadata and Tink-specific configurations are defined.
    *   Specifies end-to-end tests (`DeletingTransactions`, `UpdatingTransactions`).
    *   Lists environments and additional clusters for deployment strategies.
    *   Declares a dependency on `standard-monitors` from Datadog Helm repository, conditional on `monitors.enabled`.

**Patterns and Recurring Elements:**

*   **Cassandra V2 Migration/Integration:** There's a clear emphasis on Cassandra V2, with a dedicated `cassandraRepository` interface, `transactionStoreV2` implementation, and `enable-cassandra-v2` configuration flags. This suggests an ongoing migration or dual-write strategy involving a new Cassandra data model.
*   **Elasticsearch and Cassandra Dual-Write/Comparison:** The service implements a mechanism to fetch and compare data from both Elasticsearch and Cassandra, indicating a transition phase or a robust data validation strategy, likely leveraging Elasticsearch for primary reads and Cassandra for storage/validation.
*   **Feature Flag Driven Development:** Extensive use of feature flags (`CompareEsAndCassandraTransactionResponsesToggle`, `FetchV1FromCassandraForV2Toggle`) is evident, allowing for controlled rollout and testing of new functionalities and data sources.
*   **Helm Chart for Deployment and Configuration Management:** The `.charts` directory reveals a strong reliance on Helm for parameterized and environment-specific deployments, using Go templating for dynamic configuration generation. This includes detailed settings for logging, database connections, service endpoints, and resource allocations.
*   **Centralized Certificate Authority (CA) Management:** The Helm templates show efforts to standardize CA certificate paths, often defaulting to `/ca-trust-bundle/bundle.pem` when `awsPrivateCAIssuer.trusted` is enabled, simplifying TLS configuration across environments.
*   **Comprehensive Observability and Monitoring:** Datadog APM integration, `tink-health` annotations, and numerous Kubernetes monitoring alerts (for CPU throttling, replica counts, pod crashes, OOMKills, restarts) are configured, indicating a focus on robust operational monitoring.
*   **Secure Credential Handling:** The use of Vault (via `vaultify` init container and sidecar) and Kubernetes Secrets for sensitive environment variables (`PAGE_TOKEN_HMAC_KEY`, `SYSTEM_ACCESS_TOKEN`, `CASSANDRA_V2_PASSWORD`) highlights a commitment to secure credential management.
*   **Data Deletion Event Handling:** The presence of Kafka listeners for user and account deletion events suggests a robust mechanism for reacting to and processing data removal requests across the system.